import type { CourseUnit } from './schema'

const ACCOUNT_BOUNDARY =
  'Use an Azure subscription and an account permitted to create the listed resources. Work in a dedicated lab resource group, with sample or synthetic data only. This site does not deploy Azure resources, inspect your account, or independently verify your results.'

const OWNED_CLEANUP =
  'When finished with this lab set, list the resources in your dedicated lab resource group before deleting it. Delete the group only if every resource belongs to your exercises and is no longer needed. In a shared group, remove only your named lab resources, analyzers, indexes, containers, models, or agents; never delete shared resources or change shared credentials. Remove local credential files and downloaded results you no longer need. Closing this lesson does not stop Azure charges.'

const CU_COST =
  'Content analysis, model inference and Blob Storage can incur charges. Automatic model deployment consumes quota and may enable billable use; inspect the proposed models and current pricing before accepting. Use the small supplied samples, not a document collection. Region support and free allowances vary.'

const SEARCH_COST =
  'Use the Free Search tier only if available and sufficient; do not silently upgrade to a paid tier. Storage, attached AI enrichments and optional semantic features can incur charges. The exercise limits free enrichment to a small workload of at most 20 documents. Run the indexer once, not on an unattended schedule.'

const IQ_COST =
  'Search capacity, Blob Storage, embedding, retrieval and chat-model use can incur separate charges. Free-tier availability, preview allowances and model quotas vary. Review pricing before choosing Basic, enabling additional capabilities or accepting automatic model deployment.'

const REVIEW_CONTEXT =
  'Introductions, assessments, summaries and episode recaps are included for source traceability, not as substitutes for substantive teaching or evidence of learner mastery.'

export const RETRIEVAL_CONTENT_UNITS: CourseUnit[] = [
  {
    id: 'content-understanding-overview',
    title: 'Turn content into fields',
    cluster: 'content-document',
    examDomain: 'Implement information extraction solutions',
    summary: 'Use one extraction approach across modalities without confusing a reusable analyzer with a general chat model.',
    minutes: 6,
    order: 200,
    prerequisites: ['foundry-projects'],
    sourceIds: [126, 262, 264, 73],
    steps: [
      {
        title: 'Start with the missing structure',
        body: 'An application cannot reliably route work using a pile of PDFs, photographs and recordings alone. It needs named values: an amount, a requested action, or a description. Azure Content Understanding turns supported unstructured content into outputs that downstream software can consume. You choose an analyzer appropriate to the content and task; the same service supports multiple modalities, but that does not mean every analyzer accepts every file or extracts every possible fact.',
        example: 'A fictional equipment-rental business wants a return date from a form and a damage description from a photograph. Those are two extraction tasks, not a request to generate a new photograph.',
      },
      {
        title: 'Follow the information path',
        body: 'The source file enters an analyzer configured with processing settings and a field schema. Content extraction obtains useful text and metadata, using capabilities such as OCR, transcription or layout detection. Field extraction then produces the values requested by the schema. Markdown can support search; structured fields can support automation. The analyzer does not itself approve a payment or complete a business process: your application decides what to do with the returned information.',
        example: 'A sample invoice becomes readable content plus named fields. A separate application checks the amount and asks for review before creating a payment request.',
      },
      {
        title: 'Choose the right boundary',
        body: 'Choose Content Understanding when the task needs schema-guided extraction or generation across documents, images, audio or video. Document Intelligence is a nearby choice focused on document text, structure and form extraction, with prebuilt and trained custom models. A plain read or layout analysis is enough when you only need text or structure. Start with that smaller requirement before creating a custom schema, and inspect an actual result rather than assuming a model name proves suitability.',
        example: 'Extracting tables from a report is a layout task. Extracting requested follow-up actions from a recorded message needs content analysis beyond document layout.',
      },
    ],
    lab: {
      title: 'Rehearse the extraction boundary',
      goal: 'Separate the input modality from the output your application needs.',
      context: 'Optional local conceptual configuration only; it is not an Azure request and is never executed. The Azure exercise below is the main practical activity.',
      fields: [
        {
          id: 'cuOverviewInput',
          label: 'Incoming item',
          hint: 'The rental office receives a photographed return form.',
          options: [
            { value: 'document-image', label: 'Document image' },
            { value: 'generated-image', label: 'Image-generation prompt' },
            { value: 'search-query', label: 'Search query' },
          ],
          expected: 'document-image',
          explanation: 'The source already exists. The job is to understand it, not generate it or search an index.',
        },
        {
          id: 'cuOverviewOutput',
          label: 'Needed output',
          hint: 'A workflow must read the return date without parsing a free-form chat answer.',
          options: [
            { value: 'structured-fields', label: 'Named structured fields' },
            { value: 'new-image', label: 'A newly generated image' },
            { value: 'model-retraining', label: 'A retrained chat model' },
          ],
          expected: 'structured-fields',
          explanation: 'A field contract gives the application predictable places to look for values.',
        },
      ],
      template: '{\n  "localConcept": "extraction boundary",\n  "input": "{{cuOverviewInput}}",\n  "neededOutput": "{{cuOverviewOutput}}"\n}',
      language: 'json',
      expectedOutput: 'Expected synthetic result, not a service response: a document image is routed to analysis; the workflow receives named fields rather than a newly generated image.',
      takeaway: 'Choose the transformation first. A service name is useful only after the input and required output are clear.',
      azure: {
        title: 'Compare Read and Layout on one sample',
        sourceId: 73,
        environment: 'foundry',
        minutes: 25,
        cost: CU_COST,
        prerequisites: [
          ACCOUNT_BOUNDARY,
          'Access to Microsoft Foundry in a Content Understanding-supported region. Use the content.zip samples linked from exercise 73, not personal or customer invoices.',
        ],
        steps: [
          {
            title: 'Create an isolated project',
            surface: 'Microsoft Foundry',
            instruction: 'Open https://ai.azure.com, use the new Foundry experience, and choose the project selector > Create new project. Expand Advanced options and select your dedicated lab resource group and a supported region. Record the project name privately.',
            expected: 'The project and its parent Foundry resource appear in the intended lab group.',
            hint: 'If quota or region support blocks creation, stop here and record the blocker; no analysis has occurred.',
          },
          {
            title: 'Choose the supplied invoice',
            surface: 'Browser',
            instruction: 'Open source exercise 73, select Launch the exercise, and download its content.zip archive. Extract it locally and open invoice-1234.pdf. Note one printed line and one table before uploading anything.',
            expected: 'You can identify the printed text and table you will compare with extraction results.',
            hint: 'Use the fictional sample exactly as supplied; do not substitute a financial record from work.',
          },
          {
            title: 'Open the Layout playground',
            surface: 'Microsoft Foundry',
            instruction: 'In your project, go to Build > Deployments > AI Services and select Azure Content Understanding - Layout. Upload invoice-1234.pdf and select Run analysis.',
            expected: 'Analysis completes and the playground offers formatted content and a JSON result.',
            hint: 'This is the prebuilt playground. Custom analyzer creation is in Content Understanding Studio, not an invented extra playground control.',
          },
          {
            title: 'Inspect structure, not just text',
            surface: 'Microsoft Foundry',
            instruction: 'Open the formatted output, then the raw JSON view. Locate the printed line and the table you noted. Inspect table or structural information rather than expecting a custom InvoiceTotal field.',
            expected: 'The output describes document text and structure; you can point to an actual table or structural element.',
            hint: 'A visible number in extracted text is not the same as a named business field.',
          },
          {
            title: 'Change only the analyzer',
            surface: 'Microsoft Foundry',
            instruction: 'Return to the AI Services tab and choose Azure Content Understanding - Read. Analyze the same invoice without changing the file. Compare the returned text and structural output with Layout.',
            expected: 'Both runs provide text; the comparison exposes the additional structure supplied by Layout.',
            hint: 'Record what your actual version returns. Do not invent missing fields to match an expected screenshot.',
          },
          {
            title: 'Save one observation and stop',
            surface: 'Browser',
            instruction: 'Write a short private note naming one output shared by both runs and one difference. Decide whether the rental example needs this structural output or a custom business-field schema. You can finish here without starting another modality.',
            expected: 'You have a concrete comparison and a clear next step, rather than a claim of full service mastery.',
            hint: 'A field name and your own explanation are sufficient evidence; a full response dump is unnecessary.',
          },
        ],
        success: 'You inspected real Read and Layout results for the same sample and explained why structure extraction differs from custom field extraction.',
        evidencePrompt: 'In your own words, name one structural result you observed and whether it satisfies the rental example. Do not paste keys, endpoints, account identifiers or documents.',
        cleanup: OWNED_CLEANUP,
      },
    },
    check: {
      question: 'A service team needs requested actions from voice messages and damage descriptions from photographs in predictable fields. Which starting point best fits?',
      options: [
        { id: 'cu-overview-document-only', text: 'Use a document layout model for every input.', explanation: 'Layout extraction addresses document structure; it does not turn voice recordings into the requested action fields.' },
        { id: 'cu-overview-schema', text: 'Use Content Understanding with modality-appropriate analyzers and field schemas.', explanation: 'The analyzers turn supported inputs into the named outputs that the workflow needs, while allowing different content processing.' },
        { id: 'cu-overview-generate', text: 'Generate replacement images and messages with a chat model.', explanation: 'Generating new content does not recover evidence from the existing files and would change the task.' },
      ],
      correctOptionId: 'cu-overview-schema',
      explanation: 'The decisive requirement is structured understanding of existing multimodal content, not content generation or document structure alone.',
    },
    recall: {
      prompt: 'Without looking back, describe the path from a source file to a business action. Which part belongs to the analyzer?',
      answer: 'The analyzer processes the source and returns content plus schema-defined fields. The application validates or reviews those fields and decides on the business action. Extraction is not approval.',
    },
    transfer: 'A customer says, “We want AI to process attachments.” Ask which modalities arrive and which exact output fields drive a decision. Explain why those answers matter more than choosing a model first.',
    sourceNote: REVIEW_CONTEXT,
  },
  {
    id: 'content-understanding-analyzer',
    title: 'Design a useful analyzer',
    cluster: 'content-document',
    examDomain: 'Implement information extraction solutions',
    summary: 'Define a small field contract, test it against evidence, and build a named analyzer for reuse.',
    minutes: 8,
    order: 205,
    prerequisites: ['content-understanding-overview'],
    sourceIds: [30, 8, 73, 164, 217, 7],
    steps: [
      {
        title: 'Make the schema a contract',
        body: 'A schema states which information the application expects and how that information is shaped. A string suits a reference number that might contain leading zeros; a number suits a quantity used in arithmetic. Repeated line items need a list of objects so each description stays attached to its quantity. The analyzer combines this contract with a base analyzer and processing settings. It is not merely a list of words to search for.',
        example: 'For a fictional bicycle-parts invoice, use one order reference and a list of items containing description and quantity. Do not put all quantities into an unrelated comma-separated string.',
      },
      {
        title: 'Separate reading from inference',
        body: 'Extraction reads a value present in the source. Classification chooses among specified categories. Generation derives a description or other value from the content. These methods have different evidence expectations: a printed reference can have a source region, whereas an inferred summary may not correspond to one printed span. Clear field descriptions make the intended task explicit. A schema can constrain the shape of a generated value, but it cannot make the value automatically true.',
        example: 'Read “Order 0042” as text, classify the form as return or delivery, and generate a brief description of the problem. Each field asks for a different operation.',
      },
      {
        title: 'Prototype before building',
        body: 'Content Understanding Studio lets you upload a representative sample, choose a template, edit fields and run analysis. A template is a starting schema, not proof that every suggested field belongs in your workflow. Remove irrelevant fields and inspect the values that remain. Building gives the analyzer a reusable name for later calls. This schema-led workflow should not be confused with training a custom Document Intelligence extraction model from a labeled dataset.',
        example: 'The sample invoice has no billing-address requirement. Remove that field, test the remaining schema, and only then build the invoice analyzer.',
      },
      {
        title: 'Test something you did not tune on',
        body: 'A successful run on the setup sample proves that one run worked, not that the analyzer generalizes. Analyze a second document with different values and check the returned types, missing fields and supporting evidence. If you change a description or field, build a new named version when appropriate so existing consumers keep a known contract. Keep an explicit review path for uncertain or inferred values rather than promoting one good demonstration into automatic business approval.',
        example: 'If a generated total quantity is four on the first invoice, compare it with the visible line quantities and repeat on a second invoice. A plausible total is not enough.',
      },
    ],
    lab: {
      title: 'Rehearse a field contract',
      goal: 'Keep repeated values together and distinguish extraction from generation.',
      context: 'Local conceptual configuration only, not an Azure analyzer definition. Build and test the real analyzer in the guided exercise.',
      fields: [
        {
          id: 'cuAnalyzerItems',
          label: 'Line-item shape',
          hint: 'Each description must remain paired with its own quantity.',
          options: [
            { value: 'object-list', label: 'List of objects' },
            { value: 'single-number', label: 'One number' },
            { value: 'unrelated-text', label: 'Unrelated text fragments' },
          ],
          expected: 'object-list',
          explanation: 'An object groups the fields of one item; a list represents multiple items without losing their relationships.',
        },
        {
          id: 'cuAnalyzerMethod',
          label: 'Printed order reference',
          hint: 'The value exists on the document and must preserve its leading zeros.',
          options: [
            { value: 'generate-summary', label: 'Generate a summary' },
            { value: 'extract-string', label: 'Extract a string' },
            { value: 'classify-category', label: 'Classify a category' },
          ],
          expected: 'extract-string',
          explanation: 'A reference is source text, not an arithmetic quantity or an inferred category.',
        },
      ],
      template: '{\n  "localConcept": "field contract",\n  "lineItems": "{{cuAnalyzerItems}}",\n  "orderReference": "{{cuAnalyzerMethod}}"\n}',
      language: 'json',
      expectedOutput: 'Expected synthetic result, not a service response: order reference 0042 remains text; each line item retains its own description and quantity.',
      takeaway: 'Types preserve meaning; field methods describe where that meaning should come from.',
      azure: {
        title: 'Build and revise an invoice analyzer',
        sourceId: 73,
        environment: 'foundry',
        minutes: 40,
        cost: CU_COST,
        prerequisites: [
          ACCOUNT_BOUNDARY,
          'An isolated Foundry project in a supported region, its parent resource, and the two sample invoices in exercise 73. You may reuse your own overview-lab resources.',
          'Permission and quota for the model deployments requested by Content Understanding Studio; access to create a private Standard LRS storage account.',
        ],
        steps: [
          {
            title: 'Prepare private lab storage',
            surface: 'Azure portal',
            instruction: 'Open your dedicated lab resource group at https://portal.azure.com. Create a Standard LRS Storage account in the same region as the Foundry resource, or select the account you created for this lab set. Keep containers private.',
            expected: 'The Foundry resource and your storage account are identifiable in the same isolated group.',
            hint: 'Do not repoint a shared analyzer project at this storage account.',
          },
          {
            title: 'Connect the correct resource',
            surface: 'Browser',
            instruction: 'At https://contentunderstanding.ai.azure.com, open Settings > Add resource and select the parent Foundry resource. Review the required-model auto-deployment option and its costs, then select Next > Save. Wait for setup to finish.',
            expected: 'The Studio lists your connected resource with the required model configuration.',
            hint: 'Use the resource attached to your project, not an unrelated Azure OpenAI deployment.',
          },
          {
            title: 'Start the invoice project',
            surface: 'Browser',
            instruction: 'In Studio, choose Build or the custom-projects Get started area > Create > Extract content and fields with a custom schema. Name the project for this lab, connect your storage account and a new container, upload invoice-1234.pdf, and choose the Invoice template.',
            expected: 'The schema editor shows proposed invoice fields beside the sample.',
            hint: 'New storage permissions can take time to propagate. Retry only after checking the selected resource and account.',
          },
          {
            title: 'Make one deliberate schema change',
            surface: 'Browser',
            instruction: 'Review the template fields and delete BillingAddress as the exercise directs. Inspect Items and its subfields. Add TotalQuantity with the exercise description, String value type and Auto method; save. Treat this as an inferred quantity, not a printed field.',
            expected: 'Your saved schema contains the requested item structure and the added TotalQuantity field.',
            hint: 'Suggest can replace an edited schema. Do not use it on a shared or valuable schema without preserving your work.',
          },
          {
            title: 'Inspect the actual values',
            surface: 'Browser',
            instruction: 'Open Test > Run analysis. Inspect the Fields and Results panes. Compare one extracted field with its highlighted source and compare TotalQuantity with the visible quantities. Note whether an inferred field has different grounding or confidence.',
            expected: 'You can distinguish a value read from the invoice from a value derived from its items.',
            hint: 'Confidence and grounding availability vary by field and analyzer. An absent source region is not a reason to invent one.',
          },
          {
            title: 'Build and test the second invoice',
            surface: 'Browser',
            instruction: 'Select Build analyzer, give it a unique lab name, then Jump to analyzer list > your analyzer > Test. Upload invoice-1235.pdf and run analysis. Compare its item values with the source instead of expecting the first invoice values again.',
            expected: 'The same field contract works against a distinct input, with actual values available for inspection.',
            hint: 'Write down the analyzer name privately so you can resume; the setup-sample success alone was not the finish line.',
          },
          {
            title: 'Refine one description',
            surface: 'Browser',
            instruction: 'Return to your lab project and change only the TotalQuantity description to clarify that it is the sum of item quantities, not the count of rows. Save, retest the same invoice, and build a new named version if the contract is useful.',
            expected: 'You can report whether the clarification changed the result and justify the intended calculation even if both outputs agree.',
            hint: 'Keep the input constant so you are investigating the field description, not two changes at once.',
          },
        ],
        success: 'A named analyzer has been tested on two supplied invoices, and you inspected the effect of one schema-description change.',
        evidencePrompt: 'Describe the type and method of one field, how you checked it, and what happened after the description changed. Use fictional values only; do not upload results, keys or invoices here.',
        cleanup: OWNED_CLEANUP,
      },
    },
    check: {
      question: 'A return form lists three products, each with its own reason and quantity. A downstream workflow keeps pairing the wrong reason with the wrong product. Which schema change addresses the cause?',
      options: [
        { id: 'cu-analyzer-objects', text: 'Represent products as a list of objects containing reason and quantity.', explanation: 'Grouping related values in each object preserves the relationships that the workflow needs.' },
        { id: 'cu-analyzer-confidence', text: 'Keep separate lists and accept only high-confidence values.', explanation: 'Confidence describes extraction uncertainty; it does not repair a schema that loses the relationship between fields.' },
        { id: 'cu-analyzer-summary', text: 'Replace all fields with a generated paragraph.', explanation: 'A paragraph makes the structure less predictable and shifts the pairing problem into another parsing step.' },
      ],
      correctOptionId: 'cu-analyzer-objects',
      explanation: 'The failure is a data-shape problem. Preserve item-level relationships before tuning extraction quality.',
    },
    recall: {
      prompt: 'Explain the difference between a schema, a built analyzer and one analysis result.',
      answer: 'The schema defines requested fields and their shapes. The built analyzer is the reusable configured processor identified by name. An analysis result contains values obtained from one submitted input.',
    },
    transfer: 'A customer wants every possible field “just in case.” Propose a small first schema tied to one business decision and explain how a held-out sample would reveal missing or ambiguous requirements.',
    sourceNote: `Source 30 and the linked exercise use Content Understanding Studio for custom analyzers; Foundry exposes selected prebuilt playgrounds. The exercise deliberately uses a String/Auto TotalQuantity field, so its result needs interpretation rather than assuming a numeric API contract. ${REVIEW_CONTEXT}`,
  },
  {
    id: 'content-understanding-images',
    title: 'Read evidence from images',
    cluster: 'content-document',
    examDomain: 'Implement computer vision solutions',
    summary: 'Turn visual content into typed metadata while keeping generated descriptions separate from grounded evidence.',
    minutes: 8,
    order: 210,
    prerequisites: ['content-understanding-analyzer'],
    sourceIds: [137, 264, 8, 55, 169, 216, 9, 184],
    steps: [
      {
        title: 'Give a photo a useful shape',
        body: 'A photo collection is difficult to search until each image has descriptive information. An image analyzer can generate a description and tags, or return business-specific fields defined by your schema. Start with an appropriate prebuilt analyzer when one matches: the corpus distinguishes general image analysis from receipt, invoice and identity-document extraction. A receipt photograph is still a receipt-processing problem when the required outputs are vendor, date and total, not simply a scene description.',
        example: 'A fictional outdoor-equipment catalogue needs searchable descriptions of tent photographs. A purchase receipt for the same tent instead needs receipt fields.',
      },
      {
        title: 'Ask three different visual questions',
        body: 'Use extract for visible text, classify for a category chosen from an allowed set, and generate for a description inferred from the scene. Match the output type as well as the method: tags naturally form a list of strings, while a count is numeric. Structured output reduces parsing work, but inferred visual descriptions still need checking. When a requested property is not visible, the workflow should preserve that uncertainty instead of treating a plausible guess as observed evidence.',
        example: 'For a sample parcel photo, extract the printed label, classify visible packaging condition, and generate a short description. Do not infer the unseen contents from the box alone.',
      },
      {
        title: 'Confidence is not a guarantee',
        body: 'Grounding links an extracted value to the source region that supports it. Confidence is a reliability estimate, not a certificate that the business decision is safe. The corpus suggests 0.9 and above for higher-confidence automation, 0.7–0.9 for review consideration, and lower scores for verification. Treat those as teaching heuristics: test thresholds for the actual task and risk. Generated descriptions and classifications may not expose the same grounding or confidence as directly extracted text.',
        example: 'A parcel label read with high confidence can still belong to the wrong shipment. Check the relevant source region and workflow context before acting.',
      },
      {
        title: 'Improve the input before the prompt',
        body: 'Poor lighting, blur, clutter and rotation can remove or obscure the evidence the analyzer needs. A more elaborate schema cannot reliably recover text that is unreadable in the image. Test a clear, upright sample first, then vary one input condition and compare actual outputs. Use non-sensitive images for practice. The corpus also describes content-safety protections and privacy responsibilities; image analysis is not permission to process identity documents or biometric information without an appropriate lawful basis.',
        example: 'Compare a clear synthetic parcel label with a slightly rotated copy. Explain any changed field using the image evidence, rather than assuming every difference is a model defect.',
      },
    ],
    lab: {
      title: 'Rehearse image field methods',
      goal: 'Choose a field method and shape that match the visible evidence.',
      context: 'Local conceptual configuration, not an Azure payload. The real image exercise uses Studio and the supplied application.',
      fields: [
        {
          id: 'cuImageTags',
          label: 'Tags field type',
          hint: 'The catalogue should filter individual tags without splitting punctuation.',
          options: [
            { value: 'number', label: 'Number' },
            { value: 'string-list', label: 'List of strings' },
            { value: 'single-paragraph', label: 'One paragraph' },
          ],
          expected: 'string-list',
          explanation: 'Each tag remains a distinct value available to later indexing or filtering.',
        },
        {
          id: 'cuImageDescription',
          label: 'Scene description method',
          hint: 'The description is not printed as a sentence inside the image.',
          options: [
            { value: 'generate', label: 'Generate from visual content' },
            { value: 'extract', label: 'Extract a printed sentence' },
            { value: 'copy-filename', label: 'Copy the file name' },
          ],
          expected: 'generate',
          explanation: 'A description is inferred from visual content. Its wording should not be confused with directly extracted text.',
        },
      ],
      template: '{\n  "localConcept": "image metadata",\n  "tagsShape": "{{cuImageTags}}",\n  "descriptionMethod": "{{cuImageDescription}}"\n}',
      language: 'json',
      expectedOutput: 'Expected synthetic result, not a service response: Description summarizes a fictional tent photograph; Tags contains separate values such as tent and outdoors.',
      takeaway: 'Predictable structure helps downstream systems, but it does not remove the need to verify what the image actually supports.',
      azure: {
        title: 'Build an image metadata analyzer',
        sourceId: 55,
        environment: 'portal-and-code',
        minutes: 40,
        cost: CU_COST,
        prerequisites: [
          ACCOUNT_BOUNDARY,
          'An isolated Foundry project, supported region and private Standard LRS storage account. You may reuse only resources you own from the preceding lab.',
          'The animal samples from exercise 55, plus VS Code, the exercise-supported Python version and Azure CLI for its Microsoft Entra ID application path.',
        ],
        steps: [
          {
            title: 'Verify the isolated resources',
            surface: 'Azure portal',
            instruction: 'Open your lab resource group and confirm its Foundry resource and same-region storage account. Create a Standard LRS account if needed. Check that you are not using a team storage container or shared production resource.',
            expected: 'You have an owned resource and private storage for a new image-analysis project.',
            hint: 'The source exercise starts in Foundry; this portal check makes the resource boundary explicit before model deployment.',
          },
          {
            title: 'Configure Studio models',
            surface: 'Browser',
            instruction: 'Open Content Understanding Studio > Settings > Add resource. Select the lab Foundry resource, review required model auto-deployment, then Next > Save. Return to Build once the resource is configured.',
            expected: 'The resource is connected and the model dependencies for your analyzer are available.',
            hint: 'Model choices differ across the supplied sources. Follow the linked exercise and the supported options shown for your resource.',
          },
          {
            title: 'Create the image schema',
            surface: 'Browser',
            instruction: 'Choose Build > Create > Extract content and fields with a custom schema. Use a new project and storage container. From exercise 55 download lion.jpg, upload it, and select Image Analysis > Start from Scratch.',
            expected: 'A new image project is ready for explicit field definitions.',
            hint: 'Use the provided animal photo; there is no need to collect photographs of people.',
          },
          {
            title: 'Define two useful outputs',
            surface: 'Browser',
            instruction: 'Add Description as String with Generate method and Tags as List of Strings with Generate method, following the exercise. Save, select Run analysis and inspect both the Fields pane and the JSON result.',
            expected: 'The image has an actual generated description and separately represented tags.',
            hint: 'Compare the description with the visible animal and background. Do not require an exact sentence match.',
          },
          {
            title: 'Change one field description',
            surface: 'Browser',
            instruction: 'Change only the Description field description to request one sentence about visible animal and habitat, without inferred location. Rerun the same image and compare the output. Keep or revise the description based on observable evidence.',
            expected: 'You can explain whether a more specific field description changed the useful content or unsupported claims.',
            hint: 'Two runs can agree. The useful result is an evidence-based comparison, not a forced difference.',
          },
          {
            title: 'Build the reusable analyzer',
            surface: 'Browser',
            instruction: 'Select Build analyzer with a unique lab name. Jump to the analyzer list, open it and review Code Example. Note the Foundry resource endpoint and analyzer name privately; these identify the resource and reusable processor.',
            expected: 'The analyzer exists and its connection details refer to your lab resource.',
            hint: 'The resource endpoint is not the Foundry project endpoint. Do not paste either endpoint or a displayed key into this tutor.',
          },
          {
            title: 'Use the supplied image client',
            surface: 'Cloud Shell / local editor',
            instruction: 'Follow exercise 55 in VS Code: open its analyze-image.py starter, install its listed requirements, set the endpoint and analyzer name locally, and complete the supplied SDK code. Sign in with Azure CLI as directed. Run the app on the giraffe and elephant samples and inspect Description and Tags.',
            expected: 'The same analyzer contract produces different field values for two new images through the real service.',
            hint: 'The client is an explicit code step, not a Foundry portal feature. Use the exercise SDK version and its AnalysisInput pattern together.',
          },
        ],
        success: 'Your built image analyzer returned inspected Description and Tags fields, and the supplied client used it on new sample images.',
        evidencePrompt: 'Describe one accurate visual detail, one field type, and the effect of the description change. Do not provide photographs, credentials, account identifiers or full service responses.',
        cleanup: OWNED_CLEANUP,
      },
    },
    check: {
      question: 'An analyzer produces a plausible damage description but gives no source region for that generated field. A separate printed serial-number field has a confidence score. What is the sound interpretation?',
      options: [
        { id: 'cu-images-share-confidence', text: 'Apply the serial-number confidence to the entire result.', explanation: 'Confidence belongs to the specific returned field; it does not validate another field or the whole business decision.' },
        { id: 'cu-images-missing-means-failure', text: 'Assume every missing region proves that analysis failed.', explanation: 'Generated content need not have the same source-span grounding as text directly extracted from a region.' },
        { id: 'cu-images-inspect', text: 'Check the description against the image and validate the serial number against its own evidence.', explanation: 'The fields use different mechanisms and should be checked using the evidence each actually provides.' },
      ],
      correctOptionId: 'cu-images-inspect',
      explanation: 'Do not transfer certainty between fields or confuse a generated interpretation with a quoted source value.',
    },
    recall: {
      prompt: 'Name the three field methods and give a different image example for each.',
      answer: 'Extract reads visible text, such as a printed label. Classify selects a permitted category, such as visibly damaged or undamaged. Generate creates an interpretation, such as a scene description.',
    },
    transfer: 'An inventory customer wants to automate acceptance from product photos. Ask what is visibly verifiable and what an error would cost. Propose a review boundary rather than promising that a high confidence score guarantees correctness.',
    sourceNote: `Source 55 describes Foundry broadly, while its linked exercise and episode 9 create custom analyzers in Content Understanding Studio. Source 184 lists GPT-4.1-family defaults; the image exercise and episode also mention newer model choices. The 0.9 confidence guidance in sources 8 and 169 is a heuristic, not a safety guarantee. ${REVIEW_CONTEXT}`,
  },
  {
    id: 'content-understanding-audio-video',
    title: 'Understand recorded activity',
    cluster: 'content-document',
    examDomain: 'Implement information extraction solutions',
    summary: 'Extract actions from recordings without confusing a transcript, a visual observation and an inferred task.',
    minutes: 9,
    order: 215,
    prerequisites: ['content-understanding-analyzer'],
    sourceIds: [262, 30, 73, 7],
    steps: [
      {
        title: 'A transcript is an intermediate result',
        body: 'A voicemail transcript records speech as text, but a workflow may need the caller, requested action and callback details. An audio analyzer uses transcription and a schema to produce those structured outputs. This is different from merely transcribing the recording or synthesizing new speech. Start with one small recording and a few fields so you can compare every returned value with what was actually said before trusting the result in a process.',
        example: 'A fictional repair message says a bicycle is ready for pickup. The useful action is collect the bicycle, not a long transcript copied into an action field.',
      },
      {
        title: 'Video adds another evidence channel',
        body: 'Video analysis can combine spoken content with visual information across time. A slide shown during a meeting may add information that never appears in the transcript. The corpus exercise returns fields for video segments, so do not assume one flat record always represents the entire recording. Choose fields that distinguish participants, shared slides and assigned actions. Inspect the relevant segment before connecting an action with a person or treating a visual detail as a spoken statement.',
        example: 'A slide shows a launch date while a speaker asks someone to send an estimate. Those are separate observations, not evidence that the estimate is due on the launch date.',
      },
      {
        title: 'Keep actions attached to owners',
        body: 'A list of task objects preserves the relationship between each task and its assigned person. Separate lists of names and actions can lose that relationship when one person owns several tasks or one task has no explicit owner. Generated fields need clear descriptions about whether to include only explicit assignments or inferred suggestions. The application should represent missing evidence honestly and require review before turning an extracted meeting action into an externally visible commitment.',
        example: 'Use an AssignedActions list with Task and AssignedTo for “Mira, send the estimate.” Do not silently assign “we should revisit pricing” to Mira as well.',
      },
      {
        title: 'Change one thing and compare',
        body: 'Testing with a second recording checks whether the schema captures the intended pattern rather than memorizing the setup example. Keep the same schema while changing the input, or keep the input while refining one field description. Inspect both the structured fields and the underlying transcript or visual segment. Use the supplied synthetic recordings for practice; a real meeting may contain personal or confidential information even when the intended output seems harmless.',
        example: 'Clarify Actions to mean requests explicitly made by the caller. Rerun the same sample and see whether an inferred suggestion disappears or remains.',
      },
    ],
    lab: {
      title: 'Rehearse recording outputs',
      goal: 'Retain temporal and ownership relationships in the extracted information.',
      context: 'Local conceptual configuration only, not an Azure schema. Use the portal exercise to inspect genuine recording results.',
      fields: [
        {
          id: 'cuRecordingActions',
          label: 'Assigned-action structure',
          hint: 'Each task must stay linked to the person assigned to it.',
          options: [
            { value: 'task-owner-objects', label: 'List of task-and-owner objects' },
            { value: 'separate-lists', label: 'Independent task and name lists' },
            { value: 'participant-count', label: 'Only a participant count' },
          ],
          expected: 'task-owner-objects',
          explanation: 'An object keeps the relationship intact even if one participant has several tasks.',
        },
        {
          id: 'cuRecordingEvidence',
          label: 'Video evidence to inspect',
          hint: 'The requested fact appears on a shared slide but is never read aloud.',
          options: [
            { value: 'transcript-only', label: 'Transcript only' },
            { value: 'visual-and-transcript', label: 'Visual segment and transcript' },
            { value: 'filename-only', label: 'File name only' },
          ],
          expected: 'visual-and-transcript',
          explanation: 'Video can contain relevant visual evidence beyond speech; the source segment lets you verify where a claim came from.',
        },
      ],
      template: '{\n  "localConcept": "recording evidence",\n  "actions": "{{cuRecordingActions}}",\n  "inspect": "{{cuRecordingEvidence}}"\n}',
      language: 'json',
      expectedOutput: 'Expected synthetic result, not a service response: task “send estimate” remains paired with Mira; a slide detail is checked separately from the spoken assignment.',
      takeaway: 'A useful summary keeps the relationships and evidence that a downstream decision depends on.',
      azure: {
        title: 'Extract actions from synthetic recordings',
        sourceId: 73,
        environment: 'foundry',
        minutes: 45,
        cost: CU_COST,
        prerequisites: [
          ACCOUNT_BOUNDARY,
          'Your own configured Foundry resource and Studio storage from the analyzer lab, or complete the resource and model setup in exercise 73 first.',
          'The supplied content.zip recordings: call-1.mp3, call-2.mp3, meeting-1.mp4 and meeting-2.mp4. These are synthetic exercise content.',
        ],
        steps: [
          {
            title: 'Check the resource boundary',
            surface: 'Azure portal',
            instruction: 'Open the resource group for your Content Understanding labs. Confirm the Foundry resource and private storage account are yours and still available. If not, create the isolated resources using exercise 73 before entering Studio.',
            expected: 'You know which owned resources will process and store these samples.',
            hint: 'A saved local rehearsal is not an Azure resource or a previous successful analysis.',
          },
          {
            title: 'Create the voicemail schema',
            surface: 'Browser',
            instruction: 'In Content Understanding Studio, choose Project list > Create > Extract content and fields with a custom schema. Connect your resource and storage, upload call-1.mp3 and choose Audio analysis. Use Get transcription preview, then define Caller, Summary, Actions and CallbackNumber as strings and AlternativeContacts as a list of strings.',
            expected: 'The sample transcript and a small saved field schema are visible.',
            hint: 'Use the Generate methods in the exercise. A callback number is a string, not a value to add or average.',
          },
          {
            title: 'Analyze and verify the request',
            surface: 'Browser',
            instruction: 'Select Run analysis. Compare Caller, Actions and CallbackNumber with the supplied audio or transcript. Expand AlternativeContacts and inspect the Results JSON. Build a uniquely named voicemail analyzer, then test call-2.mp3 with it.',
            expected: 'The same field structure returns different information from two synthetic callers.',
            hint: 'You may pause after this step. Keep only a private analyzer-name note to resume with video later.',
          },
          {
            title: 'Create the video schema',
            surface: 'Browser',
            instruction: 'Create a separate custom-schema project, upload meeting-1.mp4 and select Video analysis. Add Summary, Participants, ParticipantNames and SharedSlides using the exercise types. Add AssignedActions as a list of objects with Task and AssignedTo string subfields; save.',
            expected: 'The schema preserves participant counts, repeated names, slide descriptions and task ownership separately.',
            hint: 'The list-of-objects editor has a subfield view. Return to the top-level schema before saving.',
          },
          {
            title: 'Compare two evidence channels',
            surface: 'Browser',
            instruction: 'Run analysis and inspect the returned segment fields. Check one SharedSlides description against the video and one AssignedActions entry against the conversation. Inspect Results rather than flattening all segments into an assumed single object.',
            expected: 'You can identify one visual observation and one spoken assignment in the real output.',
            hint: 'Do not infer a task owner from the list of participants alone.',
          },
          {
            title: 'Clarify one action rule',
            surface: 'Browser',
            instruction: 'Change only the AssignedActions description to request explicitly assigned tasks and their stated owners, excluding casual suggestions. Save and rerun meeting-1.mp4. Compare actual action entries before and after the change.',
            expected: 'You can explain whether the revised description improved the match to explicit assignments.',
            hint: 'A matching result is still informative if you checked the source; do not manufacture a difference.',
          },
          {
            title: 'Build and test on a new meeting',
            surface: 'Browser',
            instruction: 'Build a uniquely named meeting analyzer. Open its Test tab, upload meeting-2.mp4 and run analysis. Inspect the segment fields and JSON, especially which Task remains paired with which AssignedTo value.',
            expected: 'A second recording is analyzed with the same task-owner contract, ready for a human review step.',
            hint: 'Stop after writing one observation. There is no requirement to analyze every recording again in one sitting.',
          },
        ],
        success: 'You inspected an audio result and a video result, kept task-owner relationships intact, and tested a schema clarification against the same recording.',
        evidencePrompt: 'Explain one difference between transcript evidence and visual evidence, using only the supplied fictional meeting. Do not submit recordings, participant details from real meetings or full response files.',
        cleanup: OWNED_CLEANUP,
      },
    },
    check: {
      question: 'A meeting transcript contains an assigned task, but the deadline appears only on a shared slide. Which design best preserves verifiable evidence?',
      options: [
        { id: 'cu-recordings-transcript', text: 'Use only the transcript and ask the model to invent a likely deadline.', explanation: 'The transcript lacks the deadline; guessing would replace missing evidence rather than retrieve it.' },
        { id: 'cu-recordings-video', text: 'Analyze the video and verify the task and slide in their relevant segments.', explanation: 'The recording contains both evidence channels, allowing the relationship to be checked instead of assumed.' },
        { id: 'cu-recordings-count', text: 'Return only a count of participants.', explanation: 'A count cannot preserve task ownership, timing or the visual deadline.' },
      ],
      correctOptionId: 'cu-recordings-video',
      explanation: 'Choose processing that can access the evidence required by the output, then verify the relationship rather than merely combining nearby facts.',
    },
    recall: {
      prompt: 'Why are a transcript and a list of assigned task objects different outputs?',
      answer: 'A transcript represents speech. Task objects structure selected information, preserving each task and its owner, which may require interpretation and verification against the recording.',
    },
    transfer: 'A customer wants automatic follow-up emails from meeting recordings. Explain the extraction boundary and propose a review step before an inferred task becomes a sent commitment.',
    sourceNote: `The lab is a paced adaptation of the audio and video sections of exercise 73 and the demonstrations in episode 7. Video output may be segmented; a single flat response is not guaranteed. ${REVIEW_CONTEXT}`,
  },
  {
    id: 'content-understanding-api',
    title: 'Call an analyzer safely',
    cluster: 'content-document',
    examDomain: 'Implement information extraction solutions',
    summary: 'Connect to the right resource, separate analyzer identity from operation identity, and wait for typed results.',
    minutes: 9,
    order: 220,
    prerequisites: ['content-understanding-analyzer'],
    sourceIds: [138, 184, 29, 6, 250, 67, 160, 201, 7],
    steps: [
      {
        title: 'Connect to the processing resource',
        body: 'The Content Understanding client needs the Foundry resource endpoint and valid authentication. The API lessons use an endpoint and API key; the episode also demonstrates Microsoft Entra ID credentials. A project endpoint used for agent work is not interchangeable with this resource endpoint. Resource setup includes the required model deployments or defaults. Authentication proves the caller can connect; it does not create an analyzer, deploy missing models or validate the submitted file.',
        example: 'A fictional service-card app receives an authentication error before any fields return. Check its resource endpoint and credential configuration before changing the extraction schema.',
      },
      {
        title: 'Build once, analyze many inputs',
        body: 'Creating an analyzer and using it are separate operations. Creation submits a definition containing the base analyzer, fields and relevant model settings, then waits until the reusable analyzer is ready. Analysis supplies that analyzer identifier plus a supported file URL or uploaded bytes. The creation operation identifier tracks a particular job; it is not the analyzer name required for later analysis. Give experimental analyzers unique names instead of replacing a shared processor unintentionally.',
        example: 'An analyzer named service-card-lab can process two sample cards. The first card analysis has its own operation ID, which must not become the analyzer name for the second.',
      },
      {
        title: 'Accepted does not mean finished',
        body: 'Analysis is a long-running operation. A REST request starts work and returns an operation identifier or status location; subsequent requests check status until success or failure. The Python SDK packages that pattern into a poller. Calling result() waits for completion and returns the final result, rather than proving the work finished when the initial call returned. In this short pattern, client, analyzer_name and sample_url have already been configured; connection setup is deliberately outside the conceptual core.',
        example: 'If a recording is still being processed, an empty final-fields view is not evidence that the recording contains no information.',
        code: 'from azure.ai.contentunderstanding.models import AnalysisInput\n\npoller = client.begin_analyze(\n    analyzer_id=analyzer_name,\n    inputs=[AnalysisInput(url=sample_url)],\n)\nresult = poller.result()',
      },
      {
        title: 'Read fields according to their types',
        body: 'Results depend on the analyzer, its schema and the input. Content objects can include fields, Markdown and metadata. A string field, an array and a numeric value require different handling; do not flatten all results into strings. The REST examples use type-specific values such as valueString, while SDK properties depend on the installed version. Check for absent fields and failed operations before consuming values. Compare one parsed result with the source before handing it to another workflow.',
        example: 'A card without a phone number should remain incomplete. A client that prints the literal text “None” as a phone number has a parsing problem, not a successful extraction.',
      },
    ],
    lab: {
      title: 'Rehearse the asynchronous contract',
      goal: 'Keep reusable analyzer identity separate from the status of one analysis job.',
      context: 'Local conceptual configuration only, not executable Python or a REST payload. Use the source exercise code for the live request.',
      fields: [
        {
          id: 'cuApiIdentifier',
          label: 'Identifier sent with a new file',
          hint: 'The reusable analyzer already exists.',
          options: [
            { value: 'last-operation-id', label: 'Previous analysis operation ID' },
            { value: 'analyzer-name', label: 'Built analyzer name' },
            { value: 'resource-group-name', label: 'Resource group name' },
          ],
          expected: 'analyzer-name',
          explanation: 'The analyzer name selects the processing contract; an operation ID only tracks a particular job.',
        },
        {
          id: 'cuApiCompletion',
          label: 'When to parse final fields',
          hint: 'The initial request was accepted, but the operation may still be running.',
          options: [
            { value: 'on-acceptance', label: 'Immediately on acceptance' },
            { value: 'after-success', label: 'After successful completion' },
            { value: 'after-fixed-second', label: 'After exactly one second' },
          ],
          expected: 'after-success',
          explanation: 'Completion must be established through the poller or status response, not inferred from elapsed time.',
        },
      ],
      template: '{\n  "localConcept": "analysis lifecycle",\n  "processorIdentifier": "{{cuApiIdentifier}}",\n  "consumeFields": "{{cuApiCompletion}}"\n}',
      language: 'json',
      expectedOutput: 'Expected synthetic trace, not a service response: choose analyzer → submit sample → wait for terminal status → inspect typed fields on success or report the failure.',
      takeaway: 'One identifier chooses the reusable capability; another tracks the work currently in progress.',
      azure: {
        title: 'Create and call the business-card analyzer',
        sourceId: 67,
        environment: 'portal-and-code',
        minutes: 40,
        cost: CU_COST,
        prerequisites: [
          ACCOUNT_BOUNDARY,
          'A supported-region Foundry resource and model-deployment quota. Use only the two fictional business-card samples supplied by exercise 67.',
          'VS Code, Git and the Python version and dependencies specified by the linked exercise. The application code runs in your environment, not inside this tutor.',
        ],
        steps: [
          {
            title: 'Create or inspect the project',
            surface: 'Microsoft Foundry',
            instruction: 'Open https://ai.azure.com > project selector > Create new project, using Advanced options for a dedicated lab group and supported region. If reusing your own lab project, inspect Project details and follow its parent-resource link instead.',
            expected: 'You know the specific parent Foundry resource that will host the analyzer.',
            hint: 'Do not select an existing team project merely because it already has model quota.',
          },
          {
            title: 'Configure analyzer dependencies',
            surface: 'Browser',
            instruction: 'In Content Understanding Studio > Settings > Add resource, select that parent resource. Review the required models and auto-deployment option, then Next > Save and wait for setup. Follow the model configuration in the launched exercise.',
            expected: 'Required model defaults are configured before the client attempts analyzer creation.',
            hint: 'A missing deployment is a setup failure, not a reason to repeatedly submit the same analysis request.',
          },
          {
            title: 'Inspect the connection boundary',
            surface: 'Azure portal',
            instruction: 'Open the parent Foundry resource > Resource Management > Keys and Endpoint. Locate its resource endpoint and a key for this isolated lab, as the exercise requires. Keep them only in the local configuration you will create, never in a screenshot, source commit or tutor response.',
            expected: 'The selected endpoint and authentication belong to the resource you intend to call.',
            hint: 'This exercise uses key authentication; the episode also shows Entra ID. Do not mix unrelated setup examples.',
          },
          {
            title: 'Prepare the supplied starter',
            surface: 'Cloud Shell / local editor',
            instruction: 'In VS Code follow exercise 67 to clone MicrosoftLearning/mslearn-ai-information-extraction and open Labfiles\\02-content-understanding-api. Create the documented virtual environment and install its requirements. Set the endpoint, key and a unique lab ANALYZER_NAME in the local .env file.',
            expected: 'The provided starter points only to your lab resource and contains the two sample card images.',
            hint: 'Keep .env untracked. Do not paste its contents into the tutor or a troubleshooting ticket.',
          },
          {
            title: 'Create the reusable analyzer',
            surface: 'Cloud Shell / local editor',
            instruction: 'Inspect biz-card.json and its Company, Name, Title, Email and Phone fields. Complete create-analyzer.py with the code in the source exercise, then run it. Wait for the creation poller result before continuing; ensure any allow_replace setting can affect only your uniquely named lab analyzer.',
            expected: 'Analyzer creation completes successfully under the selected lab name.',
            hint: 'Keep the analyzer name; do not substitute the creation operation ID when analyzing content.',
          },
          {
            title: 'Analyze and inspect one card',
            surface: 'Cloud Shell / local editor',
            instruction: 'Complete read-card.py using the source exercise code, including its binary-input analysis call and poller.result(). Run it on biz-card-1.png. Open results.json and compare a returned field with the fictional card, checking its type and any source metadata.',
            expected: 'A completed real analysis returns fields from the first sample rather than only an accepted-operation response.',
            hint: 'The URL-based teaching snippet and the exercise binary-input path are alternatives, not interchangeable argument lists.',
          },
          {
            title: 'Change only the input file',
            surface: 'Cloud Shell / local editor',
            instruction: 'Run the same client against biz-card-2.png without changing the analyzer or schema. Compare one value and one field type. If a field is absent, inspect it as missing rather than inventing a value or crashing while formatting it.',
            expected: 'The reusable analyzer handles a new card, while the returned values reflect that input and keep the field contract.',
            hint: 'Stop here with a short explanation of the lifecycle; a complete JSON dump is not needed as evidence.',
          },
        ],
        success: 'You created a named analyzer through the source client, waited for completion, and inspected typed results for two sample inputs.',
        evidencePrompt: 'Explain which identifier chose the analyzer, what proved completion, and how one field changed between the samples. Never paste keys, endpoints, .env contents or business-card images.',
        cleanup: OWNED_CLEANUP,
      },
    },
    check: {
      question: 'A client submits a second card using the operation ID from the first analysis as analyzer_id. Why is this wrong?',
      options: [
        { id: 'cu-api-operation', text: 'An operation ID tracks one job; the built analyzer name selects the reusable processor.', explanation: 'The two identifiers address different resources and lifecycles. Reuse the analyzer name, not the previous job ID.' },
        { id: 'cu-api-retrain', text: 'Every card requires retraining the underlying model first.', explanation: 'The built analyzer is designed for reuse across supported inputs; per-card model retraining is not the analysis workflow.' },
        { id: 'cu-api-sync', text: 'The service can only be used synchronously.', explanation: 'The service uses long-running operations; the SDK poller makes waiting convenient without changing that fact.' },
      ],
      correctOptionId: 'cu-api-operation',
      explanation: 'Keep the processor contract, a submitted job and that job’s result separate in both code and troubleshooting.',
    },
    recall: {
      prompt: 'Reconstruct the client lifecycle from resource setup to a parsed field, without recalling exact import syntax.',
      answer: 'Configure the resource and models, authenticate a client, create or select a built analyzer, submit a supported input, wait for success or handle failure, then inspect the returned content and type-specific fields.',
    },
    transfer: 'A customer reports “the API works but fields are empty.” Propose the smallest checks that separate wrong endpoint, unfinished operation, wrong analyzer and missing source evidence before changing the schema.',
    sourceNote: `The short SDK pattern follows source 6, verified against its Microsoft Learn code example after a Microsoft code-sample search. Exercise 67 uses begin_analyze_binary instead. Use each exercise with its SDK/API version; AnalysisInput, binary-input arguments and field accessors must not be mixed across examples. ${REVIEW_CONTEXT}`,
  },
  {
    id: 'document-intelligence-overview',
    title: 'Recover document structure',
    cluster: 'content-document',
    examDomain: 'Implement information extraction solutions',
    summary: 'Understand what OCR preserves and where Document Intelligence fits between files and business workflows.',
    minutes: 6,
    order: 225,
    prerequisites: ['content-understanding-overview'],
    sourceIds: [117, 263, 251, 54],
    steps: [
      {
        title: 'Words alone lose relationships',
        body: 'A scanned form is an image, not a ready-made business record. Reading its words helps, but software also needs relationships: which value belongs to which label, which cells share a row, and whether a checkbox is selected. Azure Document Intelligence combines OCR with document models to return text and structure. Bounding regions connect detected content with its position on a page, allowing your application or a reviewer to check what the service actually read.',
        example: 'A fictional workshop checklist contains two prices and two checkboxes. A bag of recognized words cannot tell you which service was selected or which price belongs to it.',
      },
      {
        title: 'Select the document responsibility',
        body: 'Document Intelligence provides document-analysis models, prebuilt models for common document categories, and custom models for particular business forms. Read extracts text; Layout adds structure; a domain-specific model identifies fields such as an invoice total. This service is about document extraction, not recording analysis or writing a conversational answer. Content Understanding overlaps on documents but also supports broader multimodal schema-led analysis. Choose by required output and evidence, not merely because both services can accept an image.',
        example: 'A photograph of an invoice can be a Document Intelligence input. Its being an image does not make a general scene-description analyzer the best choice.',
      },
      {
        title: 'Connect a resource and valid input',
        body: 'Use a dedicated Document Intelligence resource when this is the only service you need, or a supported Foundry resource when combining tools. Studio provides a visual way to test models and inspect results before writing code. Validate the input before blaming extraction: the corpus lists supported image and PDF formats, disallows password-protected PDFs, and gives different free and standard size limits. A successful upload still needs comparison with the source before any downstream workflow treats fields as authoritative.',
        example: 'A password-protected sample failing to analyze is an input-access problem. Changing from Read to a custom neural model does not remove the password.',
      },
    ],
    lab: {
      title: 'Rehearse the document boundary',
      goal: 'Distinguish readable text from document relationships and select an appropriate resource scope.',
      context: 'Local conceptual configuration only; it neither creates resources nor submits a document.',
      fields: [
        {
          id: 'diOverviewNeed',
          label: 'Required document output',
          hint: 'The application must retain table rows and checkbox states.',
          options: [
            { value: 'text-only', label: 'Only recognized words' },
            { value: 'layout', label: 'Text and layout relationships' },
            { value: 'new-document', label: 'A newly generated document' },
          ],
          expected: 'layout',
          explanation: 'Layout relationships preserve information that a text-only representation can lose.',
        },
        {
          id: 'diOverviewResource',
          label: 'Resource scope',
          hint: 'This isolated application needs only Document Intelligence.',
          options: [
            { value: 'search-index', label: 'A Search index alone' },
            { value: 'dedicated-document', label: 'Dedicated Document Intelligence resource' },
            { value: 'chat-deployment-only', label: 'Only a chat-model deployment' },
          ],
          expected: 'dedicated-document',
          explanation: 'A dedicated resource supplies the document-analysis capability without introducing unrelated services.',
        },
      ],
      template: '{\n  "localConcept": "document extraction",\n  "outputNeed": "{{diOverviewNeed}}",\n  "resourceScope": "{{diOverviewResource}}"\n}',
      language: 'json',
      expectedOutput: 'Expected synthetic result, not a service response: table cells retain row relationships and a selected checkbox remains associated with its location.',
      takeaway: 'The essential output is often a relationship, not just the text that OCR recognized.',
      azure: {
        title: 'Inspect Read results in Document Intelligence Studio',
        sourceId: 54,
        environment: 'azure-portal',
        minutes: 25,
        cost: 'Use Free F0 only if available and sufficient. Standard S0, extra pages and optional add-on features can incur charges. Check current limits before analyzing; a free resource is not an unlimited document-processing allowance.',
        prerequisites: [
          ACCOUNT_BOUNDARY,
          'Permission to create a Document Intelligence resource and connect it to Document Intelligence Studio. Use the built-in read-german.pdf sample from the launched exercise.',
        ],
        steps: [
          {
            title: 'Create the document resource',
            surface: 'Azure portal',
            instruction: 'At https://portal.azure.com, choose Create a resource and search for Document Intelligence. Create it in a dedicated lab group, using Free F0 if available. If only a paid tier is offered, inspect the price and decide explicitly before proceeding.',
            expected: 'A deployed Document Intelligence resource belongs to your isolated lab group.',
            hint: 'Exercise 54 can also create the resource from Studio Settings; either route must select the same owned resource.',
          },
          {
            title: 'Connect Studio',
            surface: 'Browser',
            instruction: 'Open the Document Intelligence Studio link in exercise 54. In Settings > Resource, select the resource you just created. If the older documentintelligence.ai.azure.com address redirects to the Content Understanding host, stay in the Document Intelligence Studio area.',
            expected: 'Studio is connected to your intended document resource, not a Content Understanding custom project.',
            hint: 'Keep endpoint and key entry inside the trusted Studio if requested; never copy them into this tutor.',
          },
          {
            title: 'Run the supplied Read sample',
            surface: 'Browser',
            instruction: 'On the Studio home page, choose Document analysis > Read and select read-german.pdf from the sample list. Run analysis with its initial options and compare a visible line in the document with the Content tab.',
            expected: 'Actual recognized text is available for a line-by-line comparison.',
            hint: 'Use a short line you can inspect visually; you do not need to understand every word to compare the characters.',
          },
          {
            title: 'Inspect the returned structure',
            surface: 'Browser',
            instruction: 'Open the Result tab and locate the selected page, line or span in the JSON. Inspect its location information where returned. Distinguish the recognized content from metadata describing where that content occurred.',
            expected: 'You can explain how the result relates back to a particular part of the original document.',
            hint: 'Do not assume a printed label automatically becomes a domain-specific field.',
          },
          {
            title: 'Change one analysis option',
            surface: 'Browser',
            instruction: 'Open Analyze options and enable Language under Optional detection, as the linked exercise directs. Save and rerun the same document. If already enabled, first compare an off run with an on run without changing anything else.',
            expected: 'The new result includes language information you can inspect alongside the extracted text.',
            hint: 'Review any cost notice before enabling optional features; use only the option required for this comparison.',
          },
          {
            title: 'Check language evidence',
            surface: 'Browser',
            instruction: 'In the Result JSON, inspect the returned language entries and their spans, including German and any English portion in the sample. Explain why the detected language does not itself identify invoice totals or table relationships.',
            expected: 'You have an observed option-to-output connection and a clear boundary for what Read supplied.',
            hint: 'Record the property names actually returned by your version rather than assuming the screenshot schema is identical.',
          },
        ],
        success: 'You connected an owned document resource, inspected real Read output and observed the effect of enabling language detection.',
        evidencePrompt: 'Describe one text-versus-metadata distinction and one result of the option change. Use only the built-in sample; do not send documents, credentials or account information.',
        cleanup: OWNED_CLEANUP,
      },
    },
    check: {
      question: 'An application needs rows and selected checkboxes from a scanned inspection form. Recognized words alone lose which item passed. What is the missing capability?',
      options: [
        { id: 'di-overview-summary', text: 'A longer generated summary of the words.', explanation: 'A summary can omit or infer relationships and is not a replacement for extracting the actual form structure.' },
        { id: 'di-overview-layout', text: 'Document layout and selection-mark extraction.', explanation: 'Structural output preserves the row and checkbox relationships needed to interpret the form.' },
        { id: 'di-overview-speech', text: 'Speech synthesis of the recognized text.', explanation: 'Reading the words aloud changes presentation, not the missing document relationships.' },
      ],
      correctOptionId: 'di-overview-layout',
      explanation: 'The requirement is structural extraction. Match the output to the lost relationship instead of adding another presentation layer.',
    },
    recall: {
      prompt: 'Explain what a bounding region contributes beyond the recognized word.',
      answer: 'It identifies where the content was found on the page, helping preserve layout relationships and allowing a person or application to verify the extraction against the source.',
    },
    transfer: 'A customer wants to digitize scanned inspection forms. Ask whether they need text, structural relationships or specific business fields, then describe the smallest document-analysis experiment that would answer that need.',
    sourceNote: `Source 251 names the original Document Intelligence Studio address; the current linked exercise also uses the Studio under contentunderstanding.ai.azure.com. These are document-analysis surfaces, not the custom Content Understanding analyzer editor. ${REVIEW_CONTEXT}`,
  },
  {
    id: 'document-intelligence-models',
    title: 'Choose a document model',
    cluster: 'content-document',
    examDomain: 'Implement information extraction solutions',
    summary: 'Distinguish Read, Layout and domain-specific prebuilt fields before investing in custom training.',
    minutes: 8,
    order: 230,
    prerequisites: ['document-intelligence-overview'],
    sourceIds: [249, 251, 165, 54],
    steps: [
      {
        title: 'Read versus Layout',
        body: 'Read focuses on printed and handwritten text. Layout adds tables, selection marks and document structure, so it is appropriate when relationships between content matter but no specific business-field schema is required. The corpus describes table cells with positions and row or column indexes, and selection marks with their states. Layout can also enable optional key-value extraction. Neither a table nor a nearby text label guarantees that a domain-specific field such as InvoiceTotal will be returned.',
        example: 'A mixed collection of research forms needs table rows preserved. Layout is a better first fit than an invoice model simply because some forms contain numbers.',
      },
      {
        title: 'A prebuilt model knows a category',
        body: 'A domain-specific prebuilt model is already trained to recognize a common document category, such as invoices or receipts. That category gives meaning to named fields beyond generic text and positions. Start by checking whether the required category is covered before collecting training data. The model still needs a compatible document and output verification; a successful request does not mean every field was present or every supplier format was understood equally well.',
        example: 'A fictional invoice processor needs VendorName and InvoiceTotal. A prebuilt invoice model provides those domain fields without first labeling a custom training set.',
      },
      {
        title: 'Options change what is analyzed',
        body: 'Configuration can narrow or extend a request without changing the business goal. For supported multipage documents, a pages parameter limits the analyzed range. Layout can add keyValuePairs when needed, and other add-ons may have extra charges. Choose options because the output requires them, not because more checkboxes seem safer. The older general document model mentioned in historical material was deprecated; the corpus places its relevant functionality in Layout and other features instead.',
        example: 'If only the first two pages contain the requested form, a page range can avoid analyzing a large irrelevant appendix. It does not improve the handwriting on those two pages.',
      },
      {
        title: 'Inspect before integrating',
        body: 'Studio exposes model results visually; a client application receives structured output through the service API. The exercise Python client starts document analysis, waits on a poller and then inspects returned document fields. Preserve type information: currency is not simply an arbitrary string, and missing fields need an explicit path. Compare a known sample with both the visible document and the returned field, including confidence where provided, before using it in a payment or reporting workflow.',
        example: 'A result may include an amount and currency metadata for InvoiceTotal. Dropping the currency or treating an absent field as zero changes the meaning of the record.',
      },
    ],
    lab: {
      title: 'Rehearse model selection',
      goal: 'Choose a model because of its output contract, not because it accepts the file extension.',
      context: 'Local conceptual configuration only, not Document Intelligence request JSON.',
      fields: [
        {
          id: 'diModelsInvoice',
          label: 'Invoice-processing model',
          hint: 'Vendor and total fields are needed from common invoice formats.',
          options: [
            { value: 'read', label: 'Read' },
            { value: 'prebuilt-invoice', label: 'Prebuilt invoice' },
            { value: 'train-immediately', label: 'Immediately train a custom model' },
          ],
          expected: 'prebuilt-invoice',
          explanation: 'The domain-specific model supplies invoice field semantics without a custom training phase.',
        },
        {
          id: 'diModelsTables',
          label: 'Unfamiliar tables-only forms',
          hint: 'The requirement is text and table structure, not invoice labels.',
          options: [
            { value: 'layout', label: 'Layout' },
            { value: 'receipt', label: 'Receipt' },
            { value: 'identity-document', label: 'Identity document' },
          ],
          expected: 'layout',
          explanation: 'Layout preserves structure across varying documents without imposing the wrong document category.',
        },
      ],
      template: '{\n  "localConcept": "document model boundaries",\n  "invoiceFields": "{{diModelsInvoice}}",\n  "generalTables": "{{diModelsTables}}"\n}',
      language: 'json',
      expectedOutput: 'Expected synthetic result, not a service response: invoice processing yields a named total; general form processing yields text and tables without promising that invoice field.',
      takeaway: 'Generic structure and category-specific fields are different contracts even when they come from the same PDF.',
      azure: {
        title: 'Compare invoice fields with generic layout',
        sourceId: 54,
        environment: 'portal-and-code',
        minutes: 35,
        cost: 'Document analysis is metered by tier and usage; add-on features can cost extra. Use one supplied sample and Free F0 where available. Review paid-tier pricing before provisioning or enabling optional features.',
        prerequisites: [
          ACCOUNT_BOUNDARY,
          'Your own Document Intelligence resource connected to Studio, or the resource-creation steps of exercise 54. Use only its sample invoice.',
          'VS Code and the Python environment required by the prebuilt section of the linked exercise for the final client step.',
        ],
        steps: [
          {
            title: 'Inspect your resource',
            surface: 'Azure portal',
            instruction: 'Open your lab Document Intelligence resource and confirm its group and pricing tier. If it does not exist, create the dedicated resource using exercise 54 before connecting Studio.',
            expected: 'The resource used for this comparison is owned by you and has an understood billing tier.',
            hint: 'Do not upgrade a shared resource to make a sample succeed.',
          },
          {
            title: 'Run the invoice model',
            surface: 'Browser',
            instruction: 'In Document Intelligence Studio, select Prebuilt models > Invoice, confirm your resource, and load the sample invoice from exercise 54. Select Run analysis and wait for the result.',
            expected: 'The result contains invoice-oriented fields when the sample supports them.',
            hint: 'Use the sample document or its public exercise URL, never a customer invoice.',
          },
          {
            title: 'Verify one business field',
            surface: 'Browser',
            instruction: 'Select InvoiceTotal and VendorName in the field view, then inspect the corresponding JSON. Compare the amount, currency information and vendor with the visible invoice; examine confidence where returned.',
            expected: 'You can point to actual named fields and describe their types and evidence.',
            hint: 'An absent field is a result to investigate, not permission to fill it from expectation.',
          },
          {
            title: 'Change only the selected model',
            surface: 'Browser',
            instruction: 'Return to the Studio home page, choose Document analysis > Layout and analyze the identical sample with default options. Keep the document constant so the selected model is the comparison variable.',
            expected: 'The new result emphasizes text, tables and structure rather than the invoice-specific field contract.',
            hint: 'Seeing the total as text inside a table is not the same as receiving InvoiceTotal.',
          },
          {
            title: 'Compare the result contracts',
            surface: 'Browser',
            instruction: 'Inspect a table cell in the Layout output and compare it with the invoice-model field view. Write one sentence explaining which output your hypothetical invoice-payment workflow actually needs.',
            expected: 'You can justify a model choice using an observed structural versus semantic difference.',
            hint: 'If your version exposes optional key-value extraction, leave it unchanged for this comparison.',
          },
          {
            title: 'Use the source invoice client',
            surface: 'Cloud Shell / local editor',
            instruction: 'Follow exercise 54 in Labfiles\\03-document-intelligence\\prebuilt\\Python. Complete document-analysis.py using the supplied SDK code, configure the owned resource locally, and run the prebuilt-invoice example. Compare its vendor and total output with your Studio observations.',
            expected: 'The actual SDK analysis returns the named fields your application would consume after waiting for completion.',
            hint: 'Keep credentials local and untracked. Document Intelligence result handling is not interchangeable with Content Understanding contents parsing.',
          },
        ],
        success: 'You compared two models on the same invoice, verified a named business field and reproduced the prebuilt analysis with the source client.',
        evidencePrompt: 'Explain why Layout or Invoice fits the example and name the type of one returned value. Do not paste invoices, endpoints, keys or full JSON.',
        cleanup: OWNED_CLEANUP,
      },
    },
    check: {
      question: 'A records team receives many form layouts. It needs text, tables and checkbox states, but no category-specific business fields. What should it try first?',
      options: [
        { id: 'di-models-invoice', text: 'Prebuilt invoice, because all forms contain numbers.', explanation: 'Numbers do not make a form an invoice; invoice semantics would not match this general structural requirement.' },
        { id: 'di-models-train', text: 'A custom model before inspecting any prebuilt capabilities.', explanation: 'Training adds data and maintenance work before establishing whether the required generic output is already available.' },
        { id: 'di-models-layout', text: 'Layout, because the required output is document structure.', explanation: 'Layout supplies tables, selection marks and text without requiring a specific document category.' },
      ],
      correctOptionId: 'di-models-layout',
      explanation: 'The deciding clue is that no labeled business-field schema is required. Start with the model that already exposes the needed structure.',
    },
    recall: {
      prompt: 'Describe one requirement for Read, one for Layout and one for a category-specific prebuilt model.',
      answer: 'Read suits text extraction. Layout suits text plus tables, selection marks and structure. A prebuilt invoice model suits named invoice fields such as vendor and total without custom training.',
    },
    transfer: 'A customer proposes custom training for ordinary expense receipts. Explain the cheaper first experiment, what you would measure, and what evidence would justify moving beyond a prebuilt model.',
    sourceNote: `Source 249 explicitly deprecates the older general document model and describes Layout with optional key-value extraction instead. The linked exercise currently estimates longer than the short Learn wrapper; the lab estimate is separate from teaching time. ${REVIEW_CONTEXT}`,
  },
  {
    id: 'document-intelligence-custom',
    title: 'Train for your own forms',
    cluster: 'content-document',
    examDomain: 'Implement information extraction solutions',
    summary: 'Choose template or neural extraction, preserve training evidence, and separate classification from field extraction.',
    minutes: 9,
    order: 235,
    prerequisites: ['document-intelligence-models'],
    sourceIds: [224, 251, 165, 220, 54],
    steps: [
      {
        title: 'Train only for the unmet need',
        body: 'A custom extraction model learns from documents labeled with the fields your business needs. Choose it when prebuilt models do not cover the form or fail the required extraction task. Custom template models exploit a consistent visual layout and are fast to train. Custom neural models combine layout and language information and better suit varied or less structured documents, at greater training cost and time. More model complexity is not automatically the best first choice.',
        example: 'A workshop uses one fixed maintenance form every day. Start with a template model; varied supplier reports with the same logical fields make the neural option more relevant.',
      },
      {
        title: 'Labels explain the target',
        body: 'The corpus describes sample forms plus OCR layout, field definitions and per-document labels for the training workflow. Document Intelligence Studio manages the corresponding ocr.json, fields.json and labels.json artifacts while you label visually. Use at least five to six representative forms as the corpus starting point, not as a universal guarantee of accuracy. Keep a separate test form outside training so you can assess whether the model learned the intended fields on a new instance.',
        example: 'Label Merchant consistently across sample purchase forms. If some labels point to the purchaser instead, additional training reinforces a contradictory target.',
      },
      {
        title: 'Route first, extract second',
        body: 'Classification answers which document type arrived; extraction answers what values that type contains. A custom classifier can route different incoming forms to appropriate extraction models. A composed model groups multiple custom extraction models behind one analysis choice, allowing the service to select an appropriate component and return its fields. These are routing mechanisms, not a reason to train one giant template across unrelated layouts. Preserve the link between the selected category and its expected field contract.',
        example: 'A mailroom receives repair authorizations and purchase requests. Routing identifies the form family before the corresponding model extracts its approval or purchasing fields.',
      },
      {
        title: 'Hold the test constant',
        body: 'Inspect training metrics, then analyze a document that was not used to train or label the model. Check actual field values and locations, not just whether the request succeeded. When comparing template and neural modes, keep the training set and held-out input fixed so the build mode is the principal change. A tiny demonstration cannot establish production accuracy. Use observed failures to decide whether labels, sample diversity, model choice or review policy needs attention.',
        example: 'If both model types miss a cropped account number, the missing source evidence may be the cause. Switching model families cannot guarantee recovery of invisible text.',
      },
    ],
    lab: {
      title: 'Rehearse custom-model choices',
      goal: 'Match training strategy to variation and keep routing distinct from extraction.',
      context: 'Local conceptual configuration only, not a model-build API request.',
      fields: [
        {
          id: 'diCustomBuild',
          label: 'First build mode',
          hint: 'The labeled workshop forms all use the same fixed layout.',
          options: [
            { value: 'template', label: 'Custom template' },
            { value: 'neural-first', label: 'Neural without a baseline' },
            { value: 'receipt', label: 'Unrelated receipt model' },
          ],
          expected: 'template',
          explanation: 'The stable layout matches the template model boundary and provides a lower-cost baseline.',
        },
        {
          id: 'diCustomRouting',
          label: 'Several form families',
          hint: 'The system must select a suitable extractor before reading its fields.',
          options: [
            { value: 'plain-ocr', label: 'Plain OCR alone' },
            { value: 'classifier-or-composed', label: 'Classifier routing or composed models' },
            { value: 'one-field-list', label: 'One unstructured field list' },
          ],
          expected: 'classifier-or-composed',
          explanation: 'Routing connects document category to extraction behavior; reading text alone does not perform that selection.',
        },
      ],
      template: '{\n  "localConcept": "custom form processing",\n  "fixedLayoutBaseline": "{{diCustomBuild}}",\n  "multipleFormFamilies": "{{diCustomRouting}}"\n}',
      language: 'json',
      expectedOutput: 'Expected synthetic result, not a service response: a fixed workshop form goes to a template extractor; a different form family is routed to its own extraction contract.',
      takeaway: 'Model choice, label quality and routing solve different problems. Keep them separate when diagnosing an error.',
      azure: {
        title: 'Train and compare owned custom models',
        sourceId: 54,
        environment: 'azure-portal',
        minutes: 50,
        cost: 'Document analysis, storage and some training or premium capabilities can incur charges. A neural comparison can take longer and use more resources than a template baseline. Inspect your tier and current allowances before each build; never upgrade a shared resource for this lab.',
        prerequisites: [
          ACCOUNT_BOUNDARY,
          'An owned Document Intelligence resource and private Blob Storage account. Access to configure storage permissions and Studio CORS where required.',
          'The labeled sample forms and separate test form from the custom-model section of exercise 54. Keep the held-out test form out of the training container.',
        ],
        steps: [
          {
            title: 'Prepare isolated model resources',
            surface: 'Azure portal',
            instruction: 'Create or inspect your lab Document Intelligence resource and a Standard LRS Storage account in the lab group. In Storage browser > Blob containers create a private sampleforms container for this exercise only.',
            expected: 'Your training data has an isolated location and the document resource is ready.',
            hint: 'Use your own container even if a shared account is offered. Prefer a dedicated lab account for cleanup safety.',
          },
          {
            title: 'Select training and test examples',
            surface: 'Browser',
            instruction: 'Open the custom-model section of exercise 54 and obtain its sample-forms files from the linked MicrosoftLearning repository. Inspect the supplied form and labeling files. Set aside the separate test image referenced by the exercise; do not upload it as training data.',
            expected: 'You can distinguish the representative labeled training forms from an untouched test input.',
            hint: 'The corpus recommends at least five to six sample forms. If the downloaded set is incomplete, obtain the full supplied set before training.',
          },
          {
            title: 'Upload and connect the data',
            surface: 'Azure portal',
            instruction: 'Upload the sample forms and their provided label artifacts to the private sampleforms container. Configure only the storage access and Studio-origin CORS needed by the documented Studio workflow; do not enable anonymous access to solve a permissions error.',
            expected: 'The sample container holds the complete training artifacts and can be accessed by your Studio project.',
            hint: 'The source exercise also supplies setup.sh. This portal-first adaptation performs the storage setup explicitly instead of running that resource-creation script blindly.',
          },
          {
            title: 'Inspect the field labels',
            surface: 'Browser',
            instruction: 'In Document Intelligence Studio choose Custom models > Custom extraction model > Create project. Link your document resource and sampleforms container. Inspect Merchant and the other supplied field labels on several forms; correct any inconsistent labeling in your lab copy before training.',
            expected: 'The project has consistent field definitions and document-level labels.',
            hint: 'Studio manages the OCR and label artifacts. Do not rename the files arbitrarily to make them look tidier.',
          },
          {
            title: 'Build the template baseline',
            surface: 'Browser',
            instruction: 'Select Train, give the model a unique lab ID and choose Template build mode as in exercise 54. Open Models and wait for succeeded status. Review the available field metrics before selecting Test.',
            expected: 'A trained template model exists with an identifiable model ID and metrics.',
            hint: 'Training success means a model was built; it is not a claim of reliable extraction on every form.',
          },
          {
            title: 'Test a held-out form',
            surface: 'Browser',
            instruction: 'Use the Studio Test experience described in source 251 with the held-out form. Inspect an actual field value, location and confidence where returned. Compare it with the form and record a correct value and any uncertainty.',
            expected: 'You have evidence from a form that was not used to tune the model.',
            hint: 'Do not add the held-out form to training simply to improve this one test result.',
          },
          {
            title: 'Change only build mode',
            surface: 'Browser',
            instruction: 'If supported within your approved lab quota, train a second uniquely named model from the same labeled set using Neural build mode. Test the identical held-out form and compare field values, metrics and elapsed time. Keep the original template model for comparison.',
            expected: 'You can describe the observed trade-off without assuming the more complex model must win on this small sample.',
            hint: 'If the tier or quota prevents the comparison, record that constraint and retain the verified template result; do not claim a neural test ran.',
          },
        ],
        success: 'You trained and tested a custom model using separate training and test data, and either compared build modes or explicitly recorded the quota constraint preventing that comparison.',
        evidencePrompt: 'Explain the baseline model choice, one label you checked and one held-out observation. If a comparison was blocked, say so. Do not provide forms, SAS URLs, keys or full output files.',
        cleanup: OWNED_CLEANUP,
      },
    },
    check: {
      question: 'One fixed-layout authorization form needs custom fields. A different purchase-request form must use a separate extractor. Which design matches both requirements?',
      options: [
        { id: 'di-custom-layout-all', text: 'Use Read for every form and assume the same field positions.', explanation: 'Read does not supply the custom business-field contract, and different form families need not share positions.' },
        { id: 'di-custom-template-route', text: 'Start with template extraction for the stable form and use classification or composition for routing.', explanation: 'The stable layout fits template extraction, while routing selects the correct extractor for each form family.' },
        { id: 'di-custom-neural-routing', text: 'Choose neural mode and assume that automatically defines all routing rules.', explanation: 'Neural extraction addresses variation within an extraction task; document routing remains a distinct responsibility.' },
      ],
      correctOptionId: 'di-custom-template-route',
      explanation: 'Solve the extraction problem and the form-selection problem explicitly instead of treating model complexity as a substitute for architecture.',
    },
    recall: {
      prompt: 'Why do you need both representative labeled samples and a held-out form?',
      answer: 'Labeled samples define what the model should learn. A held-out form checks behavior on a new instance; reusing the training samples alone cannot establish generalization.',
    },
    transfer: 'A customer says a five-form demonstration proves readiness for all suppliers. Explain what it does prove, what it does not, and how varied held-out documents and human review would support a responsible rollout.',
    sourceNote: `The portal-first storage, labeling and testing steps follow source 251; exercise 54 supplies the sample set and template baseline. Neural comparison is an explicit extension grounded in source 224, not a claim that the original exercise proves superior accuracy. ${REVIEW_CONTEXT}`,
  },
  {
    id: 'ai-search-overview',
    title: 'Build a searchable index',
    cluster: 'search-rag',
    examDomain: 'Implement information extraction solutions',
    summary: 'Separate stored source files, the process that ingests them, and the index that applications query.',
    minutes: 7,
    order: 240,
    prerequisites: ['foundry-projects'],
    sourceIds: [130, 261, 92, 64],
    steps: [
      {
        title: 'Create a useful retrieval representation',
        body: 'A document store keeps files; a search index keeps a representation designed to find relevant information efficiently. Azure AI Search can index structured records and content extracted from less structured sources. Applications query the index rather than opening every original document for every question. Search can serve a website, a knowledge-mining process or a generative application. Returning a matching record is still retrieval, not generating a conversational answer or changing the underlying source file.',
        example: 'A fictional travel desk needs brochures mentioning accessible transport. A search index makes those descriptions findable without an employee reading every PDF on each request.',
      },
      {
        title: 'Keep the three core roles separate',
        body: 'The data source describes where original data lives and how the ingestion process can access it. An indexer runs the extraction and population process, optionally on a schedule. The index is the persistent collection of searchable records and field definitions that clients query. Document cracking obtains content and metadata from supported source files. The module uses an indexer-based workflow; the conceptual point is the separation between ingestion work and the queryable output, not that an index is itself a scheduled job.',
        example: 'A private Blob container holds six sample brochures. The indexer reads them; the resulting index contains their searchable content and metadata.',
      },
      {
        title: 'Give each field a purpose',
        body: 'Index fields have different capabilities. A key identifies a record, searchable enables full-text matching, filterable supports constraints, sortable supports ordering, facetable supports grouped navigation values, and retrievable controls inclusion in results. These flags are not synonyms. A field may need several, or only one. Decide based on application behavior before creating the index because changing field capabilities later can require a new field or rebuilding the index rather than simply changing a query.',
        example: 'A brochure title should be searchable and returned. File size needs numeric filtering or sorting, not full-text matching. A location list may support both matching and filtering.',
      },
      {
        title: 'Freshness requires an ingestion run',
        body: 'An updated source document does not automatically prove the index has the new content. In the exercise, the indexer is scheduled Once; later changes require another relevant ingestion run. Inspect indexer status and errors before blaming query syntax. Also separate access to source storage from access to the search service. A successful query does not prove the indexer can read every intended file, and a retrievable flag is not a user-specific authorization policy.',
        example: 'If a newly uploaded brochure is missing, inspect whether the indexer ran and could read the private container before rewriting the search phrase.',
      },
    ],
    lab: {
      title: 'Rehearse search responsibilities',
      goal: 'Identify the component to configure for ingestion versus query behavior.',
      context: 'Local conceptual configuration only, not an Azure Search index or indexer payload.',
      fields: [
        {
          id: 'searchOverviewScheduled',
          label: 'Scheduled component',
          hint: 'Source files must be read again after changes.',
          options: [
            { value: 'index', label: 'Index' },
            { value: 'indexer', label: 'Indexer' },
            { value: 'search-result', label: 'Search result' },
          ],
          expected: 'indexer',
          explanation: 'The indexer performs ingestion; the index stores the resulting searchable representation.',
        },
        {
          id: 'searchOverviewTitle',
          label: 'Title capabilities',
          hint: 'Users search title words and need the title in results.',
          options: [
            { value: 'sortable-only', label: 'Sortable only' },
            { value: 'key-only', label: 'Key only' },
            { value: 'searchable-retrievable', label: 'Searchable and retrievable' },
          ],
          expected: 'searchable-retrievable',
          explanation: 'Full-text matching and returning a field are distinct capabilities, both needed here.',
        },
      ],
      template: '{\n  "localConcept": "search roles",\n  "scheduledWork": "{{searchOverviewScheduled}}",\n  "titleCapabilities": "{{searchOverviewTitle}}"\n}',
      language: 'json',
      expectedOutput: 'Expected synthetic trace, not a service response: the indexer reads private sample files, populates an index, and a query returns matching titles.',
      takeaway: 'When something breaks, first identify whether the failure belongs to source access, ingestion, field design or querying.',
      azure: {
        title: 'Index the sample travel brochures',
        sourceId: 64,
        environment: 'azure-portal',
        minutes: 40,
        cost: SEARCH_COST,
        prerequisites: [
          ACCOUNT_BOUNDARY,
          'Permission to create Azure AI Search and a same-region Storage account. Use documents.zip from exercise 64 and remain within the sample enrichment limits.',
        ],
        steps: [
          {
            title: 'Create the isolated services',
            surface: 'Azure portal',
            instruction: 'Choose Create a resource > Azure AI Search. Use a dedicated lab group and the Free tier if available. Create a Standard LRS Storage account in the same group and region. Wait for both deployments and inspect their Overview pages.',
            expected: 'You have an owned Search service and a separate owned source-storage account.',
            hint: 'If Free is unavailable, review the paid alternative rather than assuming the exercise is cost-free.',
          },
          {
            title: 'Inspect the sample documents',
            surface: 'Browser',
            instruction: 'Open source 64 > Launch the exercise, download its documents.zip and extract the sample travel brochures. Open one PDF and note a location word you can later search for.',
            expected: 'You have a known, non-sensitive input and a term whose source can be checked.',
            hint: 'Use only the supplied small sample set, not a directory of personal files.',
          },
          {
            title: 'Upload to private storage',
            surface: 'Azure portal',
            instruction: 'In your storage account select Storage browser > Blob containers > Container. Create documents with Private access and upload the supplied PDFs. Confirm the uploaded names and count before leaving.',
            expected: 'The original files exist in a private container independently of any search index.',
            hint: 'Do not make the container anonymous if the import wizard cannot connect; inspect the configured authentication instead.',
          },
          {
            title: 'Configure the ingestion path',
            surface: 'Azure portal',
            instruction: 'Open Search > Overview > Import data > Azure Blob Storage > keyword search. Select your storage account and documents container. In Apply AI enrichments, follow the sample options for phrases, persons, locations and image text, using the free enrichment option only within its limit.',
            expected: 'The wizard connects the source and enrichment pipeline without changing the original files.',
            hint: 'You will inspect the skills more deeply in the next lesson; first keep the exercise configuration intact.',
          },
          {
            title: 'Set field capabilities and run once',
            surface: 'Azure portal',
            instruction: 'In Preview mappings, configure title and the enriched fields as shown in exercise 64; keep file size filterable and sortable. Review any semantic-ranker pricing notice, set Schedule to Once, and create with a unique lab objects-name prefix.',
            expected: 'The wizard creates the index, data source, skillset and indexer with the chosen field capabilities.',
            hint: 'Use a unique prefix such as your own lab name; never overwrite objects belonging to another exercise or user.',
          },
          {
            title: 'Check completion before searching',
            surface: 'Azure portal',
            instruction: 'Go to Search management > Indexers, open your indexer and refresh until the run succeeds or inspect its errors. Then open Search explorer for the created index and search for * to inspect document count, titles and returned fields.',
            expected: 'The indexer reports its actual outcome and Search explorer returns populated index records.',
            hint: 'Do not claim successful indexing if the status is still running or contains unresolved errors.',
          },
          {
            title: 'Change only the query term',
            surface: 'Azure portal',
            instruction: 'Replace * with the location word you noted, leaving the index and fields unchanged. Inspect which titles return and compare one match with its source brochure. Record the difference between the two result sets.',
            expected: 'You observe query-time selection over the same persisted index rather than a new ingestion run.',
            hint: 'A result count alone is weaker evidence than checking a returned title against its source.',
          },
        ],
        success: 'Your indexer populated an index from private sample storage, and you inspected actual results before and after changing a query term.',
        evidencePrompt: 'Name the roles of the container, indexer and index, then describe one observed query result using sample titles only. Do not share keys, endpoints or storage access details.',
        cleanup: OWNED_CLEANUP,
      },
    },
    check: {
      question: 'A new brochure is present in Blob Storage but missing from search. The last indexer run was yesterday and its schedule is Once. What should you investigate first?',
      options: [
        { id: 'search-overview-retrain', text: 'Retrain a chat model on the brochure.', explanation: 'This is a search-ingestion problem. Chat-model training neither runs the indexer nor updates the existing index.' },
        { id: 'search-overview-sort', text: 'Sort search results in reverse title order.', explanation: 'Sorting cannot make an unindexed document appear.' },
        { id: 'search-overview-ingest', text: 'Run and inspect the indexer, including source-access errors.', explanation: 'The new file needs to pass through ingestion before it can be represented in the queryable index.' },
      ],
      correctOptionId: 'search-overview-ingest',
      explanation: 'The evidence points to freshness and ingestion, so diagnose that stage before changing retrieval or generation.',
    },
    recall: {
      prompt: 'Explain data source, indexer and index without using those words as definitions.',
      answer: 'One describes the source location and connection. One executes extraction and population. One stores the field-based representation applications search.',
    },
    transfer: 'A customer says “search is stale.” Ask when source data changed, when ingestion last succeeded and what the index actually contains. Explain why these checks come before model or prompt tuning.',
    sourceNote: `The corpus lesson uses indexer-based ingestion; it should not be read as claiming that every possible index update requires an indexer. Exercise 64 supplies the actual import-wizard and sample-field settings. ${REVIEW_CONTEXT}`,
  },
  {
    id: 'knowledge-mining',
    title: 'Enrich content during ingestion',
    cluster: 'search-rag',
    examDomain: 'Implement information extraction solutions',
    summary: 'Follow document cracking, skill execution and output mappings before diagnosing missing searchable insights.',
    minutes: 8,
    order: 245,
    prerequisites: ['ai-search-overview'],
    sourceIds: [52, 92, 33, 64, 215],
    steps: [
      {
        title: 'Build an enriched document',
        body: 'Knowledge mining extracts useful information from source material before users search it. The indexer first cracks a document to obtain content and metadata, then applies a skillset to build a richer hierarchical document. Skills can add language, entities, key phrases or image-derived text. The final searchable record is a selected representation of that work, not necessarily a copy of every intermediate value. This makes ingestion a processing pipeline rather than a simple file upload.',
        example: 'A sample travel PDF contains a photograph of a station sign. OCR can contribute the sign text so a later query can find it even when the surrounding paragraph omits it.',
      },
      {
        title: 'Context determines where a skill runs',
        body: 'When image extraction is configured, the enrichment document can contain a normalized_images collection. An OCR skill can run for each image in that collection, producing text at each image node. A later merge skill can combine image text with document text. The skill context controls which part of the hierarchy is processed; the input path controls what is supplied. A correct skill applied to the wrong context can produce no useful output or the wrong granularity.',
        example: 'One document with three embedded images needs image-level OCR inputs, not an assumption that there is only one top-level image to process.',
      },
      {
        title: 'Map enriched output into the index',
        body: 'Source fields can map directly into index fields, implicitly by name or through explicit mappings. Skill outputs live at paths in the enrichment hierarchy and require explicit output mappings into the target index. A successful skill run does not prove its value is searchable. If key phrases appear during enrichment but not in query results, inspect the output mapping and target field capabilities before rerunning extraction or changing the question.',
        example: 'An extracted location list must reach the index locations field. It also needs to be retrievable to appear in results and searchable if queries should match its text.',
      },
      {
        title: 'Use built-in or custom skills deliberately',
        body: 'Built-in skills expose capabilities such as language detection, translation, entity extraction, PII handling and OCR through Foundry Tools. A custom skill can call your own logic, for example an Azure Function wrapping a document extraction service. The extension belongs in the ingestion pipeline, not in every user query. Account for service access, supported regions and enrichment costs. When output is wrong, trace source content, skill input, skill output and mapping in that order.',
        example: 'A company-specific equipment code validator belongs in a custom enrichment step; detecting common place names already has a built-in capability.',
      },
    ],
    lab: {
      title: 'Rehearse an enrichment path',
      goal: 'Find the missing connection between an image skill and a searchable field.',
      context: 'Local conceptual configuration only; these are architecture choices, not an executable skillset.',
      fields: [
        {
          id: 'miningContext',
          label: 'OCR processing scope',
          hint: 'The source PDF contains several normalized images.',
          options: [
            { value: 'each-normalized-image', label: 'Each normalized image' },
            { value: 'query-text', label: 'The future search query' },
            { value: 'resource-group', label: 'The resource group' },
          ],
          expected: 'each-normalized-image',
          explanation: 'Image-level context supplies each extracted image to OCR at the correct granularity.',
        },
        {
          id: 'miningMapping',
          label: 'Publish a skill-derived field',
          hint: 'The enriched value exists but is absent from the index record.',
          options: [
            { value: 'change-chat-prompt', label: 'Change a chat prompt' },
            { value: 'output-field-mapping', label: 'Map skill output to the index field' },
            { value: 'rename-pdf', label: 'Rename the original PDF' },
          ],
          expected: 'output-field-mapping',
          explanation: 'The mapping connects the enrichment hierarchy to the target index representation.',
        },
      ],
      template: '{\n  "localConcept": "enrichment pipeline",\n  "ocrContext": "{{miningContext}}",\n  "publishOutput": "{{miningMapping}}"\n}',
      language: 'json',
      expectedOutput: 'Expected synthetic trace, not a service response: crack PDF → normalize images → OCR each image → merge text → map the enriched output into searchable fields.',
      takeaway: 'An insight produced during ingestion is not useful to a query until it is persisted and exposed appropriately.',
      azure: {
        title: 'Inspect and compare enrichment outputs',
        sourceId: 64,
        environment: 'azure-portal',
        minutes: 40,
        cost: SEARCH_COST,
        prerequisites: [
          ACCOUNT_BOUNDARY,
          'Your owned Search service and private documents container from the overview lab, or the resource and sample-upload sections of exercise 64.',
          'Capacity for two small comparison indexes within your service limits. Do not upgrade or delete shared indexes to create capacity.',
        ],
        steps: [
          {
            title: 'Inspect the known input',
            surface: 'Azure portal',
            instruction: 'Open your lab Storage account > Storage browser > documents. Confirm the supplied brochure set and its count. In the Search service, inspect your existing lab objects so you can choose new comparison prefixes without overwriting them.',
            expected: 'You have a fixed source set and a safe naming boundary for the comparison.',
            hint: 'If service limits prevent another index, remove only an expendable index you own or record the limit; do not alter another user’s objects.',
          },
          {
            title: 'Configure the first skill selection',
            surface: 'Azure portal',
            instruction: 'Choose Search > Import data > Azure Blob Storage > keyword search and connect the same private container. In Apply AI enrichments select Extract phrases, Extract entities with Persons and Locations, and Extract text from images with tags and categorization as in exercise 64.',
            expected: 'The first pipeline requests the built-in enrichments described in the source.',
            hint: 'Use the free option only within its documented small-workload limit; otherwise inspect the cost of an approved attached resource.',
          },
          {
            title: 'Inspect the output contract',
            surface: 'Azure portal',
            instruction: 'On Preview mappings, inspect title, persons, locations and keyPhrases. Confirm the exercise field capabilities, set Schedule to Once and create with your first unique lab prefix. Open Search management > Skillsets and inspect the generated skillset definition.',
            expected: 'The selected skills and their output fields are visible as concrete configuration.',
            hint: 'This is inspection of the wizard-generated definition, not an invitation to paste an unverified skillset over a shared one.',
          },
          {
            title: 'Trace the mapping',
            surface: 'Azure portal',
            instruction: 'Open the generated indexer and inspect its definition and output field mappings. Trace one skill-produced field, such as locations, from its source path to the index field. Then inspect the index field definition.',
            expected: 'You can identify the actual connection between enrichment output and the returned index record.',
            hint: 'Names and paths can vary by generated version. Follow the actual definitions rather than guessing a path from memory.',
          },
          {
            title: 'Run and inspect the baseline',
            surface: 'Azure portal',
            instruction: 'Wait for your indexer run to report Success, inspecting errors if needed. In Search explorer query * and select title,persons,locations,keyPhrases. Compare a returned location or phrase with one sample brochure.',
            expected: 'Actual enriched values are visible in query results and can be checked against the source.',
            hint: 'An empty persons list may be valid if the document contains no detected people; use source content to interpret it.',
          },
          {
            title: 'Change one enrichment choice',
            surface: 'Azure portal',
            instruction: 'Repeat the same import into a second uniquely named lab pipeline, changing only the entity selection by disabling Persons while keeping Locations and the other settings. Keep the input documents and schedule unchanged; review the generated mappings before creating.',
            expected: 'The second pipeline deliberately omits person extraction while retaining the other enrichment tasks.',
            hint: 'Creating a comparison pipeline avoids stale values in an existing index obscuring what a removed skill produced.',
          },
          {
            title: 'Compare output and explain the cause',
            surface: 'Azure portal',
            instruction: 'After the second indexer succeeds, inspect both indexes in Search explorer. Compare the available person field or values and confirm that location extraction remains. Explain the difference using the skill selection and output mapping you inspected.',
            expected: 'You can connect a configuration change to actual output without attributing it to a query prompt.',
            hint: 'Record the observed schema and values; a disabled field can be absent or empty depending on the generated mapping.',
          },
        ],
        success: 'You traced one enrichment output into an index and compared two small pipelines that differed in one entity-extraction choice.',
        evidencePrompt: 'Name the selected skill, its output-to-index connection and one observed comparison. Use only sample titles and nonsensitive observations; do not paste service definitions containing credentials.',
        cleanup: OWNED_CLEANUP,
      },
    },
    check: {
      question: 'A skill produces location values, but Search explorer never returns them. The indexer succeeds. Which next check targets the likely missing link?',
      options: [
        { id: 'mining-mapping', text: 'Inspect the skill output mapping and target field capabilities.', explanation: 'A produced enrichment must reach the correct index field and be exposed for retrieval; success alone does not establish that connection.' },
        { id: 'mining-new-model', text: 'Deploy a larger chat model.', explanation: 'The missing link is inside ingestion and index configuration, not response generation.' },
        { id: 'mining-repeat-source', text: 'Upload every document several more times.', explanation: 'Duplicate source files do not repair a missing mapping and can increase cost or duplicate data.' },
      ],
      correctOptionId: 'mining-mapping',
      explanation: 'Trace the pipeline stage where evidence stops appearing, rather than retrying unrelated stages.',
    },
    recall: {
      prompt: 'Describe how text inside an embedded image can become searchable in the parent document.',
      answer: 'The indexer extracts normalized images, OCR runs at image context, a merge step can combine image text with document text, and output mappings persist the appropriate enriched text into index fields.',
    },
    transfer: 'A customer wants to add a custom skill for every extraction task. Explain when a built-in skill is enough and when an Azure Function wrapping domain logic is justified, including where that cost occurs.',
    sourceNote: `The two-pipeline comparison extends exercise 64 to isolate one enrichment choice. Source 52 uses older wording for the limited free enrichment resource; treat its document limit as an exercise allowance, not a universal production entitlement. ${REVIEW_CONTEXT}`,
  },
  {
    id: 'search-query',
    title: 'Query the right evidence',
    cluster: 'search-rag',
    examDomain: 'Implement information extraction solutions',
    summary: 'Separate matching, filtering, selecting and ranking so each query setting solves the intended problem.',
    minutes: 8,
    order: 250,
    prerequisites: ['knowledge-mining'],
    sourceIds: [187, 261, 64, 33, 22],
    steps: [
      {
        title: 'Match terms before ordering results',
        body: 'Full-text search parses a query, analyzes its terms, identifies matching documents and scores their relevance. The corpus uses Lucene simple and full syntax for different levels of query expression. Linguistic processing depends on the configured analyzer: lowercasing, stopword handling and word normalization are examples, not promises for every field. Searching two words with any versus all changes which terms must match. A high-ranked match is relevant to the expression, not necessarily correct for the business decision.',
        example: 'Searching “coastal hotel” with any can match either term. Requiring all narrows the candidate set to records matching both terms, without guaranteeing the phrase appears together.',
      },
      {
        title: 'Filter is not another search phrase',
        body: 'A filter applies a Boolean constraint to filterable fields. It is appropriate for a numeric range or an exact category, while full-text search matches analyzed terms. OData filter expressions can accompany a search request; they do not require switching the full-text parser to full Lucene syntax. String filter comparisons are case-sensitive by default unless normalization is configured. When a filter returns nothing, inspect the actual stored field values and capabilities before assuming relevant text was never indexed.',
        example: 'Find brochures mentioning a city, then restrict results to files smaller than a specified byte size. The size constraint belongs in a numeric filter, not the text search phrase.',
      },
      {
        title: 'Select, sort and facets do different jobs',
        body: 'searchFields chooses which searchable fields participate in matching; select chooses which retrievable fields appear in the response. orderby uses sortable fields to control result ordering. Facets return grouped values that a user interface can present, after which a selected value becomes a filter in another query. These settings affect different stages and are not security policies. Omitting a field from one response does not establish that the caller is unauthorized to access it.',
        example: 'Search brochure content, return title and locations, and order by title. A location facet can help the user narrow the next query without changing the original documents.',
      },
      {
        title: 'Ranking is not answer generation',
        body: 'Search relevance orders retrieved candidates. Keyword retrieval matches indexed terms, while vector-based retrieval uses an embedding representation to find similarity. Semantic ranking can improve the ordering of candidate results, but a ranking component does not write a grounded answer or supply information missing from the index. Keep each role separate during troubleshooting: first check whether the right record is eligible and retrievable, then whether it ranks usefully, then whether an application uses it correctly.',
        example: 'If the needed policy was never indexed, neither a different sort order nor a more fluent chat answer supplies that missing evidence.',
      },
    ],
    lab: {
      title: 'Rehearse query responsibilities',
      goal: 'Put a constraint and an output choice in their correct conceptual places.',
      context: 'Local conceptual configuration only, not a Search REST request. Use Search explorer for the real comparison.',
      fields: [
        {
          id: 'searchQueryConstraint',
          label: 'Maximum file size',
          hint: 'The requirement is a numeric eligibility rule.',
          options: [
            { value: 'text-search', label: 'Add bytes to the text search' },
            { value: 'numeric-filter', label: 'Apply a numeric filter' },
            { value: 'select-title', label: 'Select only the title' },
          ],
          expected: 'numeric-filter',
          explanation: 'A numeric filter constrains candidate eligibility; text matching and field selection do not express the same rule.',
        },
        {
          id: 'searchQueryOutput',
          label: 'Return only title and locations',
          hint: 'The matching documents should remain the same.',
          options: [
            { value: 'select-fields', label: 'Select returned fields' },
            { value: 'change-search-fields', label: 'Change which fields are searched' },
            { value: 'retrain-model', label: 'Retrain a model' },
          ],
          expected: 'select-fields',
          explanation: 'Selection changes the response shape without intentionally changing the matching criteria.',
        },
      ],
      template: '{\n  "localConcept": "query intent",\n  "sizeRequirement": "{{searchQueryConstraint}}",\n  "responseShape": "{{searchQueryOutput}}"\n}',
      language: 'json',
      expectedOutput: 'Expected synthetic result, not a service response: matching brochures satisfy the size constraint, and each returned record exposes only the selected title and locations.',
      takeaway: 'Eligibility, matching, ordering and presentation are separate controls. Diagnose the one that owns the symptom.',
      azure: {
        title: 'Compare queries in Search explorer',
        sourceId: 64,
        environment: 'azure-portal',
        minutes: 25,
        cost: SEARCH_COST,
        prerequisites: [
          ACCOUNT_BOUNDARY,
          'The successfully populated, owned brochure index from exercise 64. If absent, complete its resource, private upload and import-wizard steps first; that setup adds time.',
          'Confirm title is retrievable and sortable, locations and keyPhrases are retrievable, and metadata_storage_size is filterable as in the source exercise.',
        ],
        steps: [
          {
            title: 'Verify the index you will query',
            surface: 'Azure portal',
            instruction: 'Open your lab Search service > Search management > Indexers and confirm the sample indexer succeeded. Open the target index and inspect the field capabilities listed in the prerequisites before opening Search explorer.',
            expected: 'You know the correct populated index and which query operations its fields support.',
            hint: 'Querying an empty or different index would test setup, not search behavior.',
          },
          {
            title: 'Establish the baseline',
            surface: 'Azure portal',
            instruction: 'In Search explorer query * and inspect returned titles and @odata.count when count is enabled. Switch View to JSON view, as in exercise 64, so you can see which request properties produce the result.',
            expected: 'The baseline contains the available sample documents and an inspectable request.',
            hint: 'Do not paste credentials into the query body; the explorer already uses the selected resource context.',
          },
          {
            title: 'Change only returned fields',
            surface: 'Azure portal',
            instruction: 'Keep search set to * and count enabled, and set select to title,locations. Run again and compare the record count and response fields with the baseline.',
            expected: 'The selected fields change while the query still targets all documents.',
            hint: 'If a field cannot be returned, inspect retrievable rather than changing the search phrase.',
          },
          {
            title: 'Match a source phrase',
            surface: 'Azure portal',
            instruction: 'Use the exercise query New York with select set to title,keyPhrases. Compare a returned brochure with its source. Then compare searchMode any with all while keeping the same two query words and selected fields.',
            expected: 'You can explain the term-matching difference using actual returned records, even if this small dataset produces equal counts.',
            hint: 'Requiring both words is not identical to requiring an exact quoted phrase.',
          },
          {
            title: 'Add the numeric constraint',
            surface: 'Azure portal',
            instruction: 'Keep the search text and other settings constant. Add the exercise filter metadata_storage_size lt 380000 and run again. Include metadata_storage_size in select temporarily if needed to verify why each remaining record qualifies.',
            expected: 'Returned records obey the size constraint, which is independent of text relevance.',
            hint: 'The JSON request uses filter; query-string forms use $filter. Follow the explorer format you are actually using.',
          },
          {
            title: 'Inspect ordering and reset',
            surface: 'Azure portal',
            instruction: 'Remove the filter to restore the prior candidate set, then compare default ordering with orderby set to title asc. Explain which operation changed eligibility and which changed ordering. Save only a short private note, then stop.',
            expected: 'You can distinguish matching, filtering, field selection and sorting using observed results.',
            hint: 'Changing a query does not require rerunning the indexer or retraining a model.',
          },
        ],
        success: 'You inspected real query results and correctly separated a field-selection change, a matching change, a filter and an ordering change.',
        evidencePrompt: 'Describe two query changes and which stage each affected, using only sample document titles or counts. Do not paste service URLs, credentials or private indexed content.',
        cleanup: OWNED_CLEANUP,
      },
    },
    check: {
      question: 'Relevant brochures already match a text query. The UI should show only documents below a file-size limit and return just title and locations. Which change fits?',
      options: [
        { id: 'search-query-fields', text: 'Change searchFields to title and locations only.', explanation: 'That changes where matching occurs but does not enforce the numeric size limit or define the returned field list.' },
        { id: 'search-query-filter-select', text: 'Apply a numeric filter and select the two response fields.', explanation: 'The filter controls eligibility; select controls the response shape without conflating the two responsibilities.' },
        { id: 'search-query-sort', text: 'Sort by file size and return every field.', explanation: 'Sorting still returns oversized records unless a filter is applied, and it does not limit the response fields.' },
      ],
      correctOptionId: 'search-query-filter-select',
      explanation: 'Map each requirement to its own query control rather than asking ranking or matching to act as a filter.',
    },
    recall: {
      prompt: 'Explain searchFields, select, filter and orderby as four different questions.',
      answer: 'Where should text matching occur? Which fields should be returned? Which records are eligible? In what order should eligible results be presented?',
    },
    transfer: 'A customer calls irrelevant ordering and missing documents the same “bad search” problem. Explain how you would distinguish ingestion, filtering, matching and ranking with one known test document.',
    sourceNote: `Source 187 simplifies scoring as TF/IDF and can imply OData filters require full Lucene syntax. The current Microsoft Learn Filters for keyword search article (https://learn.microsoft.com/azure/search/search-filters) confirms filters are separate, and string filters are case-sensitive by default unless normalized. This lesson preserves the corpus pipeline without treating those simplifications as universal API rules. ${REVIEW_CONTEXT}`,
  },
  {
    id: 'knowledge-store',
    title: 'Persist enriched information',
    cluster: 'search-rag',
    examDomain: 'Implement information extraction solutions',
    summary: 'Choose object, table or file projections for uses beyond search, without confusing a knowledge store with a knowledge base.',
    minutes: 7,
    order: 255,
    prerequisites: ['knowledge-mining'],
    sourceIds: [181, 176, 64, 215, 33],
    steps: [
      {
        title: 'Search is not the only consumer',
        body: 'An enrichment pipeline can produce information useful outside a search experience. An analytics team may want rows of extracted phrases; another process may need JSON objects or extracted images. A knowledge store persists projections of enriched content in Azure Storage when the indexer runs the skillset. It is a separate output for downstream analysis and integration, not the search index itself and not a Foundry IQ knowledge base that agents query for grounding.',
        example: 'The travel desk wants a report of locations mentioned across brochures. Persisted table-shaped output can feed that analysis without treating the search-results page as a reporting database.',
      },
      {
        title: 'Project the shape the consumer needs',
        body: 'Object projections persist JSON-shaped data, table projections provide tabular structures with relationships, and file projections preserve extracted images as files. Choose based on the downstream consumer rather than using the word knowledge to imply one universal store. The projection describes which enriched content is saved and in what shape. A consumer that needs original image pixels cannot reconstruct them from a generated caption alone; preserving the appropriate asset is a deliberate design choice.',
        example: 'Use tables for a location-frequency report, an object for a downstream record-processing job, and a file projection when the original extracted brochure image must remain available.',
      },
      {
        title: 'Shape and map are different paths',
        body: 'Index output mappings publish enrichment values into search fields. Knowledge-store shapes and projections publish enrichment values into storage structures. A shaping step can gather related nodes from the hierarchical document into the object a projection expects. These paths can share upstream skills but serve different consumers. Configuring a projection is not the same as running it: inspect indexer completion and the actual stored objects or rows before claiming information has been persisted.',
        example: 'A phrase can appear in both a searchable keyPhrases field and a separate table row. One successful path does not prove the other was configured correctly.',
      },
      {
        title: 'Persisted output has its own lifecycle',
        body: 'A knowledge store creates durable data assets with storage permissions and costs of their own. Changing a search query does not rerun projections, and deleting an index should not be treated as proof that previously written storage output disappeared. Keep source, enrichment and destination access explicit. In a lab, give destinations recognizable names and inspect them directly. In production, decide how derived data should be refreshed, retained and removed rather than assuming the search service handles every lifecycle policy.',
        example: 'After renaming a table projection, an earlier lab table may still exist. Its presence does not prove the new indexer run wrote to the old destination.',
      },
    ],
    lab: {
      title: 'Rehearse projection choices',
      goal: 'Choose the persisted shape that matches the downstream requirement.',
      context: 'Local conceptual configuration only, not a knowledgeStore API definition. The live extension below uses Microsoft’s documented REST requests.',
      fields: [
        {
          id: 'storeProjection',
          label: 'Reporting output',
          hint: 'Analysts need related rows of documents and extracted phrases.',
          options: [
            { value: 'table', label: 'Table projection' },
            { value: 'file', label: 'Image-file projection' },
            { value: 'chat-answer', label: 'Generated chat answer' },
          ],
          expected: 'table',
          explanation: 'Tabular projections preserve the rows and relationships needed for analysis.',
        },
        {
          id: 'storeExecution',
          label: 'When projections are written',
          hint: 'The projection has been defined in the skillset.',
          options: [
            { value: 'on-every-query', label: 'Every time a user searches' },
            { value: 'when-indexer-runs', label: 'When the indexer runs the pipeline' },
            { value: 'when-tab-closes', label: 'When the browser tab closes' },
          ],
          expected: 'when-indexer-runs',
          explanation: 'Projection generation is part of ingestion and enrichment, not normal query execution.',
        },
      ],
      template: '{\n  "localConcept": "knowledge-store projection",\n  "reportingShape": "{{storeProjection}}",\n  "writeTrigger": "{{storeExecution}}"\n}',
      language: 'json',
      expectedOutput: 'Expected synthetic trace, not a service response: an indexer run writes document rows and related phrase rows into a separate storage destination.',
      takeaway: 'A knowledge store is persisted enrichment for downstream consumers; it is not another name for agent grounding.',
      azure: {
        title: 'Create a real table projection with the REST extension',
        sourceId: 64,
        environment: 'portal-and-code',
        minutes: 45,
        cost: SEARCH_COST,
        prerequisites: [
          ACCOUNT_BOUNDARY,
          'Azure AI Search and a StorageV2 account created using exercise 64 resource setup, with capacity for separate lab objects. Use only the public hotel-review sample in the Microsoft walkthrough, not customer records.',
          'VS Code with the REST Client extension. The current linked exercise 64 omits knowledge-store creation; use the explicitly identified Microsoft REST walkthrough for this extension.',
        ],
        steps: [
          {
            title: 'Inspect the limitation and supported route',
            surface: 'Browser',
            instruction: 'Open exercise 64 and read its Note about knowledge store. Then open https://learn.microsoft.com/azure/search/knowledge-store-create-rest and its downloadable REST sample. Review the requests before running anything; this is a documented extension, not a hidden option in the current import wizard.',
            expected: 'You know which source supplies resource setup and which supplies projection creation.',
            hint: 'Do not search indefinitely for a knowledge-store checkbox that the current keyword-import flow no longer exposes.',
          },
          {
            title: 'Prepare an isolated sample source',
            surface: 'Azure portal',
            instruction: 'Confirm your owned Search service and StorageV2 account. In Storage browser create a private hotel-reviews container and upload HotelReviews_data.csv from the REST walkthrough. Inspect that this is the small public exercise sample and stays within the free-enrichment workload limit.',
            expected: 'The sample CSV exists in the intended private container, separate from your brochure source.',
            hint: 'Use a separate lab resource group if existing names or capacity would conflict with earlier exercises.',
          },
          {
            title: 'Configure the documented REST sample',
            surface: 'Cloud Shell / local editor',
            instruction: 'Open the official knowledge-store REST file in VS Code. Set its variables using your Search endpoint, lab admin key and storage connection details obtained from the portal. Keep the file local and untracked. Check every object name is unused or belongs only to this exercise.',
            expected: 'The sample requests target only your owned Search and storage resources.',
            hint: 'Admin keys and storage connection strings are secrets. Never include them in tutor evidence; use the walkthrough’s managed-identity guidance if your environment prohibits keys.',
          },
          {
            title: 'Create the documented pipeline',
            surface: 'Cloud Shell / local editor',
            instruction: 'Send the walkthrough requests in order to create the index, data source, skillset and indexer. Inspect the skillset knowledgeStore projections and the shaped source paths. Use the request bodies and API version together as documented, rather than the local conceptual rehearsal.',
            expected: 'The service accepts the pipeline objects and starts the indexer with the configured table projections.',
            hint: 'A successful index creation response alone is not evidence that any table rows have been written.',
          },
          {
            title: 'Inspect actual persisted rows',
            surface: 'Azure portal',
            instruction: 'Check the lab indexer status until the run succeeds, then open the storage account > Storage browser > Tables. Open a document table and a related pages or key-phrases table from the walkthrough and inspect a real row and its linking identifiers.',
            expected: 'The projected tables contain source or enriched fields and identifiers linking related records.',
            hint: 'If the index has data but tables do not, inspect the projection paths and destination access separately from index output mappings.',
          },
          {
            title: 'Change one projection destination',
            surface: 'Azure portal',
            instruction: 'In your owned Search skillset definition, change only the first table projection tableName to a new valid alphanumeric lab name. Save, then reset and run only this lab indexer to reprocess the same source. Wait for its actual completion status.',
            expected: 'The same enrichment pipeline writes that projection to the newly named destination.',
            hint: 'Do not change the source path or shared skillsets. Resetting and rerunning consumes additional enrichment work.',
          },
          {
            title: 'Verify the new output, not just success',
            surface: 'Azure portal',
            instruction: 'Return to Storage browser > Tables, refresh and open the newly named table. Compare one stored field with the earlier projection. Note whether the old table still exists and include both owned outputs in your cleanup inventory.',
            expected: 'You observe a destination change while the enriched content retains its intended shape.',
            hint: 'Old output remaining is a lifecycle observation, not evidence that the new projection failed.',
          },
        ],
        success: 'You inspected actual knowledge-store table rows and verified that changing one owned projection destination produced a separate persisted output.',
        evidencePrompt: 'Describe a projected field, its table relationship and what changed after the destination was renamed. Do not paste connection strings, keys, customer reviews or full service definitions.',
        cleanup: OWNED_CLEANUP,
      },
    },
    check: {
      question: 'An enrichment pipeline already supports search. Analysts also need related rows for reports, and a downstream app needs extracted image files. What should be added?',
      options: [
        { id: 'store-more-prompts', text: 'Longer prompts in every search query.', explanation: 'A query prompt does not configure durable tabular or file output from the enrichment pipeline.' },
        { id: 'store-kb', text: 'Rename the index to “knowledge base.”', explanation: 'A name change does not create either storage projections or an agent retrieval capability.' },
        { id: 'store-projections', text: 'Knowledge-store table and file projections in the skillset.', explanation: 'These projections persist the enriched information in the shapes required by reporting and file consumers.' },
      ],
      correctOptionId: 'store-projections',
      explanation: 'The requirement concerns durable downstream assets, which is the knowledge-store role rather than query-time retrieval or answer generation.',
    },
    recall: {
      prompt: 'Distinguish a search index, a knowledge store and an agent knowledge base in one sentence each.',
      answer: 'An index is a queryable retrieval representation. A knowledge store persists enrichment projections in storage for downstream use. A knowledge base organizes sources and retrieval for agent grounding.',
    },
    transfer: 'A customer asks whether deleting a search index deletes all derived sensitive information. Explain why you would inspect projection destinations and retention policies before making that promise.',
    sourceNote: `Source 64 says the exercise covers a knowledge store, but its current launched version explicitly removes those steps. Sources 181 and 33 still teach projections. The live lab therefore combines exercise 64 resource setup with the verified Microsoft Learn REST walkthrough at https://learn.microsoft.com/azure/search/knowledge-store-create-rest; it does not invent portal-only creation. ${REVIEW_CONTEXT}`,
  },
  {
    id: 'rag-fundamentals',
    title: 'Ground answers with retrieval',
    cluster: 'search-rag',
    examDomain: 'Implement generative AI and agentic solutions',
    summary: 'Trace retrieve, augment and generate while keeping freshness, relevance and factual support separate.',
    minutes: 7,
    order: 260,
    prerequisites: ['ai-search-overview', 'model-deployment', 'agents-what-is-an-agent'],
    sourceIds: [239, 141, 261, 76, 16, 23],
    steps: [
      {
        title: 'Private knowledge is not model memory',
        body: 'A model trained on broad data does not automatically know a private product catalogue or the latest internal policy. Retrieval Augmented Generation supplies relevant external information when a question is asked. Retrieve finds useful source content, augment places that evidence with the question, and generate produces an answer using the available context. The model is still involved in generation. Updating the source knowledge is not the same operation as retraining the model.',
        example: 'A fictional camping supplier adds a new tent warranty. A grounded assistant should retrieve the current warranty text, not guess from general knowledge of outdoor products.',
      },
      {
        title: 'Freshness belongs to the source path',
        body: 'RAG can use current organizational information, but current is bounded by what the retrieval system can actually access. An indexed source may need processing after a file changes; a live source has its own availability and permissions. Chunking and indexing affect which pieces of evidence reach the answer. If the relevant policy is absent or stale in retrieval, revising the final writing style cannot repair that missing knowledge. Inspect the retrieved content first.',
        example: 'A warranty PDF changed today but the last successful indexer run was yesterday. A confidently cited answer can still reflect the older indexed version.',
      },
      {
        title: 'Vectors help retrieve, not decide truth',
        body: 'An embedding represents content numerically so a retriever can compare similarity beyond exact wording. Keyword search remains useful for precise terms, and semantic ranking can improve the order of retrieved candidates. These are retrieval responsibilities, not substitutes for the generate step. Similarity also does not establish authority or correctness. Keep document identity and supporting passages alongside retrieved content so the application can cite what it used and a person can inspect the original evidence.',
        example: 'A question about rainy-weather protection may retrieve text about waterproof seams even without the same words. It still must not cite a different product as evidence for this one.',
      },
      {
        title: 'Grounding reduces risk, not all mistakes',
        body: 'Retrieved context gives the model a better factual basis, and citations make claims easier to verify. Neither guarantees that the answer uses the context correctly or that the source itself is correct. Require the agent to retrieve for in-scope questions, cite supporting documents and state when the evidence is insufficient. Test known facts and deliberately unsupported questions. A useful refusal to guess is a successful boundary, not a failure that should be hidden with fluent text.',
        example: 'If no catalogue mentions a fictional solar-powered tent, the assistant should say it lacks supporting product information instead of inventing specifications with a decorative citation.',
      },
    ],
    lab: {
      title: 'Rehearse a grounded answer path',
      goal: 'Connect a changing fact to retrieval without confusing retrieval with training.',
      context: 'Local conceptual configuration only, not an agent configuration API payload.',
      fields: [
        {
          id: 'ragKnowledgeUpdate',
          label: 'New catalogue information',
          hint: 'The model behavior is acceptable; its private product facts need updating.',
          options: [
            { value: 'update-and-refresh-source', label: 'Update the source and refresh retrieval' },
            { value: 'rename-model', label: 'Rename the model deployment' },
            { value: 'guess-from-training', label: 'Rely on general training data' },
          ],
          expected: 'update-and-refresh-source',
          explanation: 'The changing facts belong in the retrievable source, with freshness verified along the indexing or live-access path.',
        },
        {
          id: 'ragMissingEvidence',
          label: 'No supporting product information',
          hint: 'The question asks about a product absent from the controlled catalogue.',
          options: [
            { value: 'invent-with-citation', label: 'Invent a plausible specification with a citation' },
            { value: 'state-gap', label: 'State the evidence gap' },
            { value: 'repeat-question', label: 'Repeat the question as an answer' },
          ],
          expected: 'state-gap',
          explanation: 'A clear evidence gap protects the boundary between known source content and speculation.',
        },
      ],
      template: '{\n  "localConcept": "grounded response",\n  "newFacts": "{{ragKnowledgeUpdate}}",\n  "missingEvidence": "{{ragMissingEvidence}}"\n}',
      language: 'json',
      expectedOutput: 'Expected synthetic trace, not a service response: retrieve a current catalogue passage → add it to context → answer with a supporting reference, or state that the requested fact is absent.',
      takeaway: 'A model can write an answer only after the retrieval path has supplied usable evidence.',
      azure: {
        title: 'Ground a catalogue answer in Foundry',
        sourceId: 76,
        environment: 'foundry',
        minutes: 45,
        cost: IQ_COST,
        prerequisites: [
          ACCOUNT_BOUNDARY,
          'A supported-region Foundry project and available model quota; permission to create Search and Storage resources and configure their lab connections.',
          'Use contoso-products.zip from exercise 76. Existing Foundry, Search and storage resources may be reused only if they are your isolated lab resources.',
        ],
        steps: [
          {
            title: 'Create the agent in an owned project',
            surface: 'Microsoft Foundry',
            instruction: 'Open the new Foundry experience and create a project in your dedicated lab group if needed. Go to Build > Agents > Create agent and use a unique product-expert lab name. Inspect the selected or automatically deployed model and its billing implications.',
            expected: 'An agent exists in the intended project with an available model.',
            hint: 'An agent with a model still has no automatic knowledge of the sample product catalogue.',
          },
          {
            title: 'Upload controlled sample evidence',
            surface: 'Azure portal',
            instruction: 'Download and inspect the three PDFs from the contoso-products.zip link in exercise 76. In your same-region Standard LRS Storage account, create a private contosoproducts container and upload the PDFs. Read one product fact before testing retrieval.',
            expected: 'The source facts exist in private sample storage and one fact is known for comparison.',
            hint: 'Do not make the container public just to make future citation links clickable.',
          },
          {
            title: 'Connect Search for knowledge retrieval',
            surface: 'Microsoft Foundry',
            instruction: 'Save the agent, then choose Knowledge > Add > Connect to Foundry IQ > Connect to an AI Search resource. Create an isolated Search resource in the same group and region, using Free if supported; review any paid or preview capability choices before accepting.',
            expected: 'The project has a connection to the Search service that will support the knowledge base.',
            hint: 'If the embedded resource creation fails, the source exercise offers an Azure portal creation route.',
          },
          {
            title: 'Complete only the lab authentication',
            surface: 'Azure portal',
            instruction: 'On the owned Search service, follow exercise 76 under Security + networking > Keys to configure its required API access control. Complete the linked Foundry connection authentication using only this lab service. Keep any copied key inside the trusted connection editor.',
            expected: 'The lab connection can authenticate to the intended Search service.',
            hint: 'The exercise uses key-based setup. Do not weaken a shared service’s authentication or mistake a service key for per-user document authorization.',
          },
          {
            title: 'Create and attach the knowledge base',
            surface: 'Microsoft Foundry',
            instruction: 'On Knowledge choose Create a knowledge base > Azure Blob Storage. Select your container, add a meaningful description, use the exercise minimal extraction setting and available approved embedding and chat models. Save, wait for the source to become active, then attach this knowledge base to your agent.',
            expected: 'The agent has a knowledge tool backed by processed sample documents.',
            hint: 'Complete the source exercise’s Manage connection authentication steps if prompted; do not assume that selecting a container completes processing.',
          },
          {
            title: 'Inspect one grounded answer',
            surface: 'Microsoft Foundry',
            instruction: 'Set instructions to retrieve for product questions, cite sources and state when information is missing. Remove or disable unrelated web grounding for this controlled lab. Ask about a product fact you read, inspect the actual knowledge-tool use and compare its cited document with the answer.',
            expected: 'A real answer is supported by the controlled sample catalogue and an inspectable reference.',
            hint: 'A private citation URL may require authorized access. Verify the document through your portal instead of publishing it anonymously.',
          },
          {
            title: 'Change only the question',
            surface: 'Microsoft Foundry',
            instruction: 'In a fresh test conversation ask for specifications of an invented product absent from the PDFs, such as a Contoso solar-powered hammock. Keep model, source and instructions unchanged. Inspect whether the answer clearly states the evidence gap.',
            expected: 'The controlled retrieval path supports known facts and exposes unsupported questions without fabricating a product.',
            hint: 'If the agent invents an answer, record that failed test and refine retrieval instructions before treating the lab as successful.',
          },
        ],
        success: 'You verified one catalogue answer against its actual source and tested an unsupported question with the same retrieval configuration.',
        evidencePrompt: 'Explain the retrieve, augment and generate stages using one observed sample answer, then describe the missing-evidence test. Do not submit credentials, account URLs or private documents.',
        cleanup: OWNED_CLEANUP,
      },
    },
    check: {
      question: 'A policy assistant cites yesterday’s return policy after today’s PDF update. The indexed source has not refreshed. Which action addresses the cause?',
      options: [
        { id: 'rag-fluent', text: 'Ask the model to write more confidently.', explanation: 'Presentation changes do not update the evidence available to retrieval.' },
        { id: 'rag-refresh', text: 'Refresh and verify the retrievable source before retesting the answer.', explanation: 'The model needs the updated policy in its retrieved context; the stale index explains the old answer.' },
        { id: 'rag-citations-off', text: 'Remove citations so the mismatch is less visible.', explanation: 'Removing traceability hides the symptom and makes validation harder without correcting the fact.' },
      ],
      correctOptionId: 'rag-refresh',
      explanation: 'Freshness is bounded by the retrieval path. Diagnose the source and indexing state before changing generation.',
    },
    recall: {
      prompt: 'What changes in RAG when a new private policy is added, and what does not need to change automatically?',
      answer: 'The retrievable source and its processed representation need updating. The model can use that new context at question time without automatically retraining its weights.',
    },
    transfer: 'A customer says RAG “eliminates hallucinations.” Explain the risk reduction, the remaining source and interpretation failures, and the evidence you would require before trusting a policy answer.',
    sourceNote: `Source 239 uses absolute language about eliminating fabricated information. This lesson treats grounding as risk reduction, consistent with source 23’s requirement to test actual retrieval and citations. Real-time usefulness is bounded by the source’s refresh and permission path. ${REVIEW_CONTEXT}`,
  },
  {
    id: 'foundry-iq',
    title: 'Share grounded knowledge',
    cluster: 'search-rag',
    examDomain: 'Implement generative AI and agentic solutions',
    summary: 'Reuse a knowledge base across agents while giving each agent explicit retrieval, citation and fallback rules.',
    minutes: 8,
    order: 265,
    prerequisites: ['rag-fundamentals', 'agents-tools-overview'],
    sourceIds: [85, 23, 194, 147, 16, 76],
    steps: [
      {
        title: 'Share the knowledge work',
        body: 'Foundry IQ is a managed knowledge platform built on Azure AI Search. A knowledge base organizes related information by business purpose and can serve multiple agents, instead of each team rebuilding the same retrieval pipeline. A knowledge source connects an underlying store or index to that knowledge base. Shared grounding can make improvements reusable, but it does not mean all agents or users should receive every document. Scope each connection to the information the agent actually needs.',
        example: 'A product-support agent and a sales assistant can use the same controlled product catalogue, while an unrelated personnel knowledge base remains outside both agents’ scope.',
      },
      {
        title: 'Connect capabilities, not magic memory',
        body: 'For supported managed ingestion sources, Foundry IQ handles processing such as chunking, embedding and indexing; live or existing-index sources have different paths. Agents access knowledge through a tool connection using MCP. Tool access supplies retrieved context when used, rather than placing every document permanently inside model memory. A tool approval decision is also distinct from authorization: approving a call does not grant the service new rights or establish which end user may read each document.',
        example: 'Connecting two agents to one catalogue avoids two separately maintained copies. It does not grant either agent permission to read a private source that the configured identity cannot access.',
      },
      {
        title: 'Specify three retrieval behaviors',
        body: 'Instructions should say when to retrieve, how to cite and what to do when supporting information is missing. For a controlled product assistant, require retrieval for product claims, include source references and explicitly state evidence gaps. “Use the knowledge base” alone leaves too much unspecified. Agent instructions control intended behavior; they do not replace source permissions, configure the ranking algorithm or prove that retrieval occurred. Inspect actual tool activity and responses to test the contract.',
        example: 'A catalogue assistant should not answer a warranty question from general camping knowledge merely because the response sounds reasonable.',
      },
      {
        title: 'Test and monitor the contract',
        body: 'Test direct factual questions, questions requiring comparison, ambiguous requests and deliberately missing information. Check grounding, citation, relevance and completeness rather than counting only fluent answers. In ongoing use, track citation frequency, fallback frequency, query types and whether retrieved documents contain the answer. A rise in fallbacks can reveal a source gap or a retrieval problem; suppressing the fallback message would hide useful evidence. Refine the appropriate layer rather than treating every issue as a prompt problem.',
        example: 'If product comparisons retrieve only one product document, adding “be more complete” may not solve missing retrieval coverage. Inspect the evidence reaching the agent first.',
      },
    ],
    lab: {
      title: 'Rehearse shared grounding',
      goal: 'Reuse knowledge while keeping behavior and authorization as separate responsibilities.',
      context: 'Local conceptual configuration only, not a Foundry agent definition or MCP request.',
      fields: [
        {
          id: 'iqReuse',
          label: 'Two agents need one catalogue',
          hint: 'The source facts should be maintained once.',
          options: [
            { value: 'separate-manual-copies', label: 'Maintain unrelated manual copies' },
            { value: 'shared-knowledge-base', label: 'Connect both to the controlled knowledge base' },
            { value: 'model-memory', label: 'Assume both models already know it' },
          ],
          expected: 'shared-knowledge-base',
          explanation: 'Shared retrieval makes source improvements reusable without confusing them with model training.',
        },
        {
          id: 'iqInstruction',
          label: 'Product-answer instruction contract',
          hint: 'The agent must know what to do before answering and when evidence is absent.',
          options: [
            { value: 'be-helpful', label: 'Only “be helpful”' },
            { value: 'retrieve-cite-fallback', label: 'Retrieve, cite and state evidence gaps' },
            { value: 'answer-everything', label: 'Always invent an answer if needed' },
          ],
          expected: 'retrieve-cite-fallback',
          explanation: 'The three behaviors make the expected use of retrieved evidence testable.',
        },
      ],
      template: '{\n  "localConcept": "shared agent grounding",\n  "catalogueConnection": "{{iqReuse}}",\n  "answerContract": "{{iqInstruction}}"\n}',
      language: 'json',
      expectedOutput: 'Expected synthetic trace, not a service response: two agents consult the same controlled catalogue; each retrieves, cites and handles unsupported questions according to its own instructions.',
      takeaway: 'A shared knowledge base shares evidence, not a universal permission grant or a single immutable agent behavior.',
      azure: {
        title: 'Share a knowledge base and test tool approval',
        sourceId: 76,
        environment: 'portal-and-code',
        minutes: 55,
        cost: IQ_COST,
        prerequisites: [
          ACCOUNT_BOUNDARY,
          'The owned Foundry project, private product container, Search connection and active knowledge base from exercise 76 or the RAG lab. Complete that resource setup first if absent.',
          'VS Code with the Microsoft Foundry Toolkit extension, Git, Azure CLI and the Python version supported by the linked exercise for the final approval test.',
        ],
        steps: [
          {
            title: 'Verify the owned retrieval resources',
            surface: 'Azure portal',
            instruction: 'Inspect your lab resource group and identify the Foundry resource, Search service and private product-data storage. Check the source indexer or processing status and confirm no shared resources will be reconfigured.',
            expected: 'The shared knowledge experiment has an explicit, owned resource boundary and usable sample data.',
            hint: 'Shared here means shared between your two lab agents, not published to everyone.',
          },
          {
            title: 'Inspect the reusable knowledge base',
            surface: 'Microsoft Foundry',
            instruction: 'Open Knowledge and select your catalogue knowledge base. Inspect its source description and active status. Confirm it points to the controlled sample container from exercise 76, with the intended embedding and chat configuration.',
            expected: 'You can distinguish the reusable knowledge base from its underlying source and Search connection.',
            hint: 'If setup is incomplete, follow exercise 76’s connection and authentication steps rather than creating a duplicate source blindly.',
          },
          {
            title: 'Give the first agent a precise contract',
            surface: 'Microsoft Foundry',
            instruction: 'Open Build > Agents > your product-support lab agent. Attach the existing knowledge base and save instructions requiring catalogue retrieval for product claims, source citations and a clear missing-information response. Disable unrelated web grounding for the comparison.',
            expected: 'The agent has an explicit, testable contract for using the shared evidence.',
            hint: 'Instructions describe expected behavior; the next steps must still verify it.',
          },
          {
            title: 'Connect a second agent to the same base',
            surface: 'Microsoft Foundry',
            instruction: 'Create a second uniquely named lab agent for product comparisons. Attach the same existing knowledge base, not a copied upload. Give it the same evidence rules, then ask both agents one identical catalogue question and inspect their references.',
            expected: 'Both agents can ground the question in the same controlled source while serving different conversational purposes.',
            hint: 'Do not expect identical wording. Compare factual support and cited source identity.',
          },
          {
            title: 'Change one agent instruction',
            surface: 'Microsoft Foundry',
            instruction: 'Change only the first agent’s citation rule to explicitly require the supporting document title for each product claim. Keep the source, model and question fixed, start a fresh test conversation and compare its response with the earlier run.',
            expected: 'You can separate an agent-behavior change from a knowledge-base content change.',
            hint: 'The second agent’s instructions should remain unchanged; shared knowledge does not automatically copy instruction edits.',
          },
          {
            title: 'Check retrieval quality and a gap',
            surface: 'Microsoft Foundry',
            instruction: 'Test one product comparison and one invented product absent from the PDFs. Inspect tool activity, source support, citation usefulness and missing-information behavior. Keep a small private record of any failure rather than counting every fluent answer as success.',
            expected: 'You have concrete positive and negative tests for the retrieval contract.',
            hint: 'A private citation link may require an authorized portal view; do not make source blobs public to remove that friction.',
          },
          {
            title: 'Test the explicit approval path',
            surface: 'Cloud Shell / local editor',
            instruction: 'Follow exercise 76 in VS Code: open the agent in Foundry Toolkit, locate its kb-knowledgebase tool, and choose Ask for approval for all tools on that tool. Complete and run the supplied agent_client.py. Approve one sample lookup and deny another; inspect the real behavior without changing source permissions.',
            expected: 'The client handles actual knowledge-tool approval requests, including no request or multiple requests as the source code describes.',
            hint: 'The current exercise says the portal does not expose this approval setting. Use the Toolkit and source client; a denied call is not a test of document-level authorization.',
          },
        ],
        success: 'Two owned agents used the same knowledge base, a controlled instruction change was observed, and the source client exercised the configured knowledge-tool approval path.',
        evidencePrompt: 'Explain what was shared, what changed only for one agent, and why tool approval is not a data-permission grant. Use sample observations only; do not share endpoints, identities, keys or transcripts.',
        cleanup: OWNED_CLEANUP,
      },
    },
    check: {
      question: 'Two agents share a catalogue knowledge base. One sometimes answers from training data and omits references. The source is current. What is the most direct next action?',
      options: [
        { id: 'iq-copy-catalogue', text: 'Create a second unrelated copy of every document.', explanation: 'Duplicating the data does not define when the first agent retrieves or how it cites and increases maintenance work.' },
        { id: 'iq-permission-by-prompt', text: 'Tell the agent that its prompt grants access to all company data.', explanation: 'Instructions do not grant authorization and would widen scope without solving the behavior problem.' },
        { id: 'iq-contract-test', text: 'Specify retrieval, citation and fallback rules, then inspect actual tool use and test responses.', explanation: 'The observed failure is agent retrieval behavior, so a precise contract and evidence-based testing target the correct layer.' },
      ],
      correctOptionId: 'iq-contract-test',
      explanation: 'Shared source quality and agent behavior are related but distinct. Diagnose the layer shown by the evidence.',
    },
    recall: {
      prompt: 'Name the three retrieval behaviors that instructions should specify, and one thing those instructions cannot replace.',
      answer: 'Specify when to retrieve, how to cite and what to do when evidence is missing. Instructions cannot replace source authorization or prove that the tool actually retrieved supporting evidence.',
    },
    transfer: 'A customer wants every agent to copy a large policy collection. Explain the shared-knowledge alternative, how you would scope access, and which quality checks remain agent-specific.',
    sourceNote: `Episode 16 shows approval prompts; the current linked exercise 76 says portal-created knowledge tools do not ask by default and configures approval through Foundry Toolkit for VS Code. This lab follows that explicit path and keeps approval separate from authorization. The two-agent comparison is an extension grounded in source 85. ${REVIEW_CONTEXT}`,
  },
  {
    id: 'foundry-iq-data-sources',
    title: 'Choose the grounding source',
    cluster: 'search-rag',
    examDomain: 'Implement generative AI and agentic solutions',
    summary: 'Balance source freshness, retrieval control and permissions instead of treating all knowledge connections alike.',
    minutes: 9,
    order: 270,
    prerequisites: ['foundry-iq', 'search-query'],
    sourceIds: [22, 23, 147, 194, 76, 16],
    steps: [
      {
        title: 'Start where the governed data lives',
        body: 'The corpus distinguishes six source options: an existing Azure AI Search index, Azure Blob Storage, Web, SharePoint Remote, SharePoint Indexed and OneLake. An existing index preserves an investment in custom search processing. Blob Storage suits file collections, while OneLake connects to unstructured material in Microsoft Fabric. Direct describes a simpler source connection, not a promise that no processing or indexing occurs underneath. Choose according to the actual storage system, required controls and retrieval path.',
        example: 'A fictional product team already curates a Search index with specialized fields. Reusing that index can preserve useful processing instead of uploading an unrelated copy of its files.',
      },
      {
        title: 'Freshness and control are a trade-off',
        body: 'SharePoint Remote queries current SharePoint content with Microsoft 365 governance and no separately maintained index. SharePoint Indexed preprocesses content into Azure AI Search, enabling custom pipelines and advanced retrieval but introducing index refresh responsibilities. Web grounding uses current public information through Bing, with less control over which sources are referenced. A controlled internal policy question should not silently become an unrestricted web lookup just because the internal source lacks an answer.',
        example: 'A frequently edited internal procedure may favor remote SharePoint access. A specialist archive needing custom enrichment may justify indexing and a clearly monitored refresh schedule.',
      },
      {
        title: 'Identify whose permissions apply',
        body: 'A connection authenticates access through a configured identity or credential. It does not, by itself, prove that every agent user should see every retrieved item. The corpus contrasts SharePoint Remote permission handling with permissions configured for indexed content. Preserve access restrictions across ingestion and retrieval, and test with identities that have different allowed content before production. Private Blob Storage prevents anonymous file access, but a broadly privileged service connection still needs an intentional end-user authorization design.',
        example: 'A catalogue service key may legitimately read all lab PDFs. That does not demonstrate that a production employee assistant correctly separates two departments’ restricted policies.',
      },
      {
        title: 'Tune retrieval without changing access',
        body: 'Scoring profiles can boost fields or attributes that matter to the business. Semantic ranking improves contextual relevance among candidates, and custom search analyzers handle specialized tokenization needs such as technical codes. These controls improve which eligible evidence surfaces; they do not encrypt fields, grant permission or create missing content. Pair configuration with tests of source freshness, citation support and fallback behavior. If a private document is inappropriately eligible, fix authorization rather than merely ranking it lower.',
        example: 'Boosting current official product guidance can improve a support answer. Giving a restricted policy a low score is not a valid way to protect it.',
      },
    ],
    lab: {
      title: 'Rehearse source and permission choices',
      goal: 'Choose a source path without confusing ranking, refresh and authorization.',
      context: 'Local conceptual configuration only, not a knowledge-source API payload. No tenant or SharePoint connection is created here.',
      fields: [
        {
          id: 'iqSourceChoice',
          label: 'Frequently changing SharePoint policies',
          hint: 'Existing governance and live access matter more than a custom enrichment pipeline.',
          options: [
            { value: 'sharepoint-remote', label: 'SharePoint Remote' },
            { value: 'public-web-only', label: 'Public Web only' },
            { value: 'manual-copy', label: 'An unmanaged manual copy' },
          ],
          expected: 'sharepoint-remote',
          explanation: 'Remote access fits the stated freshness and Microsoft 365 governance requirement without adding a separately maintained custom index.',
        },
        {
          id: 'iqSourceRanking',
          label: 'Boost official guidance',
          hint: 'The records are already authorized and eligible; their business importance should affect ordering.',
          options: [
            { value: 'scoring-profile', label: 'Scoring profile' },
            { value: 'grant-everyone-access', label: 'Grant everyone access' },
            { value: 'hide-with-select', label: 'Hide fields using select' },
          ],
          expected: 'scoring-profile',
          explanation: 'Scoring changes relevance ordering; access grants and response-field selection solve different problems.',
        },
        {
          id: 'iqSourcePermissions',
          label: 'Validate restricted retrieval',
          hint: 'Two test users are meant to have different allowed documents.',
          options: [
            { value: 'one-admin-test', label: 'Only test as an administrator' },
            { value: 'check-different-identities', label: 'Test authorized and unauthorized cases' },
            { value: 'low-rank-private', label: 'Give private documents a lower score' },
          ],
          expected: 'check-different-identities',
          explanation: 'Different-identity tests check the permission boundary; an administrator-only test or low score does not.',
        },
      ],
      template: '{\n  "localConcept": "source governance",\n  "source": "{{iqSourceChoice}}",\n  "relevanceControl": "{{iqSourceRanking}}",\n  "permissionEvidence": "{{iqSourcePermissions}}"\n}',
      language: 'json',
      expectedOutput: 'Expected synthetic trace, not a service response: live policy retrieval follows the governed source; authorized results are ranked; separate identities are used to test the access boundary.',
      takeaway: 'Freshness, ranking and authorization each need their own evidence. A successful admin query proves none of them universally.',
      azure: {
        title: 'Observe source scope and an indexed update',
        sourceId: 76,
        environment: 'foundry',
        minutes: 35,
        cost: IQ_COST,
        prerequisites: [
          ACCOUNT_BOUNDARY,
          'An owned Foundry project, Search connection and private Storage account configured through exercise 76. Permission to create a separate lab container, knowledge source and test agent.',
          'Use only a fictional two-sentence Markdown policy you create for this lab. No SharePoint tenant, personal document or production permission change is needed.',
        ],
        steps: [
          {
            title: 'Create a narrow sample source',
            surface: 'Azure portal',
            instruction: 'In your owned Storage account > Storage browser > Blob containers, create a private policy-lab container. Upload a local policy.md containing only: “Trail Kettle is a fictional lab product. Its return window is 14 days.” Keep this separate from all real organizational data.',
            expected: 'A private container has one small, controlled source with a known answer.',
            hint: 'Markdown support comes from source 22. The synthetic policy is a deliberate extension of the Blob setup in exercise 76.',
          },
          {
            title: 'Configure the scoped knowledge source',
            surface: 'Microsoft Foundry',
            instruction: 'Open Knowledge > Create a knowledge base > Azure Blob Storage. Use a unique lab name and a description stating that the source contains only the fictional Trail Kettle policy. Select policy-lab, follow exercise 76 authentication, and choose the approved embedding and chat models. Save and wait for processing.',
            expected: 'A separate knowledge base is connected to only the intended sample container.',
            hint: 'Do not reconnect the broader catalogue base or change a source used by someone else.',
          },
          {
            title: 'Query the known fact',
            surface: 'Microsoft Foundry',
            instruction: 'Create or select an owned test agent, attach only this knowledge base and require retrieval, citations and explicit evidence gaps. Disable unrelated web tools. Ask “What is the Trail Kettle return window?” and inspect the actual cited source and answer.',
            expected: 'The initial answer is supported by the policy document and reports the sample value if processing succeeded.',
            hint: 'If the source is not ready, inspect processing status instead of treating a model-generated answer as proof of retrieval.',
          },
          {
            title: 'Inspect access separately',
            surface: 'Azure portal',
            instruction: 'Inspect the policy-lab container access level and the authentication method used by the lab connection. Confirm the container remains private. Write down, without copying secrets, whether the connector is using a service credential rather than an individual end-user identity.',
            expected: 'You can explain what the private-container setting proves and what it does not prove about user-specific access.',
            hint: 'Do not grant broad roles or enable anonymous access. This one-account lab cannot verify a production multi-user permission design.',
          },
          {
            title: 'Change one source value',
            surface: 'Azure portal',
            instruction: 'Edit only the return-window value in your local synthetic policy from 14 to 21 days and replace the same policy.md blob. Inspect the source’s managed processing or indexer status and wait for a successful refresh; run only the owned lab indexer if that source configuration requires it.',
            expected: 'The source has changed, and you can distinguish that upload from completion of retrieval refresh.',
            hint: 'Active connection status alone does not prove that the new file contents have been indexed.',
          },
          {
            title: 'Repeat the same question',
            surface: 'Microsoft Foundry',
            instruction: 'Start a fresh conversation and ask the identical return-window question with the same model and instructions. Inspect the source reference and answer. If it still reports 14 days, investigate processing freshness before altering the prompt or claiming the update succeeded.',
            expected: 'After a verified refresh, the response can use the changed source value without model retraining.',
            hint: 'The controlled comparison changes the document value, not the model, question or retrieval contract.',
          },
          {
            title: 'Test the source boundary',
            surface: 'Microsoft Foundry',
            instruction: 'Ask for a reseller discount that the two-sentence policy never states. Inspect whether the agent declares insufficient evidence. Record the observed gap and a separate note that real user-permission validation would require appropriately authorized test identities and synthetic restricted documents.',
            expected: 'The agent exposes a content gap, while you avoid mistaking that result for a complete authorization test.',
            hint: 'Missing content and access-denied content are different causes. This lab intentionally tests source scope and freshness, not a live corporate tenant.',
          },
        ],
        success: 'You observed a controlled source update through retrieval, verified a citation, and distinguished a private source connection from an end-user permission guarantee.',
        evidencePrompt: 'Describe the original and updated fictional values, how you checked refresh, and one permission claim this lab cannot establish. Never provide keys, identities, tenant details or private documents.',
        cleanup: OWNED_CLEANUP,
      },
    },
    check: {
      question: 'A company needs live SharePoint policy access under existing governance, without maintaining a custom index. Another team needs specialized enrichment and advanced search over an archive. Which distinction matters?',
      options: [
        { id: 'iq-sources-remote-indexed', text: 'Remote fits the live-governed path; Indexed fits custom processing with refresh and permission responsibilities.', explanation: 'The choice turns on the retrieval path and required control, not merely that both sources begin in SharePoint.' },
        { id: 'iq-sources-identical', text: 'Both choices have identical processing, refresh and permission behavior.', explanation: 'The corpus explicitly distinguishes live remote queries from a preprocessed search index.' },
        { id: 'iq-sources-rank-security', text: 'Use either and lower the score of restricted documents instead of enforcing permissions.', explanation: 'Ranking is not authorization; restricted content must be ineligible to unauthorized users, not merely less prominent.' },
      ],
      correctOptionId: 'iq-sources-remote-indexed',
      explanation: 'Select the source by freshness, retrieval customization and governance requirements, then test those responsibilities separately.',
    },
    recall: {
      prompt: 'Name the six source options, then explain why “direct” does not mean “no processing” and why a scoring profile is not security.',
      answer: 'Azure AI Search index, Blob Storage, Web, SharePoint Remote, SharePoint Indexed and OneLake. A direct source can still have managed processing. A scoring profile changes ordering among eligible results; authorization determines which content may be retrieved.',
    },
    transfer: 'A customer says one successful administrator query proves their HR assistant respects employee permissions. Explain a synthetic two-identity test and the separate freshness checks you would require before making that claim.',
    sourceNote: `Source 22 provides the six source types and the Remote/Indexed decision boundary. Sources 147 and 194 mention scoring and analyzers as review context, not proof of practical configuration. The synthetic Blob update extends exercise 76; it does not claim that this one-account lab verifies SharePoint permissions or every source type. ${REVIEW_CONTEXT}`,
  },
]
