import type { CourseUnit } from './schema'

const IMPLEMENT = 'Implement generative AI and agentic solutions'
const MANAGE = 'Plan and manage an Azure AI solution'

const GUIDES = {
  agent: 'https://learn.microsoft.com/en-gb/training/modules/develop-ai-agents-azure-vs-code/9-exercise',
  responses: 'https://learn.microsoft.com/en-gb/training/modules/use-generative-ai-tools/07-exercise',
  custom: 'https://learn.microsoft.com/en-gb/training/modules/build-agent-with-custom-tools/5-exercise',
  mcp: 'https://learn.microsoft.com/en-gb/training/modules/connect-agent-to-mcp-tools/5-exercise',
  framework: 'https://learn.microsoft.com/en-gb/training/modules/develop-ai-agent-with-semantic-kernel/5-exercise',
  workflow: 'https://learn.microsoft.com/en-gb/training/modules/build-agent-workflows-microsoft-foundry/9-exercise',
  orchestration: 'https://learn.microsoft.com/en-gb/training/modules/orchestrate-semantic-kernel-multi-agent-solution/9-exercise',
  a2a: 'https://learn.microsoft.com/en-gb/training/modules/discover-agents-with-a2a/6-exercise',
  publishing: 'https://learn.microsoft.com/en-gb/training/modules/integrate-foundry-agent-with-m365/7-exercise',
} as const

const REHEARSAL =
  'Optional local rehearsal only: this is a conceptual plan, not an executable Azure configuration. It neither calls Azure nor checks your account.'

const ACCESS =
  'An approved Azure subscription, access to create or use a Foundry project and its model deployment, and sufficient model quota. Ask the resource owner for missing permissions; do not broaden access to bypass a failure.'

const PRESERVE =
  'Delete only assets you created for this lab and no longer need. Preserve shared projects, model deployments, connections, identities, storage and resource groups. Keep credentials out of notes, screenshots and exported evidence.'

const WORKFLOW_NOTE =
  'Availability note checked 20 September 2026: Microsoft now labels the Foundry visual workflow feature as preview and announces retirement on 1 December 2026, recommending Microsoft Agent Framework for new workflows. This lesson preserves the supplied corpus, not a promise of ongoing portal availability. Check the exercise and https://learn.microsoft.com/en-us/azure/foundry/agents/concepts/workflow before provisioning; if unavailable, record the blocker rather than treating rehearsal as an Azure run.'

export const AGENT_UNITS: CourseUnit[] = [
  {
    id: 'agents-what-is-an-agent',
    title: 'From answers to bounded actions',
    cluster: 'agents-orchestration',
    examDomain: IMPLEMENT,
    summary: 'Connect a model, instructions and tools into a goal-directed loop without confusing a fluent answer with a completed action.',
    minutes: 6,
    order: 100,
    prerequisites: ['ai-foundations', 'foundry-projects', 'model-deployment'],
    sourceIds: [230, 14, 129, 142, 212, 59, 41],
    steps: [
      {
        title: 'An answer is not an action',
        body: 'A language model can describe how to complete a task without doing it. An agent combines that model with instructions, available tools and a loop that can inspect results before choosing another action. The useful boundary is not whether the interface looks like chat: it is whether software can pursue the goal through permitted operations. A tool result is evidence of an operation; a confident sentence saying it happened is not.',
        example: 'A library assistant can explain how to reserve a study room. Reserving one requires an authorized booking tool and a booking result.',
      },
      {
        title: 'Foundry supplies the managed layer',
        body: 'Microsoft Foundry Agent Service places agent configuration above a deployed model inside a project. The project groups agents, deployments, tools and related assets. A named agent preserves its instructions and tool configuration so different applications can use the same definition. Foundry manages infrastructure and supported service tools, and its project APIs support conversation state. That does not mean every custom function runs in Azure automatically: the function integration determines who executes it.',
        example: 'A campus web app and a reception kiosk can reference the same room-help agent instead of independently copying its instructions.',
      },
      {
        title: 'Keep autonomy inside a boundary',
        body: 'Giving an agent tools changes its risk, because a mistaken interpretation can become an external action. Start with a narrow task, least-privilege resource access, input validation and traceable tool calls. Gate sensitive actions behind explicit approval rather than relying only on friendly instructions. Test normal, ambiguous and out-of-scope requests. A successful playground turn proves only what happened in that test; it neither certifies production reliability nor establishes durable understanding of the concept.',
        example: 'The room-help prototype may explain booking rules, but it must not claim a reservation or cancel another student’s booking.',
      },
    ],
    lab: {
      title: 'Optional rehearsal: define the boundary',
      goal: 'Prepare a small agent plan before the main Foundry walkthrough.',
      context: `${REHEARSAL} Plan a room-help agent that currently has no booking tool.`,
      fields: [
        {
          id: 'completion',
          label: 'Evidence of a reservation',
          hint: 'Separate generated language from a system action.',
          options: [
            { value: 'authorized-tool-result', label: 'An authorized booking tool returns a reservation' },
            { value: 'confident-answer', label: 'The agent says the room is booked' },
            { value: 'longer-instructions', label: 'The instructions contain a detailed booking policy' },
          ],
          expected: 'authorized-tool-result',
          explanation: 'Only an executed, authorized booking operation can establish that the external system changed. Instructions and prose cannot.',
        },
        {
          id: 'scope',
          label: 'Prototype permissions',
          hint: 'The first prototype only explains a process.',
          options: [
            { value: 'explain-only', label: 'Explain the process; do not book or cancel' },
            { value: 'all-bookings', label: 'Change every booking in the organization' },
            { value: 'model-decides-permissions', label: 'Let the model choose its own access rights' },
          ],
          expected: 'explain-only',
          explanation: 'A bounded prototype needs no booking write permission. Azure authorization remains an application and resource-owner responsibility.',
        },
      ],
      template: `{
  "kind": "conceptual-agent-plan",
  "completionEvidence": "{{completion}}",
  "permittedScope": "{{scope}}"
}`,
      language: 'json',
      expectedOutput: 'Synthetic interpretation: the prototype can explain room booking, but cannot truthfully report a reservation without a booking tool result.',
      takeaway: 'An agent combines reasoning with capabilities; capabilities and authorization are separate from confident wording.',
      azure: {
        title: 'Build a bounded agent in Foundry',
        sourceId: 59,
        environment: 'foundry',
        minutes: 20,
        cost: 'Model requests incur token charges; creating a project can provision supporting resources. Use an approved small deployment and inspect pricing before testing.',
        prerequisites: [ACCESS, 'Use only fictional room names and requests. Open source 59 for the current exercise and portal labels.'],
        steps: [
          {
            title: 'Find the approved project',
            surface: 'Azure portal',
            instruction: 'Open portal.azure.com, select the intended directory and subscription, and open the resource group containing your approved Foundry resource. Confirm it is a lab resource or that you have permission to add a lab agent.',
            expected: 'Verify the subscription, resource group and Foundry resource are the intended ones before changing anything.',
            hint: 'An empty resource list can mean the wrong directory or subscription filter, not a need to create duplicate resources.',
          },
          {
            title: 'Select the model deployment',
            surface: 'Microsoft Foundry',
            instruction: 'Open ai.azure.com, select that project, and inspect its model deployments. Use a supported deployment from the exercise; deploy one only if authorized and necessary.',
            expected: 'Confirm that an available deployment name, rather than only a catalog model name, is visible.',
            hint: 'If no model appears when configuring an agent, check deployment completion, region support and quota.',
          },
          {
            title: 'Create a narrow agent',
            surface: 'Microsoft Foundry',
            instruction: 'Go to Build > Agents > Create. Name the agent lab-room-helper, select the deployment, and instruct it to explain fictional booking requests, ask for missing details and never claim it has reserved or cancelled a room.',
            expected: 'Inspect the saved model choice, agent name and instructions in its configuration.',
            hint: 'Use source 59 if your portal labels differ; a model playground is not the saved agent configuration.',
          },
          {
            title: 'Try an ordinary request',
            surface: 'Microsoft Foundry',
            instruction: 'In the agent Playground, ask how to arrange a room for four people, then add that a projector is needed. Inspect whether the follow-up uses the earlier context.',
            expected: 'Look for a helpful explanation that retains the request details without claiming an external booking occurred.',
            hint: 'If the follow-up loses context, verify that you are still in the same playground conversation.',
          },
          {
            title: 'Test the action boundary',
            surface: 'Microsoft Foundry',
            instruction: 'Ask the agent to cancel an existing room booking. Inspect the configured tools and the response. Refine the instructions if it invents a successful cancellation, then save and retest in a new conversation.',
            expected: 'Verify the agent explains its limitation and that there is no cancellation tool result.',
            hint: 'A sentence asserting success is a failed boundary test, not proof that the booking system was contacted.',
          },
          {
            title: 'Save a resumable checkpoint',
            surface: 'Microsoft Foundry',
            instruction: 'Save the agent, note its nonsecret name and version, and record one successful test and one boundary you still need to check. Stop here if you are done for now.',
            expected: 'Reopen the agent and confirm the saved instructions match your tested version.',
            hint: 'Do not publish this prototype or add write tools merely to make the test appear successful.',
          },
        ],
        success: 'Confirm for yourself that a saved agent handles a follow-up and does not claim an unavailable action. The studio cannot inspect or verify your Azure account.',
        evidencePrompt: 'Record the agent version, a fictional prompt and what distinguished an answer from a tool-backed action. Do not include credentials or real user data.',
        cleanup: `Remove the lab-room-helper agent and its lab conversations when finished, or retain it deliberately for the next unit. ${PRESERVE}`,
      },
    },
    check: {
      question: 'A facilities assistant says “Your desk is reserved,” but its only tool searches policy documents. What is the most defensible conclusion?',
      options: [
        { id: 'a', text: 'The reservation succeeded because the agent followed its instructions.', explanation: 'Instructions shape responses; they do not grant a booking capability or prove that a system changed.' },
        { id: 'b', text: 'The statement is unsupported; verify a permitted booking operation before claiming success.', explanation: 'A policy-search result can explain the process but cannot establish that a reservation was created.' },
        { id: 'c', text: 'Increasing the model size will make reservations real.', explanation: 'A more capable model still needs an authorized tool and an executed operation to affect the booking system.' },
      ],
      correctOptionId: 'b',
      explanation: 'Trace the action to the responsible system and its result. Agent language is not an execution receipt.',
    },
    recall: {
      prompt: 'Without looking back, name the pieces of an agent loop and the evidence needed to claim an external action succeeded.',
      answer: 'A model interprets the goal under instructions, selects a permitted tool, receives its result and decides whether to continue or answer. An authorized operation and its checked result—not generated prose—support a completion claim.',
    },
    transfer: 'A customer wants an assistant to “handle bookings.” What would you clarify about permitted actions, approval and completion evidence before choosing tools?',
    sourceNote: `Main teaching: sources 230 and 14. Introduction, assessment, summary and episode 41 provide supporting context. Source 230 describes managed tool calling broadly; custom-function host responsibility is taught explicitly later. Guided exercise: ${GUIDES.agent}`,
  },
  {
    id: 'agents-types',
    title: 'Choose the agent execution model',
    cluster: 'agents-orchestration',
    examDomain: IMPLEMENT,
    summary: 'Distinguish declarative prompt agents, declarative workflow agents and hosted custom logic from the editor used to build them.',
    minutes: 6,
    order: 104,
    prerequisites: ['agents-what-is-an-agent'],
    sourceIds: [230, 84, 20, 14, 59, 212],
    steps: [
      {
        title: 'Configure one agent when that suffices',
        body: 'A declarative prompt-based agent describes a model, instructions and tools rather than implementing an execution engine. Foundry interprets that configuration. The portal gives you a visual entry point; the VS Code designer and YAML view offer another way to manage the same kind of definition. Editing YAML does not make an agent hosted. Choose this approach when a focused task can be expressed through instructions and supported tools without custom runtime control.',
        example: 'A museum opening-hours helper needs a model, concise instructions and a document-search tool, not its own container.',
      },
      {
        title: 'Make coordination explicit with workflows',
        body: 'The corpus places workflow agents inside the declarative family: their definition coordinates nodes and agents, rather than describing just one prompt-based participant. The workflow describes what runs when, while the agents handle reasoning within selected steps. A reusable prompt agent can participate in several workflows. This separates a business process from an increasingly long instruction list. The workflow designer and agent designer therefore have different configuration structures even though both can expose YAML.',
        example: 'A museum accessibility request may be classified, sent to an information agent and paused for staff review before a reply is sent.',
      },
      {
        title: 'Host code when you own the logic',
        body: 'Hosted agents are containerized agents created in code and deployed to Foundry hosting. You own the agent logic and execution choices while the platform supplies hosting infrastructure. That extra control also means maintaining code, dependencies and the container configuration. Do not select hosted merely because a developer prefers VS Code, or assume a Python function tool turns the whole agent into a hosted agent. Choose by execution requirements, then choose the development interface.',
        example: 'A team with a custom execution loop and specialized runtime dependencies may need hosted code; a YAML-only instruction change does not.',
      },
    ],
    lab: {
      title: 'Optional rehearsal: choose an execution model',
      goal: 'Identify the simplest appropriate agent type before configuring the real project.',
      context: `${REHEARSAL} A museum wants a single policy-answering agent with a supported search tool and no custom runtime.`,
      fields: [
        {
          id: 'agentType',
          label: 'Agent type',
          hint: 'The requirement is one configured participant, not a new execution engine.',
          options: [
            { value: 'declarative-prompt', label: 'Declarative prompt-based agent' },
            { value: 'declarative-workflow', label: 'Declarative workflow agent' },
            { value: 'hosted-container', label: 'Hosted containerized agent' },
          ],
          expected: 'declarative-prompt',
          explanation: 'One model, instruction set and supported tool are sufficient. A workflow coordinates steps; hosting custom code addresses a different need.',
        },
        {
          id: 'editor',
          label: 'Development interface',
          hint: 'An editor choice does not change runtime ownership.',
          options: [
            { value: 'portal-or-vscode', label: 'Foundry portal or VS Code designer/YAML' },
            { value: 'vscode-means-hosted', label: 'VS Code always creates a hosted agent' },
            { value: 'yaml-means-container', label: 'YAML automatically builds a container' },
          ],
          expected: 'portal-or-vscode',
          explanation: 'The same declarative configuration can be managed through different interfaces; neither YAML nor VS Code implies hosted execution.',
        },
      ],
      template: `{
  "kind": "conceptual-agent-choice",
  "agentType": "{{agentType}}",
  "editingApproach": "{{editor}}"
}`,
      language: 'json',
      expectedOutput: 'Synthetic interpretation: use a declarative prompt agent; changing editors does not change who owns the execution engine.',
      takeaway: 'Agent type describes execution responsibility, not whether a human clicked a form or edited a file.',
      azure: {
        title: 'Inspect a declarative agent definition',
        sourceId: 59,
        environment: 'foundry',
        minutes: 15,
        cost: 'Agent tests consume model tokens. This lab does not require a hosted container, a workflow run or a second model deployment.',
        prerequisites: [ACCESS, 'A saved lab agent from the previous unit, or permission to create a separate one.'],
        steps: [
          {
            title: 'Open the lab project',
            surface: 'Microsoft Foundry',
            instruction: 'Open your project in ai.azure.com and go to Build > Agents. Locate the saved lab agent rather than opening only its underlying model.',
            expected: 'Verify that the agent configuration has its own name and references an existing model deployment.',
            hint: 'If you only see sampling controls, you may be in the model playground rather than the agent editor.',
          },
          {
            title: 'Identify the declarative pieces',
            surface: 'Microsoft Foundry',
            instruction: 'Inspect the model selection, instructions and Tools sections. Note which parts describe behavior and which parts refer to capabilities.',
            expected: 'Identify a configuration-driven prompt agent without locating or building a container image.',
            hint: 'A tool can call external code without making this agent a hosted-code agent.',
          },
          {
            title: 'Make one focused change',
            surface: 'Microsoft Foundry',
            instruction: 'Change the lab instructions to answer museum policy questions in two short sentences and to ask for clarification when necessary. Save the agent.',
            expected: 'Confirm the new instructions are saved while the existing deployment reference remains unchanged.',
            hint: 'Only edit your own lab agent; create a separate lab copy if the selected agent is shared.',
          },
          {
            title: 'Test configuration-led behavior',
            surface: 'Microsoft Foundry',
            instruction: 'In a fresh Playground conversation, ask a fictional opening-hours question. Inspect whether the instruction change affected the reply without any container deployment.',
            expected: 'Observe a response from the saved prompt agent; record any instruction-following problem rather than assuming compliance.',
            hint: 'An unsupported factual question should not be treated as a reason to invent museum opening times.',
          },
          {
            title: 'Locate the other development paths',
            surface: 'Microsoft Foundry',
            instruction: 'Inspect the project navigation and source 59 for workflow and hosted-agent development paths. Do not create either merely to complete this comparison. Record which would add process coordination and which would require your own executable logic.',
            expected: 'Distinguish the current prompt-agent definition from workflow coordination and hosted code responsibilities.',
            hint: 'Feature visibility depends on portal version and access. A missing preview entry is an availability constraint, not evidence that YAML equals hosted code.',
          },
        ],
        success: 'Explain which configuration you actually saved and why no hosted runtime was needed. Only your observed project activity counts as Azure evidence.',
        evidencePrompt: 'Record the tested agent version and one requirement that would justify moving to a workflow or hosted agent.',
        cleanup: `Revert or remove only your lab-specific configuration if no longer needed. ${PRESERVE}`,
      },
    },
    check: {
      question: 'A developer edits model, instructions and tool references in the VS Code YAML view. There is no custom execution loop or container. What has changed?',
      options: [
        { id: 'a', text: 'The interface used to manage a declarative agent.', explanation: 'YAML is a representation of configuration; the agent remains declarative when the platform interprets that definition.' },
        { id: 'b', text: 'The agent has automatically become hosted code.', explanation: 'Hosted agents require code and a containerized runtime; opening a YAML editor does not create either.' },
        { id: 'c', text: 'The agent is now necessarily a multi-agent workflow.', explanation: 'Prompt-agent YAML and workflow YAML serve different purposes. A text representation alone does not introduce coordination nodes.' },
      ],
      correctOptionId: 'a',
      explanation: 'Choose the execution model from the required responsibilities, not the development tool.',
    },
    recall: {
      prompt: 'Explain prompt-based, workflow and hosted agents using the responsibility you define in each.',
      answer: 'A prompt-based agent configures one model, instructions and tools. A declarative workflow defines coordinated steps and their data flow. A hosted agent runs containerized code whose logic and dependencies you maintain.',
    },
    transfer: 'A customer asks for “a code-first agent.” Which execution requirement would you clarify before proposing a hosted container rather than version-controlled declarative configuration?',
    sourceNote: `Sources 230, 84 and 20 establish the type and interface distinction. Guided exercise: ${GUIDES.agent} ${WORKFLOW_NOTE}`,
  },
  {
    id: 'agents-vscode-lifecycle',
    title: 'Keep agent versions in sync',
    cluster: 'agents-orchestration',
    examDomain: MANAGE,
    summary: 'Connect VS Code to the correct Foundry project, save deliberate changes, test the right version and separate deployment from publishing.',
    minutes: 8,
    order: 108,
    prerequisites: ['agents-types'],
    sourceIds: [190, 20, 223, 84, 59, 41, 142],
    steps: [
      {
        title: 'Connect to the same project',
        body: 'The Microsoft Foundry VS Code extension exposes project resources, development tools and help inside the editor. Sign in to the intended Azure directory and subscription, then open the project in the Foundry extension. Existing agents and deployed models should appear together with connections and vector stores. This is another view of Azure resources, not a second independent backend. Missing assets are often a project-selection or refresh problem, so verify that boundary before rebuilding anything.',
        example: 'An agent created in the training subscription will not appear when VS Code is browsing the production subscription.',
      },
      {
        title: 'Configuration needs a saved version',
        body: 'The visual Agent Designer and YAML view let you edit declarative metadata, model selection, instructions and tools. YAML supports review and version control, but a local edit does not prove that Foundry is using the change. Save the configuration to Foundry and check the resulting version before testing elsewhere. Sampling settings such as temperature or Top P may be model-dependent; changing them is not a substitute for clear instructions or correct tool references.',
        example: 'A policy tool added in the browser but not saved can leave the VS Code playground testing an older agent without that tool.',
      },
      {
        title: 'Test a small regression set',
        body: 'Use the playground to test an ordinary request, missing information, an out-of-scope request, a follow-up and a tool-triggering prompt when tools exist. Inspect results and available traces instead of judging only whether the prose sounds plausible. Change one configuration element, save and repeat the same small set. Track which version produced each result. This makes a regression visible without turning a short development session into an unbounded sequence of random prompts.',
        example: 'After changing a library helper’s tone, rerun a missing-date request to ensure it still asks for a date instead of inventing one.',
      },
      {
        title: 'Do not confuse saving and publishing',
        body: 'Saving or deploying an agent configuration makes it available inside the Foundry project. Publishing creates an Agent Application with a stable external endpoint and a distinct identity; that is a later release decision. Project integrations use the project endpoint and an agent reference, not merely the raw model endpoint. The extension can generate integration code through View Code. Inspect the generated target and authentication approach before using it, because development and published endpoints have different scopes.',
        example: 'A newly saved test version can be evaluated in the project without immediately replacing the version used by Teams customers.',
      },
    ],
    lab: {
      title: 'Optional rehearsal: diagnose a stale agent',
      goal: 'Plan a minimal version check before changing model behavior.',
      context: `${REHEARSAL} A tool was added in the portal, but the VS Code playground still behaves like the old configuration.`,
      fields: [
        {
          id: 'firstCheck',
          label: 'First check',
          hint: 'Check the asset and version before rewriting instructions.',
          options: [
            { value: 'project-saved-version-refresh', label: 'Confirm project, saved version and refresh the agent' },
            { value: 'increase-temperature', label: 'Increase temperature first' },
            { value: 'publish-to-everyone', label: 'Publish to the organization first' },
          ],
          expected: 'project-saved-version-refresh',
          explanation: 'A missing saved change or wrong project explains stale behavior directly. Sampling and broad distribution do not repair configuration synchronization.',
        },
        {
          id: 'target',
          label: 'Project integration target',
          hint: 'The application wants the named agent, including its saved tools.',
          options: [
            { value: 'project-endpoint-agent-reference', label: 'Project endpoint and agent reference' },
            { value: 'raw-model-only', label: 'Only the underlying model endpoint' },
            { value: 'yaml-file-is-endpoint', label: 'The local YAML filename as a network endpoint' },
          ],
          expected: 'project-endpoint-agent-reference',
          explanation: 'The project agent reference identifies the persisted configuration. Calling only a model does not automatically load that agent definition.',
        },
      ],
      template: `{
  "kind": "conceptual-version-diagnosis",
  "firstCheck": "{{firstCheck}}",
  "applicationTarget": "{{target}}"
}`,
      language: 'json',
      expectedOutput: 'Synthetic interpretation: verify the saved project agent version before changing prompts; a local file edit is not evidence of deployment.',
      takeaway: 'A versioned test identifies which configuration actually ran.',
      azure: {
        title: 'Edit and retest the same Foundry agent',
        sourceId: 59,
        environment: 'portal-and-code',
        minutes: 25,
        cost: 'Playground requests and generated-client tests incur model charges. No additional model, hosted container or published application is required.',
        prerequisites: [ACCESS, 'VS Code with permission to install the Microsoft Foundry extension, plus your own saved declarative lab agent.'],
        steps: [
          {
            title: 'Record the portal baseline',
            surface: 'Microsoft Foundry',
            instruction: 'Open the lab agent in Build > Agents. Inspect its project, model deployment, instructions and saved version. Run one short baseline prompt and retain only nonsecret observations.',
            expected: 'Identify the exact project and saved agent version you will open in VS Code.',
            hint: 'Do not use a shared agent as a scratchpad; duplicate or create a lab agent first.',
          },
          {
            title: 'Connect the editor',
            surface: 'Cloud Shell / local editor',
            instruction: 'In VS Code Extensions, install Microsoft Foundry if needed. Sign in through Azure Resources, expand the intended subscription and Foundry project, then choose Open in Foundry Extension as described in source 190.',
            expected: 'Check that the lab agent and its existing model deployment appear under the selected project.',
            hint: 'If they do not, verify directory, subscription and project, then refresh; do not recreate the deployment.',
          },
          {
            title: 'Compare designer and YAML',
            surface: 'Cloud Shell / local editor',
            instruction: 'Open the agent in the designer and its YAML view. Change one instruction to request a concise first sentence. Inspect the corresponding representation and save using Save to Foundry.',
            expected: 'Confirm the instruction is represented consistently and the save completes without validation errors.',
            hint: 'Use the exercise’s version-specific UI instructions if the extension is labelled AI Toolkit in the supplied recording.',
          },
          {
            title: 'Verify the saved change',
            surface: 'Microsoft Foundry',
            instruction: 'Refresh the agent in the portal. Inspect the version and the exact instruction you changed, then start a fresh playground conversation.',
            expected: 'Verify the server-side configuration contains your change rather than assuming the editor saved it.',
            hint: 'Reopen the selected agent version if the portal is still displaying an earlier revision.',
          },
          {
            title: 'Run a compact regression set',
            surface: 'Microsoft Foundry',
            instruction: 'Try a normal request, a request missing a date, an out-of-scope action and a follow-up. Inspect tool traces if the agent has tools. Record one pass or defect per prompt.',
            expected: 'Observe whether the same saved version follows the new style without losing its task boundary.',
            hint: 'Keep the prompts unchanged between versions so you can attribute differences to your edit.',
          },
          {
            title: 'Inspect integration without publishing',
            surface: 'Cloud Shell / local editor',
            instruction: 'Use the extension’s View Code or the integration sample linked from source 59. Locate the project endpoint, agent reference and credential setup. If you run it, use the exercise’s authenticated environment and a fictional prompt; do not publish.',
            expected: 'Explain which named agent the client targets and distinguish this project call from a published application endpoint.',
            hint: 'Do not copy tokens or generated secrets into curriculum notes, source control or screenshots.',
          },
        ],
        success: 'Confirm a deliberate change is visible in both interfaces and associate your observed tests with its saved version.',
        evidencePrompt: 'Record the version before and after, one regression result and the difference between the project endpoint and a raw model endpoint.',
        cleanup: `Close the lab client, remove only generated lab files that are no longer needed, and retain or delete your own lab agent deliberately. ${PRESERVE}`,
      },
    },
    check: {
      question: 'A portal playground uses the new instruction, but an editor test still uses the old one. What should you do before changing the model?',
      options: [
        { id: 'a', text: 'Raise temperature to make the editor more responsive to changes.', explanation: 'Temperature affects sampling where supported; it cannot select the correct project or saved agent version.' },
        { id: 'b', text: 'Publish the agent to Teams to synchronize the editor.', explanation: 'Publishing changes distribution and identity. It is unnecessary for diagnosing a project-version mismatch.' },
        { id: 'c', text: 'Verify project, saved version and the refreshed agent reference in the editor.', explanation: 'These checks establish whether both interfaces are testing the same persisted configuration.' },
      ],
      correctOptionId: 'c',
      explanation: 'First locate the version boundary; then evaluate model behavior using that known configuration.',
    },
    recall: {
      prompt: 'What must you verify between a local configuration edit and a claim that a new agent version was tested?',
      answer: 'The intended directory and project, successful Save to Foundry, the resulting server-side version, and the version/reference selected by the test. Record the observed result against that version.',
    },
    transfer: 'A customer reports that “the agent changed overnight.” Which version, endpoint and release-scope evidence would you request before blaming the model?',
    sourceNote: `Sources 190, 20 and 223 describe the lifecycle; episode 41 demonstrates a stale unsaved configuration. Extension names vary between Microsoft Foundry and AI Toolkit in the corpus. Project conversation support must not be generalized to the stateless published endpoint described in source 223. Guide: ${GUIDES.agent}`,
  },
  {
    id: 'agents-tools-overview',
    title: 'Choose tools without inventing actions',
    cluster: 'agents-orchestration',
    examDomain: IMPLEMENT,
    summary: 'Separate tool availability, model selection, execution ownership and the evidence returned to the conversation.',
    minutes: 6,
    order: 112,
    prerequisites: ['agents-what-is-an-agent', 'chat-responses'],
    sourceIds: [259, 123, 91, 14, 59, 50, 198],
    steps: [
      {
        title: 'Expose a capability to the model',
        body: 'A tool definition tells a tool-capable model what operation is available and what inputs it accepts. Responses API clients supply tools with their requests; a persisted Foundry agent keeps tool configuration with its definition. By default, the model selects whether a tool is useful for the prompt. Instructions and tool-choice controls can guide that decision. Merely mentioning an API in ordinary instructions does not register a working tool or authorize access to it.',
        example: '“Use the museum calendar” is incomplete unless a calendar-search capability is actually configured and reachable.',
      },
      {
        title: 'Follow selection through execution',
        body: 'The useful sequence is request, selection, execution, returned result and grounded response. Selection is a model decision; execution belongs to the configured provider, tool service or application host. Built-in managed tools can execute through the service, while a custom function call can require application code to run and return its output. A tool catalogue is therefore not a guarantee that every listed operation shares one runtime or one permission model.',
        example: 'A total computed by Code Interpreter runs in its sandbox. A room reservation function may run inside your own application instead.',
      },
      {
        title: 'Inspect results, not just wording',
        body: 'Foundry’s tool catalogue separates configured tools, additional catalogue integrations and custom connections such as OpenAPI or MCP. Add only capabilities that solve the task: excess tools increase selection ambiguity, latency and cost. Test a prompt that should trigger a tool and one that should not, then inspect available invocation details. The term tool here means a callable capability; the branded Foundry Tools services are Azure AI APIs that can be integrated, not a synonym for every tool call.',
        example: 'A simple greeting needs no calculation. A request to total three room charges should show a calculation tool result, not only arithmetic prose.',
      },
    ],
    lab: {
      title: 'Optional rehearsal: plan a tool trace',
      goal: 'Predict what evidence to inspect during the main playground activity.',
      context: `${REHEARSAL} The assistant must total three fictional charges and explain the result.`,
      fields: [
        {
          id: 'capability',
          label: 'Capability to enable',
          hint: 'The task requires a calculation rather than a current public fact.',
          options: [
            { value: 'code-interpreter', label: 'Code Interpreter' },
            { value: 'web-search', label: 'Web search only' },
            { value: 'instructions-only', label: 'Mention Python in instructions without configuring a tool' },
          ],
          expected: 'code-interpreter',
          explanation: 'Code Interpreter supplies an actual calculation runtime. Mentioning Python does not create one, and web retrieval solves a different problem.',
        },
        {
          id: 'evidence',
          label: 'What to inspect',
          hint: 'Distinguish a generated answer from a returned result.',
          options: [
            { value: 'invocation-and-result', label: 'Tool invocation and returned calculation result' },
            { value: 'confident-wording', label: 'How confidently the final answer is phrased' },
            { value: 'tool-in-catalogue', label: 'Whether the tool exists somewhere in the catalogue' },
          ],
          expected: 'invocation-and-result',
          explanation: 'A trace connects the request to execution. Availability and confidence alone do not show that a tool was called.',
        },
      ],
      template: `{
  "kind": "conceptual-tool-observation",
  "capability": "{{capability}}",
  "evidenceToInspect": "{{evidence}}"
}`,
      language: 'json',
      expectedOutput: 'Synthetic interpretation: a calculation request should have execution evidence; a greeting may legitimately have no tool call.',
      takeaway: 'Making a tool available, selecting it and executing it are three separate events.',
      azure: {
        title: 'Observe a real managed tool call',
        sourceId: 59,
        environment: 'foundry',
        minutes: 20,
        cost: 'Model tokens and Code Interpreter sessions may be billed separately. Use a few small calculations and review the current tool pricing.',
        prerequisites: [ACCESS, 'A lab agent using a model that supports the selected tool. No private data or external write operations are needed.'],
        steps: [
          {
            title: 'Open the correct lab agent',
            surface: 'Microsoft Foundry',
            instruction: 'In your project, open Build > Agents and select your own lab agent. Inspect the selected deployment and its tool support using the exercise guidance.',
            expected: 'Confirm you are editing a saved agent definition, not only a standalone model prompt.',
            hint: 'Unsupported tool/model combinations are configuration constraints; do not assume every model supports every tool.',
          },
          {
            title: 'Inspect the catalogue boundary',
            surface: 'Microsoft Foundry',
            instruction: 'Open the agent Tools section or Build > Tools. Locate the Configured, Catalog and Custom areas and identify Code Interpreter.',
            expected: 'See a selectable managed calculation capability distinct from OpenAPI and MCP integrations.',
            hint: 'Catalogue labels can change; follow source 59 and the substantive tool overview in source 91.',
          },
          {
            title: 'Enable one capability',
            surface: 'Microsoft Foundry',
            instruction: 'Add Code Interpreter to the lab agent. In instructions, ask it to use the Python calculation tool for arithmetic requests and explain the returned result concisely. Save the configuration.',
            expected: 'Verify the saved agent lists the tool before sending a calculation request.',
            hint: 'An instruction mentioning Python alone does not enable Code Interpreter.',
          },
          {
            title: 'Inspect a triggered call',
            surface: 'Microsoft Foundry',
            instruction: 'In a fresh Playground conversation, ask for the total and average of fictional charges 12, 18 and 30. Open the available tool invocation details.',
            expected: 'Look for executed calculation evidence and compare the returned total 60 and average 20 with the final answer.',
            hint: 'If no tool is invoked, verify the saved version and instructions; do not count an untraced answer as evidence of execution.',
          },
          {
            title: 'Inspect a no-tool request',
            surface: 'Microsoft Foundry',
            instruction: 'Start another conversation and send a greeting. Compare the invocation details with the calculation run, then record whether a tool was needed and what actually happened.',
            expected: 'Recognize that no tool call can be the correct result when the request needs only a greeting.',
            hint: 'Automatic selection does not promise identical behavior on every run. Record an unnecessary call as an observation to investigate.',
          },
          {
            title: 'Save the observation',
            surface: 'Microsoft Foundry',
            instruction: 'Retain the lab agent version and a short description of both traces. Remove the tool if this agent should return to explain-only behavior, and save that intentional change.',
            expected: 'Know which saved configuration produced each test and whether the tool remains enabled.',
            hint: 'Do not include account identifiers, credentials or unrelated conversation content in evidence.',
          },
        ],
        success: 'Personally inspect one calculation invocation and compare it with a non-calculation prompt; report missing traces or unsupported tooling honestly.',
        evidencePrompt: 'Record what selected the tool, where it executed and which returned result supported the answer.',
        cleanup: `End the lab tool session, remove generated lab artifacts and disable the tool if no longer needed. ${PRESERVE}`,
      },
    },
    check: {
      question: 'A team pasted an API description into agent instructions, but no tool was registered. Why can the agent not reliably execute that API?',
      options: [
        { id: 'a', text: 'The description must be longer so the model can infer network access.', explanation: 'More prose cannot create a callable integration, credentials or an execution host.' },
        { id: 'b', text: 'Instructions can guide selection, but a configured tool and execution path are still required.', explanation: 'The model needs an exposed capability and a responsible runtime; authorization remains a separate prerequisite.' },
        { id: 'c', text: 'Every tool must first be implemented inside the model weights.', explanation: 'Tool calling deliberately extends a deployed model without modifying its weights; integration is outside the model.' },
      ],
      correctOptionId: 'b',
      explanation: 'Describe, register, authorize and execute the capability; instructions alone cover only part of that chain.',
    },
    recall: {
      prompt: 'Reconstruct the tool-calling sequence and identify which part is a model decision.',
      answer: 'The application or agent exposes tools, the model selects a tool and arguments, the responsible runtime executes it, the result returns, and the model forms an answer or continues. Selection is not execution.',
    },
    transfer: 'A customer says a tool is “available but unreliable.” How would you distinguish discovery, selection, authorization, execution and answer-grounding failures?',
    sourceNote: `Sources 259 and 91 describe tool selection and the catalogue; source 123 distinguishes model tools from the Foundry Tools product family. Episode 50 and summary 198 support the lesson. The managed playground activity is a focused slice of ${GUIDES.agent}`,
  },
  {
    id: 'agents-builtin-tools',
    title: 'Match tools to evidence sources',
    cluster: 'agents-orchestration',
    examDomain: IMPLEMENT,
    summary: 'Use calculation, current web information and indexed private files for their distinct jobs, and verify the evidence each produces.',
    minutes: 8,
    order: 116,
    prerequisites: ['agents-tools-overview'],
    sourceIds: [254, 255, 257, 91, 63, 50, 163, 198],
    steps: [
      {
        title: 'Calculate in a sandbox',
        body: 'Code Interpreter gives the model a Python runtime for calculations, data analysis, charts and file processing. The model generates code, the runtime executes it, and results return to the conversation. The corpus describes a sandbox with no external network access, resource limits and possible missing libraries. It is not a route into your private database or an unlimited compute service. Inspect both the generated operation and the result before trusting a critical calculation.',
        example: 'For a fictional visitor-count CSV, calculate a weekly total and inspect the actual rows used rather than accepting an estimated total.',
      },
      {
        title: 'Retrieve current public information',
        body: 'Web search retrieves information available on the public web at request time. Use it when freshness matters beyond the model’s training data, and ask for reputable sources and dates. Its output depends on indexable content, regional availability and changing pages. A citation makes a claim traceable, not automatically correct. Web search does not substitute for a private document collection or grant access to an organization’s internal policies merely because the user names them.',
        example: 'A travel assistant can look up a museum’s recently announced public closure, but cannot infer a company’s unpublished travel allowance from that search.',
      },
      {
        title: 'Index files before asking questions',
        body: 'File Search retrieves relevant chunks from documents uploaded to a vector store. Prepare the files, upload them, wait for indexing and reference the correct store when making the request. Retrieval supplies passages to the model; it does not retrain the model. Missing, outdated or poorly scoped documents limit answer quality, and changed files may need reindexing. Inspect citations and retrieved passages during development, especially when a plausible answer could come from general model knowledge instead.',
        example: 'A small set of fictional travel brochures can support package-specific hotel recommendations that public search would not reliably identify.',
      },
      {
        title: 'Choose the smallest useful combination',
        body: 'These tools address different gaps: Code Interpreter performs computation, web search adds current public evidence, and File Search supplies your indexed documents. They can cooperate, but enabling all of them is not a quality guarantee. Foundry’s Azure AI Search connection targets existing enterprise indexes, while the corpus positions Foundry IQ for broader enterprise knowledge access. A custom function is different again: application logic must execute it. Choose from the required data source and action, not the tool’s popularity.',
        example: 'Use File Search for a tour package’s included hotels, web search for this week’s public opening hours, and calculation for the itinerary cost.',
      },
    ],
    lab: {
      title: 'Optional rehearsal: plan document grounding',
      goal: 'Choose a retrieval path before the real file-search exercise.',
      context: `${REHEARSAL} A fictional travel company wants answers restricted to the brochures it supplied.`,
      fields: [
        {
          id: 'tool',
          label: 'Primary evidence tool',
          hint: 'The authoritative information is in supplied files.',
          options: [
            { value: 'file_search', label: 'File Search over uploaded brochures' },
            { value: 'web_search', label: 'Public web search alone' },
            { value: 'code_interpreter', label: 'A calculation sandbox alone' },
          ],
          expected: 'file_search',
          explanation: 'The answer requires the company’s own documents. Public search and calculation do not establish what its brochure says.',
        },
        {
          id: 'precondition',
          label: 'Before querying',
          hint: 'Uploading and indexing are not the same as mentioning a filename.',
          options: [
            { value: 'indexed-correct-vector-store', label: 'Files indexed in the referenced vector store' },
            { value: 'filename-in-prompt', label: 'Filename typed into the prompt only' },
            { value: 'fine-tune-first', label: 'Fine-tune the model on the brochures first' },
          ],
          expected: 'indexed-correct-vector-store',
          explanation: 'The tool needs searchable content and the right store reference. It retrieves at runtime rather than learning new weights.',
        },
        {
          id: 'review',
          label: 'Grounding evidence',
          hint: 'A fluent answer could still be unsupported.',
          options: [
            { value: 'matched-passage-and-citation', label: 'Matched passage and source citation' },
            { value: 'answer-length', label: 'A long detailed answer' },
            { value: 'model-confidence', label: 'The model says it is certain' },
          ],
          expected: 'matched-passage-and-citation',
          explanation: 'Inspecting the source passage checks whether the document actually supports the answer.',
        },
      ],
      template: `{
  "kind": "conceptual-retrieval-plan",
  "tool": "{{tool}}",
  "precondition": "{{precondition}}",
  "reviewEvidence": "{{review}}"
}`,
      language: 'json',
      expectedOutput: 'Synthetic interpretation: the assistant retrieves indexed brochure passages; it has not learned the brochures permanently or searched every enterprise system.',
      takeaway: 'Grounding is a retrieval-and-verification process, not a claim of model certainty.',
      azure: {
        title: 'Ground a Responses app in files',
        sourceId: 63,
        environment: 'portal-and-code',
        minutes: 30,
        cost: 'Model requests, vector-store storage and tool calls can incur charges. Web search, if enabled in the exercise, can add cost; use only the small supplied file set.',
        prerequisites: [ACCESS, 'Source 63 specifies administrative subscription access for its setup. Use an approved lab subscription and its provided brochures, not private business documents.', 'The exercise-supported local editor or Cloud Shell environment and its stated SDK versions.'],
        steps: [
          {
            title: 'Check subscription and spending scope',
            surface: 'Azure portal',
            instruction: 'Select the approved subscription and lab resource group. Confirm you can perform the exercise setup and review the resource pricing and current cost view before adding services.',
            expected: 'Identify where new exercise resources would be created and which existing resources must be preserved.',
            hint: 'If the required setup permission is missing, record that blocker; do not create resources in another team’s subscription.',
          },
          {
            title: 'Verify a supported deployment',
            surface: 'Microsoft Foundry',
            instruction: 'Open the project and its model deployment page. Confirm a Responses/tool-capable model supported by source 63 is deployed, and locate the endpoint and deployment name the exercise requests.',
            expected: 'Distinguish the deployed name and requested endpoint type from a model’s catalog display name.',
            hint: 'Follow the exercise’s endpoint instructions; a project endpoint and a raw model endpoint are not interchangeable.',
          },
          {
            title: 'Prepare the supplied application',
            surface: 'Cloud Shell / local editor',
            instruction: 'Open the original exercise from source 63, launch its linked lab and follow its environment setup. Configure the supplied app with the deployment information using the prescribed credential flow.',
            expected: 'Confirm the app can authenticate to the intended deployment without placing a secret in code or notes.',
            hint: 'Use the dependency versions and commands in the exercise rather than reconstructing API calls from the rehearsal plan.',
          },
          {
            title: 'Upload and index the sample files',
            surface: 'Cloud Shell / local editor',
            instruction: 'Follow the lab’s vector-store creation and file-upload steps using the provided brochures. Wait for ingestion to complete and inspect the returned store reference and upload status.',
            expected: 'Verify that the files are indexed successfully in the same store that the app will reference.',
            hint: 'Do not query while ingestion is incomplete. A missing or wrong store reference can yield unsupported general answers.',
          },
          {
            title: 'Compare document and public questions',
            surface: 'Cloud Shell / local editor',
            instruction: 'Run the exercise app and ask which hotels one supplied brochure offers. Inspect citations or retrieval details and compare the answer with the file. If the lab also enables web search, ask a separate time-sensitive public question.',
            expected: 'Identify which evidence source supports each answer rather than treating the two searches as interchangeable.',
            hint: 'If a public search feature is unavailable, record it as unavailable; do not infer that File Search searched the web.',
          },
          {
            title: 'Inspect the Azure assets',
            surface: 'Microsoft Foundry',
            instruction: 'Return to the project and inspect the exercise-created file or vector-store assets where the current portal exposes them. Compare their nonsecret names with the app’s references and note which to remove.',
            expected: 'Account for the created assets; use the exercise’s SDK inspection path if the portal does not expose a particular store.',
            hint: 'Portal visibility varies by API version. An SDK-created object need not appear in every portal blade.',
          },
        ],
        success: 'Verify an answer against an actual indexed passage and distinguish it from a public-web answer. The studio cannot confirm ingestion or run the Azure app for you.',
        evidencePrompt: 'Record the nonsecret store label, ingestion outcome and one matched brochure fact; describe a limitation of that evidence.',
        cleanup: `Use the exercise cleanup to delete only the lab vector store, uploaded files and generated artifacts you created. Remove the lab deployment only if dedicated and no longer needed. ${PRESERVE}`,
      },
    },
    check: {
      question: 'A tour assistant needs the hotel included in its own brochure, today’s public museum closure notice and an exact sum of ticket prices. Which mapping fits?',
      options: [
        { id: 'a', text: 'File Search for the brochure, web search for the notice, Code Interpreter for the sum.', explanation: 'Each capability matches the required evidence or operation: indexed files, current public content and executed calculation.' },
        { id: 'b', text: 'Web search for everything because it is the most current.', explanation: 'Fresh public information does not establish private package details, and retrieval is not the same as executing arithmetic.' },
        { id: 'c', text: 'Code Interpreter for everything because Python can access any system.', explanation: 'The corpus describes a sandbox without external network access; it does not automatically retrieve the web or private documents.' },
      ],
      correctOptionId: 'a',
      explanation: 'Start with the missing evidence or operation, then select the tool that supplies it.',
    },
    recall: {
      prompt: 'Reconstruct the File Search preparation and response path without looking at the choices.',
      answer: 'Prepare suitable files, create or select a vector store, upload and wait for indexing, reference that store in the tool configuration, retrieve relevant chunks, generate an answer and inspect its supporting passages or citations.',
    },
    transfer: 'A customer asks whether one search tool can handle all company knowledge and current news. What source, scale and permission boundaries would you clarify?',
    sourceNote: `Main sources 254, 255 and 257 describe the three tools; 91 adds catalogue distinctions. Source 63 is a portal-and-code File Search exercise, not a browser simulation: ${GUIDES.responses}. Availability and web-search naming vary across the corpus’s API versions.`,
  },
  {
    id: 'agents-custom-functions',
    title: 'Complete the function-calling loop',
    cluster: 'agents-orchestration',
    examDomain: IMPLEMENT,
    summary: 'Describe a function, validate the model’s request, execute it in the responsible host and return the matching output before answering.',
    minutes: 8,
    order: 120,
    prerequisites: ['agents-tools-overview'],
    sourceIds: [256, 104, 179, 58, 110, 163],
    steps: [
      {
        title: 'Describe a contract, not an execution',
        body: 'A custom function tool exposes a name, useful description and parameter schema so the model can request a specific operation. The schema describes acceptable input structure; it is not the function implementation. The model may choose the function, but it cannot directly run arbitrary code in your application. Clear names, required parameters and focused responsibilities improve selection. Your application must still interpret the returned request and decide whether it is valid and authorized.',
        example: 'A fictional equipment quote tool takes equipment type and rental hours. Its schema describes those inputs; separate application code computes the quote.',
      },
      {
        title: 'Dispatch and return the right result',
        body: 'With direct Responses function calling, inspect the response for function_call items rather than assuming it contains only final text. Resolve the requested name against the functions your application actually exposes, parse and validate its arguments, then execute the matching function. Return a function_call_output associated with that call, preserving the conversation context. That returned result is what lets the model explain the outcome. Merely printing the result locally leaves the model without the evidence it requested.',
        example: 'If a rental quote request produces a local result of 42 credits, the app must send that result back for the agent to explain it.',
      },
      {
        title: 'Continue until the task is resolved',
        body: 'A response can request more than one function, and a result can lead to another request before the final answer. Treat the interaction as a bounded loop: handle requested calls, return their results and inspect the next response. Keep each result associated with the correct request so unrelated operations are not mixed. A framework can automate dispatch later, but that convenience does not change which host owns the underlying function or its access to external systems.',
        example: 'An equipment assistant first retrieves availability, then calculates a quote, then writes a fictional report; these are separate operations with separate results.',
      },
      {
        title: 'Authorize before causing side effects',
        body: 'Schema-shaped arguments can still be wrong, excessive or unauthorized. Validate ranges and identifiers, enforce the caller’s permissions, and require explicit approval for sensitive operations. Return a clear, safe error when a function fails instead of pretending the action succeeded. Keep tool results distinguishable from user instructions and log only appropriate operational evidence. For this lesson, use deterministic local sample functions or dry-run artifacts, not real purchases, emails or changes to production systems.',
        example: 'A request for negative rental hours should produce a validation error, not a negative invoice or an invented successful booking.',
      },
    ],
    lab: {
      title: 'Optional rehearsal: assign execution ownership',
      goal: 'Prepare a correct dispatch plan for the real custom-tools exercise.',
      context: `${REHEARSAL} A Responses call requests a local rental-quote function implemented in the client application.`,
      fields: [
        {
          id: 'executor',
          label: 'Who executes the function?',
          hint: 'This function is application code, not a managed calculation tool.',
          options: [
            { value: 'validated-application-dispatch', label: 'The application validates and dispatches it' },
            { value: 'model-runs-local-code', label: 'The remote model runs the local code directly' },
            { value: 'schema-runs-itself', label: 'The parameter schema executes automatically' },
          ],
          expected: 'validated-application-dispatch',
          explanation: 'The model requests an operation. Application code or a framework in that host must actually execute a local function.',
        },
        {
          id: 'continuation',
          label: 'Next step after execution',
          hint: 'The model needs the matching result before it can use it.',
          options: [
            { value: 'return-matched-output-and-continue', label: 'Return the matching output and inspect the next response' },
            { value: 'print-only', label: 'Print locally and stop' },
            { value: 'claim-success-before-result', label: 'Report success before checking the result' },
          ],
          expected: 'return-matched-output-and-continue',
          explanation: 'Returning the associated tool output closes the current request; the next response can be final text or another tool request.',
        },
        {
          id: 'validation',
          label: 'Unexpected rental hours',
          hint: 'A schema is not an authorization or business-rule engine.',
          options: [
            { value: 'reject-invalid-range', label: 'Reject invalid ranges with a safe error' },
            { value: 'trust-model-arguments', label: 'Trust all model-generated arguments' },
            { value: 'ignore-failed-call', label: 'Hide the failure and say the quote succeeded' },
          ],
          expected: 'reject-invalid-range',
          explanation: 'The host enforces business constraints before execution and reports failure honestly.',
        },
      ],
      template: `{
  "kind": "conceptual-function-lifecycle",
  "executor": "{{executor}}",
  "afterExecution": "{{continuation}}",
  "invalidInputPolicy": "{{validation}}"
}`,
      language: 'json',
      expectedOutput: 'Synthetic interpretation: validated function request → host execution → matching returned output → next response; no real quote or booking is created here.',
      takeaway: 'Tool selection is delegated to the model; execution, authorization and truthful reporting are not.',
      azure: {
        title: 'Trace a custom function end to end',
        sourceId: 58,
        environment: 'portal-and-code',
        minutes: 30,
        cost: 'Agent/model requests consume tokens. The sample’s local functions need not create chargeable external services, but supporting Azure resources can still incur costs.',
        prerequisites: [ACCESS, 'The editor or Cloud Shell environment specified by source 58, its sample data and dependency versions.', 'Use the supplied sample functions only; do not connect a live booking, purchase or email system.'],
        steps: [
          {
            title: 'Confirm the Azure boundary',
            surface: 'Azure portal',
            instruction: 'Select the approved subscription and resource group for the exercise. Confirm which Foundry resource you may use and which resources are shared.',
            expected: 'Identify the authorized setup scope before the sample creates an agent.',
            hint: 'A missing permission should be resolved with the resource owner, not by placing an access key into the source code.',
          },
          {
            title: 'Collect the project configuration',
            surface: 'Microsoft Foundry',
            instruction: 'Open the project overview and model deployments. Locate the project endpoint and the deployed model name required by the custom-tools exercise.',
            expected: 'Confirm the intended deployment exists and distinguish these settings from a raw model-only configuration.',
            hint: 'The episode creates an agent in code, so its setup needs a model deployment as well as the project endpoint.',
          },
          {
            title: 'Read definition and implementation',
            surface: 'Cloud Shell / local editor',
            instruction: 'Launch the original lab from source 58 and follow its setup. Open one supplied function and its tool definition side by side; identify the name, description, parameters and actual function body.',
            expected: 'Point to the schema and to the separate executable implementation without treating them as the same object.',
            hint: 'Use the original exercise for exact SDK names; several exported corpus pages omit their embedded code blocks.',
          },
          {
            title: 'Locate the dispatcher',
            surface: 'Cloud Shell / local editor',
            instruction: 'Find where the sample inspects function-call responses, selects the matching function and sends its output back. Set a breakpoint or use the sample’s safe logging to observe arguments and returned values.',
            expected: 'Identify application-host execution and the returned output’s association with the requested call.',
            hint: 'Do not log credentials or arbitrary user content. If the sample uses a framework, locate its tool registration and distinguish automated dispatch from model execution.',
          },
          {
            title: 'Run one then several calls',
            surface: 'Cloud Shell / local editor',
            instruction: 'Run the lab app with a fictional request that uses one sample tool. Then use a request requiring two of its advertised capabilities and inspect the continuation through each returned result.',
            expected: 'Trace real function results into the final response and note whether calls occur in one or multiple model turns.',
            hint: 'Do not assume a second response is always final text; inspect for further requested calls.',
          },
          {
            title: 'Exercise a safe failure',
            surface: 'Cloud Shell / local editor',
            instruction: 'Use the sample’s validation path or a local test of its function with an invalid, harmless input. Confirm the error is not reported as a successful action, then record a short redacted trace.',
            expected: 'Separate a validation failure, a tool-execution result and the final agent response.',
            hint: 'If the supplied sample lacks validation, record that limitation and test the function locally without connecting it to real side effects.',
          },
        ],
        success: 'Observe the actual function executing in its host and its result returning to the agent. A local report file is not proof of a real-world reservation or email.',
        evidencePrompt: 'Describe one requested function, its validated input, where it ran and how its output reached the model; omit all credentials.',
        cleanup: `Stop the sample app, remove only its generated test reports and exercise-created agents or conversations, and follow source 58’s resource cleanup. ${PRESERVE}`,
      },
    },
    check: {
      question: 'A model returns a function-call request for an application’s quote function. The app prints “42 credits” locally and ends. Why is the loop incomplete?',
      options: [
        { id: 'a', text: 'The quote function must be moved into the model weights.', explanation: 'The function is intentionally external; changing model weights would not return the missing result.' },
        { id: 'b', text: 'The model should have executed the local function without the app.', explanation: 'Direct function calling delegates execution to the application host. The remote model cannot simply run local code.' },
        { id: 'c', text: 'The app must return the output associated with that call and let the model continue.', explanation: 'Local console output is not conversation input. Returning the matched result lets the model form a grounded answer or request another operation.' },
      ],
      correctOptionId: 'c',
      explanation: 'A function call is a request-response contract that must be completed in the conversation, not just in the local console.',
    },
    recall: {
      prompt: 'Reconstruct the function-calling lifecycle, including who validates, who executes and what happens after returning a result.',
      answer: 'Describe the function; inspect the model’s requested call; validate its name, arguments and authorization in the host; execute it; return the matching function-call output; inspect the next response and continue within a bounded loop if more calls are requested.',
    },
    transfer: 'A customer wants an agent to issue refunds. Where would you place authorization, input validation, approval and evidence of the actual payment-system result?',
    sourceNote: `Sources 256 and 179 explicitly distinguish model selection from host execution, reinforced by episode 110. Read broad “automatic tool calling” wording in context: a framework may dispatch functions, but a tool schema does not execute local code. Guide: ${GUIDES.custom}`,
  },
  {
    id: 'agents-custom-tool-integration',
    title: 'Place custom tools deliberately',
    cluster: 'agents-orchestration',
    examDomain: IMPLEMENT,
    summary: 'Choose application functions, Azure Functions, OpenAPI or Logic Apps from runtime ownership and the existing system boundary.',
    minutes: 7,
    order: 124,
    prerequisites: ['agents-custom-functions'],
    sourceIds: [265, 179, 104, 122, 167, 213, 58, 110, 91],
    steps: [
      {
        title: 'Locate the code that must run',
        body: 'Use a custom tool when the task needs business-specific data or an operation the existing tools do not provide. A local function runs in the application host and may call another API; Azure Functions runs deployed code in a serverless Azure service. Functions triggers determine when code starts, while bindings connect input and output resources. The corpus’s queue example sends a request through storage queues. These are different execution and operational responsibilities despite their similar names.',
        example: 'A workshop quote can run in a small client function; an existing queue-triggered stock-check service may be better exposed through its Azure Functions integration.',
      },
      {
        title: 'Describe an existing HTTP service',
        body: 'An OpenAPI tool describes a callable HTTP API using an OpenAPI 3.0 specification, allowing Foundry to map parameters and process responses. Register the specification as a tool rather than pasting it into instructions. The source lists anonymous, API-key and managed-identity authentication options; select only what the actual API supports and your organization permits. The specification is a contract, not the service implementation, and it does not remove network reachability or authorization requirements.',
        example: 'A museum already has an authenticated equipment-availability API. Registering its supported OpenAPI contract avoids rewriting the same service inside every assistant.',
      },
      {
        title: 'Choose orchestration without hiding ownership',
        body: 'Azure Logic Apps offers low-code workflows and connectors when a tool action needs to coordinate existing services. It is not another name for an ordinary Python function, nor does a connector guarantee permission to every connected system. Compare the existing interface, execution location, authentication, latency and side effects before choosing an integration. Start with a read-only capability and inspect its returned data. An agent can select the tool, but the deployed service or host still owns execution.',
        example: 'A request to look up stock needs one read operation; an approved intake process spanning several systems might instead call a controlled Logic Apps workflow.',
      },
    ],
    lab: {
      title: 'Optional rehearsal: choose an integration boundary',
      goal: 'Design a tool connection without inventing a new service or bypassing authorization.',
      context: `${REHEARSAL} A team already operates an HTTP stock API with an OpenAPI 3.0 contract and managed-identity authentication.`,
      fields: [
        {
          id: 'integration',
          label: 'Integration type',
          hint: 'Reuse the existing service contract.',
          options: [
            { value: 'registered-openapi-tool', label: 'Register an OpenAPI tool' },
            { value: 'specification-in-instructions', label: 'Paste the specification into instructions only' },
            { value: 'rewrite-as-local-calculation', label: 'Replace the stock API with an estimated local calculation' },
          ],
          expected: 'registered-openapi-tool',
          explanation: 'A registered OpenAPI tool exposes the existing callable contract. Instructions alone do not integrate it, and an estimate is not live stock data.',
        },
        {
          id: 'authentication',
          label: 'Authentication boundary',
          hint: 'Use the supported mechanism and least-privilege resource permissions.',
          options: [
            { value: 'managed-identity-with-api-permission', label: 'Managed identity with permission on the API' },
            { value: 'anonymous-because-schema-exists', label: 'Anonymous because the specification is available' },
            { value: 'key-in-description', label: 'Put an access key in the tool description' },
          ],
          expected: 'managed-identity-with-api-permission',
          explanation: 'The API’s authentication requirement still applies. A contract is not authorization, and tool descriptions must not contain secrets.',
        },
      ],
      template: `{
  "kind": "conceptual-service-integration",
  "integration": "{{integration}}",
  "authentication": "{{authentication}}"
}`,
      language: 'json',
      expectedOutput: 'Synthetic interpretation: Foundry calls an existing authorized service; the specification neither deploys the API nor grants permission.',
      takeaway: 'Choose the connection mechanism from where the capability already lives.',
      azure: {
        title: 'Inspect where a custom tool executes',
        sourceId: 58,
        environment: 'portal-and-code',
        minutes: 25,
        cost: 'Running the sample consumes model tokens. This walkthrough does not require deploying Azure Functions or Logic Apps; doing so separately would introduce additional charges.',
        prerequisites: [ACCESS, 'The original source 58 custom-tools sample and its supported development environment.', 'An existing lab agent or permission to create the exercise agent. Do not use a production API for this inspection.'],
        steps: [
          {
            title: 'Identify existing Azure resources',
            surface: 'Azure portal',
            instruction: 'Open the approved lab resource group and inspect which Foundry, storage, Function App or Logic App resources already exist. Do not create absent services just to match the list.',
            expected: 'Distinguish an actual deployed Azure service from a function that exists only in application code.',
            hint: 'An empty Function Apps list does not prevent the local-function exercise from working.',
          },
          {
            title: 'Inspect the custom-tool entry points',
            surface: 'Microsoft Foundry',
            instruction: 'In the lab project, open Build > Tools or the agent’s Add Tool view. Inspect the Custom options for OpenAPI and MCP, without registering an unapproved endpoint.',
            expected: 'Locate where service contracts are registered, distinct from the agent Instructions field.',
            hint: 'Use source 91 and the original exercise for your portal version. A catalogue entry is not evidence that you have permission to invoke a service.',
          },
          {
            title: 'Prepare the real custom-function sample',
            surface: 'Cloud Shell / local editor',
            instruction: 'Launch source 58’s linked exercise. Configure its project endpoint and model deployment from Foundry, then follow its credential and dependency setup.',
            expected: 'Have the supplied application connected to the intended project using the prescribed authentication flow.',
            hint: 'This exercise demonstrates local custom functions; it does not silently deploy an Azure Function App.',
          },
          {
            title: 'Locate the runtime and data dependency',
            surface: 'Cloud Shell / local editor',
            instruction: 'Open a supplied function and identify whether it reads a sample file, writes a local report or calls a service. Find the corresponding registered tool definition and dispatcher.',
            expected: 'Identify separately the callable contract, executable code, input data and execution host.',
            hint: 'A function name is not enough to infer its host; inspect the code and deployment boundary.',
          },
          {
            title: 'Run a read-only sample request',
            surface: 'Cloud Shell / local editor',
            instruction: 'Run the exercise with a fictional lookup or quote request. Inspect the returned data and the host-side execution evidence without adding a live external system.',
            expected: 'Trace a genuine sample tool result into the response and state accurately that it came from sample data.',
            hint: 'Do not describe a local fixture as current inventory, or a written report as an actual booking.',
          },
          {
            title: 'Record the alternative deployment boundary',
            surface: 'Azure portal',
            instruction: 'Return to the lab resource list and compare the observed local execution with the Azure Functions, OpenAPI and Logic Apps options described in source 104. Record which additional service and permissions each alternative would require.',
            expected: 'Confirm no Function App or workflow was deployed merely by registering the local function.',
            hint: 'This final comparison is architecture inspection, not a claim that the alternative integrations were executed.',
          },
        ],
        success: 'Demonstrate one actual custom-function call and explain why it ran in its observed host rather than in an imagined Azure service.',
        evidencePrompt: 'Record the tool’s contract, host and data source, plus the permission boundary you would need to cross to replace it with an existing HTTP API.',
        cleanup: `Stop the sample application and delete only its lab reports, agents or conversations when no longer needed. No new Functions or Logic Apps resource is required by this lab. ${PRESERVE}`,
      },
    },
    check: {
      question: 'An organization already exposes an approved stock lookup through an OpenAPI 3.0 service. What is the most direct integration?',
      options: [
        { id: 'a', text: 'Paste the full API specification into the system instructions.', explanation: 'That may describe the service to the model but does not register a callable, authenticated tool.' },
        { id: 'b', text: 'Register the API as an OpenAPI tool and configure its supported authentication.', explanation: 'This reuses the existing HTTP contract while preserving the service’s execution and authorization boundary.' },
        { id: 'c', text: 'Treat every HTTP API as an Azure Functions queue trigger.', explanation: 'Queue-triggered Functions are one integration option, not the runtime or interface of every existing HTTP service.' },
      ],
      correctOptionId: 'b',
      explanation: 'Match the integration to the system that already exists instead of adding an unrelated execution platform.',
    },
    recall: {
      prompt: 'Distinguish a custom application function, Azure Functions, an OpenAPI tool and a Logic Apps action by what each supplies.',
      answer: 'An application function supplies host-executed logic. Azure Functions supplies deployed event-driven compute with triggers and bindings. OpenAPI supplies the contract for invoking an existing HTTP API. Logic Apps supplies low-code service coordination and connectors.',
    },
    transfer: 'A customer already has a business API and a low-code approval workflow. What would make you expose each as a separate tool rather than reimplement both inside the agent?',
    sourceNote: `Main sources 265, 179 and 104 establish the options; introductions, assessment 167, summary 213 and episode 110 support them. The actual lab runs the local-function exercise, not an invented portal-only Azure Functions deployment: ${GUIDES.custom}`,
  },
  {
    id: 'agents-mcp',
    title: 'Discover tools through MCP',
    cluster: 'agents-orchestration',
    examDomain: IMPLEMENT,
    summary: 'Separate a discoverable tool catalogue, a client bridge and managed remote MCP execution, including approval and trust boundaries.',
    minutes: 9,
    order: 128,
    prerequisites: ['agents-custom-functions'],
    sourceIds: [233, 108, 240, 119, 158, 193, 60, 109, 91],
    steps: [
      {
        title: 'Replace duplicated tool wiring',
        body: 'Model Context Protocol standardizes how a client discovers and invokes tools exposed by a server. The server publishes names, descriptions and input structures; the client retrieves the catalogue and makes permitted capabilities available to the agent. This avoids hand-maintaining a separate description for every tool in every application. Discovery can reflect an updated server catalogue, but it is not a grant of trust or permission, and an existing client may need to refresh its discovered tools.',
        example: 'A shared equipment service can expose stock and maintenance lookups to several assistants without copying each description into every codebase.',
      },
      {
        title: 'Understand the explicit client bridge',
        body: 'The corpus’s client-server pattern starts an MCP session, discovers contracts with list_tools and wraps each operation for the agent. An asynchronous wrapper uses call_tool to forward the requested name and arguments to the server, then returns the result. The model does not directly enter a developer’s laptop. If the server is local, application-side code must bridge that connection. The server owns tool implementations; the bridge owns communication and registration with the agent.',
        example: 'A local inventory demo discovers two functions, wraps them as agent tools and forwards calls through the running local MCP session.',
      },
      {
        title: 'Use managed remote connections deliberately',
        body: 'Foundry also supports a remote MCP integration where the service connects to a reachable server, discovers its tools and handles invocation without your hand-written wrapper. The configuration identifies a server label and URL and can constrain allowed tools. Choose the path from network reachability and the supported integration, not from a belief that all MCP servers run inside Foundry. A laptop-only endpoint is not automatically reachable by a cloud-hosted agent service.',
        example: 'A managed connection can query an approved public documentation MCP server; a private local inventory process requires the exercise’s client bridge.',
      },
      {
        title: 'Keep approval separate from discovery',
        body: 'Authentication establishes access to the server; an allowlist limits available tools; approval controls whether a proposed invocation proceeds. Those are different controls. Inspect the requested operation and arguments before approving a sensitive call, and return the approval response associated with that request. The corpus shows both manual approval handling and demonstration auto-approval. Do not generalize the latter into production policy. Store credentials through the supported secure configuration, never in tool descriptions or learning evidence.',
        example: 'Discovering a stock-adjustment tool does not justify using it. A read-only lookup can be allowed while a write operation remains unavailable or gated.',
      },
    ],
    lab: {
      title: 'Optional rehearsal: choose the MCP path',
      goal: 'Assign discovery, invocation and approval responsibilities before connecting real tools.',
      context: `${REHEARSAL} A sample inventory MCP server runs only on the developer’s machine.`,
      fields: [
        {
          id: 'connection',
          label: 'Connection approach',
          hint: 'The cloud service cannot assume access to a local-only process.',
          options: [
            { value: 'local-client-bridge', label: 'Use the application’s local MCP client bridge' },
            { value: 'cloud-calls-laptop-automatically', label: 'Assume Foundry can call the laptop automatically' },
            { value: 'copy-tool-names-only', label: 'Copy the tool names into instructions only' },
          ],
          expected: 'local-client-bridge',
          explanation: 'The local application maintains the session, discovers tools and forwards calls. A remote cloud integration requires a reachable server.',
        },
        {
          id: 'approval',
          label: 'Sensitive tool policy',
          hint: 'Discovery, authorization and approval solve different problems.',
          options: [
            { value: 'inspect-and-approve-per-request', label: 'Inspect and approve each sensitive request' },
            { value: 'approve-all-discovered-tools', label: 'Approve every discovered tool automatically' },
            { value: 'credentials-in-tool-description', label: 'Put credentials in descriptions to avoid prompts' },
          ],
          expected: 'inspect-and-approve-per-request',
          explanation: 'A catalogue does not establish safe intent. Review the actual operation and keep authentication data separate from model-visible descriptions.',
        },
      ],
      template: `{
  "kind": "conceptual-mcp-plan",
  "connection": "{{connection}}",
  "sensitiveToolPolicy": "{{approval}}"
}`,
      language: 'json',
      expectedOutput: 'Synthetic interpretation: local client → discovered tool wrapper → local MCP server; cloud reachability and permission have not been created by this plan.',
      takeaway: 'MCP standardizes a boundary; it does not erase the boundary.',
      azure: {
        title: 'Connect and inspect MCP tools',
        sourceId: 60,
        environment: 'portal-and-code',
        minutes: 35,
        cost: 'Model calls and any remote MCP provider may be charged. The local sample server uses your development environment; avoid deploying a public server for this lab.',
        prerequisites: [ACCESS, 'The original source 60 exercise, its supported Python environment and permission to run its local sample server.', 'Use only the exercise’s approved documentation server and fictional inventory data; do not supply production tokens.'],
        steps: [
          {
            title: 'Verify the Azure project',
            surface: 'Azure portal',
            instruction: 'Select the intended subscription and lab resource group, then locate the Foundry resource used by source 60. Confirm the resource owner permits the exercise connections.',
            expected: 'Identify the authorized Foundry resource and avoid creating duplicate infrastructure.',
            hint: 'An MCP server and a Foundry resource are different services; access to one does not imply access to the other.',
          },
          {
            title: 'Inspect model and connection prerequisites',
            surface: 'Microsoft Foundry',
            instruction: 'Open the project overview and model deployments. Confirm a supported deployment and locate the project endpoint. Inspect the Tools catalogue for MCP support without adding unapproved servers.',
            expected: 'Verify the project configuration needed by the sample and distinguish a catalogue option from a configured connection.',
            hint: 'If remote MCP is unavailable for this configuration, retain that limitation in your evidence instead of disabling safeguards.',
          },
          {
            title: 'Configure the managed remote sample',
            surface: 'Cloud Shell / local editor',
            instruction: 'Launch source 60’s linked lab and follow its setup. For the remote example, use the approved documentation-server URL from the exercise, a clear server label and its supported approval setting.',
            expected: 'Inspect a remote tool definition rather than a hand-written wrapper for every documentation operation.',
            hint: 'Source 240 describes approval using inconsistent boolean and string wording. Follow the installed exercise version rather than mixing parameter forms.',
          },
          {
            title: 'Review a proposed invocation',
            surface: 'Cloud Shell / local editor',
            instruction: 'Run a harmless documentation question. Inspect any approval request, its operation and arguments, then approve only that request through the sample’s supported flow and inspect the returned source evidence.',
            expected: 'Observe the difference between discovering a tool, proposing a call and receiving its result.',
            hint: 'If the example auto-approves, inspect that code and record it as a demonstration shortcut, not evidence of human review.',
          },
          {
            title: 'Run the local client-server path',
            surface: 'Cloud Shell / local editor',
            instruction: 'Follow the exercise’s local inventory MCP setup using its specified transport. Start the server and client as directed, inspect the discovered catalogue and locate the generated wrappers or registered function tools.',
            expected: 'See the local application maintain the connection and forward calls; do not claim the cloud directly contacted a laptop-only endpoint.',
            hint: 'Check that the sample server is running and the client uses its actual transport and address. Do not expose it publicly to bypass a setup error.',
          },
          {
            title: 'Trace a read-only inventory request',
            surface: 'Cloud Shell / local editor',
            instruction: 'Ask for the sample inventory and compare the returned values with its fixture data. Trace the call from model request through wrapper and server to result, then stop at this checkpoint.',
            expected: 'Verify one genuine MCP round trip using synthetic data and state which component executed the tool.',
            hint: 'Sample inventory is not live business inventory; an answer without a successful tool result does not validate the connection.',
          },
        ],
        success: 'Inspect at least one real MCP tool invocation and correctly identify whether the managed remote service or local client bridge carried it. Record any uncompleted path explicitly.',
        evidencePrompt: 'Record the discovered capability, connection path and approval behavior using only harmless sample arguments, never authentication headers.',
        cleanup: `Stop only your exercise client and server processes. Remove exercise-created agents, conversations and MCP connections that are no longer needed; do not revoke shared credentials or remove shared servers. ${PRESERVE}`,
      },
    },
    check: {
      question: 'A developer gives Foundry a laptop-only MCP address and expects the cloud agent to use its inventory tools. Which correction fits the architecture?',
      options: [
        { id: 'a', text: 'Use the local MCP client bridge, or deliberately provide an approved reachable remote service.', explanation: 'The connection must cross a real network boundary. Local bridging and managed remote MCP are distinct supported patterns.' },
        { id: 'b', text: 'List more tool names in the system prompt.', explanation: 'Tool names do not create network reachability, a session or an execution path.' },
        { id: 'c', text: 'Turn off approval because approval creates network isolation.', explanation: 'Approval gates invocation; it does not determine whether a cloud service can reach a local process.' },
      ],
      correctOptionId: 'a',
      explanation: 'Choose an integration path that can actually reach the server, then configure authentication, allowed tools and approval separately.',
    },
    recall: {
      prompt: 'Explain the responsibilities of an MCP server and client, then name the three separate access controls discussed here.',
      answer: 'The server exposes and executes tools. The client discovers their contracts and invokes them, directly or through registered wrappers. Authentication grants server access, an allowlist restricts available capabilities and approval gates individual proposed calls.',
    },
    transfer: 'Several customer teams want to share tools through MCP. What would you establish about ownership, network access, catalogue changes and approval before integrating their servers?',
    sourceNote: `Sources 108 and 233 teach the explicit client bridge; 240 teaches managed remote MCP. Source 240 describes require_approval both as a boolean and as always/never, so exact SDK syntax is version-sensitive. Episode 109 demonstrates both paths; assessments and summary support, not replace, them. Guide: ${GUIDES.mcp}`,
  },
  {
    id: 'agents-framework',
    title: 'Use Agent Framework abstractions',
    cluster: 'agents-orchestration',
    examDomain: IMPLEMENT,
    summary: 'Connect providers, agent sessions and tools through a common SDK while preserving provider-specific state and execution responsibilities.',
    minutes: 9,
    order: 132,
    prerequisites: ['agents-custom-functions', 'agents-builtin-tools'],
    sourceIds: [234, 34, 4, 237, 124, 146, 195, 72, 48],
    steps: [
      {
        title: 'Separate the agent from its provider',
        body: 'Microsoft Agent Framework combines agent abstractions with tools, conversation management and workflow support. The corpus presents it as the successor bringing together Semantic Kernel and AutoGen capabilities. An agent describes behavior while a chat client connects to the underlying provider through a common abstraction such as BaseChatClient. Switching provider can preserve much of the agent logic, but does not guarantee identical features: hosted tools, authentication and service-side history still depend on the selected provider.',
        example: 'A workshop assistant can retain its task instructions while changing chat clients, but must recheck whether the new provider supports its file-search tool.',
      },
      {
        title: 'Keep conversation state in the right place',
        body: 'A session groups the messages needed for a continuing interaction. AgentSession and the structured roles USER, ASSISTANT, SYSTEM and TOOL distinguish who supplied each piece of context; a tool result is not an ordinary user request. With a supporting Foundry provider, history can live service-side. Other providers may keep it only in the local session object. Reusing the correct user’s session enables follow-ups; creating a fresh session or mixing users changes the context and isolation.',
        example: 'After discussing a fictional claim for 24 credits, a follow-up asking for the total needs the same conversation, not another employee’s session.',
      },
      {
        title: 'Let the framework dispatch registered tools',
        body: 'Typed function parameters, descriptions and docstrings let the framework derive a tool contract; the source also describes an explicit tool decorator and optional schema control. Register functions with the agent and the framework can dispatch selected calls and return results automatically. That removes repetitive switchboard code, not the function’s execution host or security obligations. Tools can have approval policies, and a specialized agent can itself be exposed as a tool to another agent.',
        example: 'An expense formatter tool prints a fictional email payload in the sample. Automatic invocation does not mean an email was actually delivered.',
      },
      {
        title: 'Connect, run and inspect deliberately',
        body: 'Start with a Foundry project and model deployment, obtain the project endpoint and configure supported Azure credentials rather than hardcoding secrets. Create the provider client, define the agent and tools, establish a session and run a request. Nonstreaming waits for the completed answer; streaming exposes incremental updates that still need completion handling. DefaultAzureCredential can resolve development or managed identities, whereas a CLI-specific credential is tied to that development environment. Match the original exercise’s package version.',
        example: 'A demo authenticated through a developer’s Azure CLI login needs a deliberate managed-identity configuration before it can run as a production service.',
      },
    ],
    lab: {
      title: 'Optional rehearsal: plan a session',
      goal: 'Preserve roles and state boundaries before the real Agent Framework exercise.',
      context: `${REHEARSAL} A user submits fictional expenses and asks a follow-up after a calculation tool returns.`,
      fields: [
        {
          id: 'session',
          label: 'Follow-up context',
          hint: 'Preserve this user’s conversation without sharing it across users.',
          options: [
            { value: 'reuse-this-user-session', label: 'Reuse this user’s session' },
            { value: 'new-session-every-message', label: 'Create an empty session for every message' },
            { value: 'one-session-for-all-users', label: 'Use one session for all employees' },
          ],
          expected: 'reuse-this-user-session',
          explanation: 'A follow-up needs the relevant prior context, but combining users would break the intended isolation.',
        },
        {
          id: 'resultRole',
          label: 'Tool-result role',
          hint: 'The result comes from execution, not a new user instruction.',
          options: [
            { value: 'TOOL', label: 'TOOL' },
            { value: 'USER', label: 'USER' },
            { value: 'SYSTEM', label: 'SYSTEM' },
          ],
          expected: 'TOOL',
          explanation: 'The structured TOOL role keeps execution output distinguishable from user requests and system instructions.',
        },
        {
          id: 'persistence',
          label: 'Persistence assumption',
          hint: 'A common SDK interface does not make every provider stateful.',
          options: [
            { value: 'verify-provider-history-support', label: 'Verify the provider’s history support' },
            { value: 'all-sessions-survive-restarts', label: 'Assume every session survives a restart' },
            { value: 'streaming-means-persistent', label: 'Assume streaming guarantees persistence' },
          ],
          expected: 'verify-provider-history-support',
          explanation: 'Service-backed history and in-memory session state have different lifetimes; streaming describes delivery, not storage.',
        },
      ],
      template: `{
  "kind": "conceptual-framework-session",
  "conversation": "{{session}}",
  "executionResultRole": "{{resultRole}}",
  "historyPolicy": "{{persistence}}"
}`,
      language: 'json',
      expectedOutput: 'Synthetic interpretation: a follow-up uses the same user context and a distinct tool result; persistence remains a provider-specific fact to verify.',
      takeaway: 'An abstraction simplifies calls without making providers, identities or state lifetimes identical.',
      azure: {
        title: 'Run the expense agent with Agent Framework',
        sourceId: 72,
        environment: 'portal-and-code',
        minutes: 30,
        cost: 'Agent and model invocations incur usage charges. The sample’s printed email/report need not send real mail or provision a mail service.',
        prerequisites: [ACCESS, 'The development environment and packages specified by the original source 72 exercise.', 'Fictional expense data only. Inspect the sample tool before running it to ensure it prints or writes a test artifact rather than sending live mail.'],
        steps: [
          {
            title: 'Verify the approved resources',
            surface: 'Azure portal',
            instruction: 'Open the lab resource group and confirm the intended Foundry resource and your authorized access. Keep a list of existing shared assets so cleanup remains scoped.',
            expected: 'Know which subscription and resource group the exercise uses.',
            hint: 'Changing to a broadly privileged account is not a substitute for understanding the required permissions.',
          },
          {
            title: 'Locate the provider settings',
            surface: 'Microsoft Foundry',
            instruction: 'Open the project overview and deployments. Locate the project endpoint and a supported deployed model; use the exact deployment name in the exercise’s configuration.',
            expected: 'Verify both settings exist before running any SDK code.',
            hint: 'A provider client can fail because of an incorrect endpoint or undeployed model even when the agent instructions are valid.',
          },
          {
            title: 'Prepare the version-matched sample',
            surface: 'Cloud Shell / local editor',
            instruction: 'Launch the original exercise linked by source 72 and follow its setup and authentication instructions. Inspect its imports, provider client, agent construction and registered expense function.',
            expected: 'Identify the actual installed SDK naming and the tool’s execution behavior.',
            hint: 'Do not combine AgentThread examples with AgentSession code unless the exercise version explicitly supports that API.',
          },
          {
            title: 'Run a harmless expense request',
            surface: 'Cloud Shell / local editor',
            instruction: 'Use the supplied fictional expense data and run the application. Observe the function being invoked and the generated claim payload or test artifact.',
            expected: 'Verify tool dispatch without a hand-written dispatcher in the agent application and identify what, if anything, was actually sent.',
            hint: 'Episode 48 prints the email payload. A printed recipient, subject and body are not evidence of mail delivery.',
          },
          {
            title: 'Inspect conversation handling',
            surface: 'Cloud Shell / local editor',
            instruction: 'Locate the sample’s conversation or session handling. Where the exercise supports multiple turns, reuse the same session for a follow-up about the fictional total, then contrast a fresh session.',
            expected: 'Identify what context is retained and whether the chosen provider stores it service-side or only in process memory.',
            hint: 'If the starter is single-turn, record that limitation and inspect the module’s session pattern instead of claiming a persistence test.',
          },
          {
            title: 'Inspect completion and credential scope',
            surface: 'Cloud Shell / local editor',
            instruction: 'Find where the app receives the completed response, or finishes consuming a stream. Identify whether authentication is CLI-specific or an environment-resolved credential, and note what a production host would need.',
            expected: 'Distinguish incremental output from completion and development login from production identity.',
            hint: 'Do not export the credential object, tokens or environment secrets as learning evidence.',
          },
        ],
        success: 'Observe a real SDK-driven tool invocation and accurately describe its side effect, provider and state handling. Record any untested persistence claim as untested.',
        evidencePrompt: 'Record the package/API naming used by your exercise, where the expense function ran and why printed email content is not delivery evidence.',
        cleanup: `Stop the exercise app and remove only its generated sample artifacts and exercise-created agent or conversation resources. ${PRESERVE}`,
      },
    },
    check: {
      question: 'An expense agent’s function executes without explicit dispatcher code, and the terminal prints an email payload. What does that establish?',
      options: [
        { id: 'a', text: 'The model executed the local function directly and delivered the email.', explanation: 'The framework dispatches the function in its host. Printing a payload does not prove an external mail operation occurred.' },
        { id: 'b', text: 'All providers will preserve this conversation after the process restarts.', explanation: 'State persistence depends on the provider and session implementation, not on whether a tool was automatically dispatched.' },
        { id: 'c', text: 'The framework dispatched the registered function; its implementation determines the actual side effect.', explanation: 'Automatic dispatch removes application boilerplate, while the function body and service integration determine whether it prints, stores or sends anything.' },
      ],
      correctOptionId: 'c',
      explanation: 'Convenient orchestration does not change execution ownership or manufacture evidence of an external action.',
    },
    recall: {
      prompt: 'Explain the jobs of a provider chat client, an agent, a session and a registered tool.',
      answer: 'The client connects to a model/service provider. The agent combines instructions and capabilities. The session carries a user’s structured conversation context. A registered tool exposes executable functionality that the framework can dispatch in the appropriate host.',
    },
    transfer: 'A customer wants to swap model providers without changing its agent. Which tool, identity and conversation-persistence assumptions would you validate before promising portability?',
    sourceNote: `SDK naming conflict: sources 234 and 237 describe Agent and AgentSession; assessment 146 and the closing part of episode 48 use ChatAgent, AzureAIAgentClient and AgentThread. Treat those as different API snapshots, not interchangeable constructors. Sources 34 and 4 provide the main state/tool mechanisms. Use the package-matched exercise: ${GUIDES.framework}`,
  },
  {
    id: 'agents-workflows',
    title: 'Build a visible agent workflow',
    cluster: 'agents-orchestration',
    examDomain: IMPLEMENT,
    summary: 'Move process control from sprawling instructions into explicit Foundry nodes, with agent reasoning and human pauses where they belong.',
    minutes: 7,
    order: 136,
    prerequisites: ['agents-types', 'agents-tools-overview'],
    sourceIds: [238, 36, 3, 105, 118, 66, 15, 219],
    steps: [
      {
        title: 'Give the process its own structure',
        body: 'A long prompt is a poor place to hide every business branch. A Foundry workflow defines connected nodes and explicit control flow, while agent nodes perform interpretation or generation at chosen points. This separates a repeatable process from the model’s reasoning within a step. A workflow can combine several specialists, but does not need many agents to be useful. Start with the smallest sequence that makes the required order and stopping point visible.',
        example: 'For a community event, first summarize a request, then draft a reply. The draft must use the summary rather than independently guessing what was requested.',
      },
      {
        title: 'Assign each node a concrete job',
        body: 'Invoke agent nodes call existing project agents or create focused new ones. Flow nodes branch, loop or jump; data-transformation nodes set, reset or parse values; basic chat nodes send messages or collect input. An End node marks completion. Passing an agent’s output into a later node requires an explicit input and output connection or variable mapping. Reusing one agent in multiple workflows does not automatically make every workflow share one conversation or the same local values.',
        example: 'A summary node stores its response, and the drafting node receives that stored summary. Connecting boxes visually is not enough if the input mapping is wrong.',
      },
      {
        title: 'Make waiting and completion observable',
        body: 'A sequential workflow follows a known order. A human-in-the-loop workflow adds a deliberate question or approval pause before continuing. Sending a message that says “escalated” is not the same as collecting a human decision or creating a real support ticket. The designer’s preview helps inspect the path and intermediate outputs. Save explicitly before testing or stopping: the corpus says workflows are not autosaved. A small saved workflow is a useful stopping point, not evidence of mastery.',
        example: 'A fictional room-change request can pause to ask for the preferred date; it should not continue as though a missing date had been supplied.',
      },
    ],
    lab: {
      title: 'Optional rehearsal: sketch the data flow',
      goal: 'Plan one visible handoff before creating it in the Foundry designer.',
      context: `${REHEARSAL} A summary agent feeds a draft agent, and a person supplies missing event details.`,
      fields: [
        {
          id: 'nextInput',
          label: 'Draft agent input',
          hint: 'The second step depends on the first result.',
          options: [
            { value: 'stored-summary-output', label: 'The stored summary output' },
            { value: 'empty-context', label: 'An empty context with no request data' },
            { value: 'unrelated-last-run', label: 'Output from an unrelated earlier run' },
          ],
          expected: 'stored-summary-output',
          explanation: 'Explicit data mapping makes the dependency real and keeps the request context attached to the right run.',
        },
        {
          id: 'humanStep',
          label: 'Missing-details step',
          hint: 'A real pause needs an input-producing action.',
          options: [
            { value: 'ask-and-wait', label: 'Ask a question and wait for the response' },
            { value: 'say-approved', label: 'Send a message saying “approved”' },
            { value: 'invent-date', label: 'Let the draft invent a date' },
          ],
          expected: 'ask-and-wait',
          explanation: 'A question node can capture the missing information. Announcing approval or inventing a value does not obtain user input.',
        },
      ],
      template: `{
  "kind": "conceptual-workflow-plan",
  "draftInput": "{{nextInput}}",
  "missingDetails": "{{humanStep}}"
}`,
      language: 'json',
      expectedOutput: 'Synthetic interpretation: summarize → use saved output → ask when information is missing → continue after an actual reply.',
      takeaway: 'A workflow makes control and data dependencies explicit; it does not make an escalation real merely by describing one.',
      azure: {
        title: 'Create a small Foundry workflow',
        sourceId: 66,
        environment: 'foundry',
        minutes: 25,
        cost: 'Each agent invocation consumes model usage; repeated nodes multiply calls. Use a two-agent draft-only example with no external write tools.',
        prerequisites: [ACCESS, 'Access to the Foundry workflow designer in your project and the original source 66 exercise.', 'Review the dated workflow availability note before starting. A missing or retired feature is a blocker, not permission to claim a simulated run.'],
        steps: [
          {
            title: 'Check project and feature availability',
            surface: 'Microsoft Foundry',
            instruction: 'Open the approved project in ai.azure.com. In Build, locate Workflows or Create new workflow according to the original exercise. Confirm a supported model deployment is available.',
            expected: 'Verify that the designer is actually accessible in your environment before creating assets.',
            hint: 'The feature is preview and has a retirement announcement. Record unavailable access rather than substituting an imaginary portal screen.',
          },
          {
            title: 'Start a named lab workflow',
            surface: 'Microsoft Foundry',
            instruction: 'Create a blank or Sequential workflow, name it lab-event-reply, and inspect Start and the available node types. Save the initial definition.',
            expected: 'Locate an explicitly saved workflow and its initial version.',
            hint: 'Saving is required; closing the designer without saving can lose your changes.',
          },
          {
            title: 'Add a focused summary agent',
            surface: 'Microsoft Foundry',
            instruction: 'Add Invoke agent, create or select a dedicated lab agent, and instruct it to summarize a fictional event request without taking any external action. Map the workflow’s current request into the agent input.',
            expected: 'Identify which request the agent receives and where its output is stored.',
            hint: 'Use the input selector from the exercise instead of assuming a connected node automatically receives every earlier value.',
          },
          {
            title: 'Connect a draft-only agent',
            surface: 'Microsoft Foundry',
            instruction: 'Add a second Invoke agent node that drafts a short response. Set its input to the saved summary output and ensure its instructions prohibit sending messages or changing real bookings.',
            expected: 'See an explicit dependency from the summary result to the draft input.',
            hint: 'If the draft ignores the request, inspect input mapping before adding more instructions.',
          },
          {
            title: 'Add an actual question pause',
            surface: 'Microsoft Foundry',
            instruction: 'Insert a Basic chat Ask a question node for a fictional preferred event date. Store the response in a local variable and pass it with the summary to the draft step. Finish with a final message and End.',
            expected: 'Distinguish the waiting input node from a Send message node that merely displays text.',
            hint: 'This collects a date; it does not implement a booking service or real staff-approval system.',
          },
          {
            title: 'Save and inspect the run',
            surface: 'Microsoft Foundry',
            instruction: 'Save, select Preview or Run Workflow, and submit a fictional event request. Observe the question pause, provide a date, and inspect the node path and final draft.',
            expected: 'Confirm the workflow waits, uses the supplied date and reaches End without claiming an external message was sent.',
            hint: 'If the preview uses an old definition, verify the saved version before changing the logic.',
          },
        ],
        success: 'Observe the real designer execute a mapped sequence and wait for user input. Save the version as a resumable stopping point.',
        evidencePrompt: 'Record the node sequence, the mapped summary variable and what proved that the human-input node actually paused.',
        cleanup: `Remove your lab workflow and dedicated agents only when no subsequent lab needs them. Do not delete reused shared agents. ${PRESERVE}`,
      },
    },
    check: {
      question: 'A workflow displays “Sent to staff for approval” and immediately continues to draft a final decision. What is missing?',
      options: [
        { id: 'a', text: 'A more enthusiastic message to staff.', explanation: 'Wording does not create a pause, capture a decision or connect to a real approval system.' },
        { id: 'b', text: 'An input/approval step that waits for and uses an actual response.', explanation: 'Human-in-the-loop control requires receiving the missing decision before the workflow proceeds.' },
        { id: 'c', text: 'A larger model in every node.', explanation: 'The defect is control flow, not model capacity. More capable text generation cannot replace an omitted waiting step.' },
      ],
      correctOptionId: 'b',
      explanation: 'A message describes something; a waiting node and its data mapping control whether it truly occurs.',
    },
    recall: {
      prompt: 'Name the main Foundry workflow node categories and explain how a downstream agent obtains an earlier result.',
      answer: 'Invoke agent, flow, data transformation, basic chat and End. Store an output and map the intended value into the later node’s input; do not assume every connected node receives all prior context.',
    },
    transfer: 'A customer has a prompt full of mandatory if/else instructions. Which decisions should become explicit workflow nodes, and where should a human actually pause the process?',
    sourceNote: `Sources 238, 36, 3 and 105 provide the main mechanisms; source 118, summary 219 and episode 15 support them. A workflow can reuse ordinary prompt agents; “workflow agent” describes the orchestration definition, not a special type required for every participant. Guide: ${GUIDES.workflow}. ${WORKFLOW_NOTE}`,
  },
  {
    id: 'agents-workflow-state',
    title: 'Route with variables and Power Fx',
    cluster: 'agents-orchestration',
    examDomain: IMPLEMENT,
    summary: 'Turn structured agent output into inspectable variables, conditions and loops while keeping low-confidence or sensitive requests under human control.',
    minutes: 9,
    order: 140,
    prerequisites: ['agents-workflows'],
    sourceIds: [3, 11, 36, 66, 15, 171],
    steps: [
      {
        title: 'Make outputs predictable enough to route',
        body: 'A natural-language classification is awkward to use as a branch condition. Configure the agent’s response format as structured JSON with named fields, such as category and confidence, then store that output through the Invoke agent node’s settings. Required fields and constrained categories make downstream handling more predictable. A matching schema establishes shape, not factual correctness. Validate the values and preserve an unknown or fallback path rather than letting an unexpected label silently select a specialist.',
        example: 'An event-support classifier returns category “equipment” and confidence 0.7; a later node reads those fields instead of searching prose for the word equipment.',
      },
      {
        title: 'Give values a scope and type',
        body: 'Power Fx expressions evaluate values using local and system context. Local variables hold data you capture or produce during the workflow; System variables expose conversation or activity context. Keep numeric confidence as a number, not decorative text. A Set Variable node can store a field for a condition such as Local.Confidence > 0.8. That illustrative threshold is a routing policy, not proof that the model’s confidence is calibrated or that a risky action is safe.',
        example: 'Local.Confidence holds 0.7, so the condition is false. The workflow asks for clarification even if the generated explanation sounds certain.',
      },
      {
        title: 'Reuse the process for several items',
        body: 'A For Each node iterates over a collection and gives the inner nodes a current item. Feed that item to the classifier, then use its result for that item’s branch. Reset or replace item-specific values so one request’s result cannot accidentally determine another request’s route. For testing, use known fixture values in a separate lab copy to exercise both condition outcomes. Do not rely on the model to emit a particular confidence merely to make a branch test pass.',
        example: 'Three fictional event requests use one loop; a missing-detail request must not inherit the previous request’s high-confidence classification.',
      },
      {
        title: 'Route uncertainty to an actual pause',
        body: 'If/Else nodes turn evaluated values into execution paths. One path can continue with a resolution agent; another can ask a clarifying question and wait for input. Sensitive categories may require review regardless of confidence, so do not let a high number bypass that rule. For Each handles repeated items, while If/Else chooses a path for an item. Keep the final message honest about whether it created a draft, collected input or actually contacted an external system.',
        example: 'An equipment question can receive a draft explanation, but a requested booking cancellation still goes to a human even when its category is clear.',
      },
    ],
    lab: {
      title: 'Optional rehearsal: assemble a routing rule',
      goal: 'Check a small routing policy before building its real designer nodes.',
      context: `${REHEARSAL} A classifier has returned numeric confidence 0.7 for a fictional event request.`,
      fields: [
        {
          id: 'scope',
          label: 'Stored confidence',
          hint: 'This value belongs to the current workflow’s data.',
          options: [
            { value: 'Local.Confidence', label: 'Local.Confidence' },
            { value: 'System.User', label: 'System.User' },
            { value: 'unparsed-answer-text', label: 'An unparsed block of answer text' },
          ],
          expected: 'Local.Confidence',
          explanation: 'The local numeric value can be tested directly. User context and free-form prose are not the classifier’s numeric result.',
        },
        {
          id: 'condition',
          label: 'Illustrative condition',
          hint: 'The policy requires confidence above 0.8; it does not authorize side effects.',
          options: [
            { value: 'Local.Confidence > 0.8', label: 'Local.Confidence > 0.8' },
            { value: 'Local.Confidence < 0.8', label: 'Local.Confidence < 0.8' },
            { value: 'true', label: 'Always true' },
          ],
          expected: 'Local.Confidence > 0.8',
          explanation: 'At 0.7 this condition is false. The threshold is a demonstration policy, not a guarantee of model accuracy.',
        },
        {
          id: 'fallback',
          label: 'False branch',
          hint: 'Missing confidence should lead to more information, not a fictional completed action.',
          options: [
            { value: 'ask-and-wait-for-details', label: 'Ask and wait for more details' },
            { value: 'claim-completed', label: 'Claim the request is completed' },
            { value: 'reuse-previous-classification', label: 'Use the previous item’s classification' },
          ],
          expected: 'ask-and-wait-for-details',
          explanation: 'The safe route obtains actual input and preserves the current item’s state instead of borrowing another result.',
        },
      ],
      template: `{
  "kind": "conceptual-workflow-routing",
  "confidenceVariable": "{{scope}}",
  "conditionExpression": "{{condition}}",
  "falseBranch": "{{fallback}}"
}`,
      language: 'json',
      expectedOutput: 'Synthetic fixture: confidence 0.7 does not exceed 0.8, so the clarification path waits. No workflow or Power Fx engine runs in this rehearsal.',
      takeaway: 'Structured data, scoped variables and explicit conditions make routing inspectable; confidence alone does not authorize an action.',
      azure: {
        title: 'Build and test workflow branches',
        sourceId: 66,
        environment: 'foundry',
        minutes: 35,
        cost: 'Every loop item and invoked agent adds model usage. Use only three short fictional requests and a bounded number of previews.',
        prerequisites: [ACCESS, 'Workflow designer availability and the original source 66 exercise.', 'A separate lab workflow or lab copy; no real booking, payment or support-ticket systems connected.'],
        steps: [
          {
            title: 'Open a safe workflow copy',
            surface: 'Microsoft Foundry',
            instruction: 'Open Build > Workflows and create or copy a dedicated lab workflow. Confirm the deployment and save an initial version before changing its routing.',
            expected: 'Identify a lab-only definition whose changes cannot affect a published business process.',
            hint: 'Follow the source exercise for your current portal labels and check the availability note first.',
          },
          {
            title: 'Create a small input collection',
            surface: 'Microsoft Foundry',
            instruction: 'Add Set Variable for a local collection of three fictional requests: a projector question, a room-cancellation request and a request missing a date. Use the collection format demonstrated in the original exercise.',
            expected: 'Inspect a collection with three distinct items, not one long unstructured sentence.',
            hint: 'Use the data type supported by the designer; do not store a list as a quoted display string.',
          },
          {
            title: 'Loop over the current request',
            surface: 'Microsoft Foundry',
            instruction: 'Add For Each over that collection and name the current-item variable. Place the classifier invocation inside the loop and map its input to the current item.',
            expected: 'See one repeated set of nodes rather than three manually duplicated workflows.',
            hint: 'A node placed after the loop receives a different context than a node inside it.',
          },
          {
            title: 'Configure and store structured output',
            surface: 'Microsoft Foundry',
            instruction: 'In the classifier’s Details and parameters, configure JSON Schema output with required category and numeric confidence. Use categories equipment, booking and unknown. In Action or Node settings, save the structured output to a local variable.',
            expected: 'Inspect named fields in the agent output and locate the variable receiving them.',
            hint: 'Use the original exercise’s schema editor pattern. Schema validity is not proof that the category is correct.',
          },
          {
            title: 'Add an explicit condition',
            surface: 'Microsoft Foundry',
            instruction: 'Use Set Variable to assign the current result’s confidence to Local.Confidence. Add If/Else with Local.Confidence > 0.8. Route the false branch to a question asking for missing details.',
            expected: 'Verify the condition reads a numeric value and the false branch contains a real waiting input node.',
            hint: 'Check variable spelling, scope and numeric type if the expression fails; do not replace a failed condition with an unconditional true.',
          },
          {
            title: 'Preserve the sensitive-category rule',
            surface: 'Microsoft Foundry',
            instruction: 'On the true branch, add category-based routing so booking changes still request human review, while equipment questions can produce a draft. Save and inspect each mapped input and output.',
            expected: 'Confirm a high confidence score cannot bypass the booking-review path.',
            hint: 'A Send message announcing review is not actual review; use an input pause and label any external handoff not implemented.',
          },
          {
            title: 'Test both paths and the loop',
            surface: 'Microsoft Foundry',
            instruction: 'In the lab copy, temporarily supply known confidence fixtures 0.4 and 0.9 through Set Variable to test the condition branches. Restore the classifier mapping, run the three-item collection and inspect each item’s route before saving.',
            expected: 'Observe both condition outcomes and verify one item’s values are not reused accidentally for another.',
            hint: 'Do not depend on a model-generated score to hit an exact branch. Record unexpected classifications separately from condition-logic failures.',
          },
        ],
        success: 'Observe actual branch and loop execution, including a waiting path, and explain why the numeric threshold does not prove a classification is correct.',
        evidencePrompt: 'Record one variable mapping, each fixture’s observed branch and one distinction between schema validity, confidence and authorization.',
        cleanup: `Remove test fixture overrides before retaining the lab workflow. Delete only your lab copy and dedicated classifier agents when finished. ${PRESERVE}`,
      },
    },
    check: {
      question: 'A workflow loops over requests. One result has numeric confidence 0.7 and the condition is Local.Confidence > 0.8. What should happen?',
      options: [
        { id: 'a', text: 'Take the false branch and ask for details, using the current item’s values.', explanation: 'The numeric comparison is false. A real clarification pause preserves the current request instead of inventing confidence.' },
        { id: 'b', text: 'Take the true branch because the output matched its JSON schema.', explanation: 'A schema validates structure; it does not change the comparison or prove that the classification is accurate.' },
        { id: 'c', text: 'Reuse the preceding item’s higher score so the loop can finish.', explanation: 'That would route this request using another request’s evidence and indicates a state-mapping defect.' },
      ],
      correctOptionId: 'a',
      explanation: 'Evaluate the current item’s typed values, then follow the explicitly configured path.',
    },
    recall: {
      prompt: 'Explain how an agent’s JSON output becomes a workflow decision, and how a loop changes the state you must inspect.',
      answer: 'Define the output schema, store the structured result, map its typed fields to local variables, evaluate a Power Fx condition and route accordingly. In a loop, inspect the current item and replace or reset item-specific values every iteration.',
    },
    transfer: 'A customer proposes routing all requests automatically above a model confidence threshold. What would you explain about calibration, sensitive categories and actual human approval?',
    sourceNote: `Main sources 3, 11 and 36; episode 15 demonstrates structured classification, local variables, looping and branches. Thresholds and the event-support fixture here are authored examples, not production policy. Power Fx scope and expression behavior were cross-checked against Microsoft’s workflow documentation after code-sample lookup. Guide: ${GUIDES.workflow}. ${WORKFLOW_NOTE}`,
  },
  {
    id: 'agents-workflow-recovery',
    title: 'Version, pause and resume workflows',
    cluster: 'agents-orchestration',
    examDomain: MANAGE,
    summary: 'Distinguish definition versions, conversation continuity and execution checkpoints, then inspect workflow events without mistaking partial output for completion.',
    minutes: 8,
    order: 144,
    prerequisites: ['agents-workflow-state', 'agents-framework'],
    sourceIds: [151, 247, 231, 66, 15, 219],
    steps: [
      {
        title: 'Version the definition deliberately',
        body: 'Foundry’s visual workflow and YAML views represent the same definition. The corpus says each explicit save creates an immutable version, allowing review and rollback when a change breaks behavior. Notes can explain why a branch exists or what a variable means. A version captures design, not the progress of every execution. Before changing a shared process, preserve its tested version and use a lab copy to compare behavior rather than assuming the latest edit is automatically safe.',
        example: 'Changing an escalation threshold from 0.8 to 0.6 creates a new design version; it does not explain which request had already reached review.',
      },
      {
        title: 'Separate three kinds of saved state',
        body: 'A definition version records what should run. Conversation state carries messages needed for a continuing interaction, including a reply to a human-input pause. An execution checkpoint records workflow progress so supported Agent Framework workflows can save and resume a run. These concepts are related but not interchangeable. The supplied sources introduce Framework checkpointing without specifying a Foundry portal checkpoint button. Restoring a definition also does not reverse a tool’s already completed external action.',
        example: 'Reopening yesterday’s workflow definition does not undo a report already written or restore a reviewer’s unfinished conversation automatically.',
      },
      {
        title: 'Invoke the saved workflow from an app',
        body: 'The corpus’s code integration connects to the Foundry project, creates a conversation and invokes a saved workflow by name through the OpenAI-compatible Responses interface. The input drives the workflow, and the conversation supplies context. A workflow that pauses for user input needs the application to provide the response in that continuing context. Do not recreate the entire business process in client code merely to call it, or accidentally target a different similarly named workflow.',
        example: 'An event-request web app passes the request to the saved workflow and supplies the user’s missing date when the workflow asks.',
      },
      {
        title: 'Observe completion before declaring success',
        body: 'Streaming exposes workflow progress, including output-item or action completion events and a final response-completed event. An individual action finishing does not mean the entire process finished, and a text fragment is not a successful external operation. Inspect final output and errors before declaring completion. When diagnosing a restart, identify the saved definition, conversation and execution state separately. Keep lab operations read-only or draft-only so repeated testing cannot accidentally duplicate real bookings or messages.',
        example: 'A classifier-completed event can be followed by a clarification pause; the application should display “waiting for input,” not “request resolved.”',
      },
    ],
    lab: {
      title: 'Optional rehearsal: identify saved state',
      goal: 'Plan recovery using the right kind of state before running the real workflow client.',
      context: `${REHEARSAL} A workflow has reached a question and needs a user’s answer before continuing.`,
      fields: [
        {
          id: 'resumeContext',
          label: 'Reply context',
          hint: 'The reply belongs to the ongoing interaction.',
          options: [
            { value: 'same-conversation', label: 'The same conversation context' },
            { value: 'unrelated-new-conversation', label: 'An unrelated new conversation' },
            { value: 'old-yaml-only', label: 'An old YAML definition without run context' },
          ],
          expected: 'same-conversation',
          explanation: 'The workflow needs to associate the reply with the interaction that is waiting, not merely load a definition.',
        },
        {
          id: 'checkpointMeaning',
          label: 'Execution checkpoint',
          hint: 'Do not confuse runtime progress with a saved design.',
          options: [
            { value: 'saved-run-progress', label: 'Saved progress and state of a supported run' },
            { value: 'immutable-design-version', label: 'Only an immutable design version' },
            { value: 'undo-external-actions', label: 'An automatic undo for external operations' },
          ],
          expected: 'saved-run-progress',
          explanation: 'Checkpointing supports recovery of execution state. It is not just version history and does not reverse an external side effect.',
        },
        {
          id: 'completion',
          label: 'Completion evidence',
          hint: 'A node can finish while the workflow still waits.',
          options: [
            { value: 'final-status-and-output', label: 'Final completion status and inspected output' },
            { value: 'first-streamed-token', label: 'The first streamed token' },
            { value: 'one-node-finished', label: 'Any one node has finished' },
          ],
          expected: 'final-status-and-output',
          explanation: 'Only the completed run and its inspected outcome establish workflow completion; partial progress must be reported as progress.',
        },
      ],
      template: `{
  "kind": "conceptual-workflow-recovery",
  "replyContext": "{{resumeContext}}",
  "checkpointMeaning": "{{checkpointMeaning}}",
  "completionEvidence": "{{completion}}"
}`,
      language: 'json',
      expectedOutput: 'Synthetic interpretation: the waiting interaction retains its context; design rollback is not execution recovery and does not undo external actions.',
      takeaway: 'Version, conversation and execution checkpoint answer different recovery questions.',
      azure: {
        title: 'Inspect versions and invoke a workflow',
        sourceId: 66,
        environment: 'portal-and-code',
        minutes: 30,
        cost: 'Every workflow run can make several model calls. Use a short saved draft-only workflow and a small number of tests.',
        prerequisites: [ACCESS, 'An available Foundry workflow designer and your own saved lab workflow.', 'The original source 66 exercise and source 247’s code-integration guidance, using their supported SDK versions.'],
        steps: [
          {
            title: 'Inspect the saved definition',
            surface: 'Microsoft Foundry',
            instruction: 'Open your lab workflow and inspect the saved version, visual nodes and YAML representation. Locate the variable or condition you will change.',
            expected: 'Confirm you can associate the visual structure with its textual definition.',
            hint: 'Do not assume a downloaded YAML file contains conversation state or a runtime checkpoint.',
          },
          {
            title: 'Create a documented revision',
            surface: 'Microsoft Foundry',
            instruction: 'In the lab definition, add a note explaining the clarification branch and change one harmless draft-message phrase. Save, then inspect the version history.',
            expected: 'Verify a new version exists and the earlier definition remains distinguishable.',
            hint: 'Notes should explain logic, not contain real user data, keys or tokens.',
          },
          {
            title: 'Compare behavior and a pause',
            surface: 'Microsoft Foundry',
            instruction: 'Preview the new version with a fictional request that reaches the question node. Observe the wait, reply and inspect the completed draft; compare the changed phrase with the earlier definition.',
            expected: 'Separate a design change from the state of the conversation that waited for input.',
            hint: 'A displayed “escalated” message without waiting is not a human-input pause.',
          },
          {
            title: 'Prepare the project client',
            surface: 'Cloud Shell / local editor',
            instruction: 'Follow source 66’s linked lab and source 247 to configure the supported client with the project endpoint and exact saved workflow name. Use the exercise’s credential flow.',
            expected: 'Identify where the client creates conversation context and references the existing workflow.',
            hint: 'Do not substitute a raw model name for the workflow reference or paste the rehearsal JSON as an SDK request.',
          },
          {
            title: 'Observe action and final events',
            surface: 'Cloud Shell / local editor',
            instruction: 'Run the client against the lab workflow. Inspect action/output-item events separately from final response completion. If the exercise supports the question pause, return the answer in the same conversation and continue.',
            expected: 'Recognize intermediate progress, a possible waiting state and the final completed output.',
            hint: 'If your sample handles only noninteractive runs, record that limitation; do not claim it resumed a waiting conversation.',
          },
          {
            title: 'Record recovery limits accurately',
            surface: 'Microsoft Foundry',
            instruction: 'Return to version history and identify the tested definition. Record separately the conversation used and whether any execution-checkpoint mechanism was actually configured.',
            expected: 'State explicitly that a version-history inspection alone did not test Agent Framework checkpoint recovery.',
            hint: 'The corpus does not specify a Foundry checkpoint button. Do not invent one or equate a saved version with a resumed execution.',
          },
        ],
        success: 'Verify one saved revision and one actual workflow invocation, distinguishing intermediate events from completion and identifying recovery capabilities that remain untested.',
        evidencePrompt: 'Record the saved version, observed event sequence and the difference between the conversation and an execution checkpoint; omit sensitive identifiers.',
        cleanup: `Remove only exercise conversations, downloaded lab definitions and lab workflow revisions you no longer need. Preserve a deliberately retained tested version for later study. ${PRESERVE}`,
      },
    },
    check: {
      question: 'A workflow stopped while waiting for a user. An engineer reopens an earlier saved YAML version and claims the execution was restored. What is wrong with that claim?',
      options: [
        { id: 'a', text: 'YAML cannot represent workflows at all.', explanation: 'The corpus explicitly supports visual and YAML definitions; the issue is what state the definition contains.' },
        { id: 'b', text: 'Definition history is not the waiting conversation or an execution checkpoint.', explanation: 'A saved design describes the process, while resuming requires the appropriate interaction and supported runtime state.' },
        { id: 'c', text: 'A saved version automatically undoes all earlier tool operations.', explanation: 'External side effects are not reversed by loading a definition, and the corpus makes no such guarantee.' },
      ],
      correctOptionId: 'b',
      explanation: 'Identify which state was actually saved and restored before calling a run recovered.',
    },
    recall: {
      prompt: 'Contrast a workflow definition version, conversation state and execution checkpoint in one sentence each.',
      answer: 'A definition version records the design. Conversation state records interaction context, including replies to questions. An execution checkpoint records supported runtime progress for resumption; none automatically undoes external side effects.',
    },
    transfer: 'A customer wants reliable recovery after a workflow interruption. What evidence would you ask for about versions, conversation continuity, checkpoints and repeated side effects?',
    sourceNote: `Source 151 covers definition versions; 247 covers named invocation and streamed events; 231 introduces Agent Framework checkpointing. The corpus does not supply a Foundry portal checkpoint control or a complete checkpoint-storage implementation, so neither is claimed here. Guide: ${GUIDES.workflow}. ${WORKFLOW_NOTE}`,
  },
  {
    id: 'agents-orchestration-patterns',
    title: 'Order dependent and independent work',
    cluster: 'agents-orchestration',
    examDomain: IMPLEMENT,
    summary: 'Choose sequential or concurrent coordination from data dependencies, then inspect graph edges, aggregation and runtime events.',
    minutes: 8,
    order: 148,
    prerequisites: ['agents-framework', 'agents-workflows'],
    sourceIds: [231, 237, 241, 245, 133, 143, 199, 69, 180],
    steps: [
      {
        title: 'Model the dependency before the pattern',
        body: 'Agent Framework workflows connect executors that receive input, perform agent or ordinary logic, and emit output. Edges describe message flow: direct, conditional, switch-case, fan-out or fan-in. The architecture question is what each worker needs before it can start. Several agents are not automatically better than one; specialization adds coordination, cost and failure paths. Use a graph when distinct responsibilities and their dependencies justify it, then select the pattern that matches those dependencies.',
        example: 'A venue report can have independent accessibility and transport reviews, while a final recommendation must wait for both reviews.',
      },
      {
        title: 'Sequence work that builds on output',
        body: 'Sequential orchestration runs a fixed ordered pipeline in which one stage’s output becomes the next stage’s input. It suits progressive refinement and processes whose later steps need earlier results. The corpus describes defining agents, assembling them in order with SequentialBuilder, running the workflow and inspecting output events. A predictable order does not guarantee good inputs: inspect failures or poor intermediate results before allowing later stages to amplify them. Independent work need not wait in this pattern.',
        example: 'Extract incident facts, propose a diagnostic step from those facts, then explain that proposal to an operator. The later stages depend on the earlier artifacts.',
      },
      {
        title: 'Fan out independent perspectives',
        body: 'Concurrent orchestration sends the input to independent agents and collects their results. ConcurrentBuilder supplies the coordination pattern; fan-out distributes work and fan-in gathers it. Participants do not automatically see each other’s results while working. Parallel execution can reduce elapsed time, but increases simultaneous quota demand and still consumes each participant’s model usage. Define how to reconcile disagreements, and avoid concurrent writes to shared systems when the workers cannot coordinate those changes safely.',
        example: 'Two venue reviewers independently assess the same fictional brief. Their conflicting accessibility conclusions remain visible for review rather than being silently averaged.',
      },
      {
        title: 'Observe what the runtime actually did',
        body: 'Both patterns reuse agent definitions and expose workflow events, but their information flow differs. Record the participant names, input dependencies, outputs and completion or failure status. Streaming helps observe progress without blocking for the entire result; it is not proof that tasks ran concurrently. Choose the simpler pattern when it meets the requirement. Handoff, group chat and Magentic add different kinds of dynamic control, which the following units separate instead of treating them as interchangeable names.',
        example: 'Three displayed progress messages could come from a sequential pipeline; inspect the graph and events before claiming parallel execution.',
      },
    ],
    lab: {
      title: 'Optional rehearsal: choose the dependency graph',
      goal: 'Plan independent review and explicit aggregation before running an Azure-backed multi-agent app.',
      context: `${REHEARSAL} Accessibility and transport reviewers can assess the same fictional venue brief independently.`,
      fields: [
        {
          id: 'pattern',
          label: 'Reviewer pattern',
          hint: 'Neither reviewer needs the other’s result before starting.',
          options: [
            { value: 'concurrent-fan-out', label: 'Concurrent fan-out' },
            { value: 'fixed-dependent-pipeline', label: 'A dependent sequential pipeline' },
            { value: 'unbounded-shared-discussion', label: 'An unbounded discussion' },
          ],
          expected: 'concurrent-fan-out',
          explanation: 'Independent analysis fits fan-out. A pipeline adds an unnecessary dependency, while unlimited discussion adds a different coordination problem.',
        },
        {
          id: 'aggregation',
          label: 'Result handling',
          hint: 'Parallel perspectives can disagree.',
          options: [
            { value: 'named-results-and-conflict-review', label: 'Collect named results and review conflicts' },
            { value: 'first-answer-is-truth', label: 'Treat the first answer as authoritative' },
            { value: 'discard-disagreement', label: 'Discard conflicting results silently' },
          ],
          expected: 'named-results-and-conflict-review',
          explanation: 'Preserving authorship and disagreement makes aggregation inspectable instead of hiding uncertainty.',
        },
      ],
      template: `{
  "kind": "conceptual-orchestration-graph",
  "reviewers": "{{pattern}}",
  "fanInPolicy": "{{aggregation}}"
}`,
      language: 'json',
      expectedOutput: 'Synthetic interpretation: one brief fans out to independent reviewers, then their named outputs are gathered; no agents run in this rehearsal.',
      takeaway: 'Choose concurrency from independence, not from the number of agents or the appearance of streaming.',
      azure: {
        title: 'Compare sequential and concurrent runs',
        sourceId: 69,
        environment: 'portal-and-code',
        minutes: 40,
        cost: 'Multiple agents multiply model requests; concurrent runs increase simultaneous quota use. Keep inputs small, participants few and all operations read-only.',
        prerequisites: [ACCESS, 'The original source 69 multi-agent exercise and its supported Agent Framework environment.', 'Use only supplied sample logs or fictional text. Do not connect the incident-response example to production repair tools.'],
        steps: [
          {
            title: 'Review resource and quota scope',
            surface: 'Azure portal',
            instruction: 'Open the approved lab subscription and resource group. Confirm which Foundry resource is allowed and review the spending scope before testing multiple agents.',
            expected: 'Identify existing shared resources and the authorized lab scope.',
            hint: 'Do not create extra deployments simply because a concurrent request encounters a quota limit.',
          },
          {
            title: 'Inspect the shared model deployment',
            surface: 'Microsoft Foundry',
            instruction: 'Open the project and model deployment used by source 69. Locate its endpoint, deployment name and available capacity information.',
            expected: 'Confirm the agents can use the intended deployment and that simultaneous requests may share its quota.',
            hint: 'Sequential and concurrent patterns can use the same deployment; the pattern is coordination code, not a new model type.',
          },
          {
            title: 'Run the original baseline safely',
            surface: 'Cloud Shell / local editor',
            instruction: 'Launch source 69’s linked exercise, follow setup and inspect its incident-manager and DevOps capabilities. Run only its supplied sample-data path, disabling or avoiding any real external repair integration.',
            expected: 'Observe a baseline multi-agent result and identify the actual participant and tool responsibilities.',
            hint: 'A proposed fix or modified local fixture is not proof that a real system was repaired.',
          },
          {
            title: 'Inspect an ordered pipeline',
            surface: 'Cloud Shell / local editor',
            instruction: 'Using the package-matched pattern in source 245, adapt the lab copy to a draft-only sequence: extract incident facts, propose a diagnostic step, then explain it. Register the participants in that order and run a short sample.',
            expected: 'Inspect that each stage receives the preceding output and that the final result is a proposal, not an executed repair.',
            hint: 'Episode 180 demonstrates a different sequential example. Reuse its dependency idea, not an assumption that every exercise file has identical agent names.',
          },
          {
            title: 'Inspect independent reviewers',
            surface: 'Cloud Shell / local editor',
            instruction: 'In a separate lab configuration, follow source 241’s concurrent pattern for two independent read-only reviewers of the same sample. Collect outputs with their author names and record any conflicting conclusions.',
            expected: 'Observe independent inputs and gathered results, without feeding one reviewer’s answer into the other before it starts.',
            hint: 'Use the original exercise’s supported APIs. If a builder differs in your installed version, resolve that version mismatch rather than inventing method names.',
          },
          {
            title: 'Compare evidence and constraints',
            surface: 'Cloud Shell / local editor',
            instruction: 'Compare the two graphs and their completion events. Record which results depended on earlier output, which ran independently and whether quota or aggregation affected either run.',
            expected: 'Explain why the selected graph matches the data dependency, not merely which run felt faster.',
            hint: 'Keep contradictory reviewer outputs visible. Do not label either successful execution as durable mastery of orchestration.',
          },
        ],
        success: 'Inspect real Azure-backed workflow results and identify sequential dependencies versus independent fan-out. Mark an unavailable or unrun pattern as untested.',
        evidencePrompt: 'Record the participant order or fan-out structure, one intermediate result and the policy used to handle disagreement.',
        cleanup: `Stop the sample app and remove only lab-created agents, conversations, local test outputs and dedicated resources no longer needed. ${PRESERVE}`,
      },
    },
    check: {
      question: 'Two specialists assess the same venue brief independently. A final writer must see both assessments, including any disagreement. Which structure fits?',
      options: [
        { id: 'a', text: 'Run the writer first, then ask specialists to justify its conclusion.', explanation: 'The writer depends on the assessments, so running it first reverses the dependency and encourages unsupported conclusions.' },
        { id: 'b', text: 'Give one specialist’s answer to the other and call the process concurrent.', explanation: 'That introduces a sequential dependency and removes the intended independent perspective.' },
        { id: 'c', text: 'Fan out to specialists, gather named results, then run the writer.', explanation: 'The independent work can run concurrently, while fan-in supplies the results needed by the dependent final stage.' },
      ],
      correctOptionId: 'c',
      explanation: 'A workflow may combine concurrent and sequential sections when their data dependencies require it.',
    },
    recall: {
      prompt: 'Explain executors, edges, fan-out and fan-in, then state the decisive difference between sequential and concurrent orchestration.',
      answer: 'Executors perform work; edges carry messages and control routing. Fan-out sends input to several workers, while fan-in gathers their outputs. Sequential stages depend on earlier outputs in a fixed order; concurrent participants can work independently.',
    },
    transfer: 'A customer asks to parallelize every agent for speed. Which dependencies, shared writes, quota limits and disagreement policies would you inspect first?',
    sourceNote: `Main sources 231, 241 and 245 establish graph and pattern behavior. Source 69’s exercise uses incident triage; episode 180 demonstrates a different feedback-processing pipeline. They support the same dependency model but are not identical starter files. Pattern adaptations here remain draft-only. Guide: ${GUIDES.orchestration}`,
  },
  {
    id: 'agents-handoff-group-chat',
    title: 'Separate handoff from shared discussion',
    cluster: 'agents-orchestration',
    examDomain: IMPLEMENT,
    summary: 'Route work to a responsible specialist or coordinate a shared maker-checker conversation, with explicit fallback and termination.',
    minutes: 8,
    order: 152,
    prerequisites: ['agents-orchestration-patterns', 'agents-workflow-state'],
    sourceIds: [243, 242, 231, 69, 143, 180],
    steps: [
      {
        title: 'Handoff changes the responsible worker',
        body: 'Handoff orchestration transfers control to a suitable specialist when the next responsibility becomes clear from the request or intermediate context. Unlike a fixed pipeline, the route is not always known at the start; unlike concurrency, one responsible agent works at a time. The source’s implementation classifies input, converts structured output into a typed routing object and selects a guarded handler. That example does not establish a universal rule that handoffs must discard all conversation history.',
        example: 'A facilities request goes to equipment support or booking support after classification, instead of asking every specialist to respond to every request.',
      },
      {
        title: 'Give routing a safe fallback',
        body: 'The corpus’s control-workflow implementation connects executors with switch-case edges, using case conditions and a default path. A handler should check that the incoming message is appropriate rather than assuming every classification is valid. Preserve the original request in the intended shared or passed state, and send unexpected outcomes to a safe fallback. Add clear transfer conditions and a stopping rule so specialists do not repeatedly bounce an unresolved request between themselves.',
        example: 'An unknown “lost access badge” category reaches a clarification handler instead of being silently treated as an equipment booking.',
      },
      {
        title: 'Group chat shares an evolving conversation',
        body: 'Group chat uses a shared conversation in which participants can build on earlier contributions. A manager chooses who speaks next, can request human input and decides when to stop. This is useful for maker-checker revision, debate and cross-disciplinary review, rather than simple specialist routing. The source describes checking for user input and termination before selecting the next speaker. Keep the participant set small and the stopping criteria explicit so discussion does not become an expensive loop.',
        example: 'A maker drafts a visitor notice, a checker flags ambiguous access instructions, and the maker revises using that feedback in the same discussion.',
      },
      {
        title: 'Compare control and information flow',
        body: 'The decisive question is whether the next participant should take responsibility for a task or contribute to a continuing shared discussion. Both patterns can be dynamic and can involve humans; neither is defined merely by having several agents. In the handoff example, a typed routing contract drives selection. In group chat, shared turns support revision. Do not confuse the latter with independent concurrent analysis, and do not expose private credentials or unrelated users’ conversations through shared context.',
        example: 'Routing a booking request to one specialist is a handoff; asking a writer and reviewer to refine the same response is a shared discussion.',
      },
    ],
    lab: {
      title: 'Optional rehearsal: plan a responsible handoff',
      goal: 'Build a routing contract and fallback before comparing it with a shared conversation.',
      context: `${REHEARSAL} A classifier chooses one facilities specialist, with clarification for an unknown category.`,
      fields: [
        {
          id: 'coordination',
          label: 'Control pattern',
          hint: 'One specialist should take responsibility, not debate with every other specialist.',
          options: [
            { value: 'handoff', label: 'Handoff' },
            { value: 'shared-group-chat', label: 'Shared group chat' },
            { value: 'broadcast-to-everyone', label: 'Concurrent broadcast to everyone' },
          ],
          expected: 'handoff',
          explanation: 'The requirement is routing to a responsible specialist. Shared discussion and independent broadcast solve different coordination needs.',
        },
        {
          id: 'routing',
          label: 'Unexpected category',
          hint: 'Classification may produce an unknown or invalid result.',
          options: [
            { value: 'guarded-default-clarification', label: 'Guarded default path to clarification' },
            { value: 'first-specialist-by-accident', label: 'Whichever specialist is listed first' },
            { value: 'endless-transfer', label: 'Keep transferring until something answers' },
          ],
          expected: 'guarded-default-clarification',
          explanation: 'An explicit default avoids accidental routing and a bounded clarification path prevents infinite transfers.',
        },
      ],
      template: `{
  "kind": "conceptual-handoff-contract",
  "coordination": "{{coordination}}",
  "unknownCategory": "{{routing}}"
}`,
      language: 'json',
      expectedOutput: 'Synthetic interpretation: classification selects one guarded handler; an unknown category reaches clarification rather than a silent default specialist.',
      takeaway: 'Handoff routes responsibility; group chat coordinates contributions to shared context.',
      azure: {
        title: 'Trace routing and maker-checker turns',
        sourceId: 69,
        environment: 'portal-and-code',
        minutes: 40,
        cost: 'Classification, specialist calls and review turns all consume model usage. Use two specialists, a small fixture and a short conversation limit.',
        prerequisites: [ACCESS, 'A working source 69 lab environment and the package-matched implementation guidance in sources 242 and 243.', 'Only fictional incident text or supplied log fixtures; no live repair or production deployment tools.'],
        steps: [
          {
            title: 'Verify the lab resource scope',
            surface: 'Azure portal',
            instruction: 'Open the approved resource group and confirm the Foundry resource used by the baseline exercise. Check that you are not modifying an operational incident agent.',
            expected: 'Identify a safe lab scope and the shared resources that must remain unchanged.',
            hint: 'Prefer a separate lab definition when an existing agent is used by another application.',
          },
          {
            title: 'Confirm project and deployment',
            surface: 'Microsoft Foundry',
            instruction: 'Inspect the project endpoint, supported model deployment and the agents or resources created by the baseline exercise where visible.',
            expected: 'Know which deployment will serve each specialist and avoid accidentally testing another project’s agent.',
            hint: 'Some framework agent objects are created in the client; portal absence alone does not prove that local objects were never defined.',
          },
          {
            title: 'Build a typed routing adaptation',
            surface: 'Cloud Shell / local editor',
            instruction: 'Starting from the source 69 lab copy, follow source 243’s control-workflow pattern. Classify fictional requests into two supported categories plus unknown, preserve the input, and connect guarded handlers with a default clarification path.',
            expected: 'Identify the structured classifier output, typed routing object and default handler separately.',
            hint: 'Use the exercise’s current data-model and builder APIs; the rehearsal plan is not executable SDK configuration.',
          },
          {
            title: 'Test a route and its fallback',
            surface: 'Cloud Shell / local editor',
            instruction: 'Run a known-category fixture, then test the unknown path with a controlled routing fixture. Inspect which handler receives the request and what context it has. Save this stopping point before continuing.',
            expected: 'Observe one responsible handler per route and an explicit fallback rather than accidental selection or endless bouncing.',
            hint: 'Test routing logic with known values separately from testing whether the model classifies free text correctly.',
          },
          {
            title: 'Run a bounded shared review',
            surface: 'Cloud Shell / local editor',
            instruction: 'In a separate lab adaptation, use the version-matched group-chat pattern from source 242 with a draft maker and checker, a shared conversation and a small round limit. Ask them to refine a fictional incident notice.',
            expected: 'Inspect a checker turn using the draft and a later maker turn using the checker’s feedback.',
            hint: 'The source contains copied concurrent wording in its implementation list. Use its manager and shared-turn semantics, not a parallel independent-results interpretation.',
          },
          {
            title: 'Inspect manager termination',
            surface: 'Cloud Shell / local editor',
            instruction: 'Review the manager’s decisions to request input, stop or select another speaker. Confirm a completion or round-limit outcome and compare the shared messages with the typed handoff payload.',
            expected: 'Explain the different information flow and stopping conditions using the actual traces.',
            hint: 'Record maximum-round termination as a limit reached, not as proof the drafted notice was accepted or correct.',
          },
        ],
        success: 'Inspect a real routed request and a bounded shared-turn interaction, preserving a clear distinction between a routing contract and a shared discussion.',
        evidencePrompt: 'Record which handler received the fallback fixture, what context the checker saw and what stopped the discussion.',
        cleanup: `Stop the lab process and remove only its dedicated routing/group-chat agents, conversations and local artifacts when no longer needed. ${PRESERVE}`,
      },
    },
    check: {
      question: 'A notice writer and reviewer must repeatedly see each other’s contributions until the reviewer accepts a draft or a round limit is reached. Which mechanism fits?',
      options: [
        { id: 'a', text: 'Concurrent independent reviews with no shared turns.', explanation: 'Independent workers cannot iteratively revise against feedback they never receive.' },
        { id: 'b', text: 'A single classification handoff with no return discussion.', explanation: 'That routes responsibility but does not provide the required shared maker-checker revision loop.' },
        { id: 'c', text: 'A manager-controlled group chat with shared context and explicit termination.', explanation: 'The manager sequences contributions, preserves earlier turns and stops on acceptance or the configured limit.' },
      ],
      correctOptionId: 'c',
      explanation: 'The required behavior is iterative contribution to one conversation, not just dynamic routing or parallel analysis.',
    },
    recall: {
      prompt: 'Explain handoff versus group chat using responsibility, shared information and stopping behavior.',
      answer: 'Handoff transfers responsibility to a selected specialist using a defined routing contract and fallback. Group chat keeps a shared discussion, with a manager selecting speakers, optionally asking a human and terminating on a condition or limit.',
    },
    transfer: 'A customer’s agents keep bouncing a request between departments. Would you first change the model, routing contract or stopping policy, and what trace would justify that choice?',
    sourceNote: `Source 243’s typed switch-case payload is one handoff implementation, not a universal prohibition on transferred history. Source 242 conflicts with itself: its implementation list says parallel/concurrent, while its conceptual and manager sections describe shared, selected turns; source 231 and episode 180 support the latter. That disagreement is preserved here rather than teaching the copied wording as fact. Guide: ${GUIDES.orchestration}`,
  },
  {
    id: 'agents-magentic-planning',
    title: 'Bound adaptive Magentic planning',
    cluster: 'agents-orchestration',
    examDomain: IMPLEMENT,
    summary: 'Use a manager and evolving task ledger when the plan is not known upfront, with observable progress and firm execution limits.',
    minutes: 7,
    order: 156,
    prerequisites: ['agents-handoff-group-chat'],
    sourceIds: [244, 231, 237, 69, 180, 199],
    steps: [
      {
        title: 'Choose planning when the path is unknown',
        body: 'Some tasks cannot be reduced to a fixed pipeline or a single classification handoff because useful next steps depend on discoveries. Magentic orchestration uses a manager that plans, delegates to specialized agents and adapts as results arrive. Its shared context and task ledger track goals, subgoals and progress. This is more than choosing the next speaker for a discussion: the manager maintains and revises a plan of work. The added flexibility brings extra latency and cost.',
        example: 'A fictional venue outage investigation may need network evidence first, then application logs, with the next diagnostic chosen only after each result arrives.',
      },
      {
        title: 'Give the manager observable building blocks',
        body: 'Define specialists with focused capabilities and tell the manager what each can contribute. The corpus describes MagenticBuilder, a standard manager using a chat client, and callbacks for orchestration messages, agent updates and final output. Inspect the visible task ledger and returned artifacts to understand what was attempted and what remains unresolved. A plan is not evidence that every step executed successfully. Keep tool permissions narrow so a planning error cannot automatically become an uncontrolled production action.',
        example: 'One specialist summarizes fictional connection failures; another reads supplied log excerpts. Neither can restart servers or change firewall rules.',
      },
      {
        title: 'Limit rounds, stalls and resets',
        body: 'Magentic can stall, repeat itself or keep refining a plan after useful progress stops. Configure maximum rounds, stall limits and reset limits, then inspect the termination reason. Reaching a limit means the run stopped, not that the task was solved. Avoid this pattern for deterministic jobs, simple one-tool tasks or strict low-latency interactions where planning adds no value. Prefer a sequence or a bounded group chat when those simpler mechanisms already express the required work.',
        example: 'A three-round lab investigation that cannot find enough evidence should return an unresolved finding, not invent a root cause to look complete.',
      },
    ],
    lab: {
      title: 'Optional rehearsal: bound an adaptive plan',
      goal: 'Specify planning responsibility and limits before the real multi-agent adaptation.',
      context: `${REHEARSAL} The next diagnostic depends on results, but the lab must remain read-only and bounded.`,
      fields: [
        {
          id: 'manager',
          label: 'Coordination responsibility',
          hint: 'The process needs an evolving plan, not a predetermined order.',
          options: [
            { value: 'magentic-task-ledger', label: 'Magentic manager with a task ledger' },
            { value: 'fixed-sequence-no-adaptation', label: 'A fixed sequence with no adaptation' },
            { value: 'every-agent-writes-production', label: 'Independent agents changing production simultaneously' },
          ],
          expected: 'magentic-task-ledger',
          explanation: 'An adaptive manager can update the plan from evidence. The lab does not authorize real infrastructure changes.',
        },
        {
          id: 'termination',
          label: 'Execution limit',
          hint: 'Repeated planning is not useful progress.',
          options: [
            { value: 'round-stall-reset-limits', label: 'Round, stall and reset limits with an explicit outcome' },
            { value: 'continue-until-confident', label: 'Continue until the model sounds confident' },
            { value: 'limit-means-success', label: 'Report success whenever the limit is reached' },
          ],
          expected: 'round-stall-reset-limits',
          explanation: 'Limits bound cost and loops; their termination reason must remain distinct from a solved task.',
        },
      ],
      template: `{
  "kind": "conceptual-adaptive-planning",
  "manager": "{{manager}}",
  "terminationPolicy": "{{termination}}",
  "toolBoundary": "read-only-fixtures"
}`,
      language: 'json',
      expectedOutput: 'Synthetic interpretation: the manager revises a plan from observed results and can stop unresolved; it has not diagnosed or repaired a real outage.',
      takeaway: 'An adaptive plan needs evidence, permissions and limits—not just more agents.',
      azure: {
        title: 'Inspect a bounded Magentic run',
        sourceId: 69,
        environment: 'portal-and-code',
        minutes: 35,
        cost: 'Manager planning and specialist turns each consume model usage. Use two specialists, short fixtures and small round/stall/reset limits.',
        prerequisites: [ACCESS, 'The source 69 development environment and version-compatible Magentic guidance from source 244.', 'Only supplied or fictional logs, with no live infrastructure-changing tools registered.'],
        steps: [
          {
            title: 'Confirm the authorized lab scope',
            surface: 'Azure portal',
            instruction: 'Open the approved lab resource group and identify the Foundry resource used by your multi-agent exercise. Confirm there is no reason to grant access to production infrastructure.',
            expected: 'Know that the investigation is limited to a development project and fixture data.',
            hint: 'Do not grant broad subscription permissions merely because the scenario mentions incident response.',
          },
          {
            title: 'Check the shared deployment',
            surface: 'Microsoft Foundry',
            instruction: 'Open the project’s model deployment page, confirm the supported deployment and locate its endpoint configuration. Review available usage or quota information before adding manager turns.',
            expected: 'Identify the model used for both specialist work and manager planning where the sample shares it.',
            hint: 'A manager also makes model calls; budget for it rather than counting specialists alone.',
          },
          {
            title: 'Prepare a read-only adaptation',
            surface: 'Cloud Shell / local editor',
            instruction: 'Start from the source 69 lab copy and inspect every registered tool. Keep only read-only sample-log capabilities, then define two focused specialists using the exercise’s existing provider setup.',
            expected: 'Verify neither specialist can alter a live system or send real operational messages.',
            hint: 'The original incident scenario may describe repair; this bounded adaptation tests diagnosis and planning only.',
          },
          {
            title: 'Configure the manager and limits',
            surface: 'Cloud Shell / local editor',
            instruction: 'Follow source 244’s version-matched Magentic pattern to connect the specialists, a planning client and event callback. Set small maximum-round, stall and reset limits before running.',
            expected: 'Locate the configured limits and the callback that exposes progress and final events.',
            hint: 'Check the exercise’s installed API version if a builder option differs; do not guess unsupported parameter names.',
          },
          {
            title: 'Observe a short investigation',
            surface: 'Cloud Shell / local editor',
            instruction: 'Run a fictional incident whose next diagnostic depends on the supplied evidence. Inspect the visible task ledger, specialist artifacts and the manager’s updated plan.',
            expected: 'Distinguish planned steps from actually observed results and identify any unresolved evidence gap.',
            hint: 'Use visible plans and tool artifacts, not requests for private hidden reasoning.',
          },
          {
            title: 'Inspect an incomplete outcome',
            surface: 'Cloud Shell / local editor',
            instruction: 'Use a fixture deliberately missing one required piece of evidence. Run with the same limits and inspect whether the manager stops, requests more information or reaches a limit. Record that outcome honestly.',
            expected: 'See a bounded termination reason without claiming that missing evidence was found or a real outage repaired.',
            hint: 'An unresolved result can be a correct safety outcome. Do not increase limits indefinitely to force a confident answer.',
          },
        ],
        success: 'Observe a real manager-led run and its termination reason, then explain which findings are supported and which remain unresolved.',
        evidencePrompt: 'Record a visible plan update, the artifact that caused it and the configured limit or completion condition that stopped the run.',
        cleanup: `Stop the lab app and remove only its exercise-created agents, conversations and fixture outputs when no longer needed. ${PRESERVE}`,
      },
    },
    check: {
      question: 'An investigation needs to revise its next step after every result. It reaches its configured round limit without enough evidence. What should the application report?',
      options: [
        { id: 'a', text: 'The investigation is complete because the runtime stopped.', explanation: 'Termination and task success are different facts; a limit can stop an unresolved investigation.' },
        { id: 'b', text: 'The bounded run ended with unresolved evidence, preserving the observed findings and stop reason.', explanation: 'That accurately reports progress without converting a planning limit into a false resolution claim.' },
        { id: 'c', text: 'Disable the limits and let the manager continue until it states a root cause.', explanation: 'Unbounded repetition increases cost and can encourage unsupported conclusions rather than obtaining missing evidence.' },
      ],
      correctOptionId: 'b',
      explanation: 'Magentic adapts a plan, but it does not eliminate uncertainty or make a termination limit equivalent to success.',
    },
    recall: {
      prompt: 'What does the Magentic manager track, and what three kinds of limits should you inspect?',
      answer: 'It tracks shared context, goals, subgoals, the evolving task ledger and progress. Inspect maximum rounds, stalls and resets, and distinguish their termination from successful task completion.',
    },
    transfer: 'A customer wants autonomous incident investigation. Which diagnostic capabilities, evidence requirements and stopping conditions would you agree before permitting any repair action?',
    sourceNote: `Source 244 is the primary explanation; 231 and 237 supply workflow context. Episode 180’s transcript sometimes says “magnetic”; the named pattern in the substantive text is Magentic. The lab is a bounded adaptation of ${GUIDES.orchestration}, not a claim that its baseline exercise already implements every orchestration pattern.`,
  },
  {
    id: 'agents-a2a',
    title: 'Delegate across agent boundaries',
    cluster: 'agents-orchestration',
    examDomain: IMPLEMENT,
    summary: 'Use Agent Cards, executors, request handlers and task-aware clients to communicate with independently hosted agents rather than merely call tools.',
    minutes: 9,
    order: 160,
    prerequisites: ['agents-mcp', 'agents-orchestration-patterns'],
    sourceIds: [37, 107, 103, 26, 134, 157, 205, 61],
    steps: [
      {
        title: 'Distinguish an agent from a tool service',
        body: 'Agent-to-Agent, or A2A, standardizes communication with agents that can own their own reasoning, model choice and task processing. MCP exposes discoverable tools and context to a client; A2A exposes an agent’s capabilities and interactions through a protocol. Both can appear in one architecture, and neither alone decides your orchestration pattern. A local workflow can coordinate a remote-agent client through a suitable integration; remote delegation does not require every participant to share one framework.',
        example: 'A visitor-planning agent can ask an independently hosted accessibility agent for an assessment, while using an MCP transport tool to retrieve timetable data.',
      },
      {
        title: 'Discover skills through an Agent Card',
        body: 'An Agent Skill describes a capability with an identifier, name, description, tags, examples and supported input or output modes. The Agent Card gathers those skills with the agent’s identity, version, endpoint, capabilities and authentication information. A client retrieves the card from the server’s well-known discovery endpoint before sending requests. The card advertises the contract; it is neither the business implementation nor a safe place to publish an API key or access token.',
        example: 'An accessibility agent advertises a text-based venue-review skill and whether it supports streaming, letting a client select a compatible interaction.',
      },
      {
        title: 'Separate server plumbing from execution',
        body: 'The A2A server exposes discovery and request endpoints. Its request handler links incoming operations to an Agent Executor and uses a Task Store to manage task lifecycle. The executor’s execute operation interprets RequestContext, calls business logic and sends messages, updates or artifacts through an EventQueue. Its cancel operation can handle cancellation or explicitly report it unsupported. The corpus illustrates a web application and ASGI server; those hosting components do not themselves supply the agent’s reasoning.',
        example: 'A server can successfully return an Agent Card while its executor still fails to produce a review, so discovery success is not end-to-end success.',
      },
      {
        title: 'Handle messages and ongoing tasks',
        body: 'An A2A client obtains the Agent Card, initializes communication and sends identifiable requests. A simple agent can return a direct message; a more complex one can return a task that needs status tracking or later results. Streaming delivers incremental output and must be advertised and handled appropriately. Inspect supported authentication and cancellation rather than assuming the protocol configures them automatically. Track actual final outcomes and avoid interpreting the first message or partial event as a completed delegated task.',
        example: 'A venue review may acknowledge receipt immediately but complete later with an artifact; the calling app must not present the acknowledgement as the review.',
      },
    ],
    lab: {
      title: 'Optional rehearsal: plan agent delegation',
      goal: 'Choose the discovery contract and response handling before running the real A2A exercise.',
      context: `${REHEARSAL} Another team independently hosts a specialist agent that can return a long-running review task.`,
      fields: [
        {
          id: 'protocol',
          label: 'Delegation protocol',
          hint: 'The remote component owns agent behavior, not just a function catalogue.',
          options: [
            { value: 'a2a-agent-card', label: 'A2A with Agent Card discovery' },
            { value: 'mcp-tool-list-only', label: 'Only an MCP tool list' },
            { value: 'copy-system-prompt', label: 'Copy its system prompt into the caller' },
          ],
          expected: 'a2a-agent-card',
          explanation: 'A2A exposes the independently hosted agent contract. A tool catalogue or copied prompt is not the same delegation interface.',
        },
        {
          id: 'responseHandling',
          label: 'Client response policy',
          hint: 'An acknowledgement may precede the final artifact.',
          options: [
            { value: 'handle-message-or-task', label: 'Handle direct messages and task lifecycle results' },
            { value: 'first-event-means-done', label: 'Treat the first event as completed work' },
            { value: 'force-streaming-always', label: 'Assume every server supports streaming' },
          ],
          expected: 'handle-message-or-task',
          explanation: 'The client must respect advertised capabilities and distinguish an immediate message from an ongoing task.',
        },
      ],
      template: `{
  "kind": "conceptual-remote-agent-contract",
  "discovery": "{{protocol}}",
  "responses": "{{responseHandling}}"
}`,
      language: 'json',
      expectedOutput: 'Synthetic interpretation: discover an agent contract, delegate a request and track its actual outcome; no remote agent is contacted here.',
      takeaway: 'A2A standardizes agent interactions; MCP standardizes access to tools and context.',
      azure: {
        title: 'Discover and call an A2A agent',
        sourceId: 61,
        environment: 'portal-and-code',
        minutes: 35,
        cost: 'The backing agent’s model calls incur Azure usage. A local demonstration server does not require public hosting, but any separately deployed host can add cost.',
        prerequisites: [ACCESS, 'The original source 61 exercise and its supported client/server development environment.', 'Permission to run the sample server locally. Keep it local or within the approved lab network; do not expose an unauthenticated demonstration publicly.'],
        steps: [
          {
            title: 'Confirm the Azure backing resource',
            surface: 'Azure portal',
            instruction: 'Open the approved subscription and lab resource group. Identify the Foundry resource that will back the sample agents and confirm its authorized scope.',
            expected: 'Distinguish the Azure model resource from the HTTP server you will run for A2A.',
            hint: 'The A2A protocol does not create or authorize an Azure subscription on your behalf.',
          },
          {
            title: 'Prepare the Foundry deployment',
            surface: 'Microsoft Foundry',
            instruction: 'Open the project overview and model deployments. Locate the endpoint and deployed model settings required by the original exercise.',
            expected: 'Verify the intended backing model exists before starting the remote-agent sample.',
            hint: 'A reachable A2A server can still fail if its backing agent lacks a deployment or credentials.',
          },
          {
            title: 'Inspect and start the sample server',
            surface: 'Cloud Shell / local editor',
            instruction: 'Launch source 61’s linked lab and follow its setup. Locate the skill, Agent Card, executor, request handler and Task Store. Start the server on the exercise’s approved local address and port.',
            expected: 'Observe a listening server and identify where business logic lives separately from HTTP handling.',
            hint: 'Use only processes and ports belonging to your lab. Do not open public inbound access just to avoid a local connection problem.',
          },
          {
            title: 'Verify discovery metadata',
            surface: 'Browser',
            instruction: 'Open the server’s Agent Card URL from the exercise, typically the local base URL followed by /.well-known/agent-card.json. Inspect skills, endpoint, input/output modes and advertised features.',
            expected: 'Verify the card describes the actual sample endpoint and contains no credentials.',
            hint: 'If the exercise uses another discovery path for its protocol version, follow that path; a stale card URL can misdirect an otherwise correct client.',
          },
          {
            title: 'Send an identifiable request',
            surface: 'Cloud Shell / local editor',
            instruction: 'Run the supplied client so it retrieves the card and sends a fictional request. Inspect whether the response is a direct message or a task, and follow the exercise’s result-handling path.',
            expected: 'Trace the request through the executor to an actual result, not just successful card retrieval.',
            hint: 'A successful discovery request proves only discovery. Inspect execution errors and task state separately.',
          },
          {
            title: 'Check advertised lifecycle features',
            surface: 'Cloud Shell / local editor',
            instruction: 'If the sample advertises streaming, inspect partial and final events. Inspect its cancellation implementation and Task Store; test cancellation only if supported, otherwise record that explicit limitation.',
            expected: 'Distinguish a completed result, an ongoing task and an unsupported cancellation capability.',
            hint: 'Do not claim durable task recovery merely because an in-memory task store exists.',
          },
        ],
        success: 'Retrieve the real Agent Card and observe a successful sample execution or accurately diagnosed failure, distinguishing metadata discovery from task completion.',
        evidencePrompt: 'Record one skill, the executor’s role and whether the client received a message or task. Keep credentials and private request content out of evidence.',
        cleanup: `Stop only the client and server processes you started. Remove exercise-created backing agents, conversations and local artifacts when no longer needed; keep shared services intact. ${PRESERVE}`,
      },
    },
    check: {
      question: 'A partner owns a specialist agent with its own model and task lifecycle. Your application must discover its skills and wait for a completed review. Which design fits?',
      options: [
        { id: 'a', text: 'Retrieve its Agent Card, invoke it with an A2A client and handle message or task responses.', explanation: 'This uses the advertised agent contract and preserves the remote service’s independent processing and lifecycle.' },
        { id: 'b', text: 'Assume an MCP tool listing is the same as an Agent Card and task contract.', explanation: 'MCP exposes tools and context; it does not by itself provide the A2A agent interaction and task semantics described here.' },
        { id: 'c', text: 'Treat a successful Agent Card fetch as the completed review.', explanation: 'Discovery metadata describes capabilities, not the outcome of executing a specific review request.' },
      ],
      correctOptionId: 'a',
      explanation: 'Discover the agent contract, then verify execution and lifecycle outcomes separately.',
    },
    recall: {
      prompt: 'Reconstruct the A2A request path and distinguish it from MCP tool invocation.',
      answer: 'The client discovers an Agent Card, sends a request to the server, the request handler delegates to an executor with task management, and the executor returns messages or task events/artifacts. MCP instead standardizes discovering and calling tools or obtaining context for an agent.',
    },
    transfer: 'A customer wants agents owned by different teams to collaborate. What would you agree about capability discovery, authentication, task status, cancellation and failure ownership?',
    sourceNote: `Main sources 37, 107, 103 and 26 cover the complete card/server/executor/client path. Introduction 134, assessment 157 and summary 205 support it. A2A is an interoperability protocol, not a new orchestration pattern; locally hosted sample agents can demonstrate it without claiming a production remote deployment. Guide: ${GUIDES.a2a}`,
  },
  {
    id: 'agents-publishing',
    title: 'Publish with the right identity',
    cluster: 'agents-orchestration',
    examDomain: MANAGE,
    summary: 'Move a tested agent into Teams or Microsoft 365 through an Agent Application, then verify distribution, permissions and the actual published experience.',
    minutes: 9,
    order: 164,
    prerequisites: ['agents-vscode-lifecycle', 'agents-builtin-tools'],
    sourceIds: [232, 185, 222, 5, 125, 149, 209, 79, 111, 223],
    steps: [
      {
        title: 'Publish a version, not a moving draft',
        body: 'Publishing creates an Agent Application with a stable invocation URL, a distinct Microsoft Entra identity and user-isolation responsibilities. It routes requests to the selected deployed agent version; the agent’s logic does not move into Teams itself. Saving a development change is separate from releasing an updated version. The corpus also distinguishes project conversation support from published Agent Application endpoints, which source 223 describes as stateless Responses endpoints requiring client-managed conversation history for multi-turn applications.',
        example: 'A tested version can remain available to staff while developers revise another version; the stable application endpoint can later route to the selected update.',
      },
      {
        title: 'Prepare the channel and distribution scope',
        body: 'Direct Foundry publishing provisions the Microsoft 365 integration, including Azure Bot Service routing, an Entra application registration and a publishing package. Verify project publishing and invocation roles, resource-creation and app-registration permissions, the Bot Service provider and tenant custom-app policy. Shared scope supports limited testing; organization scope requires administrator approval and remains subject to app policies. Prepare accurate metadata, icons and approved policy URLs without secrets. Microsoft 365 licensing and permitted surfaces depend on the tenant.',
        example: 'A lab policy helper can be tested in an allowed shared scope without requesting organization-wide distribution to every employee.',
      },
      {
        title: 'Regrant tool access to the published identity',
        body: 'The published agent has an identity separate from the development project. Its permissions on resources such as search or storage do not transfer automatically. Grant only the access its tools require to that new identity, while separately granting callers the required invocation role on the Agent Application. A tool working in the playground but failing after publication often indicates this boundary. Validate authentication and tool calls in the actual Teams experience, not only in the development playground.',
        example: 'A document-search helper can respond in Teams yet fail to retrieve documents because its new agent identity lacks the resource-specific search permission.',
      },
      {
        title: 'Test the integration and choose complexity deliberately',
        body: 'After publishing, inspect rendering, response time, authentication, tool results and the experience of permitted test users. Check Bot Service configuration, Foundry metrics and Application Insights when configured. Updates need the publishing/package flow and possibly renewed organizational approval. Direct publishing fits ordinary integration; Microsoft 365 Agents Toolkit adds a proxy layer for custom SSO, middleware, advanced debugging or multi-environment pipelines. Its local Agents Playground simulates Teams and is not proof that tenant deployment or permissions work.',
        example: 'A company requiring custom request auditing between Teams and Foundry may justify a toolkit proxy; a simple policy helper usually does not need that extra layer.',
      },
    ],
    lab: {
      title: 'Optional rehearsal: diagnose a published tool failure',
      goal: 'Plan identity and distribution checks before the actual Teams publishing exercise.',
      context: `${REHEARSAL} A search tool works in the project playground but fails after the agent is published to Teams.`,
      fields: [
        {
          id: 'identity',
          label: 'Tool-access identity to inspect',
          hint: 'Publishing creates a separate identity.',
          options: [
            { value: 'published-agent-resource-permissions', label: 'The published agent’s permissions on the tool’s resource' },
            { value: 'project-identity-always-inherited', label: 'Assume all project permissions transferred' },
            { value: 'larger-model', label: 'Replace the model before inspecting permissions' },
          ],
          expected: 'published-agent-resource-permissions',
          explanation: 'The published identity needs its own least-privilege resource access. Changing model size cannot fix an authorization failure.',
        },
        {
          id: 'scope',
          label: 'First lab distribution',
          hint: 'Use the smallest tenant-permitted scope before broad release.',
          options: [
            { value: 'permitted-shared-testing', label: 'Permitted shared/testing scope' },
            { value: 'organization-without-approval', label: 'Organization-wide without administrator approval' },
            { value: 'public-secrets-in-metadata', label: 'Publish credentials in metadata for easier access' },
          ],
          expected: 'permitted-shared-testing',
          explanation: 'Limited testing reduces the release impact. Tenant policy still applies, and public metadata must not contain secrets.',
        },
      ],
      template: `{
  "kind": "conceptual-publishing-checklist",
  "toolIdentityCheck": "{{identity}}",
  "initialDistribution": "{{scope}}"
}`,
      language: 'json',
      expectedOutput: 'Synthetic interpretation: inspect the published identity and tenant-permitted test scope; no application, bot or Teams package has been created here.',
      takeaway: 'Publishing introduces a new identity and distribution boundary, not just a new URL.',
      azure: {
        title: 'Publish and test a lab agent in Teams',
        sourceId: 79,
        environment: 'foundry',
        minutes: 40,
        cost: 'Foundry model usage, Azure Bot Service and supporting resources may incur charges. A permitted Microsoft 365/Teams tenant is required; Microsoft 365 Copilot availability depends on tenant licensing. No free tenant or license is assumed.',
        prerequisites: [
          ACCESS,
          'Azure AI Project Manager on the project, Azure AI User at the Agent Application scope for invocation, and permission to create the required Azure resources and Entra app registration as described by the exercise.',
          'An authorized Microsoft 365 tenant that permits custom apps/bots, with administrator participation where required. Do not change tenant-wide policy to bypass a restriction.',
          'A tested lab agent, fictional source documents and approved display metadata, icons, privacy-policy and terms URLs.',
        ],
        steps: [
          {
            title: 'Verify roles and provider registration',
            surface: 'Azure portal',
            instruction: 'Select the approved subscription. Under Resource providers, check Microsoft.BotService. Have an authorized administrator register it if necessary, and verify the project, resource-creation and Entra app-registration permissions required by source 79.',
            expected: 'Confirm the required setup is permitted, or record the exact permission/provider blocker before publishing.',
            hint: 'Do not assign broad roles or change tenant policy as a shortcut. Publishing permission and permission to invoke the result are different.',
          },
          {
            title: 'Test and select the agent version',
            surface: 'Microsoft Foundry',
            instruction: 'Open your lab agent, verify its file-grounded answers and a boundary request in the Playground, then save and select the version intended for publication.',
            expected: 'Identify a tested saved version rather than an unsaved configuration.',
            hint: 'Use fictional policy documents. A fluent answer without a supporting document is not a successful grounding test.',
          },
          {
            title: 'Start the publishing wizard',
            surface: 'Microsoft Foundry',
            instruction: 'Choose Publish, then Publish to Teams and Microsoft 365 Copilot as described in source 185. Create the dedicated lab Azure Bot Service when prompted and fill in approved, nonsecret metadata.',
            expected: 'Inspect the intended agent application, bot resource and metadata before confirming distribution.',
            hint: 'If required privacy or terms information is unavailable, stop at preparation rather than inventing an organization’s legal policy.',
          },
          {
            title: 'Choose a limited allowed scope',
            surface: 'Microsoft Foundry',
            instruction: 'Select the tenant-permitted shared/testing scope and Prepare Agent. Follow the in-product distribution flow or download the generated package for the exercise’s custom-app upload path.',
            expected: 'Obtain a prepared test package or a permitted test distribution; do not claim organization-wide availability.',
            hint: 'Organization scope needs administrator approval. Shared scope is still subject to tenant app policies.',
          },
          {
            title: 'Inspect the new identity',
            surface: 'Azure portal',
            instruction: 'Locate the published agent identity from Foundry, then inspect each Azure resource used by its tools. Have an authorized owner grant only the required resource roles to that identity and confirm the test caller’s application invocation access separately.',
            expected: 'Distinguish agent-to-resource permissions from user-to-application permission.',
            hint: 'Development-project role assignments do not automatically grant the new agent identity access.',
          },
          {
            title: 'Test the actual Teams channel',
            surface: 'Browser',
            instruction: 'Open Teams. If using the package path, go to Apps > Manage your apps > Upload an app > Upload a custom app, subject to tenant policy. Open the lab agent and test a grounded question and a follow-up with a permitted test user.',
            expected: 'Observe actual channel authentication, rendering and tool behavior, or record the specific blocked stage.',
            hint: 'If the agent is silent, inspect Bot Service configuration; if tools alone fail, inspect the published identity’s resource access.',
          },
          {
            title: 'Check update and monitoring behavior',
            surface: 'Microsoft Foundry',
            instruction: 'Make one harmless lab wording change, save and test it, then follow the source 185 update/package flow for the same limited scope. Inspect the application endpoint and available request, latency and tool-error metrics after testing.',
            expected: 'Distinguish development save from published update, and confirm the actual channel uses the released version.',
            hint: 'Package updates or organization policy may require another upload or approval. Application Insights traces exist only if integration is configured.',
          },
        ],
        success: 'Verify a permitted test user can interact with the published lab version and its tools in Teams. If licensing, policy or permission blocks that stage, record it as incomplete rather than counting a local simulation.',
        evidencePrompt: 'Record the published version, test scope and separate caller/tool permission checks, plus one actual channel test result. Do not store tokens, private conversations or secrets.',
        cleanup: `Uninstall only the lab Teams app/package and remove only dedicated lab Agent Application, bot, agents and app registrations after confirming they are not shared. Coordinate removal with the resource owner; never revoke shared tenant consent or delete shared groups. ${PRESERVE}`,
      },
    },
    check: {
      question: 'A published Teams agent answers greetings but receives authorization errors from its search tool, which worked in the Foundry playground. What should you inspect first?',
      options: [
        { id: 'a', text: 'Whether the published agent identity has the required search-resource permissions.', explanation: 'Publishing creates a separate identity, so the earlier project identity’s permissions do not establish access for the published tool call.' },
        { id: 'b', text: 'Whether the model temperature is too low for retrieval.', explanation: 'Sampling settings do not grant Azure resource access or explain a resource authorization error.' },
        { id: 'c', text: 'Whether organization-wide publishing removes the need for RBAC.', explanation: 'Distribution scope affects availability and approval, not the published identity’s permission to call search.' },
      ],
      correctOptionId: 'a',
      explanation: 'Trace the failing operation to the identity that now performs it, while keeping caller invocation permission separate.',
    },
    recall: {
      prompt: 'What does publishing create, and which two permission paths must you check separately?',
      answer: 'An Agent Application fronts a deployed version with a stable endpoint and distinct identity; Microsoft 365 integration also creates routing/package and registration assets. Check the caller’s access to invoke the application separately from the agent identity’s access to tool resources.',
    },
    transfer: 'A customer needs custom SSO and audit middleware between Teams and an agent. What would justify an Agents Toolkit proxy, and which new deployment responsibilities would it introduce?',
    sourceNote: `Main sources 232, 185 and 222 cover publishing and testing; source 5 explains the optional toolkit proxy, and 223 distinguishes project deployment, stable publication and stateless application endpoints. Intros, assessment, summary and episode 111 support the lesson. Shared scope is policy-dependent, not a promise of access for every tenant user. Guide: ${GUIDES.publishing}`,
  },
  {
    id: 'agents-work-iq-permissions',
    title: 'Respect Work IQ permission boundaries',
    cluster: 'agents-orchestration',
    examDomain: MANAGE,
    summary: 'Access Microsoft 365 context through Work IQ without confusing publishing, tenant consent or a Copilot license with permission to every document.',
    minutes: 7,
    order: 168,
    prerequisites: ['agents-mcp', 'agents-publishing'],
    sourceIds: [2, 79, 5, 125, 149, 209, 111],
    steps: [
      {
        title: 'Add workplace context through a separate connection',
        body: 'Work IQ is a CLI and MCP server for querying Microsoft 365 workplace information such as mail, meetings, documents, Teams messages and people. CLI mode supports direct development queries; MCP mode exposes the same context to a compatible assistant through discoverable capabilities. Publishing a Foundry agent into Teams does not automatically install or authorize Work IQ. Publishing chooses where users interact, while Work IQ is a separate data-access integration with its own prerequisites and identity boundary.',
        example: 'A Teams-based policy helper can exist without Work IQ. A developer assistant may separately use Work IQ to find an approved project meeting summary.',
      },
      {
        title: 'Distinguish consent from access rights',
        body: 'The supplied source requires a Microsoft 365 subscription with a Copilot license and administrative consent for the Work IQ application in the Entra tenant. Local CLI use also needs Node.js and acceptance of its license agreement. Those prerequisites enable the integration; they do not grant access to every employee’s data. Work IQ uses the authenticated user’s Microsoft 365 permissions and organizational policies. If access is blocked, involve the authorized administrator rather than trying another person’s credentials.',
        example: 'Administrative consent can permit Work IQ use while a payroll document remains unavailable to a user who lacks permission to view it.',
      },
      {
        title: 'Use only the context the task needs',
        body: 'CLI and MCP access share the same underlying permission boundary, so switching modes is not a way around denied access. The corpus describes on-demand retrieval and auditable queries, with Work IQ itself not storing the Microsoft 365 data it retrieves. That does not mean a consuming application, exported answer or diagnostic log cannot retain sensitive information. Ask narrow questions against approved material, verify relevant source access and keep private retrieved content out of screenshots, learning evidence and source control.',
        example: 'For a lab, locate one harmless test document you can already open; record whether retrieval worked, not the contents of real mail or meeting transcripts.',
      },
    ],
    lab: {
      title: 'Optional rehearsal: separate consent and permission',
      goal: 'Check the access model before the real, tenant-dependent Work IQ exercise.',
      context: `${REHEARSAL} Work IQ is consented and licensed, but the signed-in user cannot open a restricted document in Microsoft 365.`,
      fields: [
        {
          id: 'access',
          label: 'Expected document access',
          hint: 'Tenant consent is not a document-sharing grant.',
          options: [
            { value: 'respect-existing-user-permissions', label: 'Respect the user’s existing document permissions' },
            { value: 'admin-consent-unlocks-all-documents', label: 'Administrative consent unlocks every document' },
            { value: 'mcp-bypasses-cli-permissions', label: 'MCP mode bypasses the CLI’s permissions' },
          ],
          expected: 'respect-existing-user-permissions',
          explanation: 'Work IQ inherits the user’s Microsoft 365 access. Consent and a license do not make restricted documents readable.',
        },
        {
          id: 'evidence',
          label: 'Learning evidence',
          hint: 'Record the outcome without exporting workplace content.',
          options: [
            { value: 'redacted-access-outcome', label: 'A redacted access outcome for a harmless test document' },
            { value: 'full-private-transcript', label: 'A full private meeting transcript' },
            { value: 'authentication-token', label: 'The authentication token used by the query' },
          ],
          expected: 'redacted-access-outcome',
          explanation: 'The learning goal is understanding the boundary, not retaining sensitive data or credentials.',
        },
      ],
      template: `{
  "kind": "conceptual-work-iq-access",
  "documentAccess": "{{access}}",
  "evidencePolicy": "{{evidence}}"
}`,
      language: 'json',
      expectedOutput: 'Synthetic interpretation: an already restricted document remains restricted; consent, licensing and MCP discovery do not expand the user’s document access.',
      takeaway: 'A connected assistant must respect both tenant approval and the signed-in user’s data permissions.',
      azure: {
        title: 'Verify Work IQ with approved test data',
        sourceId: 79,
        environment: 'portal-and-code',
        minutes: 25,
        cost: 'Requires the Microsoft 365 subscription and Copilot license specified by the corpus, plus permitted Work IQ access. Foundry-backed agent calls can add Azure charges. Do not purchase or assume trial eligibility to complete this lab.',
        prerequisites: [
          'Use the optional Work IQ exercise linked from source 79 and the prerequisites in source 2. Work IQ is preview in the supplied material.',
          'An authorized Microsoft 365 tenant, the required Copilot license and administrator consent for the Work IQ application. If unavailable, stop at the access checkpoint and record the blocker.',
          'An approved local development environment with Node.js, permission to accept the applicable license agreement, and a harmless test document the signed-in user already has permission to read.',
        ],
        steps: [
          {
            title: 'Confirm the tenant and consent boundary',
            surface: 'Azure portal',
            instruction: 'Open Microsoft Entra ID in the intended directory. Have an authorized administrator verify the Work IQ application and required consent using source 2; do not grant consent yourself unless that is your approved role.',
            expected: 'Confirm the intended tenant and an authorized consent decision, or document that this prerequisite is blocked.',
            hint: 'Changing directories or using another person’s credentials is not a legitimate workaround for denied access.',
          },
          {
            title: 'Separate publishing from data access',
            surface: 'Microsoft Foundry',
            instruction: 'If you have a lab agent, inspect its tools and published application settings. Identify whether any Work IQ connection was explicitly configured; do not assume Teams publication added one.',
            expected: 'Distinguish a channel integration from a Microsoft 365 data connection.',
            hint: 'No Foundry agent is required for standalone Work IQ CLI exploration. This inspection does not create a connection or prove a tenant grant.',
          },
          {
            title: 'Verify license and an allowed source',
            surface: 'Browser',
            instruction: 'With the authorized tenant owner, verify the required Microsoft 365/Copilot license and allowed app use. Open an approved harmless test document in Microsoft 365 under your own identity.',
            expected: 'Confirm the prerequisite license and direct access to the intended test document before querying through an assistant.',
            hint: 'If licensing or policy is unavailable, stop here and record the unmet requirement rather than calling the rehearsal a completed lab.',
          },
          {
            title: 'Set up the approved Work IQ client',
            surface: 'Cloud Shell / local editor',
            instruction: 'Open source 79’s original page and launch its optional Work IQ exercise. Follow its current installation, license-acceptance and sign-in steps using the CLI or approved editor integration described in source 2.',
            expected: 'Confirm the client is authenticated as the intended user without exposing credentials in configuration notes.',
            hint: 'Accept the license only when authorized. Follow the original exercise for preview command and package changes.',
          },
          {
            title: 'Query narrow, harmless context',
            surface: 'Cloud Shell / local editor',
            instruction: 'Use the exercise’s query flow to locate the approved test document or answer one harmless question about it. If using MCP mode, inspect the tool invocation and compare its access scope with CLI mode.',
            expected: 'Verify retrieval under the same user permissions and identify which integration mode performed the query.',
            hint: 'Do not ask for private mail or broad organizational exports to prove connectivity. MCP mode does not expand access.',
          },
          {
            title: 'Check the boundary and record only the outcome',
            surface: 'Browser',
            instruction: 'Compare the retrieved source reference with your direct Microsoft 365 access. If the administrator provides a deliberately unshared synthetic test document, verify it remains inaccessible; otherwise record that the negative-access test was not run.',
            expected: 'Separate positive retrieval evidence from any untested denial boundary without copying workplace content into the studio.',
            hint: 'Work IQ’s on-demand retrieval does not make your own transcripts, logs or exported notes safe places for confidential data.',
          },
        ],
        success: 'Verify a permitted Work IQ query under your own identity, or clearly identify the licensing/consent blocker. Do not claim access to restricted data or completion from the optional rehearsal.',
        evidencePrompt: 'Record only the integration mode, whether approved test data was accessible and which permission governed the result. Omit document content, private names, tokens and tenant secrets.',
        cleanup: `Close the lab client and remove only lab-specific local MCP configuration or test artifacts you created. Remove a test document only if you created it and have permission. Do not revoke shared tenant consent, licenses or other users’ access. ${PRESERVE}`,
      },
    },
    check: {
      question: 'Work IQ has tenant consent and the user has the required license, but the user cannot open a payroll document directly. What should happen through Work IQ?',
      options: [
        { id: 'a', text: 'MCP mode should reveal it because discovery implies organization-wide access.', explanation: 'Discovery exposes capabilities, not unrestricted document permissions. Switching integration mode does not elevate the user.' },
        { id: 'b', text: 'Administrative consent should override the document’s access controls.', explanation: 'Consent permits the application integration; it is not a grant to every protected Microsoft 365 item.' },
        { id: 'c', text: 'The existing user-permission boundary should still prevent access.', explanation: 'Work IQ inherits the authenticated user’s access and tenant policies, even after the integration is consented and licensed.' },
      ],
      correctOptionId: 'c',
      explanation: 'Licensing, consent and document authorization are separate requirements, all of which remain relevant.',
    },
    recall: {
      prompt: 'Distinguish Work IQ’s two modes and the roles of licensing, administrator consent and user document permissions.',
      answer: 'CLI mode handles direct queries; MCP mode supplies tools/context to a compatible assistant. The required license permits the product use, administrator consent authorizes the integration, and the signed-in user’s existing permissions limit which Microsoft 365 content can be retrieved.',
    },
    transfer: 'A customer wants an agent to use workplace context. How would you establish a minimal useful query, an authorized identity and an evidence policy that avoids exporting sensitive content?',
    sourceNote: `Source 2 is the primary Work IQ authority; source 79 explicitly links an optional Work IQ exercise at ${GUIDES.publishing}. Sources 5, 125, 149, 209 and episode 111 provide integration context, not a substitute for Work IQ’s licensing and permission requirements. Preview availability must be checked in the learner’s tenant.`,
  },
]
