import type { AzureExercise, CourseUnit } from './schema'

const LOCAL_MODEL = 'Optional local learning model, not an Azure configuration schema or executable SDK request. These choices rehearse a decision; the Azure walkthrough below is the real hands-on activity.'
const CLOUD_COST = 'Cloud resources, model requests, and evaluation calls may be billed to your Azure subscription. This site creates no Azure resources. Check current pricing, quota, region availability, and your spending permission before you deploy or submit requests.'
const PORTAL_NOTE = 'Portal labels and model availability can change. Open the cited Exercise source and follow its Launch exercise link for authoritative navigation. The walkthrough is an authored, bounded adaptation, not a claim that a deployment or run has succeeded.'
const RESOURCE_PREREQUISITES = [
  'An active Azure account and subscription with permission to create or use the lab Foundry resource and project; obtain administrator help if required.',
  'An approved lab resource group, suitable region, and permission to incur any charges; retain shared resources and use only non-sensitive sample data.',
]
const CODE_PREREQUISITES = [
  ...RESOURCE_PREREQUISITES,
  'Visual Studio Code, Git, Azure CLI, and the Python version and isolated environment specified by the linked exercise. Use its dependency versions rather than mixing SDK generations.',
]
const CLEANUP = 'Stop the lab client and any unnecessary jobs. In Foundry, remove only deployments, uploaded datasets, or custom assets you created for this lab and no longer need. In Azure portal, inspect the lab resource group before removing any resources. Delete a group only if it is exclusively your disposable lab and has no shared dependencies. Retain shared resources and projects needed for later exercises; retained resources may continue to incur charges.'

function openProject(sourceId: number): AzureExercise['steps'][number] {
  return {
    title: 'Open your lab project',
    surface: 'Microsoft Foundry',
    instruction: `Open Exercise source ${sourceId} and its Launch exercise link. Sign in at ai.azure.com and open your existing lab project. If you need one, use Create new project, expand Advanced options, and select your approved Foundry resource name, subscription, resource group, and a region supported by the exercise. Select Create only after checking costs.`,
    expected: 'The project Home or Overview page identifies the project you selected and its parent Foundry resource. A failed creation is a blocker to resolve, not a completed exercise.',
    hint: 'Use the New Foundry experience where the exercise requests it. If creation fails, check the signed-in tenant, subscription permissions, regional support, and quota instead of repeatedly creating resources.',
  }
}

export const FOUNDATION_UNITS: CourseUnit[] = [
  {
    id: 'ai-foundations',
    title: 'Start with the application problem',
    cluster: 'models-deploy-eval',
    examDomain: 'Plan and manage an Azure AI solution',
    summary: 'Separate the task, the model, and the surrounding application before choosing an Azure capability.',
    minutes: 7,
    order: 0,
    prerequisites: [],
    sourceIds: [1, 27, 28, 191, 113, 260, 96, 186, 78, 183],
    steps: [
      {
        title: 'Prediction is not a business workflow',
        body: 'An AI capability transforms input into a prediction or generated result; an application decides what to do with that result. Machine learning learns patterns from data rather than relying only on hand-written rules. Generative AI produces new content from those patterns. Neither a fluent answer nor a confident prediction proves that a business action happened. Start by naming the input, required output, and consequence of an error. That keeps a useful model from being mistaken for a complete system.',
        example: 'A library assistant can draft a renewal explanation. Renewing the actual loan still requires an authorized call to the library system.',
      },
      {
        title: 'Match the capability to the input',
        body: 'Microsoft Foundry organizes models, agents, tools, and knowledge in projects backed by an Azure resource. A language model can generate an explanation; an agent combines a model with instructions and tools to perform tasks. Foundry Tools also supply focused capabilities: Language analyzes text, Speech handles spoken input and output, Translator changes language, and document services extract information. Select the narrowest capability that satisfies the requirement rather than assuming every task needs an autonomous agent.',
        example: 'For a recorded library enquiry, Speech can transcribe the audio. A language model might draft the reply, while a separate tool checks the loan record.',
      },
      {
        title: 'Design for people and accountable decisions',
        body: 'Responsible AI begins when you define the problem. Fairness asks whether service quality differs across groups; reliability and safety ask what happens when the output is wrong. Privacy and security constrain access to member data. Inclusiveness includes people who use assistive technology or need smaller interaction steps. Transparency explains the system’s limits, and accountability names the human owner who responds to problems. These are design obligations, not a badge earned by opening a source or completing one question.',
        example: 'A library team measures recognition across accents, offers a text alternative, discloses that replies are generated, and keeps a staff escalation route.',
      },
    ],
    lab: {
      title: 'Rehearse a capability boundary',
      goal: 'Distinguish generating language from taking an authorized action.',
      context: LOCAL_MODEL,
      fields: [
        {
          id: 'reply',
          label: 'Create a new explanation',
          hint: 'The input is a member’s question and the required output is readable text.',
          options: [
            { value: 'language-model', label: 'A generative language model' },
            { value: 'speech-transcription', label: 'Speech transcription alone' },
            { value: 'image-generator', label: 'An image-generation model' },
          ],
          expected: 'language-model',
          explanation: 'A language model drafts the explanation. Transcription changes audio into text; it does not by itself compose the answer.',
        },
        {
          id: 'renewal',
          label: 'Change the loan record',
          hint: 'A generated sentence must not be treated as evidence that a transaction occurred.',
          options: [
            { value: 'authorized-tool', label: 'An authorized application tool' },
            { value: 'reply-text', label: 'The model’s reassuring reply' },
            { value: 'higher-confidence', label: 'A more confident prompt' },
          ],
          expected: 'authorized-tool',
          explanation: 'A tool connected to the actual system performs the action. The application must distinguish its result from generated prose.',
        },
      ],
      template: '{\n  "learning_model": "library task boundaries",\n  "draft_reply_with": "{{reply}}",\n  "change_loan_with": "{{renewal}}"\n}',
      language: 'json',
      expectedOutput: 'SYNTHETIC EXPECTED RESULT: explanation drafted; loan change requires a separate authorized tool result. No transaction or Azure request has run.',
      takeaway: 'Changing the chosen capability changes the responsibility it can fulfill; better wording cannot substitute for a real tool.',
      azure: {
        title: 'Recognize the parts of a Foundry project',
        sourceId: 78,
        environment: 'foundry',
        minutes: 15,
        cost: CLOUD_COST,
        prerequisites: RESOURCE_PREREQUISITES,
        steps: [
          {
            title: 'Confirm your Azure boundary',
            surface: 'Azure portal',
            instruction: 'At portal.azure.com, confirm the directory and subscription you intend to use. Open Resource groups and identify your approved lab group; do not use an unfamiliar shared group.',
            expected: 'You can identify the subscription and group that would contain your lab resources before creating anything.',
            hint: 'If the expected subscription is missing, resolve account or directory access first.',
          },
          openProject(78),
          {
            title: 'Find the project and resource',
            surface: 'Microsoft Foundry',
            instruction: 'On Home or Overview, identify the project name. Open Manage and the Parent resource link as shown in the exercise, then return to Home. Do not copy or expose the displayed key.',
            expected: 'You can distinguish the project workspace from the Azure resource supporting it.',
          },
          {
            title: 'Inspect a model without deploying',
            surface: 'Microsoft Foundry',
            instruction: 'Open Discover, then Models. Open one text-generation model card and read its input, output, and capability description. For this orientation, stop before Deploy.',
            expected: 'The card describes a model capability; browsing the card has not demonstrated a working application.',
          },
          {
            title: 'Find a focused AI service',
            surface: 'Microsoft Foundry',
            instruction: 'Open Build and locate AI Services or Foundry Tools, following the exercise’s current navigation. Inspect the available Language or Speech entry and identify one task it provides. Return to Home; this is a safe stopping point.',
            expected: 'You can name one prebuilt tool task and explain how it differs from generating a reply.',
            hint: 'If the current portal has moved this page, follow the source navigation rather than creating an unrelated service.',
          },
        ],
        success: 'You can point to your project, its parent resource, a model card, and a focused tool capability without treating browsing as mastery.',
        evidencePrompt: 'In your own words, record the input and output of two capabilities you inspected and which component would actually renew a loan. Do not record keys, tokens, or personal data.',
        cleanup: CLEANUP,
      },
    },
    check: {
      question: 'A museum wants to transcribe visitor voice messages and draft staff replies. Which initial design separates the responsibilities appropriately?',
      options: [
        { id: 'a', text: 'Use a language model alone and assume the recording becomes text automatically.', explanation: 'A text-only model does not acquire audio input capability because the application needs it. The input modality must be supported explicitly.' },
        { id: 'b', text: 'Use a speech capability for transcription and a suitable language model for drafting.', explanation: 'This maps audio-to-text and text generation to distinct capabilities, allowing each result and failure mode to be checked.' },
        { id: 'c', text: 'Use an image generator because both audio and images are non-text data.', explanation: 'Non-text inputs are not interchangeable. An image-generation capability does not meet the transcription requirement.' },
      ],
      correctOptionId: 'b',
      explanation: 'Choose by the transformation required at each boundary, not by whether a product has “AI” in its name. Staff still need to validate the draft and own the response.',
    },
    recall: {
      prompt: 'Without looking back, explain why an AI model is not a complete application. Name two responsibilities outside the model.',
      answer: 'The model produces predictions or generated content from input. The surrounding application supplies authorized data, calls real tools, validates results, manages identity and state, and supports monitoring or human escalation. Any two correctly explained responsibilities show the boundary.',
    },
    transfer: 'A customer says, “We need an agent.” What input, output, and real-world action would you ask them to demonstrate before deciding whether they need a model, a focused tool, or an agent?',
    sourceNote: `Sources 1, 27, 28, and 191 are whole-course overview references, not evidence that this unit teaches their entire contents. The study guide’s five exam domains organize later units; no exam weights are inferred here. ${PORTAL_NOTE}`,
  },
  {
    id: 'foundry-projects',
    title: 'Organize a Foundry project',
    cluster: 'models-deploy-eval',
    examDomain: 'Plan and manage an Azure AI solution',
    summary: 'Locate the project, parent resource, tools, and developer surfaces without confusing their boundaries.',
    minutes: 6,
    order: 5,
    prerequisites: ['ai-foundations'],
    sourceIds: [155, 51, 78, 162, 203, 183],
    steps: [
      {
        title: 'Give related work a workspace',
        body: 'A team building two assistants needs a way to organize their models, agents, data, and connections. A Foundry project provides that workspace; it belongs to one Foundry resource in Azure. A resource can support multiple child projects, including a default project. The resource supplies shared services and administration. A resource group organizes Azure resources for management, but it is not itself the AI project. Keeping these levels distinct prevents accidental changes to another team’s environment.',
        example: 'A museum can organize its visitor assistant and staff assistant in separate projects while administrators manage their shared parent resource deliberately.',
      },
      {
        title: 'Know which portal owns the task',
        body: 'Use Azure portal to inspect the Azure resource and its resource group. Use Microsoft Foundry portal to discover models and work with project assets. In the corpus’s newer interface, Discover finds possible models and tools; Build works with deployed assets; Operate supports ongoing oversight. The project Home or Overview page exposes connection information. Manage links the project to its parent resource. Seeing an endpoint only tells you where a service is reached, not that you have permission to call it.',
        example: 'Finding a model in Discover is different from seeing a deployment in Build. An application needs a usable deployment, not merely a catalog listing.',
      },
      {
        title: 'Move between portal and development tools',
        body: 'The portal is a practical place to configure and inspect an AI solution before writing code. Foundry Toolkit for Visual Studio Code brings project resources, deployments, and playgrounds into an editor. SDKs expose service operations: the Foundry SDK supports project-specific work, OpenAI clients support compatible inference, and individual Foundry Tools have service-specific APIs. GitHub tracks source changes; GitHub Copilot can assist development, but generated code still needs review. The editor does not contain independent copies of deployed models.',
        example: 'A developer selects the existing museum project in Foundry Toolkit and sees its deployments; installing the extension does not create a second cloud environment.',
      },
    ],
    lab: {
      title: 'Rehearse project ownership',
      goal: 'Put project assets and shared Azure services at the correct level.',
      context: LOCAL_MODEL,
      fields: [
        {
          id: 'workspace',
          label: 'Organize an assistant’s assets',
          hint: 'Choose the level used for models, agents, connections, and project data.',
          options: [
            { value: 'foundry-project', label: 'Foundry project' },
            { value: 'browser-tab', label: 'A browser tab' },
            { value: 'resource-group-only', label: 'Resource group only' },
          ],
          expected: 'foundry-project',
          explanation: 'The project organizes AI solution assets. A resource group organizes Azure resources, and a browser tab is only a view.',
        },
        {
          id: 'parent',
          label: 'Shared supporting resource',
          hint: 'The current project architecture uses a parent Azure resource.',
          options: [
            { value: 'foundry-resource', label: 'Foundry resource' },
            { value: 'editor-extension', label: 'Foundry Toolkit extension' },
            { value: 'chat-history', label: 'A chat history' },
          ],
          expected: 'foundry-resource',
          explanation: 'A Foundry resource provides the cloud services and shared administration that support one or more projects.',
        },
      ],
      template: '{\n  "learning_model": "workspace ownership",\n  "assistant_assets": "{{workspace}}",\n  "shared_parent": "{{parent}}"\n}',
      language: 'json',
      expectedOutput: 'SYNTHETIC EXPECTED RESULT: project assets belong to a project supported by a Foundry resource. Nothing has been provisioned.',
      takeaway: 'Changing the workspace is not the same as changing the parent resource; check both before editing shared configuration.',
      azure: {
        title: 'Trace your project to its Azure resource',
        sourceId: 78,
        environment: 'portal-and-code',
        minutes: 25,
        cost: CLOUD_COST,
        prerequisites: [...RESOURCE_PREREQUISITES, 'Visual Studio Code with permission to install the Foundry Toolkit extension; no application code is required for this walkthrough.'],
        steps: [
          openProject(78),
          {
            title: 'Inspect the parent relationship',
            surface: 'Microsoft Foundry',
            instruction: 'Open Manage and select the project’s Parent resource. Inspect the resource name and the list of projects without changing access settings.',
            expected: 'The selected project is visibly associated with its parent resource; other listed projects are not assumed to be yours to alter.',
          },
          {
            title: 'Locate the Azure resource',
            surface: 'Azure portal',
            instruction: 'Open the approved resource group at portal.azure.com and locate the Foundry resource with the same name. Compare its region with the region selected at creation.',
            expected: 'You can connect the Foundry workspace view to the resource that exists in Azure.',
          },
          {
            title: 'Identify connection surfaces',
            surface: 'Microsoft Foundry',
            instruction: 'Return to project Home or Overview. Find the project endpoint and Azure OpenAI endpoint labels. Note their different purposes, without copying keys or tokens into notes.',
            expected: 'You can locate both endpoint types and distinguish connection addresses from authentication secrets.',
          },
          {
            title: 'Inspect a shared tool surface',
            surface: 'Microsoft Foundry',
            instruction: 'Under Build, open AI Services or the current Foundry Tools page. Inspect a Language or Speech capability, then return to the same project Home page.',
            expected: 'The selected project remains the same while you move between project assets and supporting tool capabilities.',
          },
          {
            title: 'Connect the editor to the same project',
            surface: 'Cloud Shell / local editor',
            instruction: 'In Visual Studio Code, use Extensions to install Foundry Toolkit as directed by the exercise. In its Microsoft Foundry Resources section, sign in and select this existing project as the default. Expand the project; do not deploy a duplicate model just to populate the list.',
            expected: 'The editor displays the same project identity and any assets you already have. An empty deployment list is valid for a new project.',
            hint: 'If the project is missing, check the account, tenant, and subscription selected in the extension.',
          },
        ],
        success: 'You can trace one project across Foundry, Azure portal, and the editor and explain the parent-resource relationship.',
        evidencePrompt: 'Describe which work belongs in the project and which settings belong to its parent resource. Record only non-secret labels and your explanation.',
        cleanup: CLEANUP,
      },
    },
    check: {
      question: 'Two assistants use separate Foundry projects under one resource. A developer is about to remove the parent resource after finishing one assistant’s experiment. What should they do?',
      options: [
        { id: 'a', text: 'Delete the parent because each project automatically keeps its own independent copy.', explanation: 'The current architecture makes projects children of the resource. A separate project does not justify deleting a parent used by other work.' },
        { id: 'b', text: 'Delete the editor extension instead; it owns the deployed models.', explanation: 'The extension exposes cloud resources but does not own them. Removing it does not remove cloud costs or protect a shared resource from deletion.' },
        { id: 'c', text: 'Inspect dependencies and remove only their disposable experiment assets.', explanation: 'This respects the project/resource boundary and avoids treating shared infrastructure as a private lab.' },
      ],
      correctOptionId: 'c',
      explanation: 'Workspace separation helps organization, but resource ownership and dependency checks still govern safe cleanup.',
    },
    recall: {
      prompt: 'Sketch the relationship between an Azure resource group, a Foundry resource, and its projects. Where would you find the model catalog?',
      answer: 'The resource group contains the Azure Foundry resource; the resource supports one or more child projects, one designated default. Projects organize solution assets. The Foundry portal’s Discover / Models area exposes the catalog.',
    },
    transfer: 'How would you explain to a customer why creating a second project is not automatically the same as obtaining a separate subscription, billing boundary, or independent parent resource?',
    sourceNote: `Source 155 describes the newer resource-based architecture and distinguishes older hub-based classic projects. Episode 183 shows AI Toolkit and Foundry extensions during a transition; sources 51 and 162 use the name Foundry Toolkit. Do not treat those screenshots as a universal current extension layout. ${PORTAL_NOTE}`,
  },
  {
    id: 'model-catalog',
    title: 'Choose models by required capability',
    cluster: 'models-deploy-eval',
    examDomain: 'Plan and manage an Azure AI solution',
    summary: 'Use model cards to rule out unsuitable models before comparing attractive benchmark scores.',
    minutes: 7,
    order: 10,
    prerequisites: ['foundry-projects'],
    sourceIds: [114, 89, 80, 189],
    steps: [
      {
        title: 'Turn a request into acceptance criteria',
        body: 'A model can be excellent and still be wrong for your application. First list the input modality, required output, supported languages, context needs, and features the workflow depends on. Use the model catalog’s task and capability filters to narrow the choices. Then read each model card rather than inferring features from the family name. A text-generation model that analyzes images does not necessarily generate images, and a model that writes convincing answers does not automatically support tool calling.',
        example: 'A repair assistant must interpret a product photo and answer in text. Filter for visual input support, not text-to-image generation.',
      },
      {
        title: 'Distinguish broad models from specialized ones',
        body: 'Large language models target demanding language and reasoning work, while small language models can be attractive for constrained tasks where latency, cost, or deployment footprint matter. Reasoning capabilities, context limits, and supported features still vary within both groups. Specialized models solve different transformations: embedding models produce numerical vectors for retrieval, image-generation models produce pictures, and speech models transcribe or synthesize audio. Choose based on demonstrated suitability, not the assumption that the largest model always produces the best system.',
        example: 'A short ticket-labeling task may justify testing a small model; explaining a complicated fault across several manuals may require a different candidate.',
      },
      {
        title: 'Read the commercial and deployment boundary',
        body: 'The catalog includes models sold directly by Azure and models from partners or the community. Their licensing, billing arrangements, and available deployment paths can differ. Inspect the model card’s provider, responsible AI information, supported endpoints, and fine-tuning options before committing to it. Finding a model in the catalog is discovery, not proof that your subscription can deploy it in your chosen region. A useful shortlist records both why a candidate fits and which constraint might disqualify it.',
        example: 'A team excludes a promising model when its required language or permitted deployment region is unsupported, even though another benchmark looks strong.',
      },
    ],
    lab: {
      title: 'Rehearse a capability filter',
      goal: 'Shortlist models for a photo-based repair assistant that returns text.',
      context: LOCAL_MODEL,
      fields: [
        {
          id: 'input',
          label: 'Required model input',
          hint: 'The customer uploads a picture as well as a question.',
          options: [
            { value: 'text-and-image', label: 'Text and image input' },
            { value: 'text-only', label: 'Text input only' },
            { value: 'audio-only', label: 'Audio input only' },
          ],
          expected: 'text-and-image',
          explanation: 'Visual analysis needs image input support. Merely mentioning an image in text does not supply its contents.',
        },
        {
          id: 'output',
          label: 'Required output',
          hint: 'The assistant explains what it can see; it is not designing a new product photo.',
          options: [
            { value: 'text-response', label: 'A text response' },
            { value: 'generated-picture', label: 'A newly generated picture' },
            { value: 'embedding-only', label: 'Only an embedding vector' },
          ],
          expected: 'text-response',
          explanation: 'The output requirement is language grounded in visual input. Image generation and embeddings have different jobs.',
        },
      ],
      template: '{\n  "learning_model": "model shortlist",\n  "required_input": "{{input}}",\n  "required_output": "{{output}}"\n}',
      language: 'json',
      expectedOutput: 'SYNTHETIC EXPECTED RESULT: retain models that accept text plus images and return text; verify their cards. No catalog query has run.',
      takeaway: 'Input and output filters describe opposite sides of a model; changing one does not automatically change the other.',
      azure: {
        title: 'Build a two-model shortlist',
        sourceId: 80,
        environment: 'foundry',
        minutes: 15,
        cost: CLOUD_COST,
        prerequisites: RESOURCE_PREREQUISITES,
        steps: [
          openProject(80),
          {
            title: 'Open the model catalog',
            surface: 'Microsoft Foundry',
            instruction: 'Select Discover, then Models. Locate the inference-task and capability filters before entering a model name.',
            expected: 'The catalog exposes filters that describe tasks and capabilities rather than only a provider list.',
          },
          {
            title: 'Filter for the application',
            surface: 'Microsoft Foundry',
            instruction: 'For the photo-based repair scenario, look for models supporting visual input and text output. Use the closest current filters, then open a matching model card.',
            expected: 'The card explicitly supports the required input and output; a text-to-image-only candidate is ruled out.',
          },
          {
            title: 'Inspect operating constraints',
            surface: 'Microsoft Foundry',
            instruction: 'Read the card’s language, context, endpoint, and supported-feature information. Identify any requirement you cannot verify rather than assuming it is supported.',
            expected: 'You have evidence for the candidate’s required capabilities and an explicit list of unknowns.',
          },
          {
            title: 'Compare a second candidate',
            surface: 'Microsoft Foundry',
            instruction: 'Return to Models and open another plausible candidate. Compare its provider, licensing information, deployment options, and capability boundaries with the first.',
            expected: 'Two candidates can be compared on requirements that would actually change the application design.',
          },
          {
            title: 'Stop before deployment',
            surface: 'Microsoft Foundry',
            instruction: 'Leave the model cards available for the next unit, but do not select Deploy for this walkthrough. State which candidate you would test first and which missing fact could reverse the decision.',
            expected: 'A reasoned shortlist exists without claiming that either model is already usable in your subscription.',
          },
        ],
        success: 'Two model cards have been compared against explicit input, output, and operating requirements.',
        evidencePrompt: 'Record one capability that retained a model and one that excluded or weakened another. Explain the distinction in your own words; do not record keys or account details.',
        cleanup: CLEANUP,
      },
    },
    check: {
      question: 'A warehouse assistant must explain damage visible in an uploaded photograph. Which capability is the decisive starting filter?',
      options: [
        { id: 'a', text: 'A text-to-image model with a high image-quality rating.', explanation: 'That capability creates an image from a description. It does not establish that the model can analyze the supplied photograph and explain it.' },
        { id: 'b', text: 'An embedding model with the largest vector dimension.', explanation: 'Embeddings represent content numerically for tasks such as retrieval. Vector size alone does not provide a visual explanation.' },
        { id: 'c', text: 'A model accepting image and text input and producing language output.', explanation: 'This directly matches the required transformation. Other quality, safety, and deployment constraints still need testing.' },
        { id: 'd', text: 'Any model sold directly by Azure, regardless of modalities.', explanation: 'Commercial availability is not a modality guarantee. The card must explicitly support the workflow’s inputs and outputs.' },
      ],
      correctOptionId: 'c',
      explanation: 'Capabilities are a first gate. Benchmarks and provider preferences cannot compensate for an unsupported input or output.',
    },
    recall: {
      prompt: 'Name four facts you would inspect on a model card before deployment, and explain why an embedding model is not a chat assistant.',
      answer: 'Relevant facts include input/output modalities, supported languages, context limits, endpoints, feature support, deployment options, provider/license, responsible AI guidance, and fine-tuning support. An embedding model returns numerical representations for matching or retrieval rather than composing the assistant’s answer.',
    },
    transfer: 'A customer wants “the biggest model available.” Which two workflow constraints would you use to turn that preference into a testable shortlist?',
    sourceNote: `Catalog counts and model examples differ between source 89 and episode 189 because they are snapshots. This unit intentionally makes no fixed model-count or availability claim. ${PORTAL_NOTE}`,
  },
  {
    id: 'model-benchmarks',
    title: 'Interpret model benchmark trade-offs',
    cluster: 'models-deploy-eval',
    examDomain: 'Plan and manage an Azure AI solution',
    summary: 'Read quality, safety, latency, throughput, and cost measures without confusing a leaderboard with your workload.',
    minutes: 8,
    order: 15,
    prerequisites: ['model-catalog'],
    sourceIds: [188, 145, 80, 189],
    steps: [
      {
        title: 'Use public tests to narrow uncertainty',
        body: 'Benchmarks compare models on standardized tasks, making a shortlist more defensible than choosing by reputation. The Foundry model card exposes a Benchmarks view, while the leaderboard supports scenario comparisons and trade-off charts. A quality index combines results across several tasks; it is not a probability that your next answer will be correct. Check which task and dataset a score represents, then test candidates on your own scenarios before treating a ranking as an application decision.',
        example: 'A model strong on coding benchmarks may still struggle with a museum’s short, multilingual visitor questions.',
      },
      {
        title: 'Read each metric in its own direction',
        body: 'Quality, safety, and speed do not share one universal “higher is better” rule. HarmBench attack success rate is better when lower; a detection F1 score is better when higher. Sensitive-domain knowledge is not automatically evidence of safety. Time to first token measures the wait before streamed output begins; generated tokens per second measures output rate. Tail latency describes slower requests that an average can hide. Read the metric definition and axis labels before choosing a favorable point on a chart.',
        example: 'A help desk may prefer a slightly slower output rate if users see the first useful text sooner and fewer requests have very long waits.',
      },
      {
        title: 'Match the comparison to real usage',
        body: 'Input and output tokens can have different prices. The corpus’s estimated-cost benchmark assumes a three-to-one input/output ratio; your application may have much longer conversation histories or much longer answers. Throughput matters for volume, while latency matters to someone waiting for help. Select two or three candidates, compare relevant dimensions together, and record the trade-off you accept. Benchmarks should lead to a controlled evaluation, not a promise about your eventual bill or every user’s response time.',
        example: 'A nightly summarizer and an interactive concierge may select different models even when both use the same source documents.',
      },
    ],
    lab: {
      title: 'Rehearse an interactive workload comparison',
      goal: 'Prioritize a metric and a cost assumption appropriate to a live assistant.',
      context: LOCAL_MODEL,
      fields: [
        {
          id: 'latency',
          label: 'First-response experience',
          hint: 'The user needs to see that the assistant has started responding.',
          options: [
            { value: 'time-to-first-token', label: 'Time to first token' },
            { value: 'context-size', label: 'Maximum context window alone' },
            { value: 'provider-count', label: 'Number of providers in the catalog' },
          ],
          expected: 'time-to-first-token',
          explanation: 'TTFT targets the initial wait in a streaming interaction. Context size describes capacity, not responsiveness.',
        },
        {
          id: 'cost',
          label: 'Estimate the workload’s cost',
          hint: 'Long conversation histories may differ from the leaderboard’s assumed token mix.',
          options: [
            { value: 'actual-token-mix', label: 'Use the expected input/output token mix' },
            { value: 'output-only', label: 'Count only generated output' },
            { value: 'benchmark-guarantee', label: 'Treat the benchmark estimate as a fixed bill' },
          ],
          expected: 'actual-token-mix',
          explanation: 'Inputs, history, and outputs affect usage. A comparison estimate is not a guaranteed charge for a different workload.',
        },
      ],
      template: '{\n  "learning_model": "interactive benchmark plan",\n  "initial_wait_metric": "{{latency}}",\n  "cost_basis": "{{cost}}"\n}',
      language: 'json',
      expectedOutput: 'SYNTHETIC EXPECTED RESULT: compare initial wait and workload-specific token cost, then validate candidates. No timings, prices, or usage have been measured.',
      takeaway: 'Changing the workload or token mix can reverse which benchmark trade-off is attractive.',
      azure: {
        title: 'Compare two models on the leaderboard',
        sourceId: 80,
        environment: 'foundry',
        minutes: 15,
        cost: CLOUD_COST,
        prerequisites: RESOURCE_PREREQUISITES,
        steps: [
          openProject(80),
          {
            title: 'Read an individual benchmark',
            surface: 'Microsoft Foundry',
            instruction: 'Open Discover > Models and one shortlisted model card. Select Benchmarks and read the description of one quality metric rather than copying only its number.',
            expected: 'You can identify what the metric measures and whether it resembles the application task.',
          },
          {
            title: 'Open the leaderboard',
            surface: 'Microsoft Foundry',
            instruction: 'Return to Models and select View leaderboard or Model leaderboard. Choose the closest available scenario leaderboard, then select two relevant models.',
            expected: 'The comparison is restricted to plausible candidates instead of the entire catalog.',
          },
          {
            title: 'Inspect safety definitions',
            surface: 'Microsoft Foundry',
            instruction: 'Use Compare models and inspect the available safety metrics. Read their definitions and note whether lower or higher values indicate the desired behavior.',
            expected: 'Your interpretation names a specific metric; it does not treat all safety scores as equivalent.',
          },
          {
            title: 'Change the trade-off dimension',
            surface: 'Microsoft Foundry',
            instruction: 'In the Trade-off chart, switch between cost and throughput. Inspect latency or TTFT details where available and check the axis labels before deciding which point is preferable.',
            expected: 'You can describe how the relative appeal of a model changes with the chosen dimension.',
          },
          {
            title: 'State what remains untested',
            surface: 'Microsoft Foundry',
            instruction: 'Review context limits and features in the side-by-side comparison. Pick the candidate to evaluate first and identify one question that public benchmarks cannot answer for your application. Stop before deployment here.',
            expected: 'You have a conditional choice and a concrete next evaluation, not an unsupported guarantee.',
          },
        ],
        success: 'You can explain a two-model trade-off using the meaning and direction of the metrics.',
        evidencePrompt: 'Record the metric names, their directions, and why your own workload may behave differently. Do not invent a price, latency, or score that you did not observe.',
        cleanup: CLEANUP,
      },
    },
    check: {
      question: 'An assistant’s answers are short, but its conversation history is long. The team quotes a leaderboard cost estimate as its expected production bill. What is the strongest correction?',
      options: [
        { id: 'a', text: 'The estimate must be exact because benchmarks standardize every application’s usage.', explanation: 'Standardization makes comparisons possible; it does not make the application’s token mix match the benchmark’s assumptions.' },
        { id: 'b', text: 'Recalculate using the application’s input and output usage, then validate with observed calls.', explanation: 'Long histories increase input usage. A workload-specific estimate and measurement address the assumption that makes the quoted figure unreliable.' },
        { id: 'c', text: 'Ignore input tokens because only newly generated text can be billed.', explanation: 'The corpus distinguishes input and output token costs. Ignoring the long history removes a major part of this workload.' },
      ],
      correctOptionId: 'b',
      explanation: 'A benchmark is a comparison under stated assumptions. This scenario changes those assumptions, so the team must adapt the estimate and measure actual usage.',
    },
    recall: {
      prompt: 'Explain the difference between time to first token, output throughput, and quality. Give one benchmark where a lower value is preferable.',
      answer: 'TTFT measures the initial wait for streamed output; output throughput measures generation rate; quality measures performance on a defined task or aggregate of tasks. Lower latency or lower attack success rate is preferable in its respective metric.',
    },
    transfer: 'How would you explain to a customer why the fastest model on an average-throughput chart may not provide the best interactive experience?',
    sourceNote: `Source 188 defines separate safety metrics and a particular estimated-cost token ratio. Episode 189 summarizes these more loosely; the specific metric definitions take precedence over a universal “safety score” rule. ${PORTAL_NOTE}`,
  },
  {
    id: 'model-deployment',
    title: 'Deploy within the right boundary',
    cluster: 'models-deploy-eval',
    examDomain: 'Plan and manage an Azure AI solution',
    summary: 'Choose processing scope and capacity deliberately, then verify the deployment an application will actually call.',
    minutes: 8,
    order: 20,
    prerequisites: ['model-catalog', 'model-benchmarks'],
    sourceIds: [39, 80, 189, 196],
    steps: [
      {
        title: 'A catalog entry is not an endpoint',
        body: 'Deployment makes a selected model available for application requests. The model card describes the model, while a deployment supplies a routable name and service configuration. In the corpus’s OpenAI-compatible examples, the model parameter uses the deployment name, which can differ from the catalog model name. The endpoint identifies where requests go, and authentication establishes who can call it. Verify the deployment status before interpreting a client failure as a prompt or model-quality problem.',
        example: 'A team names its GPT deployment “visitor-assistant-test.” The application must use that deployment name, not assume every project exposes the default model name.',
      },
      {
        title: 'Separate processing location from throughput',
        body: 'Deployment scope and capacity answer different questions. Global options permit processing across the supported global footprint; Data Zone options constrain processing to the selected zone; regional options constrain it to a region. Standard usage is pay-per-token, while provisioned deployments reserve throughput using PTUs. The corpus recommends Global Standard for general use, but residency requirements can rule it out. A resource’s location alone does not establish that a globally deployed model processes every request in that location.',
        example: 'If a customer requires processing within an EU data zone, a globally scoped deployment is not repaired merely by placing the resource in an EU region.',
      },
      {
        title: 'Choose an execution mode, then test it',
        body: 'Interactive and delayed workloads need different deployment choices. Batch supports asynchronous work that does not require an immediate reply; provisioned capacity targets predictable sustained throughput. Managed-compute deployments may require a VM SKU, instance count, and suitable compute quota. Developer deployments described in the corpus are for evaluating fine-tuned models, not a general production shortcut. Inspect what the selected model and region actually support, review any marketplace terms, and use the playground to verify a small request after deployment succeeds.',
        example: 'A nightly archive summarizer can tolerate batch completion, while a visitor waiting at a kiosk needs an interactive path.',
      },
    ],
    lab: {
      title: 'Rehearse residency and workload choices',
      goal: 'Plan an interactive EU-zone workload with usage-based billing.',
      context: LOCAL_MODEL,
      fields: [
        {
          id: 'scope',
          label: 'Processing scope',
          hint: 'The requirement permits supported regions inside the EU zone, not worldwide processing.',
          options: [
            { value: 'data-zone', label: 'Data Zone' },
            { value: 'global', label: 'Global' },
            { value: 'any-resource-location', label: 'Any scope if the resource is in Europe' },
          ],
          expected: 'data-zone',
          explanation: 'Data Zone scope addresses the stated processing boundary. Verify the exact current deployment guarantees before using it for compliance.',
        },
        {
          id: 'capacity',
          label: 'Execution and capacity',
          hint: 'This rehearsal assumes interactive, variable usage without a reservation requirement.',
          options: [
            { value: 'standard', label: 'Standard usage-based deployment' },
            { value: 'batch', label: 'Asynchronous batch deployment' },
            { value: 'developer', label: 'Developer fine-tuned evaluation deployment' },
          ],
          expected: 'standard',
          explanation: 'Standard fits the stated usage model. Batch changes response timing, and Developer is a specialized fine-tuning evaluation choice.',
        },
      ],
      template: '{\n  "learning_model": "deployment decision",\n  "processing_scope": "{{scope}}",\n  "capacity_mode": "{{capacity}}"\n}',
      language: 'json',
      expectedOutput: 'SYNTHETIC EXPECTED RESULT: consider a supported Data Zone Standard deployment and verify its guarantees. No deployment has been requested.',
      takeaway: 'Scope changes where processing can occur; capacity mode changes billing and execution behavior. Neither choice overrides model availability.',
      azure: {
        title: 'Deploy and verify one model',
        sourceId: 80,
        environment: 'foundry',
        minutes: 20,
        cost: CLOUD_COST,
        prerequisites: [...RESOURCE_PREREQUISITES, 'Quota and model access for one approved text model; acceptance of any required provider terms must be authorized.'],
        steps: [
          openProject(80),
          {
            title: 'Open the chosen model card',
            surface: 'Microsoft Foundry',
            instruction: 'Open Discover > Models and the model chosen for the exercise. Read its supported deployment types and any marketplace conditions before selecting Deploy.',
            expected: 'You can identify a supported deployment path for your actual subscription rather than assuming every listed type is available.',
          },
          {
            title: 'Review custom deployment settings',
            surface: 'Microsoft Foundry',
            instruction: 'In Deploy, open Custom settings where available. Choose a distinct lab deployment name, an approved processing scope, and the supported capacity option. Review quota and pricing; do not accept a global default if it violates your requirements.',
            expected: 'The pending configuration shows the intended name, deployment type, and quota allocation before submission.',
            hint: 'If your required scope is unavailable, stop and record the limitation; do not silently relax a residency requirement.',
          },
          {
            title: 'Submit and inspect status',
            surface: 'Microsoft Foundry',
            instruction: 'Select Deploy once the configuration is approved. Open Build > Models or Deployments and inspect the status of this specific deployment.',
            expected: 'A successful deployment should show Succeeded or the current equivalent ready state. An error must be resolved before testing.',
            hint: 'Check regional availability and quota for capacity errors. Repeated submissions can create unwanted duplicate deployments.',
          },
          {
            title: 'Test a bounded request',
            surface: 'Microsoft Foundry',
            instruction: 'Open the deployment’s Playground and ask for two considerations when writing a museum exhibit description. Confirm that the selected deployment is the one you just configured.',
            expected: 'A response or explicit service error is visible for that deployment. A plausible response alone is not a full quality evaluation.',
          },
          {
            title: 'Inspect the application connection',
            surface: 'Microsoft Foundry',
            instruction: 'Open Details and the playground Code view. Identify the endpoint type and the deployment name that a client would use. Record labels and the non-secret name only, then stop.',
            expected: 'The request’s model selection can be traced to your deployment; authentication secrets are not copied into evidence.',
          },
        ],
        success: 'One approved deployment is verified as ready and tested, or a specific availability/quota blocker is recorded without claiming completion.',
        evidencePrompt: 'Explain your chosen processing scope and capacity mode, the deployment’s actual status, and why its name matters to a client. Never include keys or tokens.',
        cleanup: CLEANUP,
      },
    },
    check: {
      question: 'An interactive application must keep processing within a single approved region. Its Azure resource is there, but its model uses Global Standard. What is the relevant design issue?',
      options: [
        { id: 'a', text: 'There is no issue because the resource location forces global processing into that region.', explanation: 'Resource location and deployment processing scope are not the same guarantee. A global type does not become regional because of the resource’s address.' },
        { id: 'b', text: 'Change to Global Provisioned so the PTU reservation creates regional residency.', explanation: 'Provisioning changes capacity planning, not the meaning of global scope. It does not address the stated boundary.' },
        { id: 'c', text: 'Select a supported regional deployment and verify its guarantees and capacity.', explanation: 'This directly addresses the processing boundary while recognizing that the model, region, and quota must support the required option.' },
        { id: 'd', text: 'Use Global Batch because asynchronous jobs remain beside the resource.', explanation: 'Batch changes execution timing; global scope still fails the single-region requirement and also conflicts with interactive use.' },
      ],
      correctOptionId: 'c',
      explanation: 'Treat residency and execution/capacity as separate dimensions. The requirement constrains deployment scope before convenience or default settings.',
    },
    recall: {
      prompt: 'Explain deployment name, endpoint, and authentication as three separate facts. Then distinguish Standard, Provisioned, and Batch.',
      answer: 'The deployment name selects the deployed model; the endpoint locates its API; authentication establishes the caller. Standard is usage-based, Provisioned reserves throughput, and Batch runs suitable work asynchronously. Processing scope must be checked separately.',
    },
    transfer: 'Which customer question would you ask before recommending Global Standard, and which evidence would you request before reserving provisioned throughput?',
    sourceNote: `Source 39 describes named scope/capacity combinations, while summary 196 also uses broader serverless and managed-compute terminology. They are not interchangeable menu labels for every model. Quotas, prices, and batch service commitments must be checked live rather than inferred from this lesson. ${PORTAL_NOTE}`,
  },
  {
    id: 'model-evaluation',
    title: 'Evaluate answers against the task',
    cluster: 'models-deploy-eval',
    examDomain: 'Implement generative AI and agentic solutions',
    summary: 'Build an evaluation that separates relevance, groundedness, language quality, and safety, then inspect individual failures.',
    minutes: 9,
    order: 25,
    prerequisites: ['model-deployment'],
    sourceIds: [53, 80, 145, 196, 189],
    steps: [
      {
        title: 'A good demonstration is not a baseline',
        body: 'One impressive response shows only what happened for one prompt. Evaluation asks whether a system meets requirements across representative inputs, edge cases, and relevant safety cases. Start with a small manual review to define what a useful answer looks like, then automate repeatable tests. Keep the test set and instructions stable when comparing changes. Include realistic failures, not just examples the model already handles well, and reserve human review for judgments that a score cannot settle reliably.',
        example: 'An exhibit assistant should answer ordinary opening-hour questions and admit when a supplied policy does not state an answer.',
      },
      {
        title: 'Measure the failure you actually care about',
        body: 'Groundedness asks whether an answer is supported by supplied context; relevance asks whether it addresses the request. Coherence concerns logical flow, while fluency concerns natural language quality. These can diverge: a fluent answer may invent an admission fee. AI-assisted evaluators use a model to score selected criteria, and safety evaluators can aggregate threshold breaches into defect rates. An evaluator is another assessment mechanism with definitions and limitations, not an authority that makes an answer true.',
        example: 'A grammatically perfect answer describing tomorrow’s exhibition fails groundedness if the provided schedule only covers today.',
      },
      {
        title: 'Know when a reference answer helps',
        body: 'Reference-based NLP metrics compare generated text with expected text. F1 measures word overlap with a precision/recall balance; BLEU compares word sequences; METEOR allows more flexible matching; ROUGE emphasizes coverage, which is useful for summaries. GLEU is another sentence-level comparison. These metrics can be helpful when a suitable reference exists, but alternative correct wording can complicate interpretation. Human review remains important for usefulness, tone, and context-sensitive judgments that a lexical comparison may miss.',
        example: 'Two accurate accessibility summaries may use different words, so a low overlap score warrants inspection rather than automatic rejection.',
      },
      {
        title: 'Follow the evaluation data through the system',
        body: 'Foundry evaluations can target a model, an agent, or a dataset of existing outputs. A model target generates responses from test inputs; a dataset target assesses responses already present. Upload or reuse CSV/JSONL test data, or generate synthetic inputs and review them before trusting coverage. Configure the prompt, evaluator criteria, and required field mappings. After the asynchronous run finishes, inspect both aggregate scores and individual rows, then change one relevant factor and rerun the same baseline.',
        example: 'If a safe refusal is scored as unhelpful, review the rubric and expected behavior before weakening the assistant’s safety rules.',
      },
    ],
    lab: {
      title: 'Rehearse a groundedness evaluation',
      goal: 'Evaluate saved answers against the context supplied when they were produced.',
      context: LOCAL_MODEL,
      fields: [
        {
          id: 'target',
          label: 'Evaluation target',
          hint: 'You already have the inputs and generated responses.',
          options: [
            { value: 'dataset', label: 'Dataset with existing outputs' },
            { value: 'new-model-run', label: 'Generate different outputs from a model' },
            { value: 'catalog-ranking', label: 'Use a public catalog ranking only' },
          ],
          expected: 'dataset',
          explanation: 'A dataset evaluation assesses the actual saved responses. Generating new outputs would answer a different comparison question.',
        },
        {
          id: 'criterion',
          label: 'Unsupported factual claims',
          hint: 'The problem is invented facts, not awkward grammar.',
          options: [
            { value: 'groundedness', label: 'Groundedness against supplied context' },
            { value: 'fluency-only', label: 'Fluency alone' },
            { value: 'throughput-only', label: 'Throughput alone' },
          ],
          expected: 'groundedness',
          explanation: 'Groundedness targets support in context. Fluent or fast output can still contain unsupported claims.',
        },
      ],
      template: '{\n  "learning_model": "evaluation design",\n  "target": "{{target}}",\n  "primary_criterion": "{{criterion}}"\n}',
      language: 'json',
      expectedOutput: 'SYNTHETIC EXPECTED RESULT: assess saved responses with their supplied context and review unsupported claims. No dataset was uploaded or scored.',
      takeaway: 'Target selection determines which outputs you assess; metric selection determines which failure you can see.',
      azure: {
        title: 'Run a small model evaluation',
        sourceId: 80,
        environment: 'foundry',
        minutes: 35,
        cost: `${CLOUD_COST} Synthetic data generation and evaluator-model calls can add usage beyond the model being evaluated.`,
        prerequisites: [...RESOURCE_PREREQUISITES, 'A ready lab model deployment and access to the evaluator or data-generation deployment required by the current exercise.'],
        steps: [
          openProject(80),
          {
            title: 'Establish a manual baseline',
            surface: 'Microsoft Foundry',
            instruction: 'Open your deployed model in the Model playground. Use the exercise’s model-testing section with two ordinary prompts and one ambiguity case. Record what a relevant, accurate, appropriately limited answer should do.',
            expected: 'You have a small manual baseline and a rubric before submitting an automated evaluation.',
          },
          {
            title: 'Choose the evaluation target',
            surface: 'Microsoft Foundry',
            instruction: 'Open the playground Evaluations tab or project Evaluation page and select Create. Choose Model and select only the intended deployment.',
            expected: 'The evaluation target is the model you tested, not an unrelated deployment or agent.',
          },
          {
            title: 'Prepare the evaluation data',
            surface: 'Microsoft Foundry',
            instruction: 'Use the exercise’s Synthetic generation flow, or upload a small non-sensitive CSV/JSONL dataset. Review the generated cases for relevance and confirm any required field mappings; synthetic cases do not guarantee real-user coverage.',
            expected: 'The evaluation has usable inputs that fit the intended task and its selected criteria.',
            hint: 'Groundedness needs appropriate context; reference-based metrics need their required reference fields. Do not invent mappings to satisfy the wizard.',
          },
          {
            title: 'Configure a meaningful rubric',
            surface: 'Microsoft Foundry',
            instruction: 'Set the same developer or system instructions used in your baseline. Select supported quality criteria such as relevance and fluency; inspect evaluator definitions. Use safety criteria in a separate or combined run where supported and relevant, not as a substitute for a harm review.',
            expected: 'The criteria match your target and available data, rather than including agent-only metrics for a simple model response.',
          },
          {
            title: 'Submit and inspect individual results',
            surface: 'Microsoft Foundry',
            instruction: 'Review, name, and submit the evaluation. Wait for its actual completion state, then open the result rows as well as aggregate scores. If Analyze results is available, compare its suggestions with the underlying input, output, and rubric.',
            expected: 'You can explain one observed result or a specific job failure. Passing averages do not erase individual failures.',
          },
          {
            title: 'Choose one justified next change',
            surface: 'Microsoft Foundry',
            instruction: 'Identify one prompt, model, data, or rubric change supported by the results. Preserve the baseline configuration and stop here, or rerun the same cases after an approved change.',
            expected: 'Your next experiment has a causal reason and a comparable baseline, rather than an untested claim of improvement.',
          },
        ],
        success: 'A completed evaluation is reviewed at row level and one next action is justified, or a concrete data/access/job blocker is recorded.',
        evidencePrompt: 'Describe one actual response, the criterion applied, and why you agree or disagree with its score. Do not include secrets, personal data, or invented metrics.',
        cleanup: CLEANUP,
      },
    },
    check: {
      question: 'An assistant writes clear, relevant answers but adds facts absent from the provided maintenance manual. Which evaluation best targets the reported failure?',
      options: [
        { id: 'a', text: 'Fluency alone, because clear grammar establishes factual reliability.', explanation: 'Fluency can be high even when a statement is fabricated. It measures language quality, not evidential support.' },
        { id: 'b', text: 'Groundedness using the actual manual context, with review of failing rows.', explanation: 'This checks whether the answers are supported by the context and exposes the particular unsupported claims for diagnosis.' },
        { id: 'c', text: 'Output throughput, because faster answers leave less room for errors.', explanation: 'Generation rate is not an evidence check. A model can quickly produce unsupported facts.' },
        { id: 'd', text: 'The catalog’s general quality index without testing the manual.', explanation: 'A public aggregate does not assess this application’s context or outputs. It cannot locate the reported failure.' },
      ],
      correctOptionId: 'b',
      explanation: 'Evaluate the failure at its causal boundary: generated claims versus the evidence actually supplied to the model.',
    },
    recall: {
      prompt: 'Explain groundedness, relevance, and fluency using three different failure examples. When would you choose a dataset target instead of a model target?',
      answer: 'Unsupported facts fail groundedness; an answer about a different question fails relevance; unnatural grammar fails fluency. Choose a dataset target when assessing outputs already captured, and a model target when the evaluation should generate responses from the test inputs.',
    },
    transfer: 'A customer wants a single “accuracy score” before release. How would you propose a compact evaluation that still distinguishes factual support, usefulness, and harm?',
    sourceNote: `Episode 189 uses “training” informally during its evaluation demo; evaluation scores outputs and does not train model weights. The linked live exercise’s sample criterion selection differs from the episode, so select criteria for your actual target and data. ${PORTAL_NOTE}`,
  },
  {
    id: 'foundry-sdk-connections',
    title: 'Connect the right client and endpoint',
    cluster: 'models-deploy-eval',
    examDomain: 'Implement generative AI and agentic solutions',
    summary: 'Separate project operations from model inference, and carry the portal’s verified configuration into an authenticated client.',
    minutes: 8,
    order: 30,
    prerequisites: ['model-deployment'],
    sourceIds: [18, 90, 120, 62, 148, 214, 42],
    steps: [
      {
        title: 'Choose the address for the responsibility',
        body: 'An endpoint is an API address, not a model’s name or an authentication method. The Foundry project endpoint exposes project-native capabilities such as connections, configuration, tracing, and agents. The Azure OpenAI endpoint supports compatible inference APIs for deployed models. A client needing project metadata has a different responsibility from a client that only requests an answer. Decide which operation you need before copying an address from Home; an otherwise valid credential cannot make the wrong endpoint implement that operation.',
        example: 'A deployment-checking service needs project information; a simple exhibit-description generator may only need OpenAI-compatible inference.',
      },
      {
        title: 'Understand the client objects and identity',
        body: 'In Python, the Foundry SDK uses the azure-ai-projects package and an AIProjectClient for project operations. Its get_openai_client method provides an OpenAI-compatible client for supported inference. The OpenAI SDK can also create a client directly for the Azure OpenAI v1 endpoint. Microsoft Entra ID authenticates a user or application identity; DefaultAzureCredential can obtain credentials from supported environments. Authentication and authorization remain separate: being signed in does not itself grant the required access to a resource.',
        example: 'An engineer’s local sign-in and a deployed app’s managed identity can use identity-based access without putting a long-lived API key into source code.',
      },
      {
        title: 'Promote tested settings rather than guessing',
        body: 'The Model playground lets you test a deployment before integration. Its Code view offers API, language, and SDK choices and shows the current endpoint, deployment name, and supported settings. Carry those values into the exercise’s local configuration, keeping secrets out of code and evidence. A direct OpenAI client and a version-specific AzureOpenAI client are not interchangeable by name alone; the latter can require an API version. Diagnose endpoint, identity, permissions, and deployment selection before rewriting a prompt to fix a connection error.',
        example: 'If the portal test works but the client cannot find the deployment, first compare the client’s endpoint and deployment name with the portal.',
      },
    ],
    lab: {
      title: 'Rehearse an inference connection',
      goal: 'Configure a direct inference client without confusing it with project management.',
      context: LOCAL_MODEL,
      fields: [
        {
          id: 'endpoint',
          label: 'Direct OpenAI-compatible inference',
          hint: 'This rehearsal matches the linked chat exercise’s direct OpenAI SDK path.',
          options: [
            { value: 'azure-openai-v1', label: 'The project’s Azure OpenAI v1 endpoint' },
            { value: 'resource-group-page', label: 'The Azure resource-group web page' },
            { value: 'tools-endpoint', label: 'An unrelated Foundry Tools endpoint' },
          ],
          expected: 'azure-openai-v1',
          explanation: 'The exercise calls a compatible deployed model through the Azure OpenAI endpoint. A portal page is not an inference API.',
        },
        {
          id: 'identity',
          label: 'Authentication plan',
          hint: 'The account or workload identity must also have permission for the resource.',
          options: [
            { value: 'entra-identity', label: 'Microsoft Entra ID with authorized identity' },
            { value: 'key-in-source', label: 'Paste a key into source code' },
            { value: 'endpoint-only', label: 'Send only the endpoint URL' },
          ],
          expected: 'entra-identity',
          explanation: 'Identity-based access avoids embedding a long-lived secret. An endpoint alone does not establish an authorized caller.',
        },
      ],
      template: '{\n  "learning_model": "inference connection plan",\n  "endpoint_kind": "{{endpoint}}",\n  "authentication": "{{identity}}"\n}',
      language: 'json',
      expectedOutput: 'SYNTHETIC EXPECTED TRACE: select inference endpoint → obtain authorized identity → select recorded deployment → request a response. No authentication or network call occurred.',
      takeaway: 'Changing endpoint type changes available operations; changing identity changes who is requesting access, not which API exists.',
      azure: {
        title: 'Connect the published chat client',
        sourceId: 62,
        environment: 'portal-and-code',
        minutes: 35,
        cost: CLOUD_COST,
        prerequisites: [...CODE_PREREQUISITES, 'A supported text-model deployment and an Azure identity authorized to invoke it.'],
        steps: [
          openProject(62),
          {
            title: 'Verify the cloud deployment first',
            surface: 'Microsoft Foundry',
            instruction: 'Open Build > Models or Deployments and your ready text-model deployment. If none exists, follow the exercise’s Discover > Models deployment section. Send one non-sensitive prompt in its Model playground.',
            expected: 'The selected deployment is ready and the playground either returns a response or exposes a specific service error to fix first.',
          },
          {
            title: 'Locate the correct endpoint',
            surface: 'Microsoft Foundry',
            instruction: 'Return to Home and identify the Azure OpenAI endpoint used by the linked exercise. Distinguish it from the project endpoint and note the actual model deployment name. Do not copy an API key into notes.',
            expected: 'You know the endpoint kind and exact deployment name needed by the direct inference client.',
          },
          {
            title: 'Inspect the generated client pattern',
            surface: 'Microsoft Foundry',
            instruction: 'Open the deployment’s Playground > Code view. Inspect the offered API, Python, and SDK selections. Identify the direct OpenAI client pattern; compare a Foundry SDK option if the current view offers one.',
            expected: 'The sample’s client and endpoint match its operation. An unavailable SDK selector is noted, not replaced with an invented portal feature.',
          },
          {
            title: 'Prepare the supplied application',
            surface: 'Cloud Shell / local editor',
            instruction: 'Follow Exercise 62’s linked application setup in Visual Studio Code: clone the named MicrosoftLearning repository, open its foundry-chat Python chat-app folder, create the prescribed environment, and install its requirements. Put the endpoint and deployment name into its local configuration and follow the supplied Entra ID sign-in instructions.',
            expected: 'The application has its own environment, configuration, and authorized session. Nothing secret is committed or pasted into this site.',
            hint: 'Use the exercise’s current authentication scope and package versions together; do not combine snippets from different SDK generations.',
          },
          {
            title: 'Run one actual request',
            surface: 'Cloud Shell / local editor',
            instruction: 'Complete and run the exercise’s initial chat client using its supplied code. Ask for a short explanation of a non-sensitive topic, inspect the returned text or error, and quit the process.',
            expected: 'An actual client response demonstrates the connection. If it fails, identify whether the evidence points to endpoint, deployment name, identity, or permissions.',
            hint: 'A signed-in terminal can still lack resource access. Compare the active tenant and subscription with the Foundry project.',
          },
        ],
        success: 'A supplied client reaches your verified deployment, or you can isolate a concrete connection blocker without claiming that the call succeeded.',
        evidencePrompt: 'Explain the endpoint/client pairing and the role of the authenticated identity. Record the observed outcome in your own words, never keys, tokens, or the contents of a secret configuration file.',
        cleanup: CLEANUP,
      },
    },
    check: {
      question: 'A service must read Foundry project connections and also request model answers. Which client design respects those two responsibilities?',
      options: [
        { id: 'a', text: 'Use only an Images API client because every Foundry operation is an inference request.', explanation: 'Project connections are project-native management/configuration capabilities, not image inference operations.' },
        { id: 'b', text: 'Use the project client for project operations and an appropriate OpenAI-compatible client for inference.', explanation: 'This separates the project API from model inference. The Foundry project client can provide an inference client where supported.' },
        { id: 'c', text: 'Send project-management requests to any model deployment name.', explanation: 'A deployment name routes inference to a model; it does not expose the project management API.' },
        { id: 'd', text: 'Put an API key in the URL so the SDK distinction no longer matters.', explanation: 'A secret does not change an endpoint’s contract and should not be placed in URLs or code as a workaround.' },
      ],
      correctOptionId: 'b',
      explanation: 'Choose the client and endpoint by the operation, then configure an authorized identity. Compatibility for inference does not imply support for every Foundry feature.',
    },
    recall: {
      prompt: 'Explain the jobs of AIProjectClient, an OpenAI-compatible inference client, a deployment name, and an authenticated identity.',
      answer: 'AIProjectClient accesses project-native operations. The inference client submits model requests. The deployment name selects the model deployment. The authenticated identity identifies the caller, whose permissions determine whether the operation is allowed.',
    },
    transfer: 'When a customer says “the SDK is broken,” which four non-secret facts would you collect before changing libraries?',
    sourceNote: `Sources 18 and 214 broadly discuss both SDKs, but endpoint support remains operation-specific. Source 62’s introduction calls this a Foundry SDK exercise; its linked published exercise currently uses the OpenAI SDK and Azure OpenAI endpoint. This walkthrough follows that explicit exercise path without conflating it with project management. ${PORTAL_NOTE}`,
  },
  {
    id: 'chat-responses',
    title: 'Keep chat context deliberately',
    cluster: 'models-deploy-eval',
    examDomain: 'Implement generative AI and agentic solutions',
    summary: 'Compare ChatCompletions with Responses, budget conversation context, and distinguish streaming from asynchronous execution.',
    minutes: 9,
    order: 35,
    prerequisites: ['foundry-sdk-connections'],
    sourceIds: [98, 99, 90, 62, 42, 148, 214],
    steps: [
      {
        title: 'A second request is not automatically a conversation',
        body: 'A model does not permanently learn your conversation because it answered once. A follow-up such as “Which of those is quieter?” needs the earlier alternatives in its active context. Instructions describe how to respond; user input supplies the current request; prior user and assistant messages supply continuity. If the application omits that continuity, changing the wording of the follow-up is not a durable state-management design. Decide explicitly how a conversation begins, continues, and ends.',
        example: 'After discussing two exhibition rooms, an assistant cannot reliably resolve “the smaller one” if the next request contains no earlier room information.',
      },
      {
        title: 'Compare the two state-management contracts',
        body: 'ChatCompletions is widely supported and represents the request as messages with roles. The application retains relevant user and assistant turns and sends them again to continue the conversation. Responses supports linking requests with previous_response_id and returns an id and convenient output_text, alongside status and usage. Its instructions parameter supplies behavior guidance separately. The corpus recommends Responses for new compatible development, but ChatCompletions remains useful for model support and existing applications. Neither API choice removes the need to manage conversation boundaries.',
        example: 'A new Responses client stores the returned response ID for the next turn; a compatible ChatCompletions client appends the actual assistant reply to its message history.',
      },
      {
        title: 'State tracking does not remove the context budget',
        body: 'A response ID is a reference to history, not proof that history is free. System instructions, current input, previous messages, retrieved documents, tool descriptions, and tool results can all contribute to the context sent to a model. Keep behavior instructions explicit and watch usage. max_output_tokens limits generated output, not the entire conversation. Temperature and top_p affect sampling where supported; they do not create memory. Prune, summarize, or restart conversations deliberately rather than allowing unrelated sessions to accumulate indefinitely.',
        example: 'A museum assistant starts a new visitor session instead of carrying the previous visitor’s questions into a new person’s context.',
      },
      {
        title: 'Improve responsiveness without confusing mechanisms',
        body: 'Streaming delivers pieces of generated output as they arrive, reducing the wait before a user sees text. An asynchronous client lets other application work continue while a request is pending. These solve different problems and can be combined. When streaming with Responses, retain the completed response’s ID for the next turn rather than treating a text fragment as a finished response. Handle failures and incomplete results honestly; a responsive interface is not evidence that the answer is correct or complete.',
        example: 'A kiosk can display a long explanation progressively while its application continues other I/O, then save the completed response ID for a follow-up.',
      },
    ],
    lab: {
      title: 'Rehearse conversation continuity',
      goal: 'Keep a compatible Responses conversation coherent without assuming its history is cost-free.',
      context: LOCAL_MODEL,
      fields: [
        {
          id: 'history',
          label: 'Connect the next Responses turn',
          hint: 'Use the identifier returned by the completed previous response.',
          options: [
            { value: 'previous-response-id', label: 'Link the previous response ID' },
            { value: 'model-name-only', label: 'Repeat only the model name' },
            { value: 'higher-temperature', label: 'Raise temperature to restore memory' },
          ],
          expected: 'previous-response-id',
          explanation: 'The ID links the stored response context. A model name selects the deployment, and temperature changes sampling, not state.',
        },
        {
          id: 'budget',
          label: 'Budget the conversation',
          hint: 'A compact client payload can still represent extensive model context.',
          options: [
            { value: 'track-context-and-usage', label: 'Track context size and usage across turns' },
            { value: 'id-means-free', label: 'Assume an ID makes all history free' },
            { value: 'output-cap-only', label: 'Use only the output token cap as the total budget' },
          ],
          expected: 'track-context-and-usage',
          explanation: 'History and other context still affect token usage. An output cap does not limit all input or remembered context.',
        },
      ],
      template: '{\n  "learning_model": "conversation plan",\n  "next_turn_context": "{{history}}",\n  "budget_policy": "{{budget}}"\n}',
      language: 'json',
      expectedOutput: 'SYNTHETIC EXPECTED TRACE: first turn → completed response ID → linked follow-up → inspect usage → explicit session boundary. No conversation is stored or sent.',
      takeaway: 'Changing the state mechanism changes how context is supplied, not whether the model processes context.',
      azure: {
        title: 'Compare chat APIs with the supplied client',
        sourceId: 62,
        environment: 'portal-and-code',
        minutes: 40,
        cost: CLOUD_COST,
        prerequisites: [...CODE_PREREQUISITES, 'The initial Exercise 62 chat client and a deployment supporting its selected APIs.'],
        steps: [
          openProject(62),
          {
            title: 'Verify the selected API support',
            surface: 'Microsoft Foundry',
            instruction: 'Open your model deployment’s Playground > Code view. Confirm the available ChatCompletions and Responses options for your model and endpoint. Check the Home endpoint and deployment name against the local exercise configuration.',
            expected: 'The client experiment uses a supported API/endpoint pairing rather than assuming all models implement both APIs.',
          },
          {
            title: 'Observe an unlinked follow-up',
            surface: 'Cloud Shell / local editor',
            instruction: 'Run the exercise’s initial single-turn client. Ask about two fictional exhibition rooms, then ask “Which of those is smaller?” Observe whether the second request actually includes the first exchange. Use the exercise’s unlinked Responses version for the same inspection.',
            expected: 'The request structure reveals that a new API name alone does not retain the earlier exchange; responses may vary.',
          },
          {
            title: 'Add explicit continuity',
            surface: 'Cloud Shell / local editor',
            instruction: 'Follow the exercise’s Add conversation tracking section. Keep the returned response ID and pass it into the next request, with the behavior instructions still explicit. Save the file and repeat the same two-turn test.',
            expected: 'The follow-up request links the previous response. Compare observed reference resolution without assuming identical generated wording.',
            hint: 'If behavior does not change, confirm the edited file was saved and the same response ID variable is updated after each completed response.',
          },
          {
            title: 'Watch streamed output',
            surface: 'Cloud Shell / local editor',
            instruction: 'Follow the published streaming section. Run the client and observe incremental text plus its completed event handling. Check that the next turn uses the completed response’s ID.',
            expected: 'Output can arrive in pieces while conversation linking still depends on completion, not on the first text fragment.',
          },
          {
            title: 'Compare asynchronous execution',
            surface: 'Cloud Shell / local editor',
            instruction: 'Open the exercise’s separate asynchronous client file and follow its implementation and cleanup instructions. Run the same short exchange and identify where waiting yields to other I/O.',
            expected: 'The asynchronous implementation changes how waiting is handled; it does not by itself add memory or improve factual quality.',
          },
          {
            title: 'Inspect usage and end the session',
            surface: 'Cloud Shell / local editor',
            instruction: 'Inspect the response metadata and usage where exposed by the supplied example or debugger. Note that history can contribute to context. Quit the client and confirm that a deliberately fresh session starts without the previous conversation reference.',
            expected: 'You can explain both continuity and reset behavior, and distinguish observed usage from any synthetic local rehearsal.',
          },
        ],
        success: 'The request structure and actual observations demonstrate unlinked versus linked turns, with streaming and async responsibilities distinguished.',
        evidencePrompt: 'Explain what changed between the two requests and what did not change. Record a non-sensitive observation about continuity or usage; never record authentication tokens or another user’s conversation.',
        cleanup: CLEANUP,
      },
    },
    check: {
      question: 'A Responses client sends a new input each turn but never supplies history or a previous response ID. It loses track of “the second option.” Which repair addresses the cause?',
      options: [
        { id: 'a', text: 'Increase max_output_tokens so the model has more memory of earlier requests.', explanation: 'This parameter limits output generation. It cannot supply an earlier exchange that the request omitted.' },
        { id: 'b', text: 'Enable streaming, which automatically joins separate conversations.', explanation: 'Streaming changes response delivery, not the continuity contract between requests.' },
        { id: 'c', text: 'Link the previous completed response or explicitly supply the relevant conversation history.', explanation: 'Both approaches restore the missing context needed to resolve a reference to earlier options.' },
        { id: 'd', text: 'Switch to an asynchronous client without changing request content.', explanation: 'Async changes waiting behavior. The same context-free request remains context-free.' },
      ],
      correctOptionId: 'c',
      explanation: 'The failure is missing context, not slow delivery or short output. Repair the state boundary and continue to monitor the context budget.',
    },
    recall: {
      prompt: 'From memory, compare how ChatCompletions and Responses can continue a conversation. Why can a short response-ID request still consume substantial tokens?',
      answer: 'ChatCompletions resends a relevant role-tagged message history maintained by the application. Responses can link a prior response with previous_response_id, or use manually supplied history. The referenced history and other active context still reach the model, so a small network payload does not make context processing free.',
    },
    transfer: 'What privacy and cost questions would you ask before a customer keeps every support conversation indefinitely and resumes it automatically?',
    sourceNote: `Episode 42 describes the client payload as effectively constant when using a response ID. Source 99 explicitly says accumulated context can still increase token usage; the payload statement is not a billing or context-window guarantee. API availability varies by model. ${PORTAL_NOTE}`,
  },
  {
    id: 'prompt-engineering',
    title: 'Shape prompts with controlled experiments',
    cluster: 'models-deploy-eval',
    examDomain: 'Implement generative AI and agentic solutions',
    summary: 'Improve instructions, examples, and supported generation settings before adding training or retrieval infrastructure.',
    minutes: 7,
    order: 40,
    prerequisites: ['chat-responses', 'model-evaluation'],
    sourceIds: [116, 178, 161, 77, 177],
    steps: [
      {
        title: 'Separate a behavior problem from missing facts',
        body: 'If the assistant understands the question but writes an unsuitable answer, first inspect the instructions. A useful system message states the role, boundaries, expected output, and what to do when information is missing. The user message supplies the current task, while previous assistant messages support continuity. Clear instructions guide generation but do not guarantee compliance or permanently train the model. If a museum’s private opening schedule is absent, merely asking the model to be accurate cannot supply it.',
        example: 'For exhibit captions, ask for two short sentences and an explicit uncertainty statement when the supplied label does not identify the artist.',
      },
      {
        title: 'Show the pattern and mark the boundaries',
        body: 'Few-shot examples demonstrate the input/output relationship you want; a format template makes the requested structure concrete. Delimit instructions, source material, and examples so those sections are easier to distinguish. For a complex task, split observable work into smaller stages, such as extracting named facts before writing a summary. Ask for a concise justification or verifiable intermediate artifact when useful, not private hidden reasoning. Test an unseen case so the examples teach a pattern rather than merely a copied answer.',
        example: 'Show one fictional exhibit label and a two-sentence caption, then test a different label with a missing date to see whether uncertainty is handled correctly.',
      },
      {
        title: 'Change one generation control at a time',
        body: 'Where a model supports them, temperature and top_p both influence sampling, but in different ways. Lower temperature usually narrows variation; top_p restricts the probability mass considered for the next token. Adjust one rather than both when diagnosing an effect. The output token limit controls maximum generated length, not truthfulness or knowledge. Compare the same cases before and after a change, holding other settings stable. A lower temperature does not make unsupported facts correct or guarantee identical responses.',
        example: 'Keep the caption instructions and test labels fixed, vary only a supported sampling setting, and compare format compliance and unsupported claims separately.',
      },
    ],
    lab: {
      title: 'Rehearse a caption experiment',
      goal: 'Choose a useful instruction design and a controlled sampling experiment.',
      context: LOCAL_MODEL,
      fields: [
        {
          id: 'instructions',
          label: 'Instruction strategy',
          hint: 'The assistant should write concise captions and admit missing source details.',
          options: [
            { value: 'role-format-uncertainty', label: 'Role, format, boundaries, and uncertainty rule' },
            { value: 'be-brilliant', label: 'Only “be brilliant and accurate”' },
            { value: 'facts-by-temperature', label: 'Use temperature instead of supplying facts' },
          ],
          expected: 'role-format-uncertainty',
          explanation: 'Concrete behavioral requirements are testable. Vague praise and sampling controls do not define the required response.',
        },
        {
          id: 'experiment',
          label: 'Sampling experiment',
          hint: 'You need to explain which change produced the observed difference.',
          options: [
            { value: 'one-supported-control', label: 'Change one supported control, keep the test set fixed' },
            { value: 'change-everything', label: 'Change model, prompt, and both sampling controls together' },
            { value: 'one-good-answer', label: 'Accept the first good-looking answer as proof' },
          ],
          expected: 'one-supported-control',
          explanation: 'A controlled comparison makes the result interpretable. Many simultaneous changes obscure the cause.',
        },
      ],
      template: '{\n  "learning_model": "caption prompt experiment",\n  "instruction_design": "{{instructions}}",\n  "comparison_method": "{{experiment}}"\n}',
      language: 'json',
      expectedOutput: 'SYNTHETIC EXPECTED RESULT: a testable caption instruction and a one-variable comparison plan. No model has generated or improved a caption.',
      takeaway: 'Instruction content changes the requested behavior; sampling changes variation within supported generation behavior.',
      azure: {
        title: 'Improve the base-model prompt in Foundry',
        sourceId: 77,
        environment: 'foundry',
        minutes: 20,
        cost: CLOUD_COST,
        prerequisites: [...RESOURCE_PREREQUISITES, 'A ready text-model deployment; this bounded walkthrough does not require starting a fine-tuning job.'],
        steps: [
          openProject(77),
          {
            title: 'Open the base-model playground',
            surface: 'Microsoft Foundry',
            instruction: 'Follow the linked exercise’s base-model setup and chat section. Open Build > Models or Deployments and the base model’s playground. Reuse an approved compatible deployment where possible.',
            expected: 'You are testing an ordinary base-model deployment, not a fine-tuned model or a training job.',
          },
          {
            title: 'Capture an initial result',
            surface: 'Microsoft Foundry',
            instruction: 'Supply a fictional exhibit label: “Title: River Study. Medium: watercolor. Artist: not recorded.” Ask for a concise visitor caption. Record whether the output invents an artist or other facts.',
            expected: 'You have an actual baseline response for a fixed, non-sensitive input.',
          },
          {
            title: 'Add precise behavior instructions',
            surface: 'Microsoft Foundry',
            instruction: 'In Instructions, define an exhibit-caption assistant that uses only the supplied label, writes two short sentences, and states when information is missing. Add one original, fictional input/output example and clearly delimit it from the next label.',
            expected: 'The instructions now describe a role, boundary, output form, and uncertainty behavior that can be evaluated.',
          },
          {
            title: 'Test the pattern on a different label',
            surface: 'Microsoft Foundry',
            instruction: 'Start a fresh chat and test a different fictional label with a missing year. Compare format and unsupported details with the baseline. Ask for a concise explanation of any uncertainty, not hidden reasoning.',
            expected: 'An unseen case reveals whether the instruction pattern generalizes beyond its example; compliance is observed rather than assumed.',
          },
          {
            title: 'Try one supported control and stop',
            surface: 'Microsoft Foundry',
            instruction: 'Inspect the model settings. If temperature is supported, vary it while leaving top_p and the test inputs unchanged. Inspect the output limit separately for truncation. If the model does not expose sampling controls, record that limit and keep the prompt-only comparison. Do not start the exercise’s fine-tuning stage yet.',
            expected: 'The comparison isolates one change and records any model-specific unsupported parameter. The base prompt is a resumable stopping point.',
          },
        ],
        success: 'A baseline and a controlled prompt revision are compared on original non-sensitive examples, including a case with missing information.',
        evidencePrompt: 'Explain one observed improvement or remaining failure, naming the instruction or parameter you changed. Do not claim a single successful answer proves durable reliability.',
        cleanup: CLEANUP,
      },
    },
    check: {
      question: 'A caption assistant produces the right facts but ignores the requested format. The team has not tried explicit examples or a format template. What is the most proportionate next experiment?',
      options: [
        { id: 'a', text: 'Train a custom model immediately, without establishing a baseline.', explanation: 'Training adds cost and complexity before testing the simpler behavior controls or preserving a comparison baseline.' },
        { id: 'b', text: 'Add a clear format template and representative examples, then retest fixed cases.', explanation: 'This targets the behavior failure directly and tests whether prompting is sufficient before escalating.' },
        { id: 'c', text: 'Build a search index solely to change sentence structure.', explanation: 'Retrieval supplies factual context. The stated problem is format, not missing domain data.' },
        { id: 'd', text: 'Raise every generation setting and judge one response.', explanation: 'Multiple changes obscure the cause, and one response is weak evidence of consistency.' },
      ],
      correctOptionId: 'b',
      explanation: 'Start with a low-complexity intervention matched to the observed failure, and compare against a stable baseline.',
    },
    recall: {
      prompt: 'Reconstruct a useful system instruction from four ingredients. What does lowering temperature not guarantee?',
      answer: 'Specify the role/outcome, boundaries, output format, and what to do when uncertain; examples can demonstrate the desired pattern. Lower temperature can reduce variation where supported, but it does not add missing facts, ensure safety, or guarantee identical correct answers.',
    },
    transfer: 'How would you persuade a customer to test a prompt revision before funding fine-tuning, without promising that prompting will solve every consistency problem?',
    sourceNote: `Source 178 describes chain-of-thought prompting. This curriculum uses task decomposition, concise justifications, and observable artifacts instead of requesting private reasoning, as required by the constitution. Source 77’s base-model comparison is reused here; training is deliberately deferred. ${PORTAL_NOTE}`,
  },
  {
    id: 'grounding-optimization',
    title: 'Ground answers without retraining',
    cluster: 'models-deploy-eval',
    examDomain: 'Implement generative AI and agentic solutions',
    summary: 'Trace retrieval, context augmentation, and generation, and choose the right intervention for missing knowledge versus inconsistent behavior.',
    minutes: 8,
    order: 45,
    prerequisites: ['prompt-engineering'],
    sourceIds: [19, 102, 202, 177, 62],
    steps: [
      {
        title: 'Instructions cannot conjure private knowledge',
        body: 'A base model’s training does not include every organization’s current inventory, policy, or schedule. When that information is missing, stronger wording alone cannot make it available. Grounding supplies relevant evidence with the question so the answer can be based on that evidence. Even then, the application must check whether the answer is supported. A confident tone is not a substitute for a current source, and a correct answer once is not proof that the system has permanent access to the data.',
        example: 'A museum assistant cannot know a newly cancelled tour until the application supplies the current schedule or retrieves it.',
      },
      {
        title: 'Trace retrieve, augment, and generate',
        body: 'Retrieval Augmented Generation is an application pattern: retrieve relevant content, augment the prompt with that content, then generate an answer. Azure AI Search can provide the retrieval index. An embedding model converts text into vectors that represent meaning; vector search compares the question with indexed content, while keyword search helps exact terms. The corpus also describes semantic and hybrid approaches. Index freshness, chunking, and retrieval relevance affect what evidence reaches the language model before it writes a response.',
        example: '“Step-free entrance” can retrieve an accessibility passage phrased as “wheelchair access,” while an exact gallery code benefits from lexical matching.',
      },
      {
        title: 'Distinguish supplied context from a retrieval system',
        body: 'Pasting a trusted paragraph into a playground is a useful grounding experiment, but it does not create an index or a working RAG pipeline. A real pipeline must ingest and maintain sources, retrieve relevant passages for each question, and pass them to generation. The language model is not necessarily the component that searches. If a relevant paragraph never reaches the prompt, changing the writing style cannot repair retrieval. This unit establishes the boundary; later retrieval units build and evaluate the complete pipeline.',
        example: 'A correct answer after manually supplying today’s closure notice proves the value of context, not that the assistant will fetch tomorrow’s notice.',
      },
      {
        title: 'Combine interventions only when needed',
        body: 'Prompt engineering shapes the task and behavior; RAG supplies current or private context; fine-tuning changes learned response patterns for greater behavioral consistency. These can be combined because they address different failure causes. Begin with a baseline and clear prompts, add retrieval for missing knowledge, and consider fine-tuning when representative tests still show a persistent style or format problem. Each added layer has costs and maintenance: longer prompts, indexing and retrieval work, or training and hosting a customized model.',
        example: 'An equipment assistant can retrieve changing maintenance bulletins while a concise instruction controls wording; custom training is not required merely because a new bulletin arrives.',
      },
    ],
    lab: {
      title: 'Rehearse an optimization diagnosis',
      goal: 'Address changing facts separately from a simple response-format requirement.',
      context: LOCAL_MODEL,
      fields: [
        {
          id: 'knowledge',
          label: 'Daily-changing equipment bulletins',
          hint: 'The missing information must be available at question time.',
          options: [
            { value: 'retrieve-current-context', label: 'Retrieve relevant current context' },
            { value: 'increase-temperature', label: 'Increase temperature' },
            { value: 'train-daily-by-default', label: 'Retrain by default after every update' },
          ],
          expected: 'retrieve-current-context',
          explanation: 'RAG addresses freshness by supplying relevant evidence at query time, without requiring a weight update for each bulletin.',
        },
        {
          id: 'behavior',
          label: 'Two-sentence explanation',
          hint: 'No evidence yet shows that a clear instruction is insufficient.',
          options: [
            { value: 'prompt-first', label: 'Start with a format instruction and test it' },
            { value: 'embeddings-set-style', label: 'Use the embedding vector to set the writing style' },
            { value: 'fine-tune-without-baseline', label: 'Fine-tune before testing the base model' },
          ],
          expected: 'prompt-first',
          explanation: 'A prompt is the proportionate first behavior intervention. Embeddings retrieve meaning; training needs a demonstrated need and a baseline.',
        },
      ],
      template: '{\n  "learning_model": "optimization diagnosis",\n  "knowledge_intervention": "{{knowledge}}",\n  "behavior_intervention": "{{behavior}}"\n}',
      language: 'json',
      expectedOutput: 'SYNTHETIC EXPECTED TRACE: retrieve bulletin → supply it with the question and format instruction → evaluate support. No index, search, or model request exists locally.',
      takeaway: 'Changing the evidence source and changing response behavior are complementary decisions, not competing product names.',
      azure: {
        title: 'Observe the effect of supplied evidence',
        sourceId: 62,
        environment: 'foundry',
        minutes: 15,
        cost: CLOUD_COST,
        prerequisites: [...RESOURCE_PREREQUISITES, 'A ready text-model deployment. This bounded contrast uses a fictional paragraph and does not provision Azure AI Search.'],
        steps: [
          openProject(62),
          {
            title: 'Open a fresh model conversation',
            surface: 'Microsoft Foundry',
            instruction: 'Use the deployment created in the chat exercise and open its Model playground. Start a new conversation with instructions to answer from supplied evidence and admit when information is absent.',
            expected: 'The experiment begins without unrelated conversation history or a private knowledge source.',
          },
          {
            title: 'Ask before supplying the fact',
            surface: 'Microsoft Foundry',
            instruction: 'Ask “Which entrance is open for the fictional Cedar Museum evening tour today?” Do not give the schedule yet. Observe whether the model acknowledges the missing information.',
            expected: 'You have an actual ungrounded baseline; a guessed answer is recorded as unsupported rather than accepted.',
          },
          {
            title: 'Supply a small trusted context',
            surface: 'Microsoft Foundry',
            instruction: 'Start a fresh conversation with the same instructions. Add a clearly labeled fictional notice: “Today’s evening tour uses the west entrance. The north entrance is closed for maintenance.” Ask the same question.',
            expected: 'You can compare the answer with explicit evidence in the request; the model may still require review for unsupported additions.',
          },
          {
            title: 'Change the evidence, not the model',
            surface: 'Microsoft Foundry',
            instruction: 'In another fresh conversation, replace the notice with a fictional update that uses the east entrance. Keep the question and instructions unchanged, and inspect whether the answer follows the new evidence.',
            expected: 'A supported answer should follow the supplied update without model training. Record any failure rather than assuming the change worked.',
          },
          {
            title: 'Inspect what an application must automate',
            surface: 'Microsoft Foundry',
            instruction: 'Open the playground Code view and identify the supplied text that would need to come from retrieval in a real app. Name the missing ingestion, index, and query stages. Stop without creating a search resource in this exercise.',
            expected: 'You distinguish manual prompt grounding from an implemented RAG pipeline.',
          },
        ],
        success: 'The observed answers are compared against changing evidence, and the learner can identify which stages a real retrieval system must add.',
        evidencePrompt: 'Explain which input changed the answer and which parts of a full RAG pipeline were not built. Do not claim a search index or live data integration exists.',
        cleanup: CLEANUP,
      },
    },
    check: {
      question: 'A support assistant follows the required style but cannot answer questions about policy changes published this morning. Which intervention best addresses the missing capability?',
      options: [
        { id: 'a', text: 'Raise the output token limit so the model can reveal its hidden knowledge of the update.', explanation: 'Output capacity does not supply an update that was never included in training or request context.' },
        { id: 'b', text: 'Fine-tune on last month’s policies and stop maintaining the source.', explanation: 'This does not supply this morning’s changes and creates an unnecessary dependency on retraining for freshness.' },
        { id: 'c', text: 'Retrieve the relevant current policy and include it as evidence for generation.', explanation: 'This addresses missing current knowledge at query time while leaving the already adequate style behavior intact.' },
        { id: 'd', text: 'Use a more confident system message without changing the available information.', explanation: 'Confidence does not establish access to the new policy and can make unsupported answers harder to notice.' },
      ],
      correctOptionId: 'c',
      explanation: 'The causal problem is missing current evidence. Retrieval and prompt augmentation address that problem more directly than changing tone or output length.',
    },
    recall: {
      prompt: 'Reconstruct the three RAG stages and name the role of an embedding model. How does this differ from fine-tuning?',
      answer: 'Retrieve relevant source content, augment the prompt with it, and generate the response. An embedding model represents content and queries numerically for similarity-based retrieval. RAG changes the evidence supplied at request time; fine-tuning changes learned model behavior through additional training.',
    },
    transfer: 'A customer proposes retraining each night with their latest inventory. Which freshness, provenance, and maintenance questions would help you evaluate a retrieval-based alternative?',
    sourceNote: `Source 102 introduces RAG and Azure AI Search; this unit teaches the architecture and a manual grounding contrast, not a completed retrieval implementation. The hands-on activity extends source 62’s playground setup with original fictional context. Detailed ingestion and search belong to the retrieval curriculum. ${PORTAL_NOTE}`,
  },
  {
    id: 'model-fine-tuning',
    title: 'Fine-tune for repeatable behavior',
    cluster: 'models-deploy-eval',
    examDomain: 'Implement generative AI and agentic solutions',
    summary: 'Use representative training examples only after a measured behavior problem remains, then compare the customized model with its baseline.',
    minutes: 9,
    order: 50,
    prerequisites: ['grounding-optimization', 'model-evaluation'],
    sourceIds: [95, 77, 161, 177, 202],
    steps: [
      {
        title: 'Require evidence before training',
        body: 'Fine-tuning continues training a pretrained model on task-specific examples, changing learned behavior rather than simply adding another instruction to a request. It can help with persistent style, formatting, tool-use patterns, or distilling a larger model’s behavior into a smaller one. First measure the base model with good prompts and representative tests. If the real problem is missing current facts, retrieval is usually the more direct intervention. Training without a baseline makes improvement or regression difficult to detect.',
        example: 'An equipment service wants every summary to follow a compact handover format that the base model repeatedly violates despite clear examples.',
      },
      {
        title: 'Match the training method to the evidence',
        body: 'Supervised fine-tuning learns from examples of desired responses. Direct Preference Optimization uses preferred and non-preferred responses to express a relative preference. Reinforcement fine-tuning uses grader feedback to reward better behavior over iterations. Supported methods depend on the chosen model and platform. The corpus also describes LoRA as a parameter-efficient adaptation technique, rather than retraining every parameter. Do not assume that every model supports every method, or that the implementation details of one provider apply universally.',
        example: 'A collection of approved handover answers naturally fits supervised examples; paired “better versus worse” answers express a different kind of training signal.',
      },
      {
        title: 'Treat the dataset as the behavior specification',
        body: 'For chat-style supervised training, JSONL contains one example conversation per line, typically with system, user, and assistant messages. Keep instructions consistent and make assistant examples demonstrate exactly the target behavior. Representative coverage matters more than repeating a few easy cases. Review synthetic examples before use, exclude sensitive data, and keep evaluation cases separate from training. The corpus recommends substantial high-quality coverage; a tiny teaching dataset can demonstrate the workflow without demonstrating production readiness or generalization.',
        example: 'Training only on successful repairs may leave the model unprepared to produce a handover that clearly states an unresolved fault.',
      },
      {
        title: 'Evaluate a deployed result, not just a finished job',
        body: 'A completed training job produces a customized model; it still needs a usable deployment for inference. Compare that deployment with the base model using the same instructions and held-out cases. Watch both target behavior and regressions in accuracy or safety. Training, hosting, hyperparameter experiments, and later maintenance can all add cost. Epochs, batch size, and learning rate change training behavior, so defaults are a sensible controlled starting point rather than proof of an optimal result.',
        example: 'Better handover formatting is not enough if the customized model now invents repair steps or fails to acknowledge uncertainty.',
      },
    ],
    lab: {
      title: 'Rehearse a justified training plan',
      goal: 'Select a training signal and a comparison that can reveal genuine improvement.',
      context: LOCAL_MODEL,
      fields: [
        {
          id: 'signal',
          label: 'Available training evidence',
          hint: 'The team has reviewed examples of the exact response format it wants.',
          options: [
            { value: 'supervised-examples', label: 'Supervised prompt/response examples' },
            { value: 'unreviewed-logs', label: 'All unreviewed logs, including errors and secrets' },
            { value: 'new-facts-only', label: 'A daily inventory dump with no target responses' },
          ],
          expected: 'supervised-examples',
          explanation: 'Reviewed desired outputs specify the behavior to learn. Unreviewed logs can teach mistakes or leak information.',
        },
        {
          id: 'comparison',
          label: 'Measure the result',
          hint: 'The test must compare the customized model with the original behavior.',
          options: [
            { value: 'held-out-baseline', label: 'Held-out cases and the same base-model instructions' },
            { value: 'training-rows-only', label: 'Only the examples used in training' },
            { value: 'job-succeeded', label: 'Only the job’s Succeeded status' },
          ],
          expected: 'held-out-baseline',
          explanation: 'Held-out comparisons test generalization and regressions. A job status proves processing finished, not that the model improved.',
        },
      ],
      template: '{\n  "learning_model": "fine-tuning plan",\n  "training_signal": "{{signal}}",\n  "acceptance_evidence": "{{comparison}}"\n}',
      language: 'json',
      expectedOutput: 'SYNTHETIC EXPECTED TRACE: measured behavior gap → reviewed examples → training job → deployment → held-out comparison. No training or deployment was started.',
      takeaway: 'Changing the training examples changes the behavior being learned; changing the evaluation split changes what evidence of generalization you can claim.',
      azure: {
        title: 'Compare a fine-tuned model with its baseline',
        sourceId: 77,
        environment: 'foundry',
        minutes: 90,
        cost: `${CLOUD_COST} Fine-tuning can incur training and deployment/hosting charges and may take substantially longer than the teaching lesson. Do not submit a training job without approval for its cost.`,
        prerequisites: [...RESOURCE_PREREQUISITES, 'Access and quota for a fine-tunable model in a region supporting its selected training method.', 'The linked exercise’s non-sensitive JSONL dataset, a base-model deployment, and time to return to an asynchronous job.'],
        steps: [
          openProject(77),
          {
            title: 'Verify method and regional support',
            surface: 'Microsoft Foundry',
            instruction: 'Open Discover > Models and the exercise’s supported base model. Check current fine-tuning methods and regional requirements before deploying or submitting training. Use only an approved region; the corpus’s demo region is not a universal guarantee.',
            expected: 'You can identify a supported model/method/region combination, or record a specific availability blocker.',
          },
          {
            title: 'Save the base-model comparison',
            surface: 'Microsoft Foundry',
            instruction: 'Open the base deployment in the Model playground. Follow the exercise’s prompt-refinement section, save the final instructions, and test a few travel-planning questions. Record style and format behavior without treating the model’s travel claims as verified facts.',
            expected: 'A baseline with stable instructions and questions exists before evaluating the customized model.',
          },
          {
            title: 'Review the training examples',
            surface: 'Browser',
            instruction: 'Open the JSONL dataset linked from the exercise and inspect its system, user, and assistant entries. Identify the repeated style pattern. Keep several new comparison questions outside that dataset; do not upload private production conversations.',
            expected: 'The training examples specify a recognizable behavior and the comparison questions are not just copied training rows.',
          },
          {
            title: 'Configure and submit a bounded job',
            surface: 'Microsoft Foundry',
            instruction: 'Open Fine-tune, choose Fine-tune, and follow the published wizard: supported base model, Supervised method, training type, dataset upload, suffix, and default hyperparameters. Review any automatic deployment and Developer evaluation option, then submit only after checking charges.',
            expected: 'A named job is visible with its actual state; submission is not confused with completion.',
            hint: 'Developer deployment is for supported fine-tuned evaluation, not an assumed production type. If an option differs, follow the exercise’s current supported path.',
          },
          {
            title: 'Return to the job and deployment',
            surface: 'Microsoft Foundry',
            instruction: 'Use the job’s Monitor or Logs view to inspect progress. This is a safe break point: record the non-secret job name and return later. When training completes, check Deployments for the customized model; if automatic deployment failed, inspect the error and follow the exercise’s manual deployment step.',
            expected: 'Both training completion and deployment readiness are verified independently, or a real failure remains explicitly unresolved.',
          },
          {
            title: 'Compare on held-out questions',
            surface: 'Microsoft Foundry',
            instruction: 'Open the customized deployment’s Model playground, reuse the baseline instructions, and ask the reserved questions. Compare consistency, unsupported claims, and safety with the base model before deciding whether the extra complexity is justified.',
            expected: 'Your conclusion rests on observed comparative behavior, not just a successful job or one familiar training example.',
          },
        ],
        success: 'A supported customized deployment is compared with a recorded baseline on held-out questions, or the exact training/deployment blocker is documented.',
        evidencePrompt: 'Describe the target behavior, the training signal, and one observed difference or regression on a new question. Never include private training data, credentials, or invented success claims.',
        cleanup: CLEANUP,
      },
    },
    check: {
      question: 'A fine-tuning job finishes successfully and reproduces its training examples neatly. What additional evidence is most important before claiming the change improved the assistant?',
      options: [
        { id: 'a', text: 'Compare both deployed models on held-out cases using the same instructions and criteria.', explanation: 'This tests whether the behavior generalizes and whether accuracy or safety regressed relative to the baseline.' },
        { id: 'b', text: 'Increase the training epochs until every training response is identical.', explanation: 'Fitting the training set more closely does not establish useful behavior on new requests and can worsen overfitting.' },
        { id: 'c', text: 'Treat training completion as proof of deployment readiness and quality.', explanation: 'Training, deployment, and quality acceptance are separate gates. A completed job can still have a deployment failure or poor generalization.' },
        { id: 'd', text: 'Remove all system instructions because fine-tuning makes them unnecessary.', explanation: 'The corpus recommends consistent instructions between training and inference. Removing them changes the conditions of the comparison.' },
      ],
      correctOptionId: 'a',
      explanation: 'A successful training process is not the same as a successful application change. Use comparable, unseen evidence and check for regressions.',
    },
    recall: {
      prompt: 'Distinguish SFT, DPO, and RFT by their training signals. Why keep test cases separate from the training examples?',
      answer: 'SFT uses desired prompt/response examples; DPO uses preferred and non-preferred response pairs; RFT uses grader feedback or rewards. Held-out cases test behavior beyond the examples used to adapt the model and help detect overfitting and regressions.',
    },
    transfer: 'What evidence, data-governance checks, and ongoing ownership would you ask a customer to establish before recommending a custom fine-tuned deployment?',
    sourceNote: `Source 95’s LoRA description is not a guarantee that all providers or methods use the same adaptation implementation. Episode 177 demonstrates a small dataset; that is workflow practice, not production-quality evidence. The live exercise changes model/version examples over time and its regional support must be checked. ${PORTAL_NOTE}`,
  },
  {
    id: 'responsible-ai-lifecycle',
    title: 'Map and measure potential harms',
    cluster: 'models-deploy-eval',
    examDomain: 'Plan and manage an Azure AI solution',
    summary: 'Turn responsible AI principles into a scenario-specific harm map and a repeatable measurement baseline.',
    minutes: 8,
    order: 55,
    prerequisites: ['ai-foundations', 'model-evaluation'],
    sourceIds: [112, 182, 153, 154, 57, 106],
    steps: [
      {
        title: 'Ask what could go wrong for people',
        body: 'A generated answer can be harmful without containing obviously offensive language. Incorrect public-service advice can exclude someone from assistance; a plausible answer can expose private information or encourage unjustified trust. Begin with the intended users, inputs, decisions, and consequences of mistakes. Include misuse and information arriving through documents or tools, not only direct user questions. The responsible AI task is to understand the complete application’s effects, rather than assuming a capable model or a content filter settles the issue.',
        example: 'A public-library assistant that invents an accessibility requirement could discourage a visitor who should have been able to use the service.',
      },
      {
        title: 'Build and verify a prioritized harm map',
        body: 'Mapping identifies potential harms, prioritizes them by likelihood and impact, tests whether they can occur, and documents the evidence for stakeholders. Use service transparency information and an AI Impact Assessment to record intended use and limitations. Controlled probing can verify a suspected weakness without treating every imagined risk as an observed incident. Prioritization should consider affected people and the application context, not just the frequency of a failure. Keep the map open to newly discovered problems.',
        example: 'A library team distinguishes an occasional awkward greeting from a repeated unsupported refusal of an accessible service, then tests the latter with fictional cases.',
      },
      {
        title: 'Measure with a rubric before changing controls',
        body: 'Measurement establishes how often and how severely mapped harms appear under defined conditions. Prepare diverse relevant prompts, collect outputs, and apply explicit criteria consistently. Start with a small manual review to clarify the rubric, then automate larger evaluations where appropriate. Record the baseline configuration and denominator, not just memorable failures. Recheck automated judgments manually when new scenarios appear. Without a stable measurement method, a lower reported harm rate after a change may reflect a different test rather than a safer application.',
        example: 'Count unsupported statements about a fictional membership rule across the same test cases before and after changing the assistant’s instructions.',
      },
      {
        title: 'Keep map, measure, mitigate, and manage connected',
        body: 'The corpus describes four linked stages: map relevant harms, measure their presence, mitigate at multiple layers, and manage responsible release and operation. Mapping asks which risks matter; measurement quantifies them; mitigation changes the system; management keeps people accountable after release. The loop continues when users, sources, models, or features change. A good baseline supports a decision to improve or withhold a feature, not a declaration that risk has disappeared. The next unit turns this evidence into controls and release actions.',
        example: 'Adding uploaded documents changes the assistant’s input surface, so the team revisits the harm map instead of reusing a text-only safety sign-off unchanged.',
      },
    ],
    lab: {
      title: 'Rehearse a harm-measurement plan',
      goal: 'Make an unsupported-policy risk measurable without changing the test mid-comparison.',
      context: LOCAL_MODEL,
      fields: [
        {
          id: 'priority',
          label: 'Prioritize the risk',
          hint: 'Both frequent minor failures and rarer serious failures deserve explicit consideration.',
          options: [
            { value: 'likelihood-and-impact', label: 'Likelihood and impact in the intended scenario' },
            { value: 'most-memorable-only', label: 'Only the most memorable example' },
            { value: 'model-brand-only', label: 'Only the model provider’s reputation' },
          ],
          expected: 'likelihood-and-impact',
          explanation: 'A scenario-specific priority considers who is affected, how likely the event is, and the consequence when it happens.',
        },
        {
          id: 'measurement',
          label: 'Compare a later mitigation',
          hint: 'You need to distinguish a system improvement from a change in scoring.',
          options: [
            { value: 'fixed-rubric-and-cases', label: 'A recorded rubric and comparable cases' },
            { value: 'new-rubric-each-time', label: 'A more lenient rubric for each new version' },
            { value: 'one-refusal', label: 'One refused request as complete safety proof' },
          ],
          expected: 'fixed-rubric-and-cases',
          explanation: 'Stable criteria and comparable cases make a before/after result interpretable. One refusal does not cover the risk surface.',
        },
      ],
      template: '{\n  "learning_model": "harm baseline",\n  "prioritization": "{{priority}}",\n  "comparison_basis": "{{measurement}}"\n}',
      language: 'json',
      expectedOutput: 'SYNTHETIC EXPECTED RESULT: a prioritized risk and repeatable measurement plan. No safety test or risk certification has occurred.',
      takeaway: 'Changing the rubric changes the meaning of the result; changing the system should be measured against a documented baseline.',
      azure: {
        title: 'Record a small responsible-AI baseline',
        sourceId: 57,
        environment: 'foundry',
        minutes: 20,
        cost: CLOUD_COST,
        prerequisites: [...RESOURCE_PREREQUISITES, 'A disposable text-model deployment for testing. Use only benign, fictional scenarios and no personal information.'],
        steps: [
          openProject(57),
          {
            title: 'Choose the disposable test deployment',
            surface: 'Microsoft Foundry',
            instruction: 'Follow the exercise’s deployment section or open your ready lab model under Build > Models. Verify that the model belongs to your experiment rather than a shared production workload.',
            expected: 'The target and its ownership are explicit before any testing or later guardrail changes.',
          },
          {
            title: 'Inspect the existing protection',
            surface: 'Microsoft Foundry',
            instruction: 'Open the deployment’s Details and identify its applied default guardrail. Do not disable it. Return to the Model playground.',
            expected: 'The baseline records an actual protection configuration rather than assuming the system is unfiltered.',
          },
          {
            title: 'Define a safe fictional scenario',
            surface: 'Microsoft Foundry',
            instruction: 'In Instructions, define an assistant that answers only from a supplied fictional library notice: “Opening hours are 10:00–18:00. No membership-fee policy is supplied.” Require it to acknowledge missing facts and never provide another member’s private details.',
            expected: 'A small, explicit scope exists for evaluating unsupported policy claims and privacy boundaries without real personal data.',
          },
          {
            title: 'Write the harm map and rubric',
            surface: 'Browser',
            instruction: 'Using the mapping and measurement sources linked to this unit, record two risks in your own notes: inventing a fee and claiming access to another member’s details. State the expected safe behavior, likelihood/impact rationale, and pass/fail criterion before running the cases.',
            expected: 'The rubric can be applied consistently and does not require generating dangerous or offensive content.',
          },
          {
            title: 'Run the fixed benign cases',
            surface: 'Microsoft Foundry',
            instruction: 'In fresh playground conversations, ask the opening time, ask for the membership fee, and ask whether the assistant can disclose another member’s details without supplying any real details. Record the actual responses against the same rubric.',
            expected: 'You have observed outputs, including any unsupported claim, appropriate uncertainty, or refusal. Do not assume a filter blocked a response just because the model declined.',
          },
          {
            title: 'Preserve a comparison baseline',
            surface: 'Microsoft Foundry',
            instruction: 'Record the non-secret deployment name, applied guardrail name, instructions, case count, and your manual scores. Leave the deployment ready for the next unit if you plan to continue; otherwise follow cleanup.',
            expected: 'The baseline is reproducible and its limited scope is explicit. No claim is made that these few cases cover every harm.',
          },
        ],
        success: 'A documented harm map and actual small baseline use consistent criteria, with observed behavior separated from assumptions about service filtering.',
        evidencePrompt: 'Explain one risk, its priority, the criterion you applied, and one actual result. Record no personal information, secrets, harmful text, or claim of complete safety.',
        cleanup: CLEANUP,
      },
    },
    check: {
      question: 'A team reports fewer safety failures after changing both the model’s instructions and the scoring rubric to be more lenient. What is the main limitation of that conclusion?',
      options: [
        { id: 'a', text: 'The changed rubric prevents attributing the lower failure count to safer behavior.', explanation: 'The measurement changed with the intervention. Comparable cases and stable criteria are needed to distinguish improvement from easier scoring.' },
        { id: 'b', text: 'Safety cannot be measured at all, so the team should stop collecting evidence.', explanation: 'The corpus explicitly recommends defined criteria, baselines, and repeated measurement. The problem is comparability, not the possibility of measurement.' },
        { id: 'c', text: 'A provider’s default guardrail makes application-specific tests unnecessary.', explanation: 'Application harms include unsupported claims and contextual consequences that default filtering alone does not eliminate.' },
        { id: 'd', text: 'Only aggregate quality scores matter once the instructions have improved.', explanation: 'Quality and harm are distinct concerns. Changing instructions is not itself evidence that either improved.' },
      ],
      correctOptionId: 'a',
      explanation: 'A responsible AI claim needs evidence whose meaning stays stable across the comparison. Preserve the baseline and explain any necessary rubric revisions separately.',
    },
    recall: {
      prompt: 'Name the four responsible generative AI stages and distinguish mapping from measurement without using those words in your explanation.',
      answer: 'Map, measure, mitigate, manage. First identify, prioritize, verify, and document scenario-specific risks; then assess their frequency/severity using consistent cases and criteria. Apply layered changes and operate with accountable release, monitoring, and response processes.',
    },
    transfer: 'What would you ask stakeholders to define before accepting “the model is safe” as a meaningful release statement?',
    sourceNote: `The exercise adapts source 57’s default-guardrail testing to benign fictional policy/privacy cases; it does not reproduce dangerous test prompts. Sources 153 and 154 distinguish verified risks from a quantified baseline. ${PORTAL_NOTE}`,
  },
  {
    id: 'responsible-ai-guardrails',
    title: 'Layer guardrails and release controls',
    cluster: 'models-deploy-eval',
    examDomain: 'Plan and manage an Azure AI solution',
    summary: 'Apply a scoped guardrail, distinguish a filter intervention from a model refusal, and plan responsible operations.',
    minutes: 8,
    order: 60,
    prerequisites: ['responsible-ai-lifecycle', 'prompt-engineering'],
    sourceIds: [156, 152, 170, 211, 57, 106],
    steps: [
      {
        title: 'One instruction is not a complete defense',
        body: 'Mitigation is layered because failures have different causes. At the model layer, choose a suitable model and assess its limitations. At the safety-system layer, apply platform guardrails. At the instruction and grounding layer, constrain behavior and supply trusted context. At the user-experience layer, validate inputs and outputs, explain limitations, and offer appropriate escalation. A restriction written in a system message can help, but it is not an authorization boundary or a guarantee that every harmful output will be prevented.',
        example: 'A library assistant combines limited data access, evidence-based answers, platform filtering, and a staff handoff rather than relying only on “always be safe.”',
      },
      {
        title: 'Understand severity and intervention points',
        body: 'Foundry guardrails apply controls to supported model or agent interactions. Content filters can classify categories such as hate, violence, sexual content, and self-harm at different severity levels. A stricter blocking setting intercepts more classified content, which can also affect legitimate requests. Prompt shields and other supported controls address different risks, including attempts to redirect intended behavior. Inspect the specific control and whether it applies to incoming prompts, outgoing responses, or supported tool interactions; labels and preview support can vary.',
        example: 'Blocking lower-severity content is a different policy from blocking only high-severity content; either choice needs testing for both missed harms and unnecessary blocks.',
      },
      {
        title: 'Check what actually caused the result',
        body: 'A model may refuse because of its trained behavior even when a request passes through a filter. Conversely, a guardrail may block a prompt or response before ordinary output is delivered. These are different events and require different evidence. After attaching a custom guardrail, verify the deployment’s applied configuration, allow for propagation, and repeat the same safe test cases. An unchanged response does not prove the new policy failed, and one blocked request does not prove comprehensive safety.',
        example: 'A polite refusal in plain answer text is not, by itself, evidence that the safety service intercepted the request.',
      },
      {
        title: 'Treat release as an ongoing responsibility',
        body: 'Management includes legal, security, and accessibility review before release, followed by phased rollout, monitoring, and user feedback. Define incident ownership, response expectations, and a rollback path before a problem occurs. Maintain the ability to stop harmful responses or abusive access where appropriate. Collect telemetry in line with privacy commitments and revisit the harm map when the system changes. These are application and organizational responsibilities; creating a guardrail in Foundry does not automatically create an incident-response process.',
        example: 'A pilot with a small staff group can reveal confusing refusals before a public launch, while a documented rollback lets the team restore the previous configuration.',
      },
    ],
    lab: {
      title: 'Rehearse layered harm controls',
      goal: 'Choose a stricter filter policy without mistaking it for the entire safety plan.',
      context: LOCAL_MODEL,
      fields: [
        {
          id: 'blocking',
          label: 'Stricter content filtering',
          hint: 'The chosen experiment should block more classified content than a high-severity-only policy.',
          options: [
            { value: 'include-lower-severities', label: 'Include lower-severity content in blocking' },
            { value: 'high-only', label: 'Block only high-severity content' },
            { value: 'no-filter', label: 'Remove the platform filter' },
          ],
          expected: 'include-lower-severities',
          explanation: 'A stricter setting can intercept lower-severity content too. It must still be evaluated for unnecessary blocks.',
        },
        {
          id: 'operations',
          label: 'Release plan',
          hint: 'New failure modes can appear after deployment.',
          options: [
            { value: 'phased-monitor-rollback', label: 'Phased rollout, feedback, monitoring, and rollback' },
            { value: 'prompt-only', label: 'Only a system instruction to behave safely' },
            { value: 'publish-and-forget', label: 'Release broadly and stop testing' },
          ],
          expected: 'phased-monitor-rollback',
          explanation: 'Operational controls preserve accountability and recovery when pre-release tests miss a problem.',
        },
      ],
      template: '{\n  "learning_model": "layered safety plan",\n  "filter_experiment": "{{blocking}}",\n  "release_controls": "{{operations}}"\n}',
      language: 'json',
      expectedOutput: 'SYNTHETIC EXPECTED RESULT: a stricter scoped filter plus operational controls. No policy was applied and no safety outcome was observed.',
      takeaway: 'Changing a blocking setting changes interception behavior; it does not provide data authorization, human accountability, or a rollback plan.',
      azure: {
        title: 'Apply a guardrail to your lab deployment',
        sourceId: 57,
        environment: 'foundry',
        minutes: 25,
        cost: CLOUD_COST,
        prerequisites: [...RESOURCE_PREREQUISITES, 'A disposable model deployment you may reconfigure and the baseline from the previous unit. Never change a shared production deployment for this exercise.'],
        steps: [
          openProject(57),
          {
            title: 'Record the current configuration',
            surface: 'Microsoft Foundry',
            instruction: 'Open the lab deployment’s Details page and note its current guardrail. In the Model playground, repeat the same benign baseline cases from the previous unit without disabling default protection.',
            expected: 'The before-state and the test cases are known, including any refusals or ordinary responses.',
          },
          {
            title: 'Create a custom guardrail',
            surface: 'Microsoft Foundry',
            instruction: 'Open Guardrails from the Build navigation or the current location shown in the exercise. Select Create to open Create guardrail controls. Give the experiment a recognizable lab-only name if prompted.',
            expected: 'A draft custom guardrail is open; the shared default policy has not been edited.',
          },
          {
            title: 'Add the exercise’s content controls',
            surface: 'Microsoft Foundry',
            instruction: 'Under Add controls, use Risk to select Hate and choose the exercise’s Highest blocking setting. Use Add control and confirm replacing the draft’s existing setting when prompted. Repeat for Violence, Sexual, and Self-harm; inspect the prompt and response intervention points.',
            expected: 'The draft shows the intended stricter controls for the supported categories, with their intervention points visible.',
            hint: 'The severity names and available risks can differ by portal version. Do not invent a missing category or assume a preview tool control is universally supported.',
          },
          {
            title: 'Attach only to the test model',
            surface: 'Microsoft Foundry',
            instruction: 'Select Next, then Models in Select agents and models. Select only your disposable test deployment. Review the scope and submit the new guardrail.',
            expected: 'The submitted policy targets the intended lab model, not every model or agent in the resource.',
          },
          {
            title: 'Verify the applied policy',
            surface: 'Microsoft Foundry',
            instruction: 'Return to Deployments or Models, open the test deployment’s Details, and confirm the new guardrail is applied. If it is not yet visible, allow time for propagation and refresh before testing.',
            expected: 'The actual applied guardrail matches the new policy before the comparison is interpreted.',
          },
          {
            title: 'Compare behavior without forcing a harmful case',
            surface: 'Microsoft Foundry',
            instruction: 'Repeat the same benign fictional cases and record ordinary output, model refusal, or an explicit filter intervention if one occurs. Keep testing within your approved scope. Write a short pilot/feedback/rollback plan before calling the experiment finished.',
            expected: 'Results may remain unchanged for benign prompts. The observation and the operational plan are recorded without claiming that a block must occur.',
          },
        ],
        success: 'The correct lab deployment shows the custom guardrail, and a comparable safe test distinguishes observed behavior from assumptions about filtering.',
        evidencePrompt: 'Record the policy scope, applied configuration, and one observed comparison in your own words. Add who would review feedback and roll back a problematic release; never include credentials or harmful generated text.',
        cleanup: `${CLEANUP} If retaining this lab deployment, deliberately keep the tested policy or restore its previous approved policy; do not leave a shared configuration altered accidentally.`,
      },
    },
    check: {
      question: 'After adding a guardrail, a model returns a polite refusal. A teammate says this proves the platform filter intercepted the request and the application is safe to launch broadly. Which response is justified?',
      options: [
        { id: 'a', text: 'Agree, because all model refusals are generated by the platform filter.', explanation: 'The model can refuse based on learned behavior. Plain refusal text does not identify the intervention layer.' },
        { id: 'b', text: 'Remove all guardrails to determine whether the refusal disappears.', explanation: 'Disabling protection is unnecessary for this conclusion and creates risk. Inspect configuration and available filter evidence instead.' },
        { id: 'c', text: 'Treat the refusal as proof that legal, accessibility, and incident reviews are complete.', explanation: 'Those are separate release responsibilities. A model response cannot perform organizational reviews or establish rollback readiness.' },
        { id: 'd', text: 'Verify the intervention evidence and continue layered evaluation and release preparation.', explanation: 'This separates the observed refusal from its cause and avoids extending one result into a claim of comprehensive safety.' },
      ],
      correctOptionId: 'd',
      explanation: 'Identify the layer that acted, then use broader evidence and operational readiness to decide on release.',
    },
    recall: {
      prompt: 'Name the four mitigation layers and three management actions that a guardrail configuration does not do for you.',
      answer: 'Model, safety system, system message/grounding, and user experience. Management actions include compliance reviews, phased rollout, incident ownership/response, rollback, monitoring, privacy-aware telemetry, and user feedback. Any three correctly explained actions demonstrate the operational distinction.',
    },
    transfer: 'How would you explain to a customer why “highest blocking” is a testable policy choice rather than a complete responsible AI strategy?',
    sourceNote: `Source 156 includes task-adherence among its listed risks, while episode 106 and the published exercise demonstrate four content-harm categories. Available risks and tool intervention points vary; this walkthrough follows the exercise rather than inventing a fifth mandatory slider. ${PORTAL_NOTE}`,
  },
  {
    id: 'vision-chat',
    title: 'Ask questions about visual evidence',
    cluster: 'models-deploy-eval',
    examDomain: 'Implement computer vision solutions',
    summary: 'Use a multimodal model with a multipart image-and-text prompt, and separate visible evidence from unsupported inference.',
    minutes: 8,
    order: 65,
    prerequisites: ['chat-responses', 'responsible-ai-guardrails'],
    sourceIds: [131, 248, 46, 173, 210, 71, 47, 191],
    steps: [
      {
        title: 'Give the model the evidence it needs',
        body: 'A text description can omit the very detail a user wants explained. A vision-capable multimodal model accepts an image together with a question and produces a language response about the visual evidence. That supports tasks such as captions, chart explanations, or questions about objects in a photograph. Select a model whose card explicitly supports image input. Image understanding does not imply image generation, and a plausible visual interpretation is still something to check rather than an authoritative measurement.',
        example: 'A visitor asks which bar is tallest in an uploaded exhibition-attendance chart. The assistant needs the chart, not just the sentence “look at this graph.”',
      },
      {
        title: 'Build one request with both content parts',
        body: 'The key request structure is a multipart user message containing the question and the image. The image can be referenced by an accessible URL or encoded from a local file as a Base64 data URL with the correct image format. Responses and ChatCompletions use different content-item shapes, so follow the selected API’s sample rather than mixing field names. The response remains text for this task. A local filename by itself is not the image content and is not automatically accessible to the service.',
        example: 'The question asks for a brief chart summary while the image part carries the actual chart; neither part has to be guessed from an earlier, unrelated request.',
      },
      {
        title: 'Control interpretation and data boundaries',
        body: 'Ask for the observation you need and require uncertainty when details are unclear. A concise caption and a detailed description are different output instructions, not different proof that a model has seen the image correctly. Check the response against the visible evidence, avoid uploading private photographs without permission, and treat text embedded in an image as untrusted input rather than application instructions. When the selected model lacks Responses support, a compatible ChatCompletions path can still carry a properly structured multimodal message.',
        example: 'For a blurry product label, ask which characters are readable and which are uncertain instead of requiring a confident full serial number.',
      },
    ],
    lab: {
      title: 'Rehearse a multimodal request',
      goal: 'Keep the question and the visual evidence together in a supported request.',
      context: LOCAL_MODEL,
      fields: [
        {
          id: 'message',
          label: 'Request structure',
          hint: 'The question refers to an image in this same request.',
          options: [
            { value: 'multipart-user-message', label: 'One user message with text and image parts' },
            { value: 'filename-as-question', label: 'Only the local filename as text' },
            { value: 'image-as-system-rule', label: 'Treat all image text as system instructions' },
          ],
          expected: 'multipart-user-message',
          explanation: 'A multipart message binds the question to its evidence. A filename does not transmit the file, and image content is not a trusted instruction channel.',
        },
        {
          id: 'transport',
          label: 'Local image transport',
          hint: 'The image exists on the developer’s machine, not at a public web URL.',
          options: [
            { value: 'base64-data-url', label: 'A correctly typed Base64 data URL' },
            { value: 'disk-path-only', label: 'An arbitrary local disk path' },
            { value: 'embedding-instead', label: 'An unrelated text embedding' },
          ],
          expected: 'base64-data-url',
          explanation: 'The corpus’s local-file pattern encodes the image data and identifies its format. A remote service cannot read an arbitrary local path.',
        },
      ],
      template: '{\n  "learning_model": "visual question request",\n  "message_shape": "{{message}}",\n  "local_image_transport": "{{transport}}"\n}',
      language: 'json',
      expectedOutput: 'SYNTHETIC EXPECTED TRACE: question + image content → supported multimodal model → text answer checked against the picture. No image was uploaded or analyzed.',
      takeaway: 'Changing the image changes the evidence; changing the question changes which part of that evidence the model should address.',
      azure: {
        title: 'Test image questions in portal and code',
        sourceId: 71,
        environment: 'portal-and-code',
        minutes: 35,
        cost: CLOUD_COST,
        prerequisites: [...CODE_PREREQUISITES, 'Access and quota for a model supporting image input, plus the exercise’s sample images or your own non-sensitive permitted image.'],
        steps: [
          openProject(71),
          {
            title: 'Verify and deploy a vision-capable model',
            surface: 'Microsoft Foundry',
            instruction: 'Open Discover > Models and inspect a model card supporting text and image input. Follow the exercise’s supported deployment choice or open an existing suitable lab deployment, then open its chat playground.',
            expected: 'The selected deployment’s capabilities explicitly include visual input; it is not merely an image generator.',
          },
          {
            title: 'Attach an image with a question',
            surface: 'Microsoft Foundry',
            instruction: 'Use the chat input’s attach control to upload the exercise’s fruit image or an approved sample. In the same message, ask what visible features support a short description. Submit both together.',
            expected: 'The playground displays the attached image with the question and returns either a response or a concrete input/model error.',
          },
          {
            title: 'Compare description depth',
            surface: 'Microsoft Foundry',
            instruction: 'Ask for a concise caption, then a more detailed description of the same image, resupplying the image if starting a new conversation. Compare each answer with the picture and mark uncertain or invented details.',
            expected: 'Output instructions change detail level without removing the need to check visual evidence.',
          },
          {
            title: 'Transfer the portal configuration',
            surface: 'Microsoft Foundry',
            instruction: 'On Home, locate the Azure OpenAI endpoint. In the playground Code view, inspect the supported Responses or ChatCompletions image request. Note the deployment name and keep authentication material out of evidence.',
            expected: 'The selected API and multimodal content shape are known before moving to the editor.',
          },
          {
            title: 'Run the published URL-based example',
            surface: 'Cloud Shell / local editor',
            instruction: 'Follow the linked exercise’s gen-ai-vision Python setup, configuration, and sign-in. Complete its supplied URL-based image request and run it. Inspect the question, image URL, and returned text as separate parts of the data flow.',
            expected: 'The client’s actual answer can be traced to a request carrying both text and a reachable image.',
            hint: 'If image retrieval fails, check URL access and format before assuming the model cannot reason about the picture.',
          },
          {
            title: 'Use the local-file variant',
            surface: 'Cloud Shell / local editor',
            instruction: 'Follow the exercise’s local-image modification using its sample file and matching MIME format. Confirm that it encodes the file into a data URL, then run the new question and compare the response with that image. Quit when finished.',
            expected: 'The client now transmits the local image data rather than merely its filename. Record actual results without assuming the sample is identified correctly.',
          },
        ],
        success: 'An actual image-and-text request is tested in the playground and the supplied client, with the answer checked against visual evidence.',
        evidencePrompt: 'Explain the two message parts, your image transport, and one visible detail the answer got right or could not establish. Do not upload private images or record secrets.',
        cleanup: CLEANUP,
      },
    },
    check: {
      question: 'A client sends “What does this damaged label say?” plus a local file path, but no image data or reachable image URL. What is the most direct repair?',
      options: [
        { id: 'a', text: 'Send the question and the actual image content in a supported multipart user message.', explanation: 'The service needs the visual evidence. A data URL or reachable image URL supplies it in the selected API’s supported structure.' },
        { id: 'b', text: 'Increase temperature so the model can infer the file’s contents from its name.', explanation: 'Sampling cannot make an inaccessible file visible and may encourage invented details.' },
        { id: 'c', text: 'Move the file path into the system message.', explanation: 'Changing the role of a path does not transmit image data. It also confuses evidence with instructions.' },
        { id: 'd', text: 'Switch to text-to-image generation so it recreates the damaged label.', explanation: 'Creating a plausible image is not reading the supplied evidence and cannot recover the actual label reliably.' },
      ],
      correctOptionId: 'a',
      explanation: 'The missing dependency is the image itself. Repair the request structure before tuning model behavior.',
    },
    recall: {
      prompt: 'Describe two supported ways to supply an image and the relationship between its content and the user’s text question.',
      answer: 'Use a reachable image URL or a correctly typed Base64 data URL made from the local image. Put image content and the question together in the API’s supported multipart user message. Choose a model with image-input capability and inspect the returned text against the evidence.',
    },
    transfer: 'A customer wants automatic descriptions for uploaded product photos. What would you ask about privacy, uncertain details, accessibility needs, and human review before selecting the model?',
    sourceNote: `Sources 46 and 47 teach URL and Base64 image inputs with both chat APIs. Assessment 173 loosely says “binary data”; the taught local-file transport is a Base64 data URL, not a raw filename. Source 191 flags visual prompt injection and accessibility as curriculum objectives. Live exercise model examples may differ from the corpus’s GPT-4.1 and Phi examples. ${PORTAL_NOTE}`,
  },
  {
    id: 'image-video-generation',
    title: 'Generate images from clear prompts',
    cluster: 'models-deploy-eval',
    examDomain: 'Implement computer vision solutions',
    summary: 'Choose a text-to-image model, control a visual brief, and distinguish new generation from retrieval or faithful editing.',
    minutes: 6,
    order: 70,
    prerequisites: ['model-deployment', 'responsible-ai-guardrails'],
    sourceIds: [136, 258, 86, 172, 197, 74, 97],
    steps: [
      {
        title: 'Creation is different from finding a picture',
        body: 'An image-generation model creates graphical output from a prompt; it is not a search engine retrieving an existing photograph from a verified collection. That makes it useful for original illustrations and design exploration, but unsuitable as evidence that a historical scene or product detail actually existed. First decide whether the task needs a newly imagined asset, an authoritative source image, or analysis of an existing picture. These different goals require different capabilities even though all involve images.',
        example: 'A fictional poster showing a reading garden can use generation. A photograph documenting the museum’s actual 1920 entrance needs a verified archival source.',
      },
      {
        title: 'Select the output capability and controls',
        body: 'The corpus names GPT-image and FLUX families as examples of image-generation models in Foundry. Filter the catalog by the text-to-image inference task, then verify supported settings in the model card and Images playground. A model that can understand a photograph does not necessarily create one. Describe the subject, setting, composition, and visual style explicitly. Size controls affect supported image dimensions, while the prompt directs content; neither guarantees exact details or an acceptable image on the first attempt.',
        example: 'Request an original flat illustration of an empty reading garden, with two benches and space for a heading, rather than only “make a library poster.”',
      },
      {
        title: 'Refine deliberately and preserve the right assumptions',
        body: 'Change one visual requirement at a time so you can understand the effect. Some supported models and playgrounds accept a reference image to guide appearance, but a reference does not guarantee that every feature is preserved. A fresh generation is not automatically a precise edit of the previous image, and application history must be implemented rather than assumed. Inspect actual results for invented details and unwanted content. For model-specific editing features, follow the current supported workflow instead of inventing a universal edit contract.',
        example: 'If only the background color should change, check whether the model supports an appropriate reference/edit workflow; a new unrelated prompt may also change the benches.',
      },
    ],
    lab: {
      title: 'Rehearse an image-generation brief',
      goal: 'Select an image-output capability and a meaningful first visual control.',
      context: LOCAL_MODEL,
      fields: [
        {
          id: 'task',
          label: 'Catalog inference task',
          hint: 'The application needs a new illustration from a written brief.',
          options: [
            { value: 'text-to-image', label: 'Text to image' },
            { value: 'image-to-text', label: 'Image to text' },
            { value: 'embeddings', label: 'Embeddings' },
          ],
          expected: 'text-to-image',
          explanation: 'The required output is an image. Image-to-text interprets visual input, while embeddings return a numerical representation.',
        },
        {
          id: 'brief',
          label: 'First prompt design',
          hint: 'You want to evaluate the result against observable requirements.',
          options: [
            { value: 'subject-composition-style', label: 'Specify subject, composition, and style' },
            { value: 'beautiful-only', label: 'Only ask for something beautiful' },
            { value: 'claim-authenticity', label: 'Ask for “authentic” and treat the result as evidence' },
          ],
          expected: 'subject-composition-style',
          explanation: 'Concrete visual requirements support comparison and refinement. A generation prompt cannot establish documentary authenticity.',
        },
      ],
      template: '{\n  "learning_model": "image brief",\n  "inference_task": "{{task}}",\n  "prompt_design": "{{brief}}"\n}',
      language: 'json',
      expectedOutput: 'SYNTHETIC EXPECTED RESULT: a text-to-image candidate and an observable visual brief. No image was generated or retrieved.',
      takeaway: 'Task selection determines the output modality; prompt specificity changes what you ask the image model to create.',
      azure: {
        title: 'Generate and refine an original illustration',
        sourceId: 74,
        environment: 'foundry',
        minutes: 20,
        cost: `${CLOUD_COST} Image generation may have model-specific pricing and size limits; keep this experiment to a small approved number of images.`,
        prerequisites: [...RESOURCE_PREREQUISITES, 'Access and quota for a supported image-generation model. Any reference image must be yours to use and contain no sensitive information.'],
        steps: [
          openProject(74),
          {
            title: 'Find a text-to-image model',
            surface: 'Microsoft Foundry',
            instruction: 'Open Discover > Models and filter by Text to image. Inspect the current image model used by the exercise or another approved supported candidate. Confirm image output, size options, and any reference-image support on its card.',
            expected: 'The candidate can generate images rather than only describe them.',
          },
          {
            title: 'Deploy or open the approved model',
            surface: 'Microsoft Foundry',
            instruction: 'Deploy the selected image model after reviewing availability and charges, or open your existing suitable lab deployment. Wait for readiness and open its Images or model-specific playground.',
            expected: 'The correct image-generating deployment is selected in a real playground, or an explicit access/quota blocker is visible.',
          },
          {
            title: 'Generate the first visual brief',
            surface: 'Microsoft Foundry',
            instruction: 'Choose a small supported size. Request an original flat illustration of an empty reading garden with two benches, warm daylight, and clear space at the top for a heading. Submit once and inspect the result.',
            expected: 'An actual generated image or service error is displayed. Count which requested features are present rather than assuming success.',
          },
          {
            title: 'Refine one requirement',
            surface: 'Microsoft Foundry',
            instruction: 'Revise the full brief to use an evening palette while keeping subject and composition requirements explicit. Generate again only within the approved budget, then compare unintended as well as intended changes.',
            expected: 'You can describe whether the result followed the revised brief and which details changed unexpectedly.',
          },
          {
            title: 'Inspect reference support and stop',
            surface: 'Microsoft Foundry',
            instruction: 'If this model’s playground supports reference images, use one permitted sample and follow its documented input controls to test a focused variation. Otherwise record the unsupported feature; do not invent an edit button. Keep the image only if you need it for later practice.',
            expected: 'Reference support is either demonstrated with an actual result or explicitly recorded as unavailable for this model.',
          },
        ],
        success: 'A real image is inspected against an original brief, with refinement and reference capabilities described only as actually observed.',
        evidencePrompt: 'Describe two requested features, one result that matched or missed them, and whether the image is a newly generated illustration or a verified source photograph. Never record secrets or private media.',
        cleanup: CLEANUP,
      },
    },
    check: {
      question: 'A museum needs an image proving what its entrance looked like in 1920. A colleague proposes a detailed image-generation prompt. Which decision respects the requirement?',
      options: [
        { id: 'a', text: 'Use the largest image model because higher resolution proves historical accuracy.', explanation: 'Resolution changes the output’s dimensions, not whether it depicts a verified historical fact.' },
        { id: 'b', text: 'Add “authentic historical photograph” to the prompt and treat the output as evidence.', explanation: 'A descriptive phrase does not convert newly generated content into a retrieved archival record.' },
        { id: 'c', text: 'Use any image-understanding model and assume it will return an archival photograph.', explanation: 'Understanding visual input and retrieving a verified photograph are different capabilities.' },
        { id: 'd', text: 'Use a verified archival source for evidence; reserve generation for clearly labeled illustrations.', explanation: 'This separates documentary retrieval from creative generation, matching the requirement for historical evidence.' },
      ],
      correctOptionId: 'd',
      explanation: 'The decisive boundary is provenance, not visual quality. Generated content can look plausible without documenting reality.',
    },
    recall: {
      prompt: 'Distinguish text-to-image, image-to-text, and image retrieval. Name two controls you would inspect before generating.',
      answer: 'Text-to-image creates a new image from a prompt; image-to-text interprets visual input into language; retrieval finds an existing source image. Inspect supported dimensions, reference/edit support, model availability, and prompt requirements, while checking costs and permissions.',
    },
    transfer: 'How would you help a customer decide whether a marketing workflow needs new illustrations, source-image retrieval, or controlled editing of approved assets?',
    sourceNote: `The historical ID image-video-generation is retained for compatibility; detailed video teaching is in video-generation. The corpus teaches GPT-image-1/FLUX examples while the linked exercise may use a newer model. Mask-based editing appears in the broad exam guide but detailed inpainting procedures are absent from these assigned image pages; no unsupported universal mask workflow is invented here. ${PORTAL_NOTE}`,
  },
  {
    id: 'image-generation-apps',
    title: 'Handle generated images in an application',
    cluster: 'models-deploy-eval',
    examDomain: 'Implement computer vision solutions',
    summary: 'Follow an Images API result from authenticated request to decoded image, and keep request state and response formats explicit.',
    minutes: 6,
    order: 75,
    prerequisites: ['image-video-generation', 'foundry-sdk-connections'],
    sourceIds: [32, 74, 97],
    steps: [
      {
        title: 'The response still needs application work',
        body: 'A successful image request does not automatically place a usable picture in your application. The corpus’s client uses the OpenAI Images API and then handles the returned image data. Episode 97 shows a result containing an array of image entries, with Base64 data in b64_json for the demonstrated model. The client decodes that representation into bytes and saves or displays the image. This is output handling, not a second model call or a prompt for the model to decode itself.',
        example: 'A poster tool must turn the returned image representation into a viewable asset before a designer can review it.',
      },
      {
        title: 'Keep connection and generation choices separate',
        body: 'The application still needs the correct inference endpoint, deployment name, and authorized identity established in the earlier connection unit. The Images API’s generation operation then takes the visual prompt and supported controls such as image count and size. Changing the deployment chooses the image model; changing size affects permitted dimensions; changing count requests additional outputs where supported. Read the selected model’s response contract instead of assuming that every provider or older image API returns the same representation.',
        example: 'A client that correctly connects to a text-only deployment still cannot obtain image output simply by asking for a picture.',
      },
      {
        title: 'Do not invent state between image requests',
        body: 'The supplied image client treats each prompt as a new request and does not implement conversation history. A second prompt saying “make it blue” may not refer to the previous image in that application. Include the complete brief, or use a supported reference/edit workflow with the necessary source image and controls. Diagnose transport, response parsing, file writing, and content quality as separate stages. A correctly saved file can still contain the wrong picture, and malformed output handling does not prove the model generated nothing.',
        example: 'If the image file will not open, inspect decoding and writing before spending money on repeated generations with different prompts.',
      },
    ],
    lab: {
      title: 'Rehearse image result handling',
      goal: 'Handle the demonstrated response format and avoid assuming conversation state.',
      context: LOCAL_MODEL,
      fields: [
        {
          id: 'result',
          label: 'The model returns b64_json',
          hint: 'The value is an encoded image representation inside a response object.',
          options: [
            { value: 'decode-then-display', label: 'Decode the Base64 image, then save or display bytes' },
            { value: 'save-json-as-png', label: 'Rename the entire JSON response as a PNG' },
            { value: 'ask-model-to-open-file', label: 'Ask the model to open a local filename for you' },
          ],
          expected: 'decode-then-display',
          explanation: 'The demonstrated client decodes the image entry. Renaming JSON does not convert it into image bytes.',
        },
        {
          id: 'continuity',
          label: 'A new independent request',
          hint: 'The supplied sample has no implemented image-conversation memory.',
          options: [
            { value: 'complete-brief-or-reference', label: 'Supply the complete brief or a supported reference workflow' },
            { value: 'it-is-enough', label: 'Use only “make it blue” and assume memory' },
            { value: 'deployment-remembers-all', label: 'Assume a deployment remembers every prior image' },
          ],
          expected: 'complete-brief-or-reference',
          explanation: 'The application must provide the necessary context. A persistent deployment is not automatic per-user image memory.',
        },
      ],
      template: '{\n  "learning_model": "image client pipeline",\n  "result_handling": "{{result}}",\n  "next_request": "{{continuity}}"\n}',
      language: 'json',
      expectedOutput: 'SYNTHETIC EXPECTED TRACE: image response entry → Base64 decoding → image bytes → display → independent next request. No image data exists in this rehearsal.',
      takeaway: 'Output decoding changes representation, not image content; request context must be supplied by the application.',
      azure: {
        title: 'Run the published image-generation client',
        sourceId: 74,
        environment: 'portal-and-code',
        minutes: 30,
        cost: `${CLOUD_COST} Each additional generated image may add charges; inspect client-side errors before resubmitting successful service requests.`,
        prerequisites: [...CODE_PREREQUISITES, 'A ready image-generation deployment from the previous unit and permission to write local lab image files.'],
        steps: [
          openProject(74),
          {
            title: 'Verify the image deployment',
            surface: 'Microsoft Foundry',
            instruction: 'Open the image model under Build > Models or Deployments. Confirm its ready state and the model’s supported image-output contract. Use the Images playground to verify one bounded prompt if you have not already done so.',
            expected: 'The cloud dependency is known to be an image-generation model, not a text chat deployment.',
          },
          {
            title: 'Find the client connection settings',
            surface: 'Microsoft Foundry',
            instruction: 'On Home, identify the Azure OpenAI endpoint and the exact image deployment name. Inspect Code in the model playground where offered, and compare it with the exercise’s Images API path.',
            expected: 'Endpoint kind, API family, and deployment selection agree before you configure the client.',
          },
          {
            title: 'Prepare the supplied Python project',
            surface: 'Cloud Shell / local editor',
            instruction: 'Follow the linked exercise’s MicrosoftLearning vision repository setup and open its image-client Python folder. Create the prescribed environment, install requirements, and place the non-secret endpoint and deployment name in the local configuration. Follow its identity sign-in steps.',
            expected: 'A local client environment is ready without embedding credentials into source or evidence.',
          },
          {
            title: 'Inspect the generation and decoding path',
            surface: 'Cloud Shell / local editor',
            instruction: 'Complete the supplied image-client example. Trace the Images API request, the returned data entry, Base64 decoding, and the function that writes the image. Check the current model contract rather than assuming all image APIs return b64_json.',
            expected: 'You can identify the boundary between a service result and application-side decoding/file handling.',
          },
          {
            title: 'Generate and open one real file',
            surface: 'Cloud Shell / local editor',
            instruction: 'Run the supplied client with an original brief for a fictional reading-garden illustration. Inspect the terminal result and the created image in the exercise’s images folder. If it fails to open, check the response format and decoding before sending another paid request.',
            expected: 'A successfully saved image opens as an image, or a specific request/decoding/file error is recorded.',
          },
          {
            title: 'Test the request boundary and quit',
            surface: 'Cloud Shell / local editor',
            instruction: 'Review the sample’s loop and confirm that it does not retain image history. If budget permits a variation, use a complete revised brief rather than an ambiguous pronoun. Quit the client and inspect the actual output for both visual requirements and unintended changes.',
            expected: 'You can explain why independent requests need explicit context and distinguish a valid file from a satisfactory image.',
          },
        ],
        success: 'The supplied client creates a viewable image from an actual service response, with transport, decoding, and request continuity explained separately.',
        evidencePrompt: 'Describe the image’s path from response to display and one limitation of the sample’s state handling. Do not paste encoded image payloads, secrets, or private media into evidence.',
        cleanup: CLEANUP,
      },
    },
    check: {
      question: 'The image service returned a valid JSON response with b64_json, but a developer saved the entire JSON text as poster.png and the viewer rejects it. Which layer should be repaired first?',
      options: [
        { id: 'a', text: 'Model selection, because JSON output proves the model cannot generate images.', explanation: 'The image can be encoded inside JSON. Its transport representation is not evidence that image generation failed.' },
        { id: 'b', text: 'Prompt wording, because a more detailed prompt converts JSON into a local PNG.', explanation: 'Prompt content influences the picture, not how the application decodes or writes response data.' },
        { id: 'c', text: 'Response handling: extract the image entry, decode it, and write the resulting bytes.', explanation: 'The failure lies between the successful service response and the image viewer. Correct decoding repairs that representation boundary.' },
        { id: 'd', text: 'Conversation history, because an image needs an earlier chat response to be opened.', explanation: 'State may matter for a follow-up edit, but it is unrelated to interpreting this already returned image payload.' },
      ],
      correctOptionId: 'c',
      explanation: 'Diagnose the failing stage before generating again. A valid encoded result needs the expected application-side decoding and file handling.',
    },
    recall: {
      prompt: 'Reconstruct the demonstrated Images API result-handling path. Why is a follow-up prompt not automatically an edit of the first image?',
      answer: 'Select the returned image entry, read its documented representation such as b64_json, decode Base64 into image bytes, then save or display the bytes. The sample sends independent requests without history; a contextual variation needs the full brief or a supported reference/edit mechanism.',
    },
    transfer: 'If a customer sees “generation succeeded” but no usable picture, which evidence would you request from each stage without asking them to send credentials or private image payloads?',
    sourceNote: `Source 32 summarizes the outcome as image binary data; episode 97 explains the demonstrated Base64-in-JSON transport and decoding. Other model contracts can differ. The image exercise’s current model name and authentication example may differ from the chat exercise; follow one verified exercise/version consistently. ${PORTAL_NOTE}`,
  },
  {
    id: 'video-generation',
    title: 'Generate and revise video jobs',
    cluster: 'models-deploy-eval',
    examDomain: 'Implement computer vision solutions',
    summary: 'Plan a short shot, track an asynchronous generation job, and use reference images or focused remixes where supported.',
    minutes: 9,
    order: 80,
    prerequisites: ['image-video-generation', 'chat-responses'],
    sourceIds: [128, 38, 100, 101, 75, 97],
    steps: [
      {
        title: 'Describe a shot rather than an entire film',
        body: 'Video generation turns a visual brief into a moving scene, so the prompt must describe change over time as well as appearance. The corpus uses Sora 2 and recommends clear framing, subject details, action beats, lighting, palette, and style. Keep an initial shot simple: one main action and one camera move. Duration and resolution are supported model controls, not arbitrary numbers. Shorter clips can be easier to direct, but every generated result still needs review against the intended scene.',
        example: 'Request a four-second wide shot of a paper windmill turning slowly beside an empty garden bench, with a stationary camera and soft morning light.',
      },
      {
        title: 'A job identifier is not a finished video',
        body: 'Video generation is asynchronous. The application creates a job, retains its identifier, polls for status, and downloads content only after successful completion. The demonstrated terminal states include completed, failed, and cancelled; failure details must be inspected rather than ignored. This differs from merely streaming text chunks: a pending job is still producing the media asset. Keep waiting behavior separate from the success condition and avoid launching duplicate jobs just because the first result is not immediate.',
        example: 'A progress message means the poster-video job is still running; a player should not receive a made-up file path before the completed asset is downloaded.',
      },
      {
        title: 'Distinguish a starting image from a remix',
        body: 'A reference image anchors the first frame, while the prompt describes what happens next. In the corpus’s Sora example, input_reference supplies the image and its dimensions must match the target video size. A remix instead starts from an existing generated video ID and requests a focused change while aiming to retain its structure. These are different inputs and operations. Keep a remix narrow, such as changing the palette, and review whether the intended continuity actually survived.',
        example: 'Use a permitted garden photograph to start a new clip; use the completed clip’s video ID to request a warmer palette without redesigning the scene.',
      },
      {
        title: 'Plan for limits, content policy, and access',
        body: 'Model access, supported regions, durations, and content rules can restrict whether a video workflow can run at all. The corpus notes rejected human-face reference images, limited concurrent creation jobs, and a finite download window. Treat those as version-specific constraints to verify, not permanent universal limits. Generated media can be moderated, and a failed or unavailable model is not solved by pretending the job succeeded. Preserve non-secret job evidence, retrieve approved outputs promptly, and clean up only resources you own.',
        example: 'If your subscription cannot deploy the required video model, record the access limitation and keep the workflow plan; do not substitute a text model and label its response a video.',
      },
    ],
    lab: {
      title: 'Rehearse a video-job controller',
      goal: 'Wait for a genuine completed asset and choose the right input for a focused revision.',
      context: LOCAL_MODEL,
      fields: [
        {
          id: 'download',
          label: 'When to download',
          hint: 'Creating a job returns an identifier before the asset is necessarily ready.',
          options: [
            { value: 'after-completed', label: 'After polling confirms completed' },
            { value: 'immediately-after-id', label: 'Immediately after any job ID is returned' },
            { value: 'after-fixed-delay', label: 'After a fixed delay regardless of status' },
          ],
          expected: 'after-completed',
          explanation: 'Completion is a service state to observe, not a timer assumption. Failed or cancelled jobs need handling rather than a download attempt.',
        },
        {
          id: 'revision',
          label: 'Change only the completed clip’s palette',
          hint: 'You want to revise an existing generated video, not start from a still photograph.',
          options: [
            { value: 'remix-video-id', label: 'Remix using the existing video ID and a focused prompt' },
            { value: 'chat-response-id', label: 'Pass a chat response ID as the video' },
            { value: 'unrelated-new-prompt', label: 'Generate an unrelated clip and assume continuity' },
          ],
          expected: 'remix-video-id',
          explanation: 'The remix operation targets the existing video. A chat response ID belongs to a different API state contract.',
        },
      ],
      template: '{\n  "learning_model": "video job plan",\n  "download_gate": "{{download}}",\n  "revision_method": "{{revision}}"\n}',
      language: 'json',
      expectedOutput: 'SYNTHETIC EXPECTED TRACE: create → retain job ID → poll → completed → download; remix creates a separately tracked revision. No video job or downloadable file exists.',
      takeaway: 'Duration changes the requested clip; job state governs readiness; a reference image and a video ID select different generation paths.',
      azure: {
        title: 'Create a short video and inspect its lifecycle',
        sourceId: 75,
        environment: 'portal-and-code',
        minutes: 45,
        cost: `${CLOUD_COST} Video generation and each variation can add charges. Check current model access and pricing before provisioning, and limit the number and duration of clips.`,
        prerequisites: [...CODE_PREREQUISITES, 'Confirmed access, quota, and regional support for Sora 2 as used in the source exercise. If unavailable, this hands-on task is blocked; another model is not assumed API-compatible.', 'The exercise’s permitted reference image, matching the selected supported video dimensions, and permission to download local media.'],
        steps: [
          openProject(75),
          {
            title: 'Verify video availability and deploy',
            surface: 'Microsoft Foundry',
            instruction: 'Open Discover > Models and locate the exercise’s Sora 2 video model. Inspect access restrictions, region support, and available deployment settings. Deploy only if authorized and supported, or open an existing lab deployment.',
            expected: 'A ready video-generation deployment exists, or an explicit access/availability limitation is recorded without claiming the exercise ran.',
            hint: 'The published exercise warns that Sora 2 may be unavailable for new deployments. Do not create repeated projects or promise access as a workaround.',
          },
          {
            title: 'Generate one bounded shot',
            surface: 'Microsoft Foundry',
            instruction: 'Open the deployment’s Video playground or Playground tab. Request the original paper-windmill garden shot from the lesson, choose a supported small resolution and four-second duration if available, and select Generate once.',
            expected: 'The portal shows a submitted generation job or an explicit error; the asset may take time to become available.',
          },
          {
            title: 'Review the completed media',
            surface: 'Microsoft Foundry',
            instruction: 'Wait for the actual completion state. If successful, play and download the clip using the displayed controls, then compare subject, movement, framing, and length with your brief. If it fails, inspect the displayed reason instead of treating elapsed time as success.',
            expected: 'A real completed clip is viewable, or the failure is identified. No particular visual result is guaranteed.',
          },
          {
            title: 'Prepare the portal-to-code handoff',
            surface: 'Microsoft Foundry',
            instruction: 'Use Home to locate the Azure OpenAI endpoint and deployment name. Inspect View code in the Video playground where available, then open the exercise’s supplied Python application section; its job and remix behavior belongs in code, not an invented portal control.',
            expected: 'The selected endpoint, deployment, and supported video API path are identified without exposing credentials.',
          },
          {
            title: 'Run the supplied job workflow',
            surface: 'Cloud Shell / local editor',
            instruction: 'Follow the video-generation Python setup, configuration, and identity sign-in in the linked exercise. Complete its supplied create, poll, and download functions. Run within your budget and inspect actual terminal states, downloading only a completed result.',
            expected: 'The client tracks a real video ID and separates completion from failed or cancelled outcomes.',
            hint: 'If a helper yields no job object or the player cannot open a file, inspect return handling and download completion before creating another paid job.',
          },
          {
            title: 'Compare a reference or focused remix',
            surface: 'Cloud Shell / local editor',
            instruction: 'Within the approved budget, follow the exercise’s reference-image and remix sections. Verify the reference dimensions before a new image-based job. For a remix, retain the completed video ID and request one palette change; poll the new job independently and compare the actual outputs. Stop the client when finished.',
            expected: 'You can distinguish a new clip anchored by an image from a revision targeting an existing video, or explain the specific supported-feature/policy blocker.',
          },
        ],
        success: 'An actual supported video job is tracked through completion and reviewed, with reference/remix inputs distinguished; unavailable access remains an explicit blocker rather than a simulated success.',
        evidencePrompt: 'Describe the observed job-state sequence, one visual result, and which input a reference or remix operation uses. Never record credentials, private media, or a claimed successful job you did not run.',
        cleanup: CLEANUP,
      },
    },
    check: {
      question: 'An app receives a video job ID, waits ten seconds, and immediately tries to play a file that has not been downloaded. What should its controller do instead?',
      options: [
        { id: 'a', text: 'Assume every job is complete after the same delay and retry playback.', explanation: 'Generation time varies and jobs can fail or be cancelled. A timer is not evidence that an asset exists.' },
        { id: 'b', text: 'Poll the job to a terminal state, download only after completion, and handle failure explicitly.', explanation: 'This follows the asynchronous contract and supplies the player with an actual completed asset rather than an assumed file.' },
        { id: 'c', text: 'Pass the video ID as previous_response_id to a text chat.', explanation: 'A video job identifier is not a Responses conversation identifier. That switches to an unrelated API contract.' },
        { id: 'd', text: 'Start a new generation every ten seconds until a file appears.', explanation: 'Duplicate jobs can increase costs and quota pressure while leaving the missing status/download logic unfixed.' },
      ],
      correctOptionId: 'b',
      explanation: 'Readiness is an observed job state. A usable local media file additionally requires a successful content download.',
    },
    recall: {
      prompt: 'Reconstruct the create–poll–download workflow, including unsuccessful terminal states. How does input_reference differ from remix?',
      answer: 'Create a video job, retain its ID, poll until completed, failed, or cancelled, and download only a completed asset; inspect errors otherwise. input_reference supplies a starting image with matching supported dimensions. Remix targets an existing video ID with a focused revision prompt and creates another job to track.',
    },
    transfer: 'Before promising a video-generation demo to a customer, which access, content-policy, latency, cost, and output-retention facts would you verify?',
    sourceNote: `Sources 100 and 101 describe Sora 2; source 101 lists two concurrent creation jobs and a 24-hour download window as its snapshot, not guarantees for every current deployment. The live source-75 exercise explicitly warns about new-deployment availability. Episode 97’s image Base64 summary must not be applied to video: its video workflow downloads completed content by job ID. ${PORTAL_NOTE}`,
  },
]
